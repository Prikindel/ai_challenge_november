# 🔥 День 29. Локальный аналитик

Цель: создать локального аналитика данных, который умеет анализировать CSV, JSON, логи проекта и отвечать на аналитические вопросы ("какая ошибка чаще всего?", "где больше всего пользователей теряется?") используя локальную LLM на VPS.

## 🎥 Демонстрация

> 📹 Видео появится после записи

**Сценарий использования:**
1. Загрузка данных (CSV, JSON, логи)
2. Данные сохраняются в локальную БД
3. Задавание аналитических вопросов на естественном языке
4. Анализ через локальную LLM (VPS)
5. Получение структурированных ответов

## Что делаем

- Берем структуру урока 24 (анализатор отзывов с БД)
- Заменяем облачную LLM на локальную LLM из урока 28 (VPS)
- Добавляем поддержку загрузки CSV, JSON, логов
- Создаём сервис анализа данных
- Реализуем ответы на аналитические вопросы
- Всё работает локально, без облачных сервисов

## Пайплайн

1. **Копирование структуры** — копируем структуру из lesson-24-real-task
2. **Интеграция локальной LLM** — заменяем OpenRouter на локальную LLM (VPS)
3. **Поддержка CSV** — парсер и загрузка CSV файлов
4. **Поддержка JSON** — парсер и загрузка JSON файлов
5. **Поддержка логов** — парсер и загрузка логов проекта
6. **Сервис анализа** — анализ данных через локальную LLM
7. **API для вопросов** — endpoint для аналитических вопросов
8. **UI** — интерфейс для загрузки данных и вопросов
9. **Примеры вопросов** — предустановленные аналитические вопросы
10. **Документация** — примеры данных и инструкции

## Быстрый старт

### 1. Предварительные требования

- Java 17+
- Gradle 8+
- Локальная LLM на VPS (из урока 28)
- SQLite (встроен)

### 2. Конфигурация

**config/server.yaml:**
```yaml
localLLM:
  enabled: true
  provider: "ollama"
  baseUrl: "https://185.31.165.227"  # VPS из урока 28
  model: "llama3.2"
  apiPath: "/api/generate"
  timeout: 120000
  auth:
    type: "basic"
    user: "user"
    password: "pass"
  parameters:
    temperature: 0.3  # Низкая для точного анализа
    maxTokens: 2048
    contextWindow: 4096
```

### 3. Запуск

```bash
cd server
./gradlew build
./gradlew run
```

Откройте: `http://localhost:8080`

## Использование

### Загрузка данных

1. **CSV файл:**
   - Перейдите в раздел "Загрузка данных"
   - Выберите тип "CSV"
   - Загрузите файл
   - Данные автоматически парсятся и сохраняются в БД

2. **JSON файл:**
   - Выберите тип "JSON"
   - Загрузите файл
   - Данные парсятся и сохраняются

3. **Логи:**
   - Выберите тип "Логи"
   - Загрузите файл логов
   - Логи парсятся (JSON или plain text)

### Аналитические вопросы

**Примеры вопросов:**

**Для логов:**
- "Какая ошибка встречается чаще всего?"
- "В какое время больше всего ошибок?"
- "Какие компоненты системы генерируют больше всего ошибок?"
- "Какие паттерны в ошибках?"

**Для CSV/JSON:**
- "Где больше всего пользователей теряется?"
- "Какой день недели самый активный?"
- "Какие топ-5 проблем по частоте?"
- "Есть ли сезонность в данных?"
- "Какие аномалии можно обнаружить?"

**Использование:**
1. Перейдите в раздел "Анализ"
2. Введите вопрос на естественном языке
3. Выберите источник данных (опционально)
4. Получите структурированный ответ

## Поддерживаемые форматы

### CSV

**Формат:**
```csv
name,age,city,error
John,25,New York,Connection timeout
Jane,30,London,Database error
```

**Требования:**
- Первая строка — заголовки
- Разделитель: запятая или точка с запятой
- Кодировка: UTF-8

### JSON

**Формат (массив объектов):**
```json
[
  {"timestamp": "2024-01-01", "user": "John", "action": "login"},
  {"timestamp": "2024-01-02", "user": "Jane", "action": "logout"}
]
```

**Формат (один объект):**
```json
{
  "users": [
    {"name": "John", "age": 25},
    {"name": "Jane", "age": 30}
  ]
}
```

### Логи

**JSON логи:**
```json
{"timestamp": "2024-01-01T10:00:00Z", "level": "ERROR", "message": "Connection failed"}
```

**Plain text логи:**
```
2024-01-01 10:00:00 ERROR: Connection failed
2024-01-01 10:01:00 INFO: User logged in
```

## API Endpoints

### POST /api/data/upload/csv

Загрузка CSV файла.

**Запрос:**
- `multipart/form-data`
- Поле `file`: CSV файл

**Ответ:**
```json
{
  "success": true,
  "recordsCount": 150,
  "source": "csv",
  "filename": "data.csv"
}
```

### POST /api/data/upload/json

Загрузка JSON файла.

**Запрос:**
- `multipart/form-data`
- Поле `file`: JSON файл

**Ответ:**
```json
{
  "success": true,
  "recordsCount": 200,
  "source": "json",
  "filename": "data.json"
}
```

### POST /api/data/upload/logs

Загрузка файла логов.

**Запрос:**
- `multipart/form-data`
- Поле `file`: файл логов

**Ответ:**
```json
{
  "success": true,
  "recordsCount": 500,
  "source": "logs",
  "filename": "app.log"
}
```

### POST /api/analyze

Аналитический вопрос.

**Запрос:**
```json
{
  "question": "Какая ошибка встречается чаще всего?",
  "source": "logs",  // опционально: "csv", "json", "logs"
  "limit": 1000      // опционально: ограничение количества записей
}
```

**Ответ:**
```json
{
  "question": "Какая ошибка встречается чаще всего?",
  "answer": "Согласно анализу данных, наиболее часто встречающаяся ошибка: 'Connection timeout' (45 случаев, 30% от общего числа ошибок). Следующие по частоте: 'Database error' (30 случаев, 20%) и 'Authentication failed' (25 случаев, 17%).",
  "source": "logs",
  "recordsCount": 150
}
```

### GET /api/data/sources

Получение списка источников данных.

**Ответ:**
```json
{
  "sources": [
    {"type": "csv", "count": 150, "files": ["data.csv"]},
    {"type": "json", "count": 200, "files": ["data.json"]},
    {"type": "logs", "count": 500, "files": ["app.log"]}
  ]
}
```

## Архитектура

```
lesson-29-local-analyst/
├── config/
│   └── server.yaml          # Конфигурация (локальная LLM)
├── client/                  # Frontend
│   ├── index.html          # Главная страница
│   ├── analysis.html       # Страница анализа
│   └── app.js              # JavaScript
├── server/                  # Backend (Kotlin, Ktor)
│   └── src/main/kotlin/com/prike/
│       ├── data/
│       │   ├── parser/      # Парсеры (CSV, JSON, Logs)
│       │   └── repository/  # Доступ к БД
│       ├── domain/
│       │   ├── model/      # Модели данных
│       │   └── service/    # Сервисы анализа
│       └── presentation/
│           └── controller/ # REST API
└── data/
    └── analyst.db          # SQLite база данных
```

## Примеры данных

### Пример CSV (errors.csv)

```csv
timestamp,level,component,message,error
2024-01-01 10:00:00,ERROR,Database,Connection failed,Connection timeout
2024-01-01 10:01:00,ERROR,API,Request failed,Database error
2024-01-01 10:02:00,WARN,Auth,Login attempt,Authentication failed
2024-01-01 10:03:00,ERROR,Database,Connection failed,Connection timeout
```

### Пример JSON (users.json)

```json
[
  {"id": 1, "name": "John", "city": "New York", "status": "active"},
  {"id": 2, "name": "Jane", "city": "London", "status": "inactive"},
  {"id": 3, "name": "Bob", "city": "Paris", "status": "active"}
]
```

### Пример логов (app.log)

```
2024-01-01 10:00:00 ERROR [Database] Connection failed: Connection timeout
2024-01-01 10:01:00 ERROR [API] Request failed: Database error
2024-01-01 10:02:00 WARN [Auth] Login attempt: Authentication failed
```

## Рекомендации

### Формулировка вопросов

✅ **Хорошие вопросы:**
- "Какая ошибка встречается чаще всего?"
- "Где больше всего пользователей теряется?"
- "Какие топ-5 проблем по частоте?"

❌ **Плохие вопросы:**
- "Проанализируй данные" (слишком общий)
- "Что здесь?" (неконкретный)
- "Покажи всё" (слишком широкий)

### Ограничения

- **Объём данных:** рекомендуется до 5000 записей за раз
- **Размер файла:** до 10 МБ
- **Контекст:** LLM получает агрегированную статистику, не все записи

### Оптимизация

- Используйте фильтры по источнику данных
- Ограничивайте количество записей (`limit`)
- Задавайте конкретные вопросы
- Используйте структурированные данные (CSV, JSON)

## Файлы урока

- `PROMPT.md` — промпт для реализации (по коммитам)
- `README.md` — этот файл
- `DATA_FORMATS.md` — описание форматов данных
- `ANALYTIC_EXAMPLES.md` — примеры аналитических вопросов
- `CHAT_PROMPT.txt` — краткий промпт для агента
- `examples/` — примеры данных для тестирования

## 💡 Советы

1. **Начните с малых данных** — протестируйте на небольшом файле
2. **Используйте структурированные данные** — CSV и JSON лучше, чем plain text
3. **Задавайте конкретные вопросы** — чем конкретнее, тем лучше ответ
4. **Фильтруйте по источнику** — используйте параметр `source` для точности
5. **Проверяйте ответы** — локальная LLM может ошибаться, проверяйте важные выводы

## 🐛 Решение проблем

### Данные не загружаются

- Проверьте формат файла (CSV, JSON, логи)
- Убедитесь, что файл в кодировке UTF-8
- Проверьте размер файла (до 10 МБ)

### LLM не отвечает

- Проверьте подключение к VPS
- Убедитесь, что локальная LLM запущена
- Проверьте авторизацию в конфигурации

### Ответы неточные

- Уменьшите `temperature` до 0.3
- Ограничьте количество записей (`limit`)
- Задавайте более конкретные вопросы

