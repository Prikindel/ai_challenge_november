# 🔥 День 29. Локальный аналитик данных

Локальный аналитик данных, который умеет анализировать данные из БД урока 24 (отзывы) и отвечать на аналитические вопросы используя локальную LLM на VPS.

## 🎥 Демонстрация

> 📹 [Видео демонстрация](https://disk.yandex.ru/i/8uhokJZk7usGgg)

**Сценарий использования:**
1. Использование готовой БД из урока 24 (отзывы)
2. Задавание аналитических вопросов на естественном языке
3. Анализ через локальную LLM (VPS)
4. Получение структурированных ответов с конкретными цифрами и фактами

## 🎯 Цель урока

Создать систему анализа данных, которая:
- Использует готовую БД из урока 24 (отзывы)
- Использует локальную LLM (VPS) для анализа
- Отвечает на аналитические вопросы на естественном языке
- Работает полностью локально (без облачных сервисов)

## 🚀 Быстрый старт

### Требования

- Java 17+
- Gradle
- Доступ к VPS с локальной LLM (Ollama)

### Установка

1. Клонируйте репозиторий и перейдите в директорию урока:
```bash
cd lesson-29-local-analyst
```

2. Настройте конфигурацию в `config/server.yaml`:
```yaml
localLLM:
  enabled: true
  provider: "ollama"
  baseUrl: "https://185.31.165.227"  # VPS из урока 28
  model: "llama3.2"
  auth:
    type: "basic"
    user: "user"
    password: "mypassword123"
  parameters:
    temperature: 0.3  # Низкая для точного анализа
    maxTokens: 2048
```

3. Запустите сервер:
```bash
cd server
./gradlew run
```

4. Откройте браузер:
```
http://localhost:8080
```

## 📋 Возможности

### Загрузка данных

- **CSV файлы** — автоматический парсинг с заголовками
- **JSON файлы** — поддержка массивов объектов и одиночных объектов
- **Логи** — поддержка JSON логов и plain text логов (log4j, winston и др.)

### Анализ данных

- **Аналитические вопросы** на естественном языке
- **Статистика данных** — автоматический расчет метрик
- **Локальная LLM** — анализ через VPS без облачных сервисов
- **Фильтрация** — анализ по источникам (CSV, JSON, логи)

## 📊 Примеры аналитических вопросов

- "Какая ошибка встречается чаще всего?"
- "Где больше всего пользователей теряется?"
- "Какой день недели самый активный?"
- "Какие топ-5 проблем по частоте?"
- "Есть ли сезонность в данных?"
- "Какие аномалии можно обнаружить?"
- "В какое время больше всего ошибок?"
- "Какие компоненты системы генерируют больше всего ошибок?"

## 🏗️ Архитектура

### Компоненты

1. **Парсеры данных**
   - `CsvParser` — парсинг CSV файлов
   - `JsonParser` — парсинг JSON файлов
   - `LogParser` — парсинг логов (JSON и plain text)

2. **Репозиторий данных**
   - `DataRepository` — работа с БД (SQLite)
   - Хранение загруженных данных

3. **Сервисы анализа**
   - `DataAnalysisService` — анализ данных через LLM
   - `DataStatisticsService` — расчет статистики
   - `LLMService` — работа с локальной LLM

4. **API**
   - `/api/data/upload/csv` — загрузка CSV
   - `/api/data/upload/json` — загрузка JSON
   - `/api/data/upload/logs` — загрузка логов
   - `/api/data/stats` — статистика данных
   - `/api/analyze` — аналитический вопрос

## 📁 Структура проекта

```
lesson-29-local-analyst/
├── server/                    # Backend (Kotlin + Ktor)
│   ├── src/main/kotlin/
│   │   └── com/prike/
│   │       ├── config/        # Конфигурация
│   │       ├── data/          # Парсеры, репозитории
│   │       ├── domain/        # Бизнес-логика
│   │       └── presentation/  # Контроллеры, DTO
│   └── build.gradle.kts
├── client/                    # Frontend (HTML + JS)
│   ├── index.html            # Загрузка данных
│   ├── analysis.html         # Аналитические вопросы
│   ├── app.js                # JavaScript логика
│   └── style.css             # Стили
├── config/
│   └── server.yaml           # Конфигурация сервера
├── examples/                 # Примеры данных
│   ├── example.csv
│   ├── example.json
│   └── example.log
├── DATA_FORMATS.md          # Описание форматов данных
└── README.md                # Этот файл
```

## 🔧 Конфигурация

### Локальная LLM

```yaml
localLLM:
  enabled: true
  provider: "ollama"
  baseUrl: "https://185.31.165.227"  # VPS URL
  model: "llama3.2"
  auth:
    type: "basic"
    user: "user"
    password: "mypassword123"
  parameters:
    temperature: 0.3        # Низкая для точного анализа
    maxTokens: 2048
    topP: 0.9
    topK: 40
    repeatPenalty: 1.1
    contextWindow: 4096
```

### База данных

```yaml
database:
  path: "../lesson-24-real-task/data/reviews.db"  # Используется БД из урока 24
```

**Важно:** Проект использует существующую БД из урока 24 с таблицами:
- `review_summaries` - саммари отзывов
- `week_analyses` - анализы недель

Загрузка данных через UI отключена.

## 📝 Использование

### 1. Проверка данных

1. Откройте главную страницу: `http://localhost:8080`
2. Просмотрите статистику данных из БД урока 24
3. Данные включают отзывы с категориями, рейтингами, темами

### 2. Анализ данных

1. Перейдите на страницу "Анализ"
2. Выберите фильтр по категории (все отзывы, положительные, отрицательные, нейтральные)
3. Введите аналитический вопрос или выберите из примеров
4. Нажмите "Задать вопрос"
5. Получите ответ от локальной LLM

### 3. Примеры вопросов

- **Для отзывов:**
  - "Какая категория отзывов встречается чаще всего?"
  - "Какие темы чаще всего упоминаются в отрицательных отзывах?"
  - "Какой средний рейтинг отзывов?"
  - "Какие топ-5 проблем по частоте в отзывах?"
  - "Есть ли сезонность в отзывах?"
  - "Какие отзывы имеют высокую критичность?"

## 🔍 API Endpoints

### Загрузка данных (отключена)

Загрузка данных отключена. Используется БД из урока 24.

```bash
# Все endpoints возвращают 501 Not Implemented
POST /api/data/upload/csv
POST /api/data/upload/json
POST /api/data/upload/logs
```

### Статистика

```bash
GET /api/data/stats
```

Ответ:
```json
{
  "total": 1000,
  "bySource": {
    "reviews": 1000,
    "positive": 600,
    "negative": 300,
    "neutral": 100
  }
}
```

### Анализ

```bash
POST /api/analyze
Content-Type: application/json

{
  "question": "Какая ошибка встречается чаще всего?",
  "source": "logs",
  "limit": 1000
}
```

Ответ:
```json
{
  "question": "Какая категория отзывов встречается чаще всего?",
  "answer": "Согласно анализу данных...",
  "source": "reviews",
  "recordsCount": 1000
}
```

## 📚 Документация

- [PROMPT.md](PROMPT.md) — полное описание задачи
- [ANALYTIC_EXAMPLES.md](ANALYTIC_EXAMPLES.md) — примеры аналитических вопросов

**Примечание:** Загрузка данных отключена. Используется БД из урока 24.

## 🛠️ Технологии

- **Backend:** Kotlin, Ktor, Exposed (SQLite)
- **Frontend:** HTML, JavaScript, CSS
- **LLM:** Ollama (локальная LLM на VPS)
- **База данных:** SQLite

## ⚠️ Ограничения

- **Источник данных:** используется БД из урока 24 (загрузка отключена)
- **Анализ:** до 1000-5000 записей за раз
- **БД:** должна существовать БД из урока 24 по пути `../lesson-24-real-task/data/reviews.db`

## 🎓 Цели урока

- Реализовать локального аналитика данных
- Использовать локальную LLM вместо облачных сервисов
- Использовать готовую БД из урока 24
- Создать систему аналитических вопросов
- Получить опыт работы с локальными LLM

## 📝 Примечания

- Используется структура урока 24 как база
- Заменена облачная LLM (OpenRouter) на локальную LLM (VPS)
- Используется готовая БД из урока 24 (отзывы)
- Загрузка данных через UI отключена
- Ограничен объём данных для анализа (1000-5000 записей)
- Используется низкая temperature (0.3) для точных ответов
- Данные структурируются перед отправкой в LLM

## 🔗 Связанные уроки

- **Урок 24** — анализатор отзывов (базовая структура)
- **Урок 28** — локальная LLM на VPS (использована конфигурация)

---

**Автор:** AI Challenge November  
**Дата:** 2024
