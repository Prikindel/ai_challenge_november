# Lesson 04 — Experiments with Reasoning Strategies

Заготовка для реализация задания «🔥 День 4. Разные способы рассуждения».
Урок будет показывать, как одна и та же логическая задача решается четырьмя подходами:

1. **Direct** — запрос без дополнительных инструкций.  
2. **Step-by-step** — запрос с явным указанием «решай пошагово».  
3. **Prompt from other AI** — сначала просим модель придумать лучший промт, затем выполняем его.  
4. **Expert panel** — симулируем группу экспертов, которые обсуждают задачу и формируют общий вывод.

## План реализации

- [ ] Задать базовую логическую задачу (можно захардкодить для удобства).  
- [ ] Реализовать доменный агент, который будет запускать все четыре режима.  
- [ ] Для «prompt from other AI» предусмотреть дополнительный запрос к LLM для генерации промта.  
- [ ] Для «expert panel» описать роли экспертов и объединить их выводы в финальный ответ.  
- [ ] На фронтенде отобразить ответы всех режимов и блок сравнения результатов.  
- [ ] Сохранить/логировать JSON запросы и ответы, чтобы можно было анализировать различия.

## Структура проекта

```
lesson-04-multi-reasoning/
├── server/
│   └── src/main/kotlin/com/prike/
│       ├── Main.kt
│       ├── Config.kt
│       ├── domain/
│       │   ├── agent/
│       │   │   ├── BaseAgent.kt
│       │   │   └── (будущий) ReasoningAgent.kt
│       │   └── exception/
│       ├── data/
│       │   ├── client/ (OpenAIClient и др.)
│       │   ├── repository/
│       │   ├── mapper/
│       │   └── dto/
│       ├── presentation/
│       │   ├── controller/
│       │   └── dto/
│       └── di/
├── client/ (index.html, style.css, app.js)
├── config/
│   ├── ai.yaml.example
│   └── (при необходимости) reasoning.yaml
├── .env.example
└── README.md (этот файл)
```

## Шаги по запуску

1. Убедитесь, что в корне репозитория есть `.env` с `OPENAI_API_KEY`.  
2. Скопируйте `config/ai.yaml.example` → `config/ai.yaml` и впишите параметры модели.  
3. Запустите сервер:
   ```bash
   cd lesson-04-multi-reasoning/server
   ./gradlew run
   ```
4. Откройте `http://localhost:8080` — должен отдаться клиент из `client/`.

## Следующие задачи

- Добавить доменные и клиентские DTO для фиксации результатов каждого режима.  
- Подготовить UI: ввод задачи, запуск режимов, таблица сравнений.  
- Реализовать финальный отчёт: какой подход дал наиболее корректный ответ и почему.  

Этот README будет дополняться по мере выполнения задания.
