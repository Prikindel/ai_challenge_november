# 🧠 Теория: RAG (Retrieval-Augmented Generation)

## Что такое RAG?

**RAG (Retrieval-Augmented Generation)** — это техника, которая объединяет поиск информации (Retrieval) с генерацией текста (Generation). Вместо того чтобы полагаться только на знания LLM, система сначала ищет релевантную информацию в базе знаний, а затем использует её как контекст для генерации ответа.

### Ключевая идея:

**Вопрос → Поиск в базе знаний → Контекст → Генерация ответа LLM**

---

## Зачем нужен RAG?

### Проблемы обычных LLM:

1. **Ограниченные знания:**
   - LLM обучена на данных до определённой даты
   - Не знает актуальную информацию
   - Не знает специфичную документацию

2. **Галлюцинации:**
   - LLM может выдумывать информацию
   - Неточные детали
   - Несуществующие факты

3. **Отсутствие источников:**
   - Нельзя проверить, откуда взялась информация
   - Нет ссылок на документы

### Решение RAG:

1. **Актуальная информация:**
   - Использует информацию из базы знаний
   - Можно обновлять документы без переобучения модели

2. **Точность:**
   - Ответы основаны на реальных документах
   - Меньше галлюцинаций

3. **Источники:**
   - Можно показать, откуда взялась информация
   - Прозрачность ответов

---

## Как работает RAG?

### Пайплайн RAG:

```
1. Пользователь задаёт вопрос
   ↓
2. Генерация эмбеддинга для вопроса
   ↓
3. Поиск релевантных чанков в базе знаний
   (семантический поиск по эмбеддингам)
   ↓
4. Получение текста найденных чанков из БД
   ↓
5. Формирование промпта с контекстом:
   "Контекст: [чанки]
   Вопрос: [вопрос]
   Ответь на основе контекста."
   ↓
6. Запрос к LLM с контекстом
   ↓
7. Генерация ответа на основе контекста
```

### Пример:

**Вопрос:** "Как создать MCP сервер?"

**Шаг 1:** Генерация эмбеддинга для вопроса
```
Эмбеддинг: [0.123, -0.456, 0.789, ...]
```

**Шаг 2:** Поиск в базе знаний
```
Найдено 3 релевантных чанка:
- Чанк 1: сходство 0.89 (о создании MCP сервера)
- Чанк 2: сходство 0.76 (примеры кода)
- Чанк 3: сходство 0.65 (настройка конфигурации)
```

**Шаг 3:** Формирование промпта
```
Контекст из базы знаний:

[Чанк 1] (документ: documents/01-mcp-server-creation.md)
Как создать MCP сервер? Сначала нужно установить SDK...

[Чанк 2] (документ: documents/01-mcp-server-creation.md)
...установить SDK. Затем создать класс Server...

[Чанк 3] (документ: documents/02-embeddings-and-vector-search.md)
Для генерации эмбеддингов используйте Ollama...

Вопрос: Как создать MCP сервер?

Ответь на основе предоставленного контекста.
```

**Шаг 4:** Генерация ответа LLM
```
Ответ: Для создания MCP сервера нужно:
1. Установить SDK
2. Создать класс Server
3. Настроить конфигурацию
...
```

---

## Компоненты RAG-системы

### 1. База знаний (Knowledge Base)

**Что это:**
- Хранилище документов с эмбеддингами
- Индексированные чанки для быстрого поиска
- Метаданные (источники, даты, категории)

**Из урока 15:**
- SQLite база данных
- Таблицы `documents` и `document_chunks`
- Эмбеддинги для семантического поиска

### 2. Поиск (Retrieval)

**Что это:**
- Семантический поиск по эмбеддингам
- Ранжирование по релевантности (косинусное сходство)
- Выбор топ-K наиболее релевантных чанков

**Алгоритм:**
1. Генерация эмбеддинга для вопроса
2. Поиск похожих чанков в базе знаний
3. Сортировка по сходству
4. Выбор топ-K результатов

### 3. Генерация (Generation)

**Что это:**
- Формирование промпта с контекстом
- Запрос к LLM с контекстом
- Генерация ответа на основе контекста

**Промпт:**
```
Системное сообщение: "Ты — помощник, который отвечает на вопросы на основе контекста."

Контекст:
[Чанк 1]
[Чанк 2]
[Чанк 3]

Вопрос: [вопрос пользователя]

Инструкции:
- Отвечай только на основе контекста
- Если информации нет, скажи об этом
- Используй конкретные детали
```

---

## Сравнение: RAG vs Обычный режим

### Обычный режим (без RAG):

**Промпт:**
```
Вопрос: Как создать MCP сервер?
```

**Ответ LLM:**
```
MCP сервер можно создать, используя соответствующий SDK...
(общие знания LLM, может быть неточным)
```

**Проблемы:**
- Может быть неточным
- Может устареть
- Нет источников

### RAG-режим (с контекстом):

**Промпт:**
```
Контекст: [релевантные чанки из базы знаний]

Вопрос: Как создать MCP сервер?
```

**Ответ LLM:**
```
Для создания MCP сервера нужно:
1. Установить SDK (из документации)
2. Создать класс Server (конкретный пример)
3. Настроить конфигурацию (детали из документа)
...
```

**Преимущества:**
- Точные детали из документации
- Актуальная информация
- Можно показать источники

---

## Когда RAG помогает?

### ✅ RAG помогает, когда:

1. **Вопросы по документации:**
   - "Как использовать API?"
   - "Какие параметры у функции?"
   - "Как настроить конфигурацию?"

2. **Технические детали:**
   - Конкретные примеры кода
   - Точные инструкции
   - Специфичная информация

3. **Актуальная информация:**
   - Последние обновления
   - Текущие версии
   - Новые функции

4. **Специфичные знания:**
   - Внутренняя документация
   - Корпоративные стандарты
   - Проектная документация

### ❌ RAG не помогает, когда:

1. **Вопросы вне области документов:**
   - "Как приготовить борщ?" (не в документах)
   - "Что такое квантовая физика?" (не в документах)

2. **Общие вопросы:**
   - "Что такое программирование?" (LLM может ответить сам)
   - "Как работает интернет?" (общие знания)

3. **Устаревшая информация:**
   - Документы не обновлены
   - Информация устарела

4. **Нерелевантный контекст:**
   - Найдены нерелевантные чанки
   - Контекст не соответствует вопросу

---

## Параметры RAG

### 1. Top-K (количество чанков)

**Что это:**
- Количество наиболее релевантных чанков для контекста

**Рекомендации:**
- **Маленький (1-3):** Высокая точность, меньше контекста
- **Средний (3-5):** Баланс между точностью и полнотой
- **Большой (5-10):** Больше контекста, но может быть шум

**Пример:**
```kotlin
topK = 3  // использовать 3 наиболее релевантных чанка
```

### 2. Минимальное сходство (Min Similarity)

**Что это:**
- Порог релевантности для включения чанка в контекст

**Рекомендации:**
- **Высокий (>0.8):** Только очень релевантные чанки
- **Средний (0.5-0.8):** Релевантные чанки
- **Низкий (<0.5):** Может включить нерелевантные чанки

**Пример:**
```kotlin
minSimilarity = 0.5  // включать только чанки с сходством >0.5
```

### 3. Максимальная длина контекста

**Что это:**
- Максимальная длина контекста в токенах

**Рекомендации:**
- Зависит от модели LLM
- Обычно 1000-4000 токенов
- Нужно оставить место для ответа

**Пример:**
```kotlin
maxContextLength = 2000  // максимум 2000 токенов контекста
```

---

## Формирование промпта

### Структура промпта:

```
{Системное сообщение}

Контекст из базы знаний:

{Чанк 1}
{Чанк 2}
{Чанк 3}

Вопрос: {вопрос пользователя}

Инструкции:
- Отвечай только на основе контекста
- Если информации нет, скажи об этом
- Используй конкретные детали
- Если информация противоречива, укажи на это

Ответ:
```

### Формат чанков:

```
[Чанк 1] (документ: documents/01-mcp-server-creation.md, сходство: 0.89)
{содержимое чанка}

[Чанк 2] (документ: documents/01-mcp-server-creation.md, сходство: 0.76)
{содержимое чанка}
```

**Почему важно:**
- Показывает источники информации
- Показывает релевантность (сходство)
- Помогает LLM понять контекст

---

## Оценка качества RAG

### Метрики:

1. **Релевантность:**
   - Найденные чанки соответствуют вопросу?
   - Сходство >0.5?

2. **Точность:**
   - Ответ содержит правильную информацию?
   - Основан на контексте?

3. **Полнота:**
   - Ответ содержит все необходимые детали?
   - Нет пропущенной информации?

4. **Конкретность:**
   - Ответ содержит конкретные примеры?
   - Есть детали из документов?

### Сравнение ответов:

**RAG vs Обычный режим:**
- RAG более точный? ✅
- RAG более полный? ✅
- RAG более конкретный? ✅
- RAG менее релевантный? ❌

---

## Ограничения RAG

### 1. Качество базы знаний

**Проблема:**
- Если документы неточные → ответы неточные
- Если документы устарели → ответы устарели

**Решение:**
- Регулярно обновлять документы
- Проверять качество индексации

### 2. Качество поиска

**Проблема:**
- Если поиск находит нерелевантные чанки → ответы нерелевантные
- Если поиск не находит нужные чанки → ответы неполные

**Решение:**
- Улучшать эмбеддинги
- Настраивать параметры поиска (topK, minSimilarity)

### 3. Ограничения LLM

**Проблема:**
- LLM может игнорировать контекст
- LLM может галлюцинировать даже с контекстом

**Решение:**
- Улучшать промпты
- Добавлять инструкции для LLM

### 4. Размер контекста

**Проблема:**
- Слишком много контекста → перегрузка LLM
- Слишком мало контекста → неполные ответы

**Решение:**
- Настраивать topK
- Ограничивать длину контекста

---

## Лучшие практики

### 1. Качество документов

- **Актуальность:** Регулярно обновляйте документы
- **Полнота:** Включайте всю необходимую информацию
- **Структура:** Хорошо структурированные документы легче индексировать

### 2. Параметры поиска

- **TopK:** Начните с 3-5, настройте под задачу
- **MinSimilarity:** Начните с 0.5, настройте под задачу
- **ChunkSize:** 500-1000 токенов для большинства задач

### 3. Формирование промпта

- **Чёткие инструкции:** Указывайте LLM, как использовать контекст
- **Источники:** Показывайте источники информации
- **Релевантность:** Показывайте сходство для понимания качества

### 4. Обработка ошибок

- **Нет релевантных чанков:** Предупреждайте пользователя
- **Файл не найден:** Логируйте и пропускайте
- **LLM недоступен:** Показывайте понятную ошибку

---

## Выводы

1. **RAG** — техника объединения поиска информации с генерацией текста
2. **Преимущества:** Актуальная информация, точность, источники
3. **Когда помогает:** Вопросы по документации, технические детали
4. **Когда не помогает:** Вопросы вне области документов, общие вопросы
5. **Параметры:** TopK, minSimilarity, maxContextLength
6. **Оценка:** Релевантность, точность, полнота, конкретность

---

## Дополнительные ресурсы

- [RAG: Retrieval-Augmented Generation (Paper)](https://arxiv.org/abs/2005.11401)
- [LangChain RAG Tutorial](https://python.langchain.com/docs/use_cases/question_answering/)
- [Building RAG Applications](https://www.pinecone.io/learn/retrieval-augmented-generation/)
- [RAG Best Practices](https://www.anyscale.com/blog/rag-best-practices)

