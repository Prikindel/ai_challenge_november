Ты — разработчик, создающий урок «🔥 День 16. Первый RAG-запрос» в модуле `lesson-16-rag-query`.

Полное техническое задание находится в файле `PROMPT.md`. Прочитай его внимательно перед началом работы.

## Краткое описание задачи

Реализовать RAG (Retrieval-Augmented Generation) систему: вопрос → поиск релевантных чанков → объединение с вопросом → запрос к LLM. Сравнить ответы модели с RAG и без RAG, сделать выводы о том, где RAG помогает, а где — нет.

**Ключевые требования:**
1. Интеграция с базой знаний из урока 15 (SQLite с таблицами `documents` и `document_chunks`)
2. Поиск релевантных чанков по семантическому сходству
3. Получение текста чанков из базы знаний (поле `content`)
4. Формирование промпта с контекстом из найденных чанков
5. Генерация ответов с RAG (с контекстом) и без RAG (обычный режим)
6. Сравнение ответов и анализ преимуществ/недостатков RAG

## Пайплайн RAG-запроса

```
1. Пользователь задаёт вопрос
   ↓
2. Генерация эмбеддинга для вопроса
   ↓
3. Поиск релевантных чанков в базе знаний (косинусное сходство)
   ↓
4. Получение текста чанков из базы знаний (поле content)
   ↓
5. Формирование промпта с контекстом из чанков
   ↓
6. Запрос к LLM с контекстом (RAG-режим)
   ↓
7. Запрос к LLM без контекста (обычный режим)
   ↓
8. Сравнение ответов и выводы
```

## Структура коммитов

Реализуй пошагово, каждый шаг — отдельный git коммит:

1. **Коммит 1:** Интеграция с базой знаний (KnowledgeBaseService)
2. **Коммит 2:** RAG-сервис (RAGService, PromptBuilder)
3. **Коммит 3:** Сравнение ответов (ComparisonService, ResponseAnalyzer)
4. **Коммит 4:** API для RAG-запросов (RAGController, DTO)
5. **Коммит 5:** UI для RAG-запросов (rag.html, compare.html)
6. **Коммит 6:** Конфигурация и документация

## Важные моменты

1. **База знаний из урока 15:**
   - Должна быть проиндексирована база знаний
   - SQLite база данных с таблицами `documents` и `document_chunks`
   - В БД хранится полный текст чанков в поле `content` таблицы `document_chunks`
   - Текст чанков берётся напрямую из БД, не нужно извлекать из файлов
   - Путь к файлу берётся из таблицы `documents.file_path` (для отображения источника)

2. **Получение текста чанков:**
   - Текст чанков берётся напрямую из БД (поле `content` таблицы `document_chunks`)
   - Не нужно извлекать текст из файлов
   - Все данные уже есть в базе знаний

3. **Ollama:**
   - Должна быть установлена и запущена: https://ollama.ai/
   - Загружена модель для генерации ответов: `ollama pull llama3.2` (или другая)
   - Проверка: `curl http://localhost:11434/api/tags`

4. **Формирование промпта:**
   - Используй структуру: системное сообщение + контекст из чанков + вопрос + инструкции
   - Показывай источники информации (путь к документу, сходство)
   - Форматируй контекст для удобства чтения LLM

5. **Сравнение ответов:**
   - Генерируй оба ответа (RAG и STANDARD)
   - Анализируй различия (релевантность, точность, полнота)
   - Выявляй случаи, когда RAG помог (более точные ответы, конкретные детали)
   - Выявляй случаи, когда RAG не помог (устаревшая информация, нерелевантный контекст)

6. **Конфигурация:**
   - Вся конфигурация хранится в `config/server.yaml` (не в `.env`)
   - `.env` файл не обязателен, используется только для опционального переопределения
   - Параметры: `ollama.baseUrl`, `ollama.chatModel`, `knowledgeBase.databasePath`, `rag.topK`, `rag.minSimilarity`

7. **Обработка ошибок:**
   - Если база знаний недоступна — показывай понятную ошибку
   - Если LLM недоступен — показывай понятную ошибку
   - Если чанк не найден в БД — логируй и пропускай
   - Если нет релевантных чанков — предупреждай пользователя

## Технологии

- Kotlin + Ktor + Clean Architecture
- SQLite для работы с базой знаний из урока 15
- Ollama API для генерации ответов LLM
- Vanilla JS для фронтенда

## Документация

- `PROMPT.md` — полное техническое задание
- `THEORY.md` — теория по RAG, поиску, генерации
- `README.md` — описание урока

## Цели урока

- Научиться реализовывать RAG-запросы
- Понять, когда RAG помогает, а когда — нет
- Сравнить ответы с RAG и без RAG
- Интегрировать базу знаний в агента

Начни с чтения `PROMPT.md` и `THEORY.md`, затем приступай к реализации по шагам.

