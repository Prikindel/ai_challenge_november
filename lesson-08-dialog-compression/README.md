# 🔥 День 8. Сжатие диалога

## Цель модуля

Построить автономный пример, в котором агент ведёт диалог с LLM, динамически сжимает историю и показывает экономию токенов относительно «полного» контекста.

## Основные возможности

- автоматическое создание summary после каждых `N` сообщений;
- хранение истории в виде «сырых» сообщений и summary-узлов;
- сбор отчёта о контексте, попавшем в запрос (summary + raw);
- учёт метрик токенов, включая гипотетический промпт без сжатия;
- контрольный прогон сценария с режимами «без компрессии»/«со компрессией»;
- визуализация диалога, summary, истории запусков и экономии токенов.

## Демонстрация

[Видео с демонстрацией работы модуля](https://disk.yandex.ru/i/bYj8v5fL8jVsHw)

## Структура модуля

```
lesson-08-dialog-compression/
├── README.md                 # Общее описание, запуск, проверка
├── THEORY.md                 # Полная теория по компрессии диалогов
├── example.env               # Шаблон окружения
├── config/
│   └── compression.yaml      # Интервалы, лимиты, сценарии
├── server/                   # Kotlin + Ktor + Clean Architecture
└── client/                   # Vanilla JS интерфейс
```

## Конфигурация

1. Скопируйте `example.env` в `.env` внутри директории урока и заполните `OPENAI_API_KEY`. При необходимости укажите `SERVER_PORT`.
2. В `config/compression.yaml` задайте параметры:
   - `summaryInterval` — сколько пользовательских сообщений накапливать перед созданием summary;
   - `maxSummariesInContext` — сколько summary включать в промпт (ограничение контекста);
   - `rawHistoryLimit` — глубина несжатой истории, попадающей в промпт;
   - `compressionModel` и `compressionPromptTemplate` — модель и инструкция для сжатия;
   - `scenarios` — сценарии для контрольного прогона (последовательности сообщений).

## Запуск

```bash
cd lesson-08-dialog-compression/server
./gradlew clean run
```

Сервер поднимает Ktor-приложение, отдаёт статические файлы из `client/` и предоставляет API:
- `POST /api/dialog-compression/message` — отправка сообщения в диалог
- `POST /api/dialog-compression/reset` — сброс состояния диалога
- `GET /api/dialog-compression/state` — получение текущего состояния
- `GET /api/dialog-compression/scenarios` — список доступных сценариев
- `GET /api/dialog-compression/scenario/{id}` — детали конкретного сценария для клиентского выполнения

## Настройки интерфейса

В верхней форме доступны параметры, которые напрямую влияют на работу агента:

- `Интервал summary` — сколько пользовательских сообщений накапливаем перед попыткой сжатия. Считаются только сообщения пользователя (не ответы ассистента). Значение передаётся как `summaryIntervalOverride` и может превышать конфигурированное значение.
- `Макс. summary в контексте` — ограничение на количество summary-узлов, которые добавляются в промпт. Сбрасывает параметр `maxSummariesInContext`.
- `Стратегия суммирования` — выбор между двумя подходами:
  - **Независимая** — каждое summary создаётся только из новых, несуммаризированных сообщений. Все summary хранятся отдельно и включаются в контекст.
  - **Кумулятивная** — каждое новое summary включает предыдущие summary, создавая единый эволюционирующий конспект. В контекст попадает только последнее summary.
- `Режим` — переключатель между обычным диалогом и «Контрольным прогоном». В обычном режиме пользователь отправляет сообщения вручную. В режиме сравнения отправка блокируется, а истории воспроизводятся автоматически по сценарию при нажатии на кнопку «Запустить контрольный прогон».

Карточки справа отображают результаты:

- **Метрики запроса** — фактические токены и состав контекста (сколько summary и raw сообщений пошло в LLM). Оценка «гипотетический промпт» появляется только после первого summary, чтобы не показывать ложную экономию.
- **Контрольный прогон** — выбор сценария и сравнительный отчёт «без компрессии / со компрессией». Контрольный прогон выполняется на клиенте пошагово: каждое сообщение отправляется и обрабатывается последовательно, имитируя реальный диалог.
- **История запусков** — список запросов с превью, контекстом и накопленной экономией токенов.
- В диалоге архивные сообщения помечаются бейджем «В summary», а после блока сообщений, попавших в свёртку, появляется индикатор с номером summary и экономией токенов для этого блока.
- В модальном окне summary показаны исходные/сжатые токены и экономия для каждой свёртки.

## Проверка сценариев

1. Откройте `http://localhost:8080/` (или порт из `.env`).
2. Отправьте сообщения в обычном режиме — в карточке «Метрики» появятся токены и экономия, а история диалога отметит, какие сообщения вошли в текущий контекст.
3. Наблюдайте за summary в модальном окне: новые блоки появятся после каждых `summaryInterval` пользовательских сообщений.
4. Попробуйте разные стратегии суммирования:
   - **Независимая** — каждое summary создаётся отдельно, все summary включаются в контекст
   - **Кумулятивная** — summary эволюционирует, включая предыдущие, в контекст попадает только последнее
5. Запустите контрольный прогон — выберите сценарий, установите параметры (интервал summary, макс. summary в контексте, стратегию) и нажмите «Запустить контрольный прогон». Сначала выполнится прогон без сжатия (базовая линия), затем со сжатием с указанными параметрами. Интерфейс покажет сравнительный отчёт по токенам, количеству summary и экономию.
6. Во время контрольного прогона UI воспроизводит диалог пошагово: для каждого сообщения видно обращение к модели и ответ, как в обычном чате.
7. Изучите накопительную экономию токенов в карточке «История запусков».

## Выводы

- **Экономия токенов зависит от стратегии суммирования**: независимая стратегия создаёт отдельные summary-узлы для каждого блока сообщений, что позволяет точно контролировать, сколько summary попадает в контекст. Кумулятивная стратегия создаёт единый эволюционирующий конспект, который на короткой дистанции занимает меньше токенов, но может потерять детали при длительных диалогах.

- **Суммирование влияет на скорость ответа**: чем меньше токенов в промпте, тем быстрее модель обрабатывает запрос и генерирует ответ. При правильной настройке интервала суммирования можно значительно сократить время ожидания ответа, особенно в длинных диалогах.

- **Качество суммирования критично для качества ответов**: правильно построенная суммизация сохраняет ключевые факты, открытые вопросы и контекст диалога, позволяя модели давать релевантные и точные ответы. Некачественная суммизация может привести к тому, что модель будет искать ответы в системных промптах summary вместо того, чтобы анализировать текущий вопрос, что снижает качество диалога.

- **Кумулятивная суммизация эффективнее на короткой дистанции**: на начальных этапах диалога кумулятивная стратегия сохраняет больше деталей и занимает меньше токенов, чем последовательная независимая суммизация. Однако при длительных диалогах единый конспект может стать слишком объёмным и потерять структурированность.

- **Последовательная независимая суммизация имеет ограничения масштабирования**: при большом количестве сообщений количество summary-узлов растёт линейно, что в конечном итоге может привести к переполнению контекста, если не установлены ограничения на `maxSummariesInContext`. Требуется балансировка между экономией токенов и сохранением релевантного контекста.

- **Потеря контекста между summary-узлами**: в независимой стратегии каждый summary создаётся изолированно, что может привести к потере связей между разными блоками диалога. Модель может не увидеть связи между темами, обсуждёнными в разных summary, что требует тщательной настройки промптов для суммирования и включения в контекст достаточного количества summary-узлов.

Для теоретических пояснений по компрессии диалогов см. `THEORY.md`.
