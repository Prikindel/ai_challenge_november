# Теория: Автоматизация ревью кода с RAG и MCP

## 📖 Введение

Автоматизация ревью кода объединяет несколько важных компонентов:
1. **Git MCP** — получение diff и изменённых файлов из PR
2. **RAG-поиск** — проверка кода против документации проекта
3. **LLM анализ** — генерация структурированного ревью
4. **Структурированный вывод** — парсинг JSON ответов от LLM

Это позволяет создавать системы, которые:
- Автоматически анализируют изменения в коде
- Проверяют соответствие стилю и best practices проекта
- Находят потенциальные баги и проблемы безопасности
- Предлагают конкретные улучшения
- Используют документацию проекта для контекста

---

## 🎯 Проблема: Ручное ревью vs Автоматическое ревью

### Ручное ревью (медленно и не всегда объективно)

**Пример:**
```
Разработчик создаёт PR с изменениями
  ↓
Ревьюер просматривает diff вручную
  ↓
Ищет проблемы: баги, стиль, безопасность
  ↓
Пишет комментарии вручную
  ↓
Разработчик исправляет замечания
  ↓
Повторное ревью...
```

**Проблемы:**
- Занимает много времени
- Зависит от опыта ревьюера
- Может пропустить проблемы
- Не всегда учитывает документацию проекта
- Субъективность оценок

### Автоматическое ревью (быстро и объективно)

**Пример:**
```
Разработчик создаёт PR
  ↓
Система автоматически получает diff через MCP
  ↓
Проверяет код против документации через RAG
  ↓
LLM анализирует изменения и генерирует ревью
  ↓
Структурированный отчёт с проблемами и предложениями
  ↓
Разработчик видит все замечания сразу
```

**Преимущества:**
- Быстрое получение обратной связи
- Объективный анализ на основе правил проекта
- Не пропускает очевидные проблемы
- Использует документацию проекта для контекста
- Структурированный формат (JSON)

---

## 🏗️ Архитектура системы ревью

### Компоненты

1. **Git MCP сервер** — получение diff и файлов
2. **RAG MCP сервер** — поиск в документации проекта
3. **CodeReviewService** — оркестрация процесса ревью
4. **CodeReviewPromptBuilder** — формирование промптов
5. **LLMService** — генерация ревью через LLM
6. **CodeReviewController** — API endpoints
7. **UI** — интерфейс для запуска и просмотра ревью

### Пайплайн ревью

```
1. Получение diff
   ↓
   Git MCP: get_diff(base, head)
   ↓
2. Получение списка файлов
   ↓
   Git MCP: get_changed_files(base, head)
   ↓
3. Базовый анализ
   ↓
   - Подсчёт добавленных/удалённых строк
   - Определение типов файлов
   - Статистика изменений
   ↓
4. Проверка против документации
   ↓
   RAG MCP: rag_search_project_docs(query)
   ↓
   - Поиск правил стиля
   - Поиск best practices
   - Поиск архитектурных паттернов
   ↓
5. Формирование промпта
   ↓
   CodeReviewPromptBuilder:
   - Системный промпт (роль code reviewer)
   - Пользовательский промпт (diff + статистика + RAG-контекст)
   ↓
6. Генерация ревью
   ↓
   LLMService: generateAnswer(prompt)
   ↓
   - Анализ кода
   - Поиск проблем
   - Генерация предложений
   ↓
7. Парсинг результата
   ↓
   Парсинг JSON ответа:
   - issues (проблемы)
   - suggestions (предложения)
   - summary (резюме)
   - overallScore (оценка)
   ↓
8. Возврат результата
```

---

## 🔧 Технические детали

### Расширение Git MCP сервера

**Новые инструменты:**

1. **get_diff(base, head)**
   - Использует `git diff base..head`
   - Возвращает unified diff формат
   - Обрабатывает ошибки (ветки не найдены)

2. **get_changed_files(base, head)**
   - Использует `git diff --name-status base..head`
   - Возвращает список файлов со статусами (A/M/D/R/C)
   - Парсит статусы для извлечения путей

3. **get_file_content(filePath)**
   - Алиас для `read_file` с явным названием
   - Используется для получения содержимого файлов при ревью

### CodeReviewService

**Основные методы:**

- `reviewPR(base, head)` — ревью PR между ветками
- `reviewDiff(diff, changedFiles)` — ревью по diff файлу
- `analyzeDiff(diff, changedFiles)` — базовый анализ diff
- `checkAgainstDocs(diff, changedFiles)` — проверка через RAG
- `generateReview(...)` — генерация ревью через LLM
- `parseLLMResponse(...)` — парсинг JSON ответа

**Логика работы:**

1. Получает diff через Git MCP
2. Анализирует diff (статистика, типы файлов)
3. Ищет релевантную документацию через RAG
4. Формирует промпт с diff, статистикой и RAG-контекстом
5. Генерирует ревью через LLM
6. Парсит JSON ответ в структуру `CodeReview`

### CodeReviewPromptBuilder

**Методы:**

- `buildReviewPrompt(...)` — основной промпт для ревью
- `buildFileReviewPrompt(...)` — промпт для ревью файла
- `buildQuickReviewPrompt(...)` — быстрый промпт для больших PR

**Структура промпта:**

1. **Системный промпт:**
   - Роль: code reviewer
   - Инструкции по анализу
   - Формат ответа (JSON)
   - Типы проблем и серьёзность

2. **Пользовательский промпт:**
   - Список изменённых файлов
   - Статистика изменений
   - RAG-контекст из документации
   - Diff изменений (ограничен по размеру)

### Модели данных

**CodeReview:**
- `reviewId` — уникальный ID
- `baseBranch`, `headBranch` — ветки
- `changedFiles` — список файлов
- `issues` — найденные проблемы
- `suggestions` — предложения
- `summary` — резюме
- `overallScore` — общая оценка

**ReviewIssue:**
- `type` — тип (BUG, SECURITY, PERFORMANCE, STYLE, LOGIC, DOCUMENTATION)
- `severity` — серьёзность (critical, high, medium, low)
- `file` — путь к файлу
- `line` — номер строки
- `message` — описание проблемы
- `suggestion` — предложение по исправлению

**ReviewSuggestion:**
- `file` — путь к файлу
- `line` — номер строки
- `message` — описание предложения
- `priority` — приоритет (high, medium, low)

---

## 🎨 Типы проблем и их обнаружение

### BUG (Потенциальные баги)

**Что ищет LLM:**
- Утечки ресурсов (не закрытые файлы, соединения)
- Null pointer exceptions
- Неправильная обработка ошибок
- Проблемы с памятью

**Пример:**
```kotlin
// Проблема: файл не закрывается
val file = File("data.txt")
val content = file.readText()
// Нет file.close()

// Предложение: использовать use {}
File("data.txt").use { it.readText() }
```

### SECURITY (Проблемы безопасности)

**Что ищет LLM:**
- SQL injection
- XSS уязвимости
- Небезопасная обработка паролей
- Отсутствие валидации входных данных

**Пример:**
```kotlin
// Проблема: SQL injection
val query = "SELECT * FROM users WHERE name = '$name'"

// Предложение: использовать параметризованные запросы
val query = "SELECT * FROM users WHERE name = ?"
```

### PERFORMANCE (Проблемы производительности)

**Что ищет LLM:**
- Неоптимальные алгоритмы (O(n²) вместо O(n))
- N+1 запросы к БД
- Неэффективное использование памяти
- Отсутствие кэширования

**Пример:**
```kotlin
// Проблема: O(n²) сложность
for (item in items) {
    for (other in items) {
        if (item.id == other.id) { ... }
    }
}

// Предложение: использовать Set для O(1) поиска
val itemSet = items.toSet()
```

### STYLE (Стиль кода)

**Что ищет LLM:**
- Неправильное именование
- Слишком длинные функции
- Дублирование кода
- Несоблюдение конвенций проекта

**Пример:**
```kotlin
// Проблема: слишком длинная функция
fun processData() {
    // 200 строк кода...
}

// Предложение: разбить на меньшие функции
fun processData() {
    validateInput()
    transformData()
    saveData()
}
```

### LOGIC (Логические ошибки)

**Что ищет LLM:**
- Неправильная бизнес-логика
- Неправильные условия
- Пропущенные edge cases

**Пример:**
```kotlin
// Проблема: не обрабатывается случай, когда список пуст
fun getFirstItem(items: List<String>): String {
    return items[0]  // IndexOutOfBoundsException если пусто
}

// Предложение: добавить проверку
fun getFirstItem(items: List<String>): String? {
    return items.firstOrNull()
}
```

### DOCUMENTATION (Документация)

**Что ищет LLM:**
- Отсутствие комментариев
- Неясные имена функций
- Отсутствие документации для публичных API

**Пример:**
```kotlin
// Проблема: нет документации
fun calculate(x: Int, y: Int): Int {
    return x * y + 10
}

// Предложение: добавить KDoc
/**
 * Вычисляет значение по формуле: x * y + 10
 * @param x первый параметр
 * @param y второй параметр
 * @return результат вычисления
 */
fun calculate(x: Int, y: Int): Int {
    return x * y + 10
}
```

---

## 🔍 Использование RAG для контекста

### Зачем нужен RAG в ревью?

1. **Правила стиля проекта** — проверка соответствия конвенциям
2. **Best practices** — использование паттернов проекта
3. **Архитектурные решения** — соответствие архитектуре
4. **Примеры кода** — сравнение с существующим кодом

### Как работает RAG в ревью?

```
1. Формирование запроса для RAG
   ↓
   "стиль кода, архитектура проекта, best practices, правила разработки"
   ↓
2. Поиск в документации проекта
   ↓
   RAG MCP: rag_search_project_docs(query)
   ↓
3. Получение релевантных чанков
   ↓
   - Правила именования
   - Архитектурные паттерны
   - Примеры кода
   ↓
4. Добавление в промпт
   ↓
   CodeReviewPromptBuilder включает RAG-контекст в промпт
   ↓
5. LLM использует контекст для анализа
   ↓
   - Проверяет соответствие стилю
   - Сравнивает с best practices
   - Предлагает улучшения на основе документации
```

### Пример использования RAG-контекста

**Документация проекта:**
```
В проекте используется Clean Architecture:
- Domain слой не зависит от других слоёв
- Data слой зависит от Domain
- Presentation слой зависит от Domain и Data
```

**Изменение в коде:**
```kotlin
// Domain слой импортирует классы из Presentation
import com.prike.presentation.dto.UserDto  // ❌ Нарушение архитектуры
```

**Ревью с RAG-контекстом:**
```
Проблема: Нарушение архитектуры проекта
Файл: domain/UserService.kt
Сообщение: Domain слой не должен зависеть от Presentation слоя.
Согласно документации проекта, используется Clean Architecture,
где Domain слой не зависит от других слоёв.
Предложение: Использовать domain модели вместо DTO.
```

---

## 📊 Структурированный вывод от LLM

### Формат JSON ответа

```json
{
  "summary": "Краткое резюме ревью",
  "overallScore": "approve" | "request_changes" | "comment",
  "issues": [
    {
      "type": "BUG",
      "severity": "high",
      "file": "path/to/file.kt",
      "line": 42,
      "message": "Описание проблемы",
      "suggestion": "Предложение по исправлению"
    }
  ],
  "suggestions": [
    {
      "file": "path/to/file.kt",
      "line": 10,
      "message": "Описание предложения",
      "priority": "medium"
    }
  ]
}
```

### Парсинг ответа

1. **Извлечение JSON** из текстового ответа LLM
2. **Валидация структуры** — проверка наличия обязательных полей
3. **Парсинг issues** — преобразование в `ReviewIssue`
4. **Парсинг suggestions** — преобразование в `ReviewSuggestion`
5. **Fallback** — если парсинг не удался, создаётся упрощённое ревью

### Обработка ошибок

- Если LLM не вернул JSON — создаётся fallback ревью
- Если JSON невалидный — логируется ошибка, создаётся fallback
- Если отсутствуют обязательные поля — используются значения по умолчанию

---

## 🎯 Индексация кода для RAG

### Зачем индексировать код?

1. **Контекст существующего кода** — сравнение с паттернами проекта
2. **Примеры использования** — как правильно использовать API
3. **Архитектурные паттерны** — соответствие структуре проекта

### Конфигурация индексации

```yaml
indexing:
  codeFiles:
    enabled: false
    paths: ["server/src", "client"]
    extensions: [".kt", ".java", ".js", ".ts", ".py"]
    excludePatterns: ["**/build/**", "**/node_modules/**"]
```

### Методы индексации

- `indexCodeFiles(paths, extensions, excludePatterns)` — индексация директорий
- `indexCodeFilesList(filePaths)` — индексация списка файлов

### Использование в ревью

При ревью можно:
1. Индексировать изменённые файлы перед ревью
2. Использовать существующий код как контекст для RAG
3. Сравнивать изменения с паттернами проекта

---

## 🚀 UI для ревью кода

### Возможности UI

1. **Ревью по веткам:**
   - Указание базовой и целевой веток
   - Автоматическое получение diff

2. **Ревью по diff файлу:**
   - Загрузка diff файла
   - Вставка diff напрямую
   - Автоматическое извлечение файлов

3. **Отображение результатов:**
   - Цветовая индикация серьёзности
   - Группировка по типам проблем
   - Подсветка файлов и строк

4. **Экспорт ревью:**
   - JSON — полная структура
   - Markdown — форматированный отчёт
   - Текст — простой формат

### Асинхронная обработка

1. Запрос на ревью → `202 Accepted` с `reviewId`
2. Опрос статуса каждые 2 секунды
3. Отображение результатов при завершении

---

## 🔗 GitHub Action (опционально)

### Упрощённый подход

Вместо полного запуска сервера в CI/CD:
1. Собирается diff и список файлов
2. Оставляется комментарий в PR с информацией
3. Рекомендуется использовать UI для полного ревью

### Полный подход (сложнее)

1. Запуск сервера в CI/CD
2. Отправка запроса на ревью
3. Ожидание результата
4. Комментарий в PR с результатами

**Проблемы:**
- Требует настройки окружения
- Долгое время выполнения
- Сложность отладки

---

## 📈 Метрики качества ревью

### Что оценивает система

1. **Количество проблем** — сколько найдено
2. **Типы проблем** — распределение по типам
3. **Серьёзность** — критичные vs низкие
4. **Общая оценка** — approve/request_changes/comment

### Улучшение качества

1. **Точные промпты** — чёткие инструкции для LLM
2. **RAG-контекст** — использование документации проекта
3. **Примеры** — показ примеров хорошего кода
4. **Итерации** — улучшение промптов на основе результатов

---

## 🎓 Ключевые выводы

1. **Автоматизация ревью** — экономит время и повышает качество
2. **Git MCP** — удобный способ получения diff и файлов
3. **RAG для контекста** — проверка против документации проекта
4. **Структурированный вывод** — JSON формат для парсинга
5. **UI для удобства** — визуализация результатов ревью
6. **Типы проблем** — категоризация для лучшего понимания
7. **Серьёзность** — приоритизация замечаний
8. **Экспорт результатов** — сохранение для дальнейшего использования

---

## 🔮 Будущие улучшения

1. **Интеграция с GitHub** — автоматические комментарии в PR
2. **Обучение на примерах** — улучшение промптов
3. **Кастомные правила** — настройка под проект
4. **История ревью** — отслеживание улучшений
5. **Метрики** — статистика по ревью
6. **Интеграция с IDE** — плагины для редакторов
