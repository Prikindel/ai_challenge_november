# Примеры вопросов для ассистента разработчика

## Пример 1: Вопрос о API (использует RAG)

**Вопрос:**
```
/help как работает API?
```

**Что происходит:**
1. Система определяет команду `/help`
2. Выполняется RAG-поиск только в документации проекта (`project/docs/` и `project/README.md`)
3. Находятся релевантные чанки из `project/docs/api.md`
4. LLM генерирует ответ с цитатами из найденных чанков

**Ожидаемый ответ:**
```
API работает через следующие endpoints:

1. POST /api/chat/sessions - создание новой сессии чата
2. GET /api/chat/sessions - получение списка сессий
3. POST /api/chat/sessions/{sessionId}/messages - отправка сообщения

[Источник: API Документация](project/docs/api.md)
```

---

## Пример 2: Вопрос с fallback на MCP (если RAG не находит)

**Вопрос:**
```
/help покажи полное содержимое файла api.md
```

**Что происходит:**
1. Система определяет команду `/help`
2. Выполняется RAG-поиск, но не находится достаточно релевантных чанков
3. Система автоматически использует MCP инструмент `read_file` для чтения `project/docs/api.md`
4. Содержимое файла добавляется в системный промпт как дополнительный контекст
5. LLM генерирует ответ на основе прочитанного файла

**Ожидаемый ответ:**
```
Содержимое файла project/docs/api.md:

# API Документация

## Endpoints

### POST /api/chat/sessions
Создание новой сессии чата...

[Источник: project/docs/api.md]
```

---

## Пример 3: Вопрос о структуре проекта (использует list_directory)

**Вопрос:**
```
/help какие файлы есть в project/docs?
```

**Что происходит:**
1. Система определяет команду `/help`
2. RAG-поиск не находит релевантную информацию о списке файлов
3. Система использует MCP инструмент `list_directory` для получения списка файлов
4. LLM генерирует ответ на основе списка файлов

**Ожидаемый ответ:**
```
В директории project/docs находятся следующие файлы:

📄 api.md (2.5 KB) - документация API
📄 schema.md (1.8 KB) - схема базы данных

[Источник: MCP list_directory]
```

---

## Пример 4: Обычный вопрос (без /help)

**Вопрос:**
```
Как работает RAG в этом проекте?
```

**Что происходит:**
1. Система определяет, что это обычный вопрос (не команда `/help`)
2. Выполняется RAG-поиск во всех документах (включая `documents/` и `project/docs/`)
3. Находятся релевантные чанки из различных документов
4. LLM генерирует ответ с цитатами

**Ожидаемый ответ:**
```
RAG (Retrieval-Augmented Generation) в этом проекте работает следующим образом:

1. Документы индексируются и разбиваются на чанки
2. Для каждого чанка генерируется эмбеддинг
3. При запросе выполняется семантический поиск по эмбеддингам
4. Найденные чанки передаются в LLM для генерации ответа

[Источник: RAG Теория](documents/rag-theory.md)
[Источник: Архитектура](documents/architecture.md)
```

---

## Пример 5: Вопрос с историей диалога

**Диалог:**
```
Пользователь: /help что такое API?
Ассистент: API (Application Programming Interface) - это набор правил и протоколов...

Пользователь: А какие endpoints есть?
Ассистент: [отвечает с учетом предыдущего контекста об API]
```

**Что происходит:**
1. Первый вопрос обрабатывается через `/help` с RAG-поиском
2. Второй вопрос обрабатывается с учетом истории диалога
3. Система понимает контекст предыдущего вопроса об API
4. Выполняется RAG-поиск с учетом истории
5. LLM генерирует ответ, учитывая предыдущий контекст

---

## Технические детали

### MCP Инструменты

1. **get_current_branch** — получение текущей ветки git
   - Используется автоматически для каждого запроса
   - Кэшируется на 5 минут

2. **read_file(path: String)** — чтение файла проекта
   - Максимальный размер файла: 100KB
   - Путь указывается относительно корня проекта
   - Используется как fallback, если RAG не находит чанки

3. **list_directory(path: String)** — список файлов в директории
   - По умолчанию: текущая директория (`.`)
   - Возвращает список файлов и директорий с размерами

### Fallback механизм

Если для команды `/help`:
- RAG не находит релевантные чанки ИЛИ
- Найдено недостаточно чанков

То система автоматически:
1. Пытается прочитать `project/docs/api.md`, если вопрос содержит "API" или "api"
2. Иначе пытается прочитать `project/README.md`
3. Добавляет прочитанное содержимое в системный промпт как дополнительный контекст
