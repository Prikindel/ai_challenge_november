# 🔥 День 13. Композиция MCP-инструментов

Урок о том, как создать систему, где **LLM сама решает**, какие MCP инструменты вызывать и в каком порядке для выполнения сложных задач.

## 📋 Описание

В этом уроке мы создаём систему, где пользователь формулирует сложную задачу, а LLM разбивает её на шаги и последовательно вызывает MCP инструменты. Вместо программирования последовательности вызовов, мы даём LLM доступ к инструментам, и она сама строит "пайплайн" вызовов.

### Пример использования:

```
Пользователь: "Собери переписку за последние 24 часа и отправь мне в TG краткую сводку"

LLM сама решает:
1. Вызвать get_chat_history() для получения переписки
2. Суммаризировать полученную переписку
3. Вызвать send_telegram_message() для отправки сводки
4. Сообщить пользователю о результате
```

## 🎯 Цели урока

- Понять концепцию композиции MCP-инструментов
- Научиться создавать LLM агента, который управляет каскадными вызовами
- Реализовать систему, где LLM сама решает последовательность вызовов
- Обработать длительные операции с индикатором загрузки

## 🏗️ Архитектура

```
Пользователь
    ↓
LLMCompositionAgent (управляет циклом)
    ↓
LLM (с function calling)
    ↓
MCPToolAgent (преобразует MCP → LLM tools)
    ↓
MCPClientManager (управляет MCP серверами)
    ↓
MCP Servers (chat-history, telegram-sender)
```

## 📁 Структура проекта

```
lesson-13-mcp-composition/
├── README.md
├── THEORY.md
├── PROMPT.md
├── CHAT_PROMPT.txt
├── example.env
├── config/
│   ├── mcp-servers.yaml
│   └── server.yaml
├── mcp-servers/
│   ├── chat-history-mcp-server.jar  # Из lesson-12
│   └── telegram-sender-mcp-server.jar  # Новый
├── telegram-sender-mcp-server/
│   └── src/main/kotlin/...
├── server/
│   └── src/main/kotlin/...
└── client/
    ├── index.html
    ├── style.css
    └── app.js
```

## 🚀 Быстрый старт

### 1. Настройка окружения

Скопируй `example.env` в `.env` и заполни:

```env
OPENAI_API_KEY=your_openai_api_key
OPENROUTER_API_KEY=your_openrouter_api_key
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_USER_ID=your_telegram_user_id
```

### 2. Подготовка MCP серверов

#### Chat History MCP Server

Используй JAR из lesson-12 или пересобери:

```bash
cd ../lesson-12-reminder-mcp/chat-history-mcp-server
./gradlew shadowJar
cp build/libs/chat-history-mcp-server-1.0.0.jar ../lesson-13-mcp-composition/mcp-servers/
```

#### Telegram Sender MCP Server

Собери новый MCP сервер:

```bash
cd telegram-sender-mcp-server
./gradlew shadowJar
cp build/libs/telegram-sender-mcp-server-1.0.0.jar ../mcp-servers/
```

### 3. Запуск приложения

```bash
cd server
./gradlew run
```

Приложение запустится на `http://localhost:8080`

### 4. Открыть UI

Открой в браузере: `http://localhost:8080`

## 📖 Использование

### Примеры запросов:

1. **Простой запрос:**
   ```
   Собери переписку за последние 24 часа
   ```
   → LLM вызовет `get_chat_history()` и вернёт результат

2. **Сложный запрос (композиция):**
   ```
   Собери переписку за последние 24 часа и отправь мне в TG краткую сводку
   ```
   → LLM вызовет `get_chat_history()`, суммаризирует, вызовет `send_telegram_message()`

3. **Запрос с обработкой ошибок:**
   ```
   Отправь мне в TG сводку за последнюю неделю
   ```
   → Если нет переписки, LLM сообщит об этом

## 🔧 Конфигурация

### MCP Servers (`config/mcp-servers.yaml`)

```yaml
mcp:
  servers:
    - id: "chat-history"
      name: "Chat History MCP Server"
      jarPath: "../lesson-12-reminder-mcp/chat-history-mcp-server/build/libs/chat-history-mcp-server-1.0.0.jar"
      configPath: "../lesson-12-reminder-mcp/config/chat-history-mcp-server.yaml"
      tools:
        - get_chat_history
    
    - id: "telegram-sender"
      name: "Telegram Sender MCP Server"
      jarPath: "mcp-servers/telegram-sender-mcp-server.jar"
      configPath: "config/telegram-sender-mcp-server.yaml"
      tools:
        - send_telegram_message
```

## 🧪 Тестирование

### Тест каскадных вызовов:

```bash
# Отправь запрос через UI или curl:
curl -X POST http://localhost:8080/api/chat/message \
  -H "Content-Type: application/json" \
  -d '{"message": "Собери переписку за последние 24 часа и отправь мне в TG краткую сводку"}'
```

Ожидаемое поведение:
1. LLM вызывает `get_chat_history()`
2. Получает переписку
3. Суммаризирует
4. Вызывает `send_telegram_message()`
5. Отправляет сводку в Telegram
6. Возвращает финальный ответ пользователю

## 📚 Документация

- **THEORY.md** — теоретическая часть про композицию MCP-инструментов
- **PROMPT.md** — полное техническое задание для реализации
- **CHAT_PROMPT.txt** — краткий промпт для другого агента

## 🔍 Ключевые концепции

### 1. Композиция инструментов

LLM сама решает последовательность вызовов, а не мы программируем её.

### 2. Каскадные вызовы

Результат одного инструмента используется для следующего вызова.

### 3. Function Calling

Механизм, который позволяет LLM запрашивать вызов инструментов.

### 4. История диалога

LLM видит контекст предыдущих вызовов для принятия решений.

## ⚠️ Ограничения

- Максимальное количество итераций: 10 (защита от бесконечного цикла)
- Таймаут вызова инструмента: 30 секунд
- Размер истории диалога: последние 50 сообщений

## 🐛 Отладка

### Логирование:

Все вызовы инструментов логируются:

```
INFO: LLM requested tool: get_chat_history with args: {"startTime": 1234567890, "endTime": 1234654290}
INFO: Tool result: [{"role": "user", "content": "..."}]
INFO: LLM requested tool: send_telegram_message with args: {"userId": "123", "message": "..."}
INFO: Tool result: {"success": true, "messageId": 456}
```

### Проверка MCP серверов:

```bash
# Проверь, что JAR файлы существуют
ls -la mcp-servers/

# Проверь конфигурацию
cat config/mcp-servers.yaml
```

## 📝 Чеклист реализации

- [ ] MCP Server с инструментом отправки в Telegram
- [ ] MCP клиент и менеджер для работы с несколькими серверами
- [ ] LLM агент с каскадными вызовами
- [ ] API для обработки запросов пользователя
- [ ] UI с индикатором загрузки
- [ ] Обработка ошибок и таймаутов
- [ ] Логирование всех вызовов инструментов

## 🎓 Что изучили

1. Как создать систему, где LLM управляет последовательностью вызовов
2. Как преобразовать MCP инструменты в формат LLM tools
3. Как реализовать каскадные вызовы инструментов
4. Как обработать длительные операции с индикатором загрузки
5. Как обработать ошибки и передать их в LLM для принятия решений

## 🔗 Связанные уроки

- **Lesson 11** — Первый MCP инструмент (базовая интеграция)
- **Lesson 12** — Планировщик + MCP (автоматическая суммаризация)
- **Lesson 13** — Композиция MCP-инструментов (каскадные вызовы)

---

**Готово к использованию!** 🚀

