Реализуй урок "🔥 День 13. Композиция MCP-инструментов" в модуле lesson-13-mcp-composition.

Полное техническое задание находится в файле: lesson-13-mcp-composition/PROMPT.md
Теория находится в файле: lesson-13-mcp-composition/THEORY.md

Краткое описание задачи:
- Создать систему, где LLM сама решает, какие MCP инструменты вызывать и в каком порядке (каскадный вызов)
- Пользователь формулирует сложную задачу, LLM разбивает её на шаги и последовательно вызывает инструменты
- Пример: "Собери переписку за последние 24 часа и отправь мне в TG краткую сводку"
  → LLM вызывает get_chat_history → суммаризирует → вызывает send_telegram_message → отвечает пользователю

ВАЖНО: Реализуй пошагово, каждый шаг — отдельный git коммит:
1. Коммит 1: MCP Server - инструмент отправки в Telegram (send_telegram_message)
2. Коммит 2: Основное приложение - MCP клиент и менеджер (подключение к серверам через JAR)
3. Коммит 3: LLM агент с каскадными вызовами (LLM сама решает последовательность)
4. Коммит 4: API и обработка длительных операций (индикатор загрузки)
5. Коммит 5: UI - чат с индикатором загрузки

После каждого коммита покажи изменения пользователю, он закоммитит и скажет "приступай к следующему шагу".

Технологии:
- Backend: Kotlin + Ktor + Clean Architecture
- Frontend: HTML + CSS + Vanilla JavaScript
- MCP SDK: официальный Kotlin SDK (io.modelcontextprotocol:kotlin-sdk)
- LLM: OpenRouter API с function calling
- Telegram Bot API: библиотека для работы с Telegram
- База данных: используем существующие БД из lesson-12 (memory.db, summary.db)

Структура проекта:
- telegram-sender-mcp-server/ — MCP сервер с инструментом отправки в Telegram (отдельный процесс, JAR)
- server/ — основное приложение (MCP клиент, менеджер, LLM агент, API)
- client/ — веб-интерфейс (чат с индикатором загрузки)

Ключевые моменты:
- LLM полностью управляет последовательностью вызовов инструментов
- Мы не программируем последовательность, а даём LLM доступ к инструментам
- Каскадные вызовы: LLM → tool_call → MCP → результат → LLM (с историей) → финальный ответ
- Работаем только с JAR файлами MCP серверов (не делаем доп обработки в логике менеджера)
- Код разбит на логические части (MCP клиент, менеджер, агент, API, UI)
- Обработка длительных операций: индикатор загрузки в UI, неблокирующая обработка на сервере
- Защита от бесконечного цикла: ограничь максимальное количество итераций (например, 10)

Примерный flow:
1. Пользователь: "Собери переписку за последние 24 часа и отправь мне в TG краткую сводку"
2. Сервер отправляет запрос в LLM (с информацией о доступных MCP инструментах)
3. LLM анализирует задачу и решает: нужно вызвать get_chat_history
4. LLM вызывает MCP инструмент: get_chat_history(startTime, endTime)
5. MCP возвращает переписку → передаём результат обратно LLM (с учётом истории диалога)
6. LLM анализирует переписку и суммаризирует (используя свои возможности)
7. LLM решает: нужно отправить результат в Telegram
8. LLM вызывает MCP инструмент: send_telegram_message(userId, message)
9. MCP отправляет сообщение → возвращаем результат LLM (успех/ошибка)
10. LLM анализирует результат и решает, что делать дальше (успех → финальный ответ, ошибка → повторить или сообщить)
11. LLM возвращает финальный ответ → отправляем пользователю в UI чат

MCP инструменты:
- get_chat_history (из lesson-12, можно переиспользовать JAR или пересобрать)
- send_telegram_message (новая, создаём в Коммите 1)

Следуй инструкциям из PROMPT.md пошагово, начиная с Коммита 1.

