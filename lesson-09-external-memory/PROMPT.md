# 🔥 День 9. Внешняя память - Промпт для разработки

> **Начало работы**: Начните с этапа "Базовый набросок кода" (Этап 1). Создайте структуру проекта, определите модели данных и интерфейсы. Затем последовательно реализуйте хранилище, сервисы, агенты и UI. Используйте чеклист в конце документа для отслеживания прогресса.

## Цель модуля

Построить агента с долговременной памятью, который сохраняет промежуточные результаты диалога и контекст в SQLite или JSON-файл, обеспечивая доступность данных между запусками приложения.

## Основные требования

### Функциональность

1. **Внешняя память для модели**:
   - Сохранение истории диалога (сообщения пользователя и ответы ассистента)
   - Сохранение контекста и метаданных (временные метки, токены, метрики)
   - Поддержка двух вариантов хранения: SQLite (основной) и JSON-файл (альтернативный)
   - Возможность переключения между вариантами через конфигурацию

2. **Персистентность данных**:
   - Данные сохраняются между запусками приложения
   - При старте приложения данные загружаются из хранилища
   - Возможность сброса/очистки памяти через API
   - Валидация целостности данных при загрузке

3. **Управление памятью**:
   - Сохранение каждого сообщения и ответа
   - Загрузка истории при инициализации агента
   - Поиск и фильтрация по истории (по дате, по ключевым словам)
   - Ограничение размера истории (опционально, через конфигурацию)

### Архитектурные требования

1. **Модульная структура**:
   - ❌ **НЕ ДЕЛАТЬ**: Один большой класс-агент со всеми дата-классами и несколькими агентами в одном файле
   - ✅ **ДЕЛАТЬ**: Разделение на отдельные агенты, репозитории, сервисы и оркестратор
   - Каждый агент должен отвечать за свою конкретную задачу
   - Оркестратор координирует работу агентов, но не содержит бизнес-логику

2. **Структура агентов**:
   - **MemoryRepository** (data layer) - репозиторий для работы с хранилищем (SQLite/JSON)
   - **MemoryService** (domain layer) - сервис для управления памятью (сохранение, загрузка, поиск)
   - **ConversationAgent** (domain layer) - агент для работы с диалогом (отправка сообщений в LLM)
   - **MemoryOrchestrator** (domain layer) - оркестратор, который:
     - Координирует работу ConversationAgent и MemoryService
     - Управляет жизненным циклом диалога
     - Обеспечивает сохранение данных после каждого взаимодействия
     - **ВАЖНО**: Оркестратор должен быть понятным и не перегруженным лишним кодом

3. **Читаемость кода**:
   - Понятные имена классов и методов
   - Комментарии, объясняющие назначение каждого компонента
   - Разделение ответственности между слоями (data, domain, presentation)
   - Использование Clean Architecture принципов

### Этапы разработки

#### Этап 1: Базовый набросок кода

1. **Создать структуру проекта**:
   - Скопировать структуру из `lesson-08-dialog-compression` как базу
   - Создать необходимые директории и файлы
   - Настроить зависимости (SQLite драйвер, если используется)

2. **Определить модели данных**:
   - `MemoryEntry` - запись в памяти (id, role, content, timestamp, metadata)
   - `MemoryMetadata` - метаданные записи (токены, модель, и т.д.)
   - DTO для API (запросы и ответы)

3. **Создать интерфейсы репозиториев**:
   - `MemoryRepository` - интерфейс для работы с хранилищем
   - Методы: `save()`, `load()`, `find()`, `clear()`, `getAll()`

4. **Набросать структуру агентов**:
   - `MemoryRepository` (data) - реализация для SQLite и JSON
   - `MemoryService` (domain) - бизнес-логика работы с памятью
   - `ConversationAgent` (domain) - базовый агент для диалога
   - `MemoryOrchestrator` (domain) - координатор

#### Этап 2: Реализация хранилища

1. **SQLite реализация**:
   - Создать схему БД (таблица для сообщений)
   - Реализовать CRUD операции
   - Миграции (если нужны)

2. **JSON реализация**:
   - Реализовать сохранение/загрузку в JSON-файл
   - Обработка ошибок чтения/записи
   - Валидация структуры JSON

3. **Конфигурация**:
   - Параметр выбора типа хранилища (SQLite/JSON)
   - Путь к файлу БД или JSON
   - Настройки подключения (для SQLite)

#### Этап 3: Интеграция с диалогом

1. **MemoryService**:
   - Сохранение сообщений после каждого взаимодействия
   - Загрузка истории при старте
   - Поиск по истории (опционально)

2. **MemoryOrchestrator**:
   - Инициализация с загрузкой истории
   - Обработка нового сообщения:
     1. Загрузить историю из памяти
     2. Добавить новое сообщение пользователя
     3. Отправить в ConversationAgent
     4. Сохранить ответ ассистента
     5. Сохранить обновлённую историю
   - Метод сброса памяти

#### Этап 4: API и UI

1. **API endpoints**:
   - `POST /api/memory/message` - отправка сообщения (с сохранением)
   - `GET /api/memory/history` - получение истории
   - `POST /api/memory/reset` - сброс памяти
   - `GET /api/memory/stats` - статистика (количество записей, размер и т.д.)

2. **UI**:
   - Отображение истории диалога
   - Индикатор сохранения данных
   - Кнопка сброса памяти
   - Показ статистики памяти
   - Визуализация загрузки истории при старте

#### Этап 5: Теория и документация

1. **THEORY.md**:
   - Что такое внешняя память для LLM
   - Зачем нужна персистентность в диалогах
   - Сравнение SQLite vs JSON для хранения
   - Паттерны работы с долговременной памятью
   - Проблемы и решения (конфликты, миграции, производительность)
   - Best practices

2. **README.md**:
   - Описание модуля
   - Инструкции по запуску
   - Конфигурация
   - Примеры использования
   - Выводы и наблюдения

## Технические детали

### Стек технологий

- **Backend**: Kotlin, Ktor, Clean Architecture
- **Хранилище**: 
  - SQLite (через `org.xerial:sqlite-jdbc` драйвер)
  - JSON-файл (через `kotlinx-serialization-json`)
- **Frontend**: HTML, CSS, Vanilla JS
- **Конфигурация**: YAML + `.env`
- **Зависимости**: 
  - Ktor (server, client, serialization)
  - SQLite JDBC драйвер
  - Kotlinx Serialization
  - SnakeYAML для конфигурации
  - Dotenv для переменных окружения

### Структура проекта

```
lesson-09-external-memory/
├── README.md                 # Общее описание, запуск, проверка
├── THEORY.md                 # Полная теория по внешней памяти
├── PROMPT.md                 # Этот файл (промпт для разработки)
├── example.env               # Шаблон окружения
├── config/
│   ├── ai.yaml               # Конфигурация API
│   └── memory.yaml           # Конфигурация памяти (тип хранилища, путь, лимиты)
├── server/                   # Kotlin + Ktor + Clean Architecture
│   └── src/main/kotlin/com/prike/
│       ├── data/
│       │   ├── repository/
│       │   │   ├── MemoryRepository.kt        # Интерфейс
│       │   │   ├── SqliteMemoryRepository.kt  # SQLite реализация
│       │   │   └── JsonMemoryRepository.kt     # JSON реализация
│       │   └── model/
│       │       └── MemoryModels.kt            # Модели данных для хранения
│       ├── domain/
│       │   ├── agent/
│       │   │   ├── ConversationAgent.kt       # Агент для диалога с LLM
│       │   │   └── MemoryOrchestrator.kt       # Оркестратор (координация)
│       │   ├── service/
│       │   │   └── MemoryService.kt            # Сервис управления памятью
│       │   └── repository/
│       │       └── MemoryRepository.kt         # Интерфейс (domain layer)
│       ├── presentation/
│       │   ├── controller/
│       │   │   └── MemoryController.kt         # API endpoints
│       │   └── dto/
│       │       └── MemoryDtos.kt               # DTO для API
│       └── config/
│           └── MemoryConfig.kt                # Конфигурация памяти
└── client/                   # Vanilla JS интерфейс
    ├── index.html
    ├── style.css
    └── app.js
```

### Конфигурация (config/memory.yaml)

```yaml
memory:
  storageType: "sqlite"  # или "json"
  sqlite:
    databasePath: "data/memory.db"
  json:
    filePath: "data/memory.json"
  limits:
    maxEntries: null  # null = без ограничений
    maxHistoryDays: null  # null = без ограничений
```

## Критерии готовности

1. ✅ Данные сохраняются в выбранное хранилище (SQLite или JSON)
2. ✅ При перезапуске приложения история загружается из хранилища
3. ✅ Каждое сообщение и ответ сохраняются автоматически
4. ✅ Код разделён на отдельные агенты, сервисы и репозитории
5. ✅ Оркестратор понятный и не перегружен лишним кодом
6. ✅ Есть API для работы с памятью (отправка, получение истории, сброс)
7. ✅ UI показывает историю и позволяет взаимодействовать с агентом
8. ✅ Написана теория в THEORY.md
9. ✅ README содержит полное описание и инструкции
10. ✅ Код читаемый, с комментариями и понятными именами

## Важные замечания

1. **Не создавать монолитный класс**: Разделять ответственность между компонентами
2. **Оркестратор должен быть простым**: Только координация, без бизнес-логики
3. **Обработка ошибок**: Валидация данных, обработка ошибок БД/файловой системы
4. **Производительность**: Для больших объёмов данных использовать пагинацию
5. **Тестирование персистентности**: Проверить, что данные действительно сохраняются между запусками

## Примеры использования

### Сценарий 1: Базовый диалог с сохранением
1. Пользователь отправляет сообщение
2. Агент загружает историю из памяти
3. Отправляет запрос в LLM с историей
4. Сохраняет новое сообщение и ответ в память
5. Возвращает ответ пользователю

### Сценарий 2: Перезапуск приложения
1. Приложение запускается
2. MemoryOrchestrator загружает историю из хранилища
3. Пользователь может продолжить диалог с учётом предыдущей истории

### Сценарий 3: Сброс памяти
1. Пользователь нажимает "Сбросить память"
2. Вызывается API endpoint для очистки хранилища
3. История очищается, диалог начинается заново

## Детали реализации

### Зависимости (build.gradle.kts)

Добавить в зависимости для SQLite:
```kotlin
// SQLite JDBC драйвер
implementation("org.xerial:sqlite-jdbc:3.44.1.0")
```

### Пример структуры MemoryEntry

```kotlin
data class MemoryEntry(
    val id: String,
    val role: MessageRole,  // USER или ASSISTANT
    val content: String,
    val timestamp: Instant,
    val metadata: MemoryMetadata?
)

data class MemoryMetadata(
    val model: String?,
    val promptTokens: Int?,
    val completionTokens: Int?,
    val totalTokens: Int?
)
```

### Пример интерфейса MemoryRepository

```kotlin
interface MemoryRepository {
    suspend fun save(entry: MemoryEntry): Result<Unit>
    suspend fun loadAll(): Result<List<MemoryEntry>>
    suspend fun findById(id: String): Result<MemoryEntry?>
    suspend fun findByDateRange(start: Instant, end: Instant): Result<List<MemoryEntry>>
    suspend fun clear(): Result<Unit>
    suspend fun getStats(): Result<MemoryStats>
}

data class MemoryStats(
    val totalEntries: Int,
    val userMessages: Int,
    val assistantMessages: Int,
    val oldestEntry: Instant?,
    val newestEntry: Instant?
)
```

### Пример MemoryOrchestrator (упрощённый)

```kotlin
class MemoryOrchestrator(
    private val conversationAgent: ConversationAgent,
    private val memoryService: MemoryService
) {
    // Загрузка истории при инициализации
    suspend fun initialize() {
        memoryService.loadHistory()
    }
    
    // Обработка нового сообщения
    suspend fun handleMessage(userMessage: String): AgentResponse {
        // 1. Загрузить историю из памяти
        val history = memoryService.getHistory()
        
        // 2. Добавить новое сообщение пользователя
        val userEntry = memoryService.createUserEntry(userMessage)
        
        // 3. Отправить в ConversationAgent с историей
        val response = conversationAgent.respond(
            messages = history + userEntry.toMessageDto()
        )
        
        // 4. Сохранить ответ ассистента
        val assistantEntry = memoryService.createAssistantEntry(
            content = response.message,
            metadata = response.usage
        )
        
        // 5. Сохранить оба сообщения в память
        memoryService.saveEntries(listOf(userEntry, assistantEntry))
        
        return response
    }
    
    // Сброс памяти
    suspend fun reset() {
        memoryService.clear()
    }
}
```

**Важно**: Оркестратор НЕ должен содержать:
- Логику работы с БД/файлами (это в репозитории)
- Логику форматирования данных (это в сервисе)
- Детали реализации хранилища (это в репозитории)

Оркестратор ТОЛЬКО координирует вызовы агентов и сервисов.

## Чеклист разработки

### Этап 1: Подготовка
- [ ] Создать структуру директорий
- [ ] Настроить build.gradle.kts с зависимостями
- [ ] Создать конфигурационные файлы (memory.yaml.example, example.env)
- [ ] Определить модели данных (MemoryEntry, MemoryMetadata)

### Этап 2: Хранилище
- [ ] Создать интерфейс MemoryRepository
- [ ] Реализовать SqliteMemoryRepository
  - [ ] Создать схему БД
  - [ ] Реализовать CRUD операции
  - [ ] Обработка ошибок
- [ ] Реализовать JsonMemoryRepository
  - [ ] Сериализация/десериализация
  - [ ] Обработка ошибок чтения/записи
- [ ] Создать фабрику для выбора реализации по конфигурации

### Этап 3: Сервисы и агенты
- [ ] Создать MemoryService (domain layer)
  - [ ] Загрузка истории
  - [ ] Сохранение записей
  - [ ] Поиск и фильтрация
- [ ] Создать ConversationAgent (domain layer)
  - [ ] Отправка сообщений в LLM
  - [ ] Обработка ответов
- [ ] Создать MemoryOrchestrator
  - [ ] Инициализация с загрузкой истории
  - [ ] Обработка сообщений
  - [ ] Сброс памяти

### Этап 4: API и DI
- [ ] Настроить DI (AppModule)
  - [ ] Создание репозитория по конфигурации
  - [ ] Создание сервисов и агентов
  - [ ] Создание оркестратора
- [ ] Создать MemoryController
  - [ ] POST /api/memory/message
  - [ ] GET /api/memory/history
  - [ ] POST /api/memory/reset
  - [ ] GET /api/memory/stats
- [ ] Создать DTO для API

### Этап 5: UI
- [ ] Создать базовый HTML интерфейс
- [ ] Реализовать отправку сообщений
- [ ] Отображение истории диалога
- [ ] Индикатор загрузки истории при старте
- [ ] Кнопка сброса памяти
- [ ] Отображение статистики памяти

### Этап 6: Тестирование
- [ ] Проверить сохранение данных в SQLite
- [ ] Проверить сохранение данных в JSON
- [ ] Проверить загрузку истории при перезапуске
- [ ] Проверить работу сброса памяти
- [ ] Проверить обработку ошибок (недоступный файл, повреждённые данные)

### Этап 7: Документация
- [ ] Написать THEORY.md
- [ ] Написать README.md
- [ ] Добавить комментарии в код
- [ ] Проверить все примеры в документации

