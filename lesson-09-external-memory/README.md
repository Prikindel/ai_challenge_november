# 🔥 День 9. Внешняя память

## Описание модуля

Модуль демонстрирует работу с **долговременной памятью** для LLM-агентов. История диалогов сохраняется во внешнее хранилище (SQLite или JSON) и восстанавливается при перезапуске приложения, обеспечивая непрерывность диалога.

### Основные возможности:

- ✅ **Персистентная память** — история диалогов сохраняется между запусками
- ✅ **Два типа хранилищ** — SQLite (продакшен) и JSON (разработка)
- ✅ **Автоматическая загрузка** — история восстанавливается при старте
- ✅ **Полная статистика** — количество записей, даты, тип хранилища
- ✅ **Clean Architecture** — модульная структура с разделением ответственности

---

## Быстрый старт

### 1. Подготовка окружения

```bash
cd lesson-09-external-memory

# Скопировать и заполнить переменные окружения
cp example.env .env
# Отредактировать .env и указать OPENAI_API_KEY
```

**Файл `.env`:**
```env
OPENAI_API_KEY=your_api_key_here
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
```

### 2. Конфигурация памяти

Настройте тип хранилища в `config/memory.yaml`:

**SQLite (по умолчанию):**
```yaml
memory:
  storageType: "sqlite"
  sqlite:
    databasePath: "data/memory.db"
```

**JSON:**
```yaml
memory:
  storageType: "json"
  json:
    filePath: "data/memory.json"
    prettyPrint: true
```

### 3. Запуск сервера

```bash
cd server
./gradlew run
```

Сервер запустится на `http://localhost:8080`

### 4. Открыть в браузере

```
http://localhost:8080
```

---

## Структура проекта

```
lesson-09-external-memory/
├── README.md                 # Этот файл
├── THEORY.md                 # Теория по внешней памяти
├── ARCHITECTURE.md           # Архитектура и схемы работы
├── PROMPT.md                 # Полное техническое задание
├── example.env               # Шаблон переменных окружения
├── .gitignore                # Игнорирование data/ директории
├── config/
│   ├── ai.yaml               # Конфигурация AI API
│   ├── memory.yaml           # Конфигурация памяти (тип хранилища)
│   └── memory.yaml.example   # Шаблон конфигурации памяти
├── server/                   # Kotlin + Ktor сервер
│   └── src/main/kotlin/com/prike/
│       ├── data/             # Data layer (репозитории, модели)
│       │   ├── repository/
│       │   │   ├── SqliteMemoryRepository.kt
│       │   │   └── JsonMemoryRepository.kt
│       │   └── model/
│       │       └── MemoryModels.kt
│       ├── domain/           # Domain layer (бизнес-логика)
│       │   ├── agent/
│       │   │   ├── ConversationAgent.kt
│       │   │   └── MemoryOrchestrator.kt
│       │   └── service/
│       │       └── MemoryService.kt
│       └── presentation/     # Presentation layer (API)
│           ├── controller/
│           │   └── MemoryController.kt
│           └── dto/
│               └── MemoryDtos.kt
└── client/                   # Vanilla JS интерфейс
    ├── index.html
    ├── style.css
    └── app.js
```

---

## Конфигурация

### AI API (`config/ai.yaml`)

```yaml
ai:
  apiUrl: "https://openrouter.ai/api/v1/chat/completions"
  model: "meta-llama/llama-3.1-8b-instruct"
  temperature: 0.3
  maxTokens: 600
  requestTimeout: 90
  systemPrompt: |
    Ты — полезный ассистент с долговременной памятью.
  useJsonFormat: false
```

### Память (`config/memory.yaml`)

```yaml
memory:
  # Тип хранилища: "sqlite" или "json"
  storageType: "sqlite"
  
  # Настройки SQLite
  sqlite:
    databasePath: "data/memory.db"
  
  # Настройки JSON
  json:
    filePath: "data/memory.json"
    prettyPrint: true
  
  # Ограничения (null = без ограничений)
  limits:
    maxEntries: null
    maxHistoryDays: null
    autoCleanup: false
```

---

## API Endpoints

### POST `/api/memory/message`

Отправить сообщение и получить ответ (с автоматическим сохранением).

**Запрос:**
```json
{
  "message": "Привет! Как дела?"
}
```

**Ответ:**
```json
{
  "message": "Привет! У меня всё отлично!",
  "usage": {
    "promptTokens": 45,
    "completionTokens": 12,
    "totalTokens": 57
  }
}
```

### GET `/api/memory/history`

Получить всю историю диалогов.

**Ответ:**
```json
{
  "history": [
    {
      "id": "550e8400-...",
      "role": "user",
      "content": "Привет!",
      "timestamp": 1700000000000,
      "metadata": null
    },
    {
      "id": "550e8400-...",
      "role": "assistant",
      "content": "Привет! Как дела?",
      "timestamp": 1700000001000,
      "metadata": {
        "model": "llama-3.1",
        "promptTokens": 45,
        "completionTokens": 12,
        "totalTokens": 57
      }
    }
  ]
}
```

### POST `/api/memory/reset`

Сбросить всю память (очистить историю).

**Ответ:**
```json
{
  "message": "Память успешно сброшена"
}
```

### GET `/api/memory/stats`

Получить статистику памяти.

**Ответ:**
```json
{
  "storageType": "sqlite",
  "totalEntries": 10,
  "userMessages": 5,
  "assistantMessages": 5,
  "oldestEntry": 1700000000000,
  "newestEntry": 1700000100000
}
```

---

## Примеры использования

### Сценарий 1: Базовый диалог

1. Пользователь отправляет: `"Привет! Меня зовут Иван"`
2. Ассистент отвечает: `"Привет, Иван! Рад познакомиться!"`
3. Оба сообщения автоматически сохраняются в БД/JSON

### Сценарий 2: Перезапуск приложения

1. Пользователь отправляет несколько сообщений
2. Приложение перезапускается
3. При старте история загружается из БД/JSON
4. Пользователь: `"Как меня зовут?"`
5. Ассистент: `"Вас зовут Иван!"` (помнит из истории!)

### Сценарий 3: Смена типа хранилища

**Было SQLite:**
```yaml
storageType: "sqlite"
```
→ История в `data/memory.db`

**Меняем на JSON:**
```yaml
storageType: "json"
```
→ История будет в `data/memory.json` (новая сессия)

**Важно:** История не мигрируется автоматически между типами хранилищ!

---

## Архитектура

### Clean Architecture

```
┌─────────────────────────────────────┐
│  Presentation Layer                 │
│  (API Controllers, DTO)             │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│  Domain Layer                       │
│  (Business Logic, Agents, Services) │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│  Data Layer                         │
│  (Repositories, Models, Storage)    │
└─────────────────────────────────────┘
```

### Компоненты

1. **MemoryOrchestrator** — координатор (координирует работу агентов)
2. **MemoryService** — бизнес-логика (создание записей, преобразование)
3. **ConversationAgent** — работа с LLM (отправка запросов)
4. **MemoryRepository** — работа с хранилищем (SQLite/JSON)
5. **MemoryController** — API endpoints

### Поток данных

```
Пользователь → API → MemoryOrchestrator → ConversationAgent → LLM
                ↓
           MemoryService → MemoryRepository → БД/JSON
```

Подробнее см. [ARCHITECTURE.md](./ARCHITECTURE.md)

---

## Переключение между SQLite и JSON

### SQLite → JSON

1. Измените `config/memory.yaml`:
   ```yaml
   storageType: "json"
   ```

2. Перезапустите сервер:
   ```bash
   ./gradlew run
   ```

3. История будет в `data/memory.json` (новая сессия)

### JSON → SQLite

1. Измените `config/memory.yaml`:
   ```yaml
   storageType: "sqlite"
   ```

2. Перезапустите сервер:
   ```bash
   ./gradlew run
   ```

3. История будет в `data/memory.db` (новая сессия)

**Важно:** История НЕ мигрируется автоматически. Каждый тип хранилища имеет свою базу данных.

---

## Тестирование

### Проверка сохранения в SQLite

```bash
# Отправьте несколько сообщений через UI
# Проверьте файл БД
ls -lh data/memory.db

# Посмотрите содержимое (нужен sqlite3)
sqlite3 data/memory.db "SELECT * FROM memory_entries;"
```

### Проверка сохранения в JSON

```bash
# Измените storageType на "json" в config/memory.yaml
# Отправьте сообщения
# Проверьте файл
cat data/memory.json
```

### Проверка перезапуска

1. Отправьте несколько сообщений
2. Остановите сервер (Ctrl+C)
3. Запустите снова (`./gradlew run`)
4. Проверьте UI — история должна быть загружена!

---

## Выводы и наблюдения

### Что работает хорошо:

1. **SQLite** — отличная производительность и надёжность для продакшена
2. **JSON** — удобен для разработки и отладки
3. **Кэширование** — быстрое чтение истории из кэша (не из БД)
4. **Модульность** — легко сменить тип хранилища через конфигурацию

### Потенциальные улучшения:

1. **Миграция данных** между типами хранилищ
2. **Сжатие истории** для длинных диалогов
3. **Пагинация** для больших объёмов данных
4. **Поиск по истории** по ключевым словам
5. **Экспорт/импорт** истории диалогов

### Уроки:

1. Внешняя память критически важна для долгосрочных взаимодействий
2. Разделение ответственности (Clean Architecture) упрощает поддержку
3. Кэширование необходимо для производительности
4. Обработка ошибок обязательна для надёжности

---

## Дополнительные материалы

- [THEORY.md](./THEORY.md) — полная теория по внешней памяти для LLM
- [ARCHITECTURE.md](./ARCHITECTURE.md) — архитектура и схемы работы
- [PROMPT.md](./PROMPT.md) — техническое задание и чеклист

---

## Troubleshooting

### Проблема: "Ошибка загрузки истории"

**Решение:**
- Проверьте, что файл БД/JSON существует и доступен
- Проверьте права доступа на файл
- Проверьте конфигурацию в `config/memory.yaml`

### Проблема: "Не найден OPENAI_API_KEY"

**Решение:**
- Создайте файл `.env` в корне проекта
- Добавьте `OPENAI_API_KEY=your_key_here`
- Или экспортируйте переменную: `export OPENAI_API_KEY=your_key`

### Проблема: "История не сохраняется"

**Решение:**
- Проверьте логи сервера на ошибки
- Убедитесь, что директория `data/` существует и доступна для записи
- Проверьте конфигурацию типа хранилища

---

## Технологии

- **Backend:** Kotlin 1.9.22, Ktor 2.3.7
- **Хранилище:** SQLite (org.xerial:sqlite-jdbc:3.44.1.0), JSON (kotlinx-serialization-json)
- **Frontend:** HTML, CSS, Vanilla JavaScript
- **Конфигурация:** YAML (SnakeYAML), .env (dotenv-kotlin)

---

**Готово к использованию!** 🚀
