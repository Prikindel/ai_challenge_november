# 🔥 День 30. Personalization

Добавление персонализации агенту, чтобы он знал пользователя, его привычки, предпочтения и стиль работы. Персонализация настраивается через конфиг и влияет на поведение агента.

## 🎥 Демонстрация

> 📹 Видео появится после записи

**Сценарий использования:**
1. Настройка профиля пользователя через конфиг или UI
2. Агент использует профиль для персонализации ответов
3. Адаптация стиля общения под пользователя
4. Учет контекста работы и проектов
5. Сохранение истории для улучшения персонализации

## 🎯 Цель урока

Создать систему персонализации, которая:
- Хранит профиль пользователя (имя, предпочтения, привычки)
- Использует персонализацию в промптах агента
- Адаптирует стиль общения под пользователя
- Учитывает контекст работы и проекты
- Работает полностью локально

## Что делаем

- Создаём модель профиля пользователя
- Настраиваем персонализацию через конфиг
- Интегрируем персонализацию в LLMService
- Создаём UI для настройки профиля
- Адаптируем ответы под стиль пользователя
- Сохраняем историю взаимодействий

## Пайплайн

1. **Копирование структуры** — копируем структуру из урока 29 или 24
2. **Модель профиля** — создаём `UserProfile` с предпочтениями, стилем работы, контекстом
3. **Репозиторий и сервис** — создаём `UserProfileRepository` и `UserProfileService`
4. **Интеграция в LLMService** — используем профиль для персонализации промптов
5. **Персонализированные шаблоны** — адаптируем шаблоны под профиль
6. **UI для профиля** — создаём интерфейс настройки профиля
7. **Форматирование ответов** — адаптируем формат ответов под предпочтения
8. **История взаимодействий** — сохраняем историю для обучения
9. **Документация** — создаём руководство по персонализации

## Быстрый старт

### 1. Конфигурация профиля

**config/user-profile.yaml:**
```yaml
user:
  id: "default"
  name: "Иван"
  preferences:
    language: "ru"
    responseFormat: "detailed"  # brief, detailed, structured
    timezone: "Europe/Moscow"
    dateFormat: "dd.MM.yyyy"
  workStyle:
    preferredWorkingHours: "09:00-18:00"
    focusAreas:
      - "backend"
      - "devops"
    tools:
      - "git"
      - "docker"
      - "kubernetes"
    projects:
      - "ai-challenge"
      - "mobile-app"
  communicationStyle:
    tone: "professional"  # professional, casual, friendly
    detailLevel: "medium"  # low, medium, high
    useExamples: true
    useEmojis: false
  context:
    currentProject: "ai-challenge"
    role: "developer"
    team: "backend"
    goals:
      - "Изучить LLM"
      - "Автоматизировать задачи"
```

### 2. Использование

После настройки профиля агент автоматически:
- Обращается к пользователю по имени
- Использует предпочтительный стиль общения
- Адаптирует формат ответов
- Учитывает контекст работы и проекты

## Параметры персонализации

### Предпочтения

- **language** — язык общения (ru, en)
- **responseFormat** — формат ответа:
  - `brief` — краткие ответы (512 токенов)
  - `detailed` — подробные ответы (2048 токенов)
  - `structured` — структурированные (списки, таблицы)
- **timezone** — часовой пояс
- **dateFormat** — формат даты

### Стиль работы

- **preferredWorkingHours** — предпочтительные рабочие часы
- **focusAreas** — области интересов (backend, frontend, devops и т.д.)
- **tools** — используемые инструменты
- **projects** — проекты пользователя

### Стиль общения

- **tone** — тон общения:
  - `professional` — деловой стиль
  - `casual` — неформальный
  - `friendly` — дружелюбный
- **detailLevel** — уровень детализации:
  - `low` — минимум деталей
  - `medium` — средний уровень
  - `high` — максимум деталей
- **useExamples** — использовать примеры
- **useEmojis** — использовать эмодзи

### Контекст

- **currentProject** — текущий проект
- **role** — роль пользователя (developer, manager, analyst)
- **team** — команда
- **goals** — цели пользователя

## Примеры конфигураций

### Для разработчика

```yaml
user:
  name: "Алексей"
  preferences:
    responseFormat: "structured"
    language: "ru"
  workStyle:
    focusAreas:
      - "backend"
      - "kotlin"
    tools:
      - "git"
      - "docker"
  communicationStyle:
    tone: "professional"
    detailLevel: "high"
    useExamples: true
  context:
    role: "developer"
    currentProject: "ai-challenge"
```

### Для менеджера

```yaml
user:
  name: "Мария"
  preferences:
    responseFormat: "brief"
    language: "ru"
  workStyle:
    focusAreas:
      - "project-management"
      - "analytics"
  communicationStyle:
    tone: "professional"
    detailLevel: "medium"
    useExamples: false
  context:
    role: "manager"
    goals:
      - "Улучшить процессы"
      - "Повысить эффективность"
```

### Для аналитика

```yaml
user:
  name: "Дмитрий"
  preferences:
    responseFormat: "detailed"
    language: "ru"
  workStyle:
    focusAreas:
      - "data-analysis"
      - "statistics"
  communicationStyle:
    tone: "professional"
    detailLevel: "high"
    useExamples: true
  context:
    role: "analyst"
    goals:
      - "Найти паттерны в данных"
      - "Улучшить точность анализа"
```

## API Endpoints

### GET /api/profile

Получение профиля пользователя.

**Ответ:**
```json
{
  "id": "default",
  "name": "Иван",
  "preferences": {
    "language": "ru",
    "responseFormat": "detailed",
    "timezone": "Europe/Moscow"
  },
  "workStyle": {
    "focusAreas": ["backend", "devops"],
    "tools": ["git", "docker"]
  },
  "communicationStyle": {
    "tone": "professional",
    "detailLevel": "medium"
  },
  "context": {
    "currentProject": "ai-challenge",
    "role": "developer"
  }
}
```

### POST /api/profile

Обновление профиля пользователя.

**Запрос:**
```json
{
  "name": "Иван",
  "preferences": {
    "responseFormat": "brief"
  },
  "communicationStyle": {
    "tone": "friendly"
  }
}
```

**Ответ:**
```json
{
  "success": true
}
```

## Влияние персонализации

### На промпты

Персонализация добавляется в промпт:
```
Контекст пользователя:
- Имя: Иван
- Текущий проект: ai-challenge
- Роль: developer
- Области интересов: backend, devops
- Стиль общения: professional
- Формат ответа: detailed
- Уровень детализации: medium

[Оригинальный промпт]

Учти предпочтения пользователя при ответе.
```

### На параметры LLM

- **responseFormat = brief** → maxTokens = 512
- **responseFormat = detailed** → maxTokens = 2048
- **detailLevel = low** → temperature = 0.5
- **detailLevel = high** → temperature = 0.8

### На формат ответов

- **brief** — сокращение до ключевых моментов
- **detailed** — полный ответ
- **structured** — форматирование в списки, таблицы

## Архитектура

```
lesson-30-personalization/
├── config/
│   ├── server.yaml          # Конфигурация сервера
│   └── user-profile.yaml    # Профиль пользователя
├── client/
│   ├── index.html          # Главная страница
│   ├── profile.html        # Настройка профиля
│   └── app.js              # JavaScript
├── server/
│   └── src/main/kotlin/com/prike/
│       ├── domain/
│       │   ├── model/      # UserProfile, UserPreferences
│       │   └── service/     # UserProfileService, ResponseFormatter
│       ├── data/
│       │   └── repository/  # UserProfileRepository
│       └── presentation/
│           └── controller/  # ProfileController
└── data/
    └── interactions.db     # История взаимодействий (опционально)
```

## Файлы урока

- `PROMPT.md` — промпт для реализации (по коммитам)
- `README.md` — этот файл
- `PERSONALIZATION_GUIDE.md` — руководство по персонализации
- `CHAT_PROMPT.txt` — краткий промпт для агента
- `config/user-profile.yaml` — пример конфигурации профиля

## 💡 Советы

1. **Начните с базового профиля** — используйте значения по умолчанию
2. **Настройте под себя** — измените параметры под свои предпочтения
3. **Экспериментируйте** — попробуйте разные стили общения
4. **Используйте контекст** — укажите текущий проект и роль
5. **Обновляйте профиль** — меняйте настройки по мере необходимости

## 🐛 Решение проблем

### Профиль не применяется

- Проверьте путь к конфигу `config/user-profile.yaml`
- Убедитесь, что конфиг валидный YAML
- Проверьте загрузку конфига в `Config.kt`

### Персонализация не работает

- Проверьте, что `UserProfileService` используется в `LLMService`
- Убедитесь, что промпт содержит персонализацию
- Проверьте логи для отладки

### UI не сохраняет изменения

- Проверьте API endpoint `/api/profile`
- Убедитесь, что изменения записываются в конфиг
- Проверьте права доступа к файлу конфига

