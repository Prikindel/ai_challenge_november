# 🔥 День 11. Первый инструмент MCP

Модуль для создания собственного MCP сервера с инструментами для работы с Telegram Bot API, подключения его к приложению и демонстрации вызова инструментов из агента.

## 📋 Описание модуля

Этот модуль демонстрирует создание собственного **MCP (Model Context Protocol) сервера** с использованием официального Kotlin SDK. MCP сервер предоставляет инструменты для работы с внешним API (в данном случае — Telegram Bot API) и подключается к основному приложению через stdio транспорт.

### Основные возможности:

- ✅ Создание собственного MCP сервера на Kotlin
- ✅ Интеграция с Telegram Bot API
- ✅ Регистрация инструментов (tools) в MCP сервере
- ✅ Подключение MCP сервера к основному приложению
- ✅ Вызов инструментов из приложения
- ✅ Визуализация работы с инструментами через веб-интерфейс

## 🧠 Теория

Подробная теория о работе MCP сервера, интеграции с LLM, function calling и цикле вызовов инструментов находится в отдельном файле:

📖 **[THEORY.md](THEORY.md)** — полная теория с примерами кода и диаграммами

## 🎯 Выбранная функциональность

### Telegram Bot API MCP Server

**Обоснование выбора:**
- Простота настройки — получение Bot Token занимает 2-3 минуты через @BotFather
- Практичность — реальная работа с внешним API
- Низкая сложность — простой REST API без сложных зависимостей
- Быстрый старт — можно сразу протестировать без долгой настройки

**Инструменты:**
- `get_bot_info` — получить информацию о Telegram боте (getMe)
- `send_message` — отправить сообщение в Telegram чат

**Примеры использования:**
```json
// Получить информацию о боте
{"toolName": "get_bot_info", "arguments": {}}

// Отправить сообщение
{"toolName": "send_message", "arguments": {
  "chatId": "123456789",
  "text": "Привет из MCP!"
}}
```

## 🚀 Быстрый старт

### Предварительные требования

- Java 17 или выше
- Gradle (включён в проект через wrapper)
- Telegram аккаунт (для получения Bot Token)

### Получение Telegram Bot Token

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота:
   - Укажите имя бота
   - Укажите username бота (должен заканчиваться на `bot`)
4. Скопируйте полученный токен (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### Настройка проекта

1. **Скопируйте example.env в .env:**
   ```bash
   cd lesson-11-first-mcp-tool
   cp example.env .env
   ```

2. **Отредактируйте .env и добавьте ваш Telegram Bot Token:**
   ```env
   TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
   ```

3. **Убедитесь, что config/mcp-server.yaml существует:**
   ```bash
   ls config/mcp-server.yaml
   ```

### Сборка и запуск

1. **Соберите MCP сервер:**
   ```bash
   cd mcp-server
   ./gradlew build
   ```
   
   JAR файл будет создан в `mcp-server/build/libs/telegram-bot-mcp-server-1.0.0.jar`

2. **Запустите основное приложение:**
   ```bash
   cd ../server
   ./gradlew run
   ```

3. **Откройте браузер:**
   ```
   http://localhost:8080
   ```

4. **Использование:**
   - Нажмите "Подключиться к MCP серверу"
   - Дождитесь загрузки списка инструментов
   - Выберите инструмент и вызовите его с нужными аргументами

## 📁 Структура проекта

```
lesson-11-first-mcp-tool/
├── README.md                        # Документация модуля
├── THEORY.md                        # Теория MCP и LLM интеграции
├── BRAINSTORM.md                    # Мозговой штурм и выбор функциональности
├── example.env                      # Пример переменных окружения
├── config/
│   ├── mcp-server.yaml              # Конфигурация MCP сервера
│   └── mcp-server.yaml.example      # Пример конфигурации
├── mcp-server/                      # MCP сервер (отдельный модуль/процесс)
│   ├── build.gradle.kts
│   └── src/main/kotlin/com/prike/mcpserver/
│       ├── Main.kt                  # Точка входа MCP сервера
│       ├── Config.kt                # Загрузка конфигурации
│       ├── server/
│       │   └── MCPServer.kt         # Основной класс MCP сервера
│       ├── tools/
│       │   └── ToolRegistry.kt      # Регистрация инструментов
│       ├── api/
│       │   └── TelegramApiClient.kt  # Клиент для работы с Telegram Bot API
│       └── dto/
│           └── TelegramModels.kt    # DTO для Telegram API
├── server/                          # Основное приложение (клиент MCP)
│   ├── build.gradle.kts
│   └── src/main/kotlin/com/prike/
│       ├── Main.kt                  # Точка входа приложения
│       ├── Config.kt                # Загрузка конфигурации
│       ├── data/
│       │   ├── client/
│       │   │   └── MCPClient.kt     # Клиент для подключения к MCP серверу
│       │   └── dto/
│       │       └── MCPModels.kt     # DTO для работы с MCP
│       ├── domain/
│       │   └── agent/
│       │       └── MCPToolAgent.kt   # Агент для вызова инструментов MCP
│       └── presentation/
│           ├── controller/
│           │   ├── MCPConnectionController.kt # HTTP контроллер для подключения
│           │   └── ToolController.kt         # HTTP контроллер для инструментов
│           └── dto/
│               └── ToolDtos.kt       # DTO для HTTP API
└── client/                          # Веб-клиент
    ├── index.html
    ├── style.css
    └── app.js
```

## ⚙️ Конфигурация

### config/mcp-server.yaml

Файл конфигурации для настройки MCP сервера:

```yaml
mcpServer:
  info:
    name: "telegram-bot-mcp-server"
    version: "1.0.0"
    description: "MCP сервер для работы с Telegram Bot API"
  
  api:
    baseUrl: "https://api.telegram.org"
    token: "${TELEGRAM_BOT_TOKEN}"  # из .env
  
  transport:
    type: "stdio"
```

**Примечание:** Инструменты регистрируются напрямую в коде (`ToolRegistry.kt`), а не через конфигурацию YAML. Это позволяет иметь полный контроль над схемами аргументов и логикой инструментов.

### Переменные окружения (.env)

```env
# Telegram Bot API Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
```

## 🔌 API Endpoints

### POST /api/mcp/connect

Подключиться к MCP серверу.

**Запрос:**
```json
{
  "serverJarPath": "mcp-server/build/libs/telegram-bot-mcp-server-1.0.0.jar"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Connected to MCP server"
}
```

### POST /api/mcp/disconnect

Отключиться от MCP сервера.

**Ответ:**
```json
{
  "success": true,
  "message": "Disconnected from MCP server"
}
```

### GET /api/mcp/status

Получить статус подключения.

**Ответ:**
```json
{
  "success": true,
  "message": "Connected"
}
```

### GET /api/tools

Получить список доступных инструментов.

**Ответ:**
```json
{
  "tools": [
    {
      "name": "get_bot_info",
      "description": "Получить информацию о Telegram боте (getMe)"
    },
    {
      "name": "send_message",
      "description": "Отправить сообщение в Telegram чат"
    }
  ]
}
```

### POST /api/tools/call

Вызвать инструмент.

**Запрос:**
```json
{
  "toolName": "send_message",
  "arguments": {
    "chatId": "123456789",
    "text": "Привет из MCP!"
  }
}
```

**Ответ:**
```json
{
  "success": true,
  "result": "Сообщение отправлено:\n{\"message_id\":123,\"chat_id\":123456789,\"chat_type\":\"private\",\"text\":\"Привет из MCP!\"}"
}
```

## 📝 Примеры использования

### Пример 1: Получение информации о боте

1. Подключитесь к MCP серверу через веб-интерфейс
2. Выберите инструмент `get_bot_info`
3. Оставьте аргументы пустыми или `{}`
4. Нажмите "Вызвать инструмент"
5. Просмотрите информацию о боте (ID, имя, username)

### Пример 2: Отправка сообщения

1. Убедитесь, что у вас есть chatId получателя:
   - Для личных сообщений: ваш Telegram User ID
   - Для групп: ID группы (можно получить через бота @userinfobot)
2. Выберите инструмент `send_message`
3. Введите аргументы в формате JSON:
   ```json
   {
     "chatId": "123456789",
     "text": "Привет из MCP сервера!"
   }
   ```
4. Нажмите "Вызвать инструмент"
5. Проверьте, что сообщение было отправлено

**Примечание:** Для отправки сообщения бот должен быть запущен и иметь возможность отправлять сообщения в указанный чат. Если вы отправляете сообщение себе, сначала отправьте боту любое сообщение, чтобы он мог вам ответить.

## 🎯 Выводы

### Что получилось:

✅ **Создание собственного MCP сервера** с использованием Kotlin SDK  
✅ **Интеграция с Telegram Bot API** для работы с ботами  
✅ **Регистрация инструментов** в MCP сервере  
✅ **Подключение MCP сервера** к основному приложению через stdio транспорт  
✅ **Вызов инструментов** из приложения через веб-интерфейс  
✅ **Отображение результатов** вызова инструментов в читаемом формате  
✅ **Полная документация** с примерами использования  
✅ **Мозговой штурм** с анализом различных вариантов API

### Что можно улучшить:

- 🔄 Добавление больше инструментов Telegram Bot API (получение обновлений, отправка фото, и т.д.)
- 🔄 Поддержка SSE и WebSocket транспортов
- 🔄 Кэширование информации о боте
- 🔄 Обработка ошибок API с более детальными сообщениями
- 🔄 Поддержка других внешних API (GitHub, Яндекс.Трекер, и т.д.)
- 🔄 Автоматическое переподключение при обрыве связи
- 🔄 Валидация аргументов инструментов на стороне сервера

## 📚 Дополнительные ресурсы

- [Официальный репозиторий MCP](https://github.com/modelcontextprotocol)
- [Kotlin SDK для MCP](https://github.com/modelcontextprotocol/kotlin-sdk)
- [Документация MCP](https://modelcontextprotocol.io)
- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)

## 🔧 Технологии

- **Backend:** Kotlin + Ktor + Clean Architecture
- **Frontend:** HTML + CSS + Vanilla JavaScript
- **MCP SDK:** официальный Kotlin SDK от Model Context Protocol (`io.modelcontextprotocol:kotlin-sdk:0.7.7`)
- **Telegram Bot API:** REST API для работы с ботами

