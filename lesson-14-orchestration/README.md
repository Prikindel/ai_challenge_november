# 🔥 День 14. Orchestration

Система оркестрации нескольких MCP серверов, где **LLM сама выбирает правильные инструменты** из разных источников для выполнения сложных многошаговых задач. Системный промпт универсален — он не знает о конкретных инструментах, а LLM сама изучает доступные инструменты из их описаний.

## 📋 Описание

Этот урок демонстрирует создание системы оркестрации MCP-инструментов, где:
- **Несколько MCP серверов** (3+) с разными инструментами
- **Универсальный системный промпт** — не знает о конкретных инструментах
- **LLM сама изучает инструменты** из их описаний и выбирает правильные
- **Длинные флоу взаимодействия** с переключением между серверами

## 🎯 Сценарий: "Аналитика и отчётность"

Система может:
1. **Собирать данные** из разных источников (веб-чат, Telegram, файлы)
2. **Обрабатывать данные** (анализ тональности, извлечение ключевых слов, статистика)
3. **Генерировать отчёты** и доставлять их (сохранение в файл, отправка в Telegram)

### Пример работы:

```
Пользователь: "Проанализируй переписку за последние 7 дней, извлеки ключевые темы, 
                рассчитай статистику и отправь мне отчёт в Telegram"

Итерация 1:
  LLM: "Мне нужно собрать данные"
  → вызывает get_chat_history (data-collection)
  → вызывает get_telegram_messages (data-collection)
  → получает данные

Итерация 2:
  LLM: "Теперь обработаю данные"
  → вызывает analyze_sentiment (data-processing)
  → вызывает extract_keywords (data-processing)
  → вызывает calculate_statistics (data-processing)
  → получает результаты

Итерация 3:
  LLM: "Сформирую отчёт и отправлю"
  → вызывает generate_report (reporting)
  → вызывает send_telegram_message (reporting)
  → получает результат

Итерация 4:
  LLM: "Задача выполнена"
  → формирует финальный ответ пользователю
```

## 🏗️ Архитектура

```
Пользователь → ChatController → OrchestrationAgent
    ↓
LLM (с tools из всех серверов)
    ↓
tool_call → MCPToolAgent → MCPClientManager → MCP Server
    ↓
Результат → LLM (с историей) → tool_call → ... → Финальный ответ
```

### MCP Серверы:

1. **Data Collection Server** — сбор данных
   - `get_chat_history` — история веб-чата
   - `get_telegram_messages` — сообщения из Telegram
   - `get_file_content` — чтение файлов

2. **Data Processing Server** — обработка данных
   - `analyze_sentiment` — анализ тональности
   - `extract_keywords` — извлечение ключевых слов
   - `calculate_statistics` — расчёт статистики

3. **Reporting Server** — отчёты и доставка
   - `generate_report` — генерация отчёта
   - `save_to_file` — сохранение в файл
   - `send_telegram_message` — отправка в Telegram

## 📁 Структура проекта

```
lesson-14-orchestration/
├── README.md
├── THEORY.md
├── PROMPT.md
├── example.env
├── config/
│   ├── mcp-servers.yaml              # Конфигурация всех MCP серверов
│   ├── server.yaml                   # Конфигурация основного сервера
│   ├── data-collection-mcp-server.yaml
│   ├── data-processing-mcp-server.yaml
│   └── reporting-mcp-server.yaml
├── mcp-servers/
│   ├── data-collection-mcp-server-1.0.0.jar
│   ├── data-processing-mcp-server-1.0.0.jar
│   └── reporting-mcp-server-1.0.0.jar
├── data-collection-mcp-server/        # MCP сервер для сбора данных
│   └── src/main/kotlin/com/prike/mcpserver/...
├── data-processing-mcp-server/       # MCP сервер для обработки данных
│   └── src/main/kotlin/com/prike/mcpserver/...
├── reporting-mcp-server/             # MCP сервер для отчётов
│   └── src/main/kotlin/com/prike/mcpserver/...
├── server/                           # Основное приложение
│   └── src/main/kotlin/com/prike/...
└── client/                           # Веб-интерфейс
    ├── index.html
    ├── style.css
    └── app.js
```

## 🚀 Быстрый старт

### 1. Переменные окружения

Создайте `.env` файл в корне проекта (`ai_challenge_november/.env`):

```env
# LLM
OPENROUTER_API_KEY=your_openrouter_api_key
OPENROUTER_MODEL=gpt-4o-mini

# Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_USER_ID=your_telegram_user_id

# Базы данных (опционально)
CHAT_HISTORY_DB_PATH=../lesson-09-web-chat-memory/data/memory.db
TELEGRAM_MESSAGES_DB_PATH=data/orchestration.db
```

### 2. Сборка JAR файлов

Соберите все MCP серверы:

```bash
# Data Collection MCP Server
cd data-collection-mcp-server
./gradlew build
cp build/libs/data-collection-mcp-server-1.0.0.jar ../mcp-servers/

# Data Processing MCP Server
cd ../data-processing-mcp-server
./gradlew build
cp build/libs/data-processing-mcp-server-1.0.0.jar ../mcp-servers/

# Reporting MCP Server
cd ../reporting-mcp-server
./gradlew build
cp build/libs/reporting-mcp-server-1.0.0.jar ../mcp-servers/
```

### 3. Конфигурация

Настройте `config/mcp-servers.yaml`:

```yaml
mcp:
  servers:
    - id: "data-collection"
      name: "Data Collection MCP Server"
      jarPath: "mcp-servers/data-collection-mcp-server-1.0.0.jar"
      configPath: "config/data-collection-mcp-server.yaml"
      type: "local"
    
    - id: "data-processing"
      name: "Data Processing MCP Server"
      jarPath: "mcp-servers/data-processing-mcp-server-1.0.0.jar"
      configPath: "config/data-processing-mcp-server.yaml"
      type: "local"
    
    - id: "reporting"
      name: "Reporting MCP Server"
      jarPath: "mcp-servers/reporting-mcp-server-1.0.0.jar"
      configPath: "config/reporting-mcp-server.yaml"
      type: "local"
```

### 4. Запуск

```bash
cd server
./gradlew run
```

Или через JAR:

```bash
cd server
./gradlew build
java -jar build/libs/server-1.0.0.jar
```

Сервер будет доступен на http://localhost:8080

## 🎯 API Endpoints

- `GET /` - веб-интерфейс
- `GET /api/health` - health check
- `GET /api/mcp/servers` - список подключённых MCP серверов
- `GET /api/mcp/tools` - список всех доступных инструментов
- `GET /api/mcp/connections` - статус подключений
- `POST /api/chat/message` - отправка сообщения в чат с LLM агентом

## 🔧 Доступные инструменты

### Data Collection Server:

1. **get_chat_history** — получить историю веб-чата за период
   - Параметры: `startTime`, `endTime` (Unix timestamp в миллисекундах)

2. **get_telegram_messages** — получить сообщения из Telegram за период
   - Параметры: `groupId`, `startTime`, `endTime`

3. **get_file_content** — прочитать содержимое файла
   - Параметры: `filePath`

### Data Processing Server:

1. **analyze_sentiment** — анализ тональности текста
   - Параметры: `text`
   - Возвращает: `{"sentiment": "positive|negative|neutral", "score": 0.0-1.0}`

2. **extract_keywords** — извлечение ключевых слов
   - Параметры: `text`, `count` (по умолчанию 10)

3. **calculate_statistics** — расчёт статистики по данным
   - Параметры: `data` (JSON массив сообщений)

### Reporting Server:

1. **generate_report** — генерация отчёта
   - Параметры: `title`, `content`, `format` ("markdown"|"text")

2. **save_to_file** — сохранение содержимого в файл
   - Параметры: `filePath`, `content`

3. **send_telegram_message** — отправка сообщения в Telegram
   - Параметры: `userId`, `message`

## 📊 Особенности реализации

### Универсальный системный промпт

Системный промпт **не знает о конкретных инструментах**. Вся информация об инструментах берётся из их описаний в MCP серверах. LLM сама изучает доступные инструменты и выбирает правильные.

### Оркестрация нескольких серверов

- LLM может вызывать инструменты из разных серверов в одной задаче
- `MCPClientManager` маршрутизирует вызовы к правильному серверу
- История диалога сохраняется между вызовами разных инструментов

### Длинные флоу взаимодействия

- Максимум 10 итераций (защита от бесконечного цикла)
- История диалога сохраняется между итерациями
- LLM сама решает, когда вызвать инструмент и когда дать финальный ответ

### Детальные описания инструментов

Каждый инструмент должен иметь:
- Понятное описание (`description`)
- Детальную схему параметров (`inputSchema`)
- Примеры использования (в описании, если нужно)

## 🧪 Примеры запросов

### Простой запрос (один инструмент):

```
Получи переписку за последние 24 часа
```

→ LLM вызовет `get_chat_history` и вернёт результат

### Композиция (несколько инструментов одного сервера):

```
Проанализируй тональность переписки за последние 7 дней
```

→ LLM вызовет `get_chat_history`, затем `analyze_sentiment`

### Оркестрация (несколько серверов):

```
Проанализируй переписку за последние 7 дней, извлеки ключевые темы, 
рассчитай статистику и отправь мне отчёт в Telegram
```

→ LLM вызовет инструменты из всех трёх серверов последовательно

## ✅ Проверка готовности

Все компоненты должны быть собраны:

- ✓ Data Collection MCP Server JAR собран и скопирован в `mcp-servers/`
- ✓ Data Processing MCP Server JAR собран и скопирован в `mcp-servers/`
- ✓ Reporting MCP Server JAR собран и скопирован в `mcp-servers/`
- ✓ Основной сервер JAR собран
- ✓ Конфигурация MCP серверов настроена
- ✓ UI файлы на месте

## 📚 Дополнительные материалы

- [THEORY.md](./THEORY.md) - теория по оркестрации MCP инструментов
- [PROMPT.md](./PROMPT.md) - полное техническое задание
- [MCP_GUIDE.md](../MCP_GUIDE.md) - руководство по работе с MCP

## 🎯 Итоговый результат

1. ✅ 3 MCP сервера с 9 инструментами
2. ✅ MCP клиент и менеджер для работы с несколькими серверами
3. ✅ LLM агент с универсальным системным промптом
4. ✅ Оркестрация: LLM выбирает правильные инструменты из разных серверов
5. ✅ Длинные флоу взаимодействия (5+ шагов)
6. ✅ UI с визуализацией оркестрации

**Важно:** 
- Системный промпт универсален — не знает о конкретных инструментах
- Вся информация об инструментах в их описаниях в MCP серверах
- LLM сама изучает доступные инструменты и выбирает правильные
- Работаем только с JAR файлами MCP серверов

---

**Готово к использованию!** 🚀

