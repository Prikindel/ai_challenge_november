# Конфигурация AI API (опционально)
# Скопируйте этот файл в config/ai.yaml и заполните своими значениями

ai:
  # URL API провайдера
  apiUrl: "https://api.openai.com/v1/chat/completions"
  # apiUrl: "https://openrouter.ai/api/v1/chat/completions"  # для OpenRouter
  
  # Модель AI
  model: "gpt-3.5-turbo"
  
  # Параметры генерации
  temperature: 0.7  # 0.0 - детерминировано, 1.0 - креативно
  maxTokens: 500    # Максимальное количество токенов в ответе
  
  # Таймаут запроса в секундах
  requestTimeout: 60  # 60 секунд по умолчанию
  
  # Системный промпт (опционально)
  # Задает контекст, стиль и правила для AI
  systemPrompt: |
    Ты полезный AI-ассистент. Отвечай кратко и по делу.
    Используй Markdown для форматирования ответов.
