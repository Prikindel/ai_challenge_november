# Шаблон проекта

Базовый шаблон для создания новых заданий в AI Challenge.

## Структура

```
lesson-XX-description/
├── server/              # Kotlin + Ktor сервер
│   └── src/main/kotlin/com/prike/
│       ├── Main.kt      # Точка входа, настройка Ktor
│       ├── Config.kt    # Загрузка конфигурации из .env
│       │
│       ├── domain/      # 💖 Доменный слой (логика работы с LLM)
│       │   ├── agent/       # Агенты для работы с LLM
│       │   │   └── BaseAgent.kt        # Базовый абстрактный класс агента
│       │   └── exception/    # Доменные исключения
│       │
│       ├── data/        # 🔌 Слой данных (работа с внешними API/БД)
│       │   ├── client/       # HTTP клиенты для внешних API
│       │   │   └── OpenAIClient.kt  # Базовый клиент для OpenAI API
│       │   ├── repository/   # Репозитории для работы с DTO
│       │   │   └── AIRepository.kt   # Репозиторий для работы с LLM DTO
│       │   └── dto/          # DTO для внешних API (LLM API)
│       │       ├── OpenAIRequest.kt
│       │       ├── OpenAIResponse.kt
│       │       └── OpenAIErrorResponse.kt
│       │
│       ├── presentation/ # 🌐 Слой презентации (HTTP API)
│       │   ├── controller/   # HTTP контроллеры (вызывают агентов)
│       │   └── dto/          # DTO для клиента (только примитивы)
│       │       ├── ChatRequestDto.kt
│       │       ├── ChatResponseDto.kt
│       │       └── ErrorResponseDto.kt
│       │
│       ├── di/          # 💉 Dependency Injection
│       │   └── AppModule     # Создание и связывание зависимостей
│       │
│       └── config/      # ⚙️ Конфигурация
│           └── AIConfig.kt  # Data class для настроек AI
│
├── client/              # Веб-клиент (опционально)
│   ├── index.html
│   ├── style.css
│   └── app.js
│
├── config/              # Конфигурационные файлы
│   └── ai.yaml.example  # Пример конфигурации AI
│
├── .env.example         # Пример переменных окружения
└── README.md            # Описание задания
```

## Использование

### Создание нового задания

1. Скопируйте шаблон:
   ```bash
   cp -r lesson-00-project-template lesson-XX-description
   ```

2. Обновите названия (опционально):
   - Замените `com.prike` на ваш package в файлах, или оставьте как есть
   - Обновите `rootProject.name` в `server/settings.gradle.kts`

3. Настройте конфигурацию:
   ```bash
   cd lesson-XX-description
   # .env файл находится в корне проекта (ai_challenge_november/.env)
   # Убедитесь, что OPENAI_API_KEY установлен в корневом .env
   
   cp config/ai.yaml.example config/ai.yaml
   # Отредактируйте config/ai.yaml (опционально)
   ```

4. Добавьте свою логику:
   - Создайте контроллеры в `presentation/controller/` (вызывают агентов)
   - Создайте клиентские DTO в `presentation/dto/` (только примитивы)
   - Создайте специализированные агенты в `domain/agent/` (наследуют BaseAgent)
   - При необходимости создайте репозитории в `data/repository/` для работы с DTO
   - Настройте DI в `di/AppModule.kt`

5. Обновите `Main.kt`:
   - Добавьте свои контроллеры в блок `routing { }`
   - Настройте DI в `AppModule`, если нужно

6. Запустите сервер:
   ```bash
   cd server
   ./gradlew run
   ```

## Архитектура

Шаблон использует **Clean Architecture** с разделением на слои:

- **Domain** - логика работы с LLM (агенты)
  - Агенты находятся в `domain/agent/` (это логика, а не данные)
  - Базовый агент (`BaseAgent`) - абстрактный класс с общей логикой запросов
  - Специализированные агенты наследуют `BaseAgent` и добавляют свою логику
  
- **Data** - инфраструктура для работы с внешними API
  - `client/` - HTTP клиенты (OpenAIClient)
  - `repository/` - репозитории для работы с DTO (парсинг, валидация)
  - `dto/` - DTO для LLM API (не для клиента)
  
- **Presentation** - HTTP слой
  - Контроллеры вызывают агентов напрямую
  - Клиентские DTO содержат только примитивы, не зависят от LLM DTO
  
- **DI** - связывание всех слоев

**Важно:** 
- Агенты вместо Use Cases для работы с LLM
- Клиентские DTO (`presentation/dto/`) не зависят от LLM DTO (`data/dto/`)
- Репозитории в Data слое работают только с DTO, не содержат бизнес-логику

## Настройка сервера

Сервер настроен с:
- CORS для работы с веб-клиентом
- JSON сериализация
- Логирование запросов
- Автоматическая отдача статических файлов из папки `client/`

## Базовая реализация

Шаблон включает базовую реализацию для работы с AI API:

- **BaseAgent** - базовый абстрактный класс агента (`domain/agent/BaseAgent.kt`)
  - Содержит базовые методы `getMessage()` и `getMessageWithHistory()`
  - Использует `AIRepository` для получения данных от LLM
  - `getMessageWithHistory()` возвращает `MessageResult` с текстом и JSON запросом/ответом
  - Специализированные агенты наследуют этот класс и добавляют свою логику

- **AIRepository** - репозиторий для работы с DTO (`data/repository/AIRepository.kt`)
  - Работа с DTO от LLM API через `OpenAIClient`
  - `getMessage()` - простой текстовый ответ
  - `getMessageWithHistory()` - возвращает `MessageResult` (текст + JSON запрос/ответ)
  - Используется агентами для получения данных от LLM

- **OpenAIClient** - HTTP клиент для OpenAI/OpenRouter API
  - Поддержка системных промптов
  - Обработка ошибок и таймаутов
  - Логирование запросов/ответов в формате OkHttp
  - `getCompletionWithHistory()` возвращает `CompletionResult` (ответ + JSON)
  - Настройка через конфигурацию

- **Конфигурация AI** - загрузка из YAML файла (`config/ai.yaml`)
  - API ключ берется из `.env` в корне проекта (безопасность)
  - Параметры модели, таймауты, системный промпт - из YAML

- **DTO классы:**
  - LLM DTO (`data/dto/`) - для работы с OpenAI API
  - Клиентские DTO (`presentation/dto/`) - только примитивы, для HTTP API

**Примечание:** 
- `.env` файл находится в корне проекта (`ai_challenge_november/.env`), общий для всех уроков
- В шаблоне `Config.aiConfig` возвращает `null`, если API ключ не установлен (нормально для шаблона)
- В реальном проекте создайте специализированные агенты, наследуя `BaseAgent`

## Зависимости

- Kotlin 1.9.22
- Ktor 2.3.7
- Kotlinx Serialization
- SnakeYAML (для конфигурации)
- Dotenv Kotlin (для .env файлов)

