# 🔥 День 27. Локальная LLM на VPS

Цель: развернуть локальную LLM на VPS (Ollama или llama.cpp server), закрыть её за HTTPS + авторизацией и подключить клиент (урок 25/26), чтобы чат отвечал через удалённую локальную модель.

## 🎥 Демонстрация

**Видео демонстрация работы:** [Смотреть на Яндекс Диске](https://disk.yandex.ru/i/KrBfpzQSQ8Z_Vw)

В демонстрации показано:
- Подключение к VPS через SSH
- Настройка Ollama на VPS
- Работа чата с VPS LLM
- Логирование запросов к VPS
- Отображение статуса подключения в UI

## ⚠️ Важно: Разделение задач

**Этот урок состоит из двух частей:**

1. **Ручная работа на VPS** (выполняется вручную):
   - Установка Ollama на VPS
   - Настройка реверс-прокси (nginx/caddy) с HTTPS и авторизацией
   - Настройка firewall (UFW)
   - Проверка доступности через curl
   - **См. подробные инструкции в [`VPS_SETUP.md`](./VPS_SETUP.md)**

2. **Автоматизируемая часть (код приложения)** (может делать агент):
   - Обновление конфигурации клиента для подключения к VPS
   - Доработка `LocalLLMClient` для поддержки HTTPS и авторизации
   - Обновление UI для отображения статуса VPS
   - Тестирование подключения
   - **См. [`PROMPT.md`](./PROMPT.md) для детального плана**

---

## Что делаем
- Ставим LLM на VPS (Ubuntu 22.04, Ollama/llama.cpp) — **ручная работа**
- Закрываем за реверс-прокси (HTTPS, auth, firewall) — **ручная работа**
- Проверяем доступ внешним curl — **ручная работа**
- Подключаем клиент (chat) к VPS-LLM — **автоматизируемая часть (агент)**
- Тестируем ответы в чате — **автоматизируемая часть (агент)**
- **Все запросы к LLM** (включая RAG чат) идут через VPS, если локальная LLM включена

## Пайплайн
1) **VPS и LLM**: установка Ollama, скачивание модели (`llama3.2` и др.), smoke-тест `ollama run`.
2) **Безопасность**: UFW (22/80/443), реверс-прокси (nginx/caddy) с HTTPS и basic/bearer auth, таймауты 200–300s.
3) **Проверка снаружи**: curl через публичный IP с auth (`curl -k -u user:pass https://185.31.165.227/api/version`).
4) **Клиент**: в конфиге клиента прописать `baseUrl=https://185.31.165.227`, auth, таймауты.
5) **SSL**: автоматическое отключение проверки SSL для самоподписанных сертификатов (аналог `curl -k`).
6) **UI**: отображение провайдера с полным URL (`🌐 VPS: https://185.31.165.227`) и статусом подключения.
7) **Логирование**: подробное логирование всех запросов в формате OkHttp (см. `LOGGING.md`).
8) **Тесты**: короткий запрос, длинный ответ/таймаут, ошибка авторизации. Результаты — в `TESTING.md`.

## Подключение к VPS

**SSH подключение:**
```bash
ssh root@185.31.165.227
```

**Или с указанием пользователя:**
```bash
ssh username@185.31.165.227
```

**Примечание:** IP адрес VPS: `185.31.165.227`

---

## Быстрый старт (VPS)

После подключения к VPS выполните:

```bash
# установка Ollama
curl -fsSL https://ollama.com/install.sh | sh
systemctl enable --now ollama

# модель
ollama pull llama3.2
ollama run llama3.2 "hello"
```

**Подробные инструкции:** см. [`VPS_SETUP.md`](./VPS_SETUP.md)

## Пример реверс-прокси (nginx + basic auth)
```
server {
  listen 80;
  server_name 185.31.165.227;
  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl;
  server_name 185.31.165.227;

  ssl_certificate     /etc/letsencrypt/live/185.31.165.227/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/185.31.165.227/privkey.pem;

  auth_basic "LLM";
  auth_basic_user_file /etc/nginx/.htpasswd;

  location /api/ {
    proxy_pass http://127.0.0.1:11434/;
    proxy_set_header Host $host;
    proxy_read_timeout 300;
    proxy_send_timeout 300;
  }
}
```

## Проверка извне
```bash
curl https://185.31.165.227/api/generate \
  -u user:pass \
  -H "Content-Type: application/json" \
  -d '{"model":"llama3.2","prompt":"Привет!","stream":false}'
```

## Конфиг клиента

### Пример для VPS с Basic Auth
```yaml
localLLM:
  enabled: true
  provider: "ollama"
  baseUrl: "https://185.31.165.227"  # HTTPS обязателен для VPS
  model: "llama3.2"
  apiPath: "/api/generate"
  timeout: 120000  # 2 минуты (для длинных ответов)
  auth:
    type: "basic"
    user: "user"        # имя пользователя для basic auth
    password: "mypassword123"  # пароль для basic auth (замените на свой)
    token: ""           # не используется для basic auth
```

**Важно:** 
- Для работы с самоподписанными SSL сертификатами проверка SSL автоматически отключена (аналог `curl -k`)
- Все запросы к LLM (включая RAG) будут идти через VPS, если `enabled: true`

### Пример для VPS с Bearer Token
```yaml
localLLM:
  enabled: true
  provider: "ollama"
  baseUrl: "https://185.31.165.227"
  model: "llama3.2"
  apiPath: "/api/generate"
  timeout: 120000
  auth:
    type: "bearer"
    user: ""            # не используется для bearer auth
    password: ""         # не используется для bearer auth
    token: "your-secret-token-here"
```

### Пример для локальной LLM (без авторизации)
```yaml
localLLM:
  enabled: true
  provider: "ollama"
  baseUrl: "http://localhost:11434"  # локальный адрес
  model: "llama3.2"
  apiPath: "/api/generate"
  timeout: 120000
  # auth не требуется для локальной LLM
```

### Использование переменных окружения
Для безопасности можно использовать переменные окружения в `.env`:
```yaml
localLLM:
  enabled: true
  provider: "ollama"
  baseUrl: "${VPS_LLM_URL}"  # из .env
  model: "llama3.2"
  apiPath: "/api/generate"
  timeout: 120000
  auth:
    type: "basic"
    user: "${VPS_LLM_USER}"      # из .env
    password: "${VPS_LLM_PASS}"  # из .env
```

В `.env` файле:
```
VPS_LLM_URL=https://185.31.165.227
VPS_LLM_USER=user
VPS_LLM_PASS=pass
```

## Настройка клиента

1. **Откройте конфигурационный файл:**
   ```bash
   # В корне проекта lesson-27-llm-vps
   nano config/server.yaml
   ```

2. **Обновите секцию `localLLM`** согласно примерам выше

3. **Перезапустите сервер:**
   ```bash
   ./gradlew run
   ```

4. **Проверьте статус в UI:**
   - Откройте `http://localhost:8080/client/chat.html`
   - В верхней части должен отображаться статус: `🌐 VPS: https://185.31.165.227`
   - Зеленый индикатор `✓ VPS (llama3.2)` означает успешное подключение к VPS
   - Синяя рамка вокруг информационной панели указывает на использование VPS
   - При наведении мыши на статус виден полный URL и модель

5. **Проверьте логи сервера:**
   - Все запросы к VPS логируются в формате OkHttp
   - Видно полный URL, тип подключения (VPS/локальная), запросы и ответы
   - См. [`LOGGING.md`](./LOGGING.md) для подробностей

## Обработка ошибок

Клиент автоматически обрабатывает следующие ошибки:

- **401 Unauthorized**: Неверные учетные данные
  - Сообщение: "Ошибка авторизации: неверные учетные данные для VPS LLM"
  - Fallback на OpenRouter (если настроен)

- **403 Forbidden**: Недостаточно прав
  - Сообщение: "Доступ запрещен: недостаточно прав для доступа к VPS LLM"

- **5xx Server Error**: Ошибка сервера VPS
  - Сообщение: "Ошибка сервера VPS LLM: HTTP [код]"

- **SSL Certificate Error**: Ошибка проверки SSL сертификата
  - **Решение**: Проверка SSL отключена автоматически для HTTPS соединений (аналог `curl -k`)
  - В логах будет предупреждение: `⚠️ SSL certificate verification is DISABLED`
  - Это необходимо для работы с самоподписанными сертификатами
  - Для production рекомендуется настроить правильный SSL сертификат (Let's Encrypt)

- **Timeout**: Превышено время ожидания
  - Проверьте настройку `timeout` в конфиге
  - Увеличьте значение для длинных ответов

## Рекомендации

### Безопасность
- ✅ Не открывать порт 11434 наружу; только через HTTPS + auth
- ✅ Использовать сильные пароли для basic auth
- ✅ Регулярно обновлять сертификаты SSL (Let's Encrypt)
- ✅ Ограничить доступ по IP, если возможно (nginx allow/deny)
- ⚠️ **SSL проверка отключена** для самоподписанных сертификатов (аналог `curl -k`)
  - Это необходимо для работы с VPS без доверенных сертификатов
  - Для production рекомендуется настроить Let's Encrypt

### Производительность
- ✅ Следить за ресурсами (CPU/RAM/GPU) на VPS
- ✅ Ограничивать параллельные запросы в nginx:
  ```nginx
  limit_conn_zone $binary_remote_addr zone=llm_limit:10m;
  limit_conn llm_limit 2;  # максимум 2 одновременных соединения
  ```
- ✅ Настроить таймауты в nginx (proxy_read_timeout, proxy_send_timeout)

### Логирование
- ✅ Все запросы к VPS логируются в формате OkHttp (см. `LOGGING.md`)
- ✅ В логах видно полный URL, тип подключения, запросы и ответы
- ✅ Добавить логирование в nginx для мониторинга запросов
- ✅ Настроить ротацию логов (logrotate)
- ✅ Мониторить логи Ollama на VPS

### Альтернативы
- Если нужен OpenAI-совместимый API — рассмотреть llama.cpp server `--api` или text-generation-inference
- Для production можно использовать более мощные модели (llama3.1, mistral и др.)

## Использование VPS LLM

Когда `localLLM.enabled: true` в конфиге, **все запросы к LLM** идут через VPS:

- ✅ **Простой чат** (`/api/llm/chat`) — использует VPS LLM
- ✅ **RAG чат** (`/api/chat`) — использует VPS LLM для генерации ответов
- ✅ **Reranker** (если включен) — может использовать VPS LLM для ранжирования чанков
- ✅ **Структурированные ответы** — используют VPS LLM с fallback на OpenRouter

Все запросы логируются с указанием типа подключения (VPS/локальная) и полного URL.

## Логирование

Все запросы к VPS логируются подробно в формате OkHttp:

- **Полный URL** запроса (видно, к какому серверу идет запрос)
- **Тип подключения** (VPS или локальная)
- **Заголовки** (Content-Type, Authorization без паролей)
- **Полный JSON запроса** (промпт, модель, параметры)
- **Полный JSON ответа** (или первые 5000 символов для длинных)
- **Статус код** и ошибки

Пример логов:
```
--> POST https://185.31.165.227/api/generate
Connection Type: VPS
Content-Type: application/json
Authorization: Basic (user: user)

{
  "model": "llama3.2",
  "prompt": "Привет!",
  "options": {
    "temperature": 0.7
  }
}
--> END POST

<-- 200 https://185.31.165.227/api/generate
Connection Type: VPS

{
  "model": "llama3.2",
  "response": "Привет! Как дела?",
  "done": true
}
<-- END HTTP
```

**Подробнее:** см. [`LOGGING.md`](./LOGGING.md)

## UI индикаторы

В интерфейсе чата отображается:

- **Синяя рамка** вокруг информационной панели = используется VPS
- **Текст**: `🌐 VPS: https://185.31.165.227` = полный URL сервера
- **Статус**: `✓ VPS (llama3.2)` = успешное подключение (зеленый)
- **Статус**: `⚠ VPS недоступна` = ошибка подключения (красный)
- **Подсказка при наведении**: полный URL и модель

**Подробнее:** см. [`VPS_UI_CHECK.md`](./VPS_UI_CHECK.md)

## Файлы урока
- `PROMPT.md` — промпт для реализации (по коммитам)
- `README.md` — этот файл
- `VPS_SETUP.md` — пошаговая установка и прокси
- `VPS_SETUP_FULL.md` — полная инструкция по настройке VPS
- `TESTING.md` — результаты тестов
- `CHAT_PROMPT.txt` — краткий промпт для агента
- `LOGGING.md` — документация по логированию запросов
- `VPS_UI_CHECK.md` — как проверить подключение к VPS в UI
- `SSH_SETUP_CURSOR.md` — настройка SSH подключения в Cursor

