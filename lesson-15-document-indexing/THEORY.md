# 🧠 Теория: Индексация документов и поиск по базе знаний

## Что такое индексация документов?

**Индексация документов** — это процесс подготовки документов для быстрого семантического поиска. Вместо поиска по ключевым словам, мы ищем по **смыслу** с помощью векторных представлений текста (эмбеддингов).

### Ключевая идея:

**Документы → Чанки → Эмбеддинги → База знаний → Семантический поиск**

---

## Эмбеддинги (Embeddings)

### Что это?

**Эмбеддинг** — это числовое представление текста в виде вектора (массива чисел). Похожие по смыслу тексты имеют похожие эмбеддинги.

### Пример:

```
Текст 1: "Как создать MCP сервер?"
Эмбеддинг 1: [0.123, -0.456, 0.789, ...]  (768 чисел)

Текст 2: "Создание MCP сервера на Kotlin"
Эмбеддинг 2: [0.125, -0.458, 0.791, ...]  (похожие числа)

Текст 3: "Рецепт борща"
Эмбеддинг 3: [-0.234, 0.567, -0.123, ...]  (совсем другие числа)
```

### Как работают эмбеддинги?

1. **Модель эмбеддингов** (например, `nomic-embed-text`) обучена на большом количестве текстов
2. Она понимает семантические связи между словами
3. Преобразует текст в вектор чисел фиксированной размерности (768 для nomic-embed-text)
4. Похожие тексты → похожие векторы

### Размерность эмбеддинга:

- **nomic-embed-text**: 768 чисел
- **text-embedding-ada-002** (OpenAI): 1536 чисел
- **all-MiniLM-L6-v2**: 384 числа

Чем больше размерность, тем точнее представление, но больше места для хранения.

---

## Чанки (Chunks)

### Что это?

**Чанк** — это фрагмент документа фиксированного размера. Длинные документы разбиваются на чанки для более точного поиска.

### Зачем нужны чанки?

1. **Ограничение контекста LLM**: LLM не может обработать очень длинный текст за раз
2. **Точность поиска**: Легче найти конкретный фрагмент, чем весь документ
3. **Эффективность**: Можно индексировать и искать только нужные части

### Пример разбивки:

```
Документ (3000 токенов):
┌─────────────────────────────────────────┐
│ Введение                                │
│ Глава 1: Основы                         │
│ Глава 2: Продвинутые темы               │
│ Заключение                               │
└─────────────────────────────────────────┘

Чанки (по 800 токенов):
┌─────────────────┐
│ Введение        │ ← Чанк 1
│ Глава 1 (часть) │
└─────────────────┘
┌─────────────────┐
│ Глава 1 (часть) │ ← Чанк 2 (перекрытие)
│ Глава 2 (часть) │
└─────────────────┘
┌─────────────────┐
│ Глава 2 (часть) │ ← Чанк 3 (перекрытие)
│ Заключение      │
└─────────────────┘
```

### Размер чанка:

- **Маленький (200-400 токенов)**: Высокая точность, много чанков
- **Средний (500-1000 токенов)**: Баланс между точностью и количеством
- **Большой (1500+ токенов)**: Меньше чанков, но ниже точность

**Рекомендация:** 500-1000 токенов для большинства задач.

---

## Перекрытия (Overlaps)

### Что это?

**Перекрытие** — это общая часть между соседними чанками. Соседние чанки имеют общий фрагмент текста.

### Зачем нужны перекрытия?

1. **Сохранение контекста**: Информация на границе чанков не теряется
2. **Улучшение поиска**: Если запрос попадает на границу, он найдётся в обоих чанках
3. **Связность**: Чанки лучше связаны между собой

### Пример перекрытия:

```
Чанк 1 (токены 0-800):
"Как создать MCP сервер? Сначала нужно установить SDK..."

Чанк 2 (токены 700-1500):
"...установить SDK. Затем создать класс Server..."

Перекрытие: токены 700-800
"установить SDK"
```

### Размер перекрытия:

- **Маленькое (20-50 токенов)**: Минимальное перекрытие, меньше дублирования
- **Среднее (50-100 токенов)**: Баланс между контекстом и дублированием
- **Большое (100-200 токенов)**: Больше контекста, но больше дублирования

**Рекомендация:** 50-100 токенов для большинства задач.

### Как вычисляется перекрытие?

```
Чанк 1: [0, 800] токенов
Чанк 2: [700, 1500] токенов  ← начинается с 700, а не с 801

Перекрытие = 800 - 700 = 100 токенов
```

---

## Нормализация векторов

### Что это?

**Нормализация** — это приведение вектора к определённому диапазону или масштабу. Эмбеддинги от разных моделей могут иметь разные диапазоны значений.

### Зачем нужна нормализация?

1. **Сравнимость**: Можно сравнивать векторы от разных моделей
2. **Стабильность**: Избегаем проблем с очень большими или очень маленькими числами
3. **Точность поиска**: Нормализованные векторы дают более точные результаты

### Методы нормализации:

#### 1. Нормализация к диапазону [0; 1]

**Формула:**
```
normalized_value = value / dimension
```

**Пример:**
```
Эмбеддинг: [123.45, -67.89, 234.56, ...]  (768 чисел)
Размерность: 768

Нормализованный: [0.161, -0.088, 0.305, ...]
```

**Плюсы:**
- Простая формула
- Все значения в [0; 1] (или около того)

**Минусы:**
- Не сохраняет относительные пропорции
- Может исказить семантику

#### 2. L2 нормализация (единичный вектор)

**Формула:**
```
magnitude = sqrt(sum(value²))
normalized_value = value / magnitude
```

**Пример:**
```
Эмбеддинг: [3, 4, 0, ...]
Magnitude = sqrt(3² + 4² + 0² + ...) = 5

Нормализованный: [0.6, 0.8, 0, ...]
```

**Плюсы:**
- Сохраняет направление вектора
- Стандартный метод для векторного поиска

**Минусы:**
- Сложнее вычислять

#### 3. Min-Max нормализация

**Формула:**
```
min = min(values)
max = max(values)
normalized_value = (value - min) / (max - min)
```

**Плюсы:**
- Все значения точно в [0; 1]

**Минусы:**
- Нужно знать min и max заранее
- Чувствительна к выбросам

### Рекомендация:

Для этого урока используй **нормализацию через деление на размерность** (метод 1) — она простая и достаточная для начала.

---

## Косинусное сходство (Cosine Similarity)

### Что это?

**Косинусное сходство** — это мера схожести между двумя векторами. Показывает, насколько "направлены" векторы в одну сторону.

### Формула:

```
cosine_similarity = (A · B) / (||A|| × ||B||)

где:
- A · B — скалярное произведение векторов
- ||A|| — длина вектора A (L2 норма)
- ||B|| — длина вектора B (L2 норма)
```

### Диапазон значений:

- **1.0**: Векторы полностью совпадают по направлению
- **0.0**: Векторы перпендикулярны (не связаны)
- **-1.0**: Векторы противоположны

### Пример:

```
Вектор A: [1, 0, 0]
Вектор B: [1, 0, 0]
Сходство: 1.0 (идентичны)

Вектор A: [1, 0, 0]
Вектор B: [0, 1, 0]
Сходство: 0.0 (перпендикулярны)

Вектор A: [1, 0, 0]
Вектор B: [0.8, 0.6, 0]
Сходство: 0.8 (похожи)
```

### Зачем использовать косинусное сходство?

1. **Не зависит от длины вектора**: Сравнивает только направление
2. **Быстро вычисляется**: Эффективный алгоритм
3. **Хорошо работает с эмбеддингами**: Эмбеддинги лучше сравнивать по направлению, а не по величине

### Реализация:

```kotlin
fun cosineSimilarity(vector1: List<Float>, vector2: List<Float>): Float {
    require(vector1.size == vector2.size) {
        "Vectors must have the same size"
    }
    
    // Скалярное произведение
    val dotProduct = vector1.zip(vector2).sumOf { (a, b) -> a * b }
    
    // Длины векторов (L2 норма)
    val magnitude1 = sqrt(vector1.sumOf { it * it }.toDouble())
    val magnitude2 = sqrt(vector2.sumOf { it * it }.toDouble())
    
    // Косинусное сходство
    return (dotProduct / (magnitude1 * magnitude2)).toFloat()
}
```

---

## Поиск по базе знаний

### Как работает поиск?

```
1. Пользователь: "Как создать MCP сервер?"
   ↓
2. Генерация эмбеддинга для запроса
   Эмбеддинг: [0.123, -0.456, ...]
   ↓
3. Нормализация эмбеддинга
   Нормализованный: [0.00016, -0.00059, ...]
   ↓
4. Поиск в базе знаний
   - Вычисление сходства с каждым чанком
   - Сортировка по убыванию сходства
   ↓
5. Возврат топ-N результатов
   - Чанк 1: сходство 0.89
   - Чанк 2: сходство 0.76
   - Чанк 3: сходство 0.65
```

### Алгоритм поиска:

1. **Полный перебор** (для начала):
   - Загрузить все чанки из БД
   - Вычислить сходство с каждым
   - Отсортировать и вернуть топ-N

2. **Векторные БД** (для больших баз):
   - FAISS, Qdrant, Pinecone
   - Индексы для быстрого поиска
   - Поддержка миллионов векторов

3. **Гибридный поиск**:
   - Семантический поиск (эмбеддинги)
   + Ключевые слова (BM25, TF-IDF)

### Порог релевантности:

- **Высокий (>0.8)**: Очень релевантные результаты
- **Средний (0.6-0.8)**: Релевантные результаты
- **Низкий (<0.6)**: Мало релевантные результаты

**Рекомендация:** Показывать результаты с сходством >0.5, но сортировать по убыванию.

---

## Пайплайн индексации

### Полный процесс:

```
1. Загрузка документа
   ↓
2. Разбивка на чанки (500-1000 токенов)
   ↓
3. Перекрытие между чанками (50-100 токенов)
   ↓
4. Генерация эмбеддингов для каждого чанка
   ↓
5. Нормализация эмбеддингов
   ↓
6. Сохранение в базу знаний
   - Документ
   - Чанки
   - Эмбеддинги
   ↓
7. Готово к поиску!
```

### Оптимизации:

1. **Батчинг**: Генерировать эмбеддинги для нескольких чанков одновременно
2. **Кэширование**: Не генерировать эмбеддинги повторно для одинаковых текстов
3. **Инкрементальная индексация**: Индексировать только новые документы
4. **Параллельная обработка**: Обрабатывать несколько документов параллельно

---

## Хранение в базе знаний

### Структура БД:

```
documents
├── id
├── file_path
├── title
├── content
├── indexed_at
└── chunk_count

document_chunks
├── id
├── document_id
├── chunk_index
├── content
├── start_index
├── end_index
├── token_count
├── embedding (JSON или BLOB)
└── created_at
```

### Форматы хранения эмбеддингов:

1. **JSON строка**:
   ```json
   "[0.123, -0.456, 0.789, ...]"
   ```
   - Плюсы: Легко читать и отлаживать
   - Минусы: Больше места

2. **BLOB (бинарные данные)**:
   ```
   [байты вектора]
   ```
   - Плюсы: Меньше места, быстрее
   - Минусы: Сложнее отлаживать

3. **Отдельная таблица**:
   ```
   embeddings
   ├── chunk_id
   ├── dimension_index
   └── value
   ```
   - Плюсы: Гибкость
   - Минусы: Сложнее и медленнее

**Рекомендация:** JSON строка для начала, BLOB для production.

---

## Интеграция с агентом

### Как использовать базу знаний в агенте?

```
1. Пользователь задаёт вопрос
   ↓
2. Агент ищет в базе знаний
   ↓
3. Получает релевантные чанки
   ↓
4. Добавляет чанки в контекст LLM
   ↓
5. LLM генерирует ответ на основе найденной информации
```

### Пример промпта для LLM:

```
Контекст из базы знаний:
[Чанк 1: информация о MCP сервере]
[Чанк 2: примеры кода]

Вопрос пользователя: "Как создать MCP сервер?"

Ответь на основе предоставленного контекста.
```

### Преимущества:

1. **Актуальная информация**: Агент использует информацию из документов
2. **Точность**: Ответы основаны на реальных данных
3. **Масштабируемость**: Можно добавлять новые документы без переобучения модели

---

## Выводы

1. **Эмбеддинги** — числовые представления текста для семантического поиска
2. **Чанки** — фрагменты документов фиксированного размера (500-1000 токенов)
3. **Перекрытия** — общие части между соседними чанками (50-100 токенов)
4. **Нормализация** — приведение векторов к определённому диапазону
5. **Косинусное сходство** — мера схожести между векторами
6. **Поиск по базе знаний** — поиск релевантных чанков по эмбеддингам
7. **Интеграция с агентом** — использование базы знаний для ответов на вопросы

---

## Дополнительные ресурсы

- [Ollama Documentation](https://ollama.ai/docs)
- [nomic-embed-text Model](https://ollama.ai/library/nomic-embed-text)
- [FAISS Documentation](https://github.com/facebookresearch/faiss)
- [Vector Databases Comparison](https://www.pinecone.io/learn/vector-database/)
- [Semantic Search Guide](https://www.sbert.net/examples/applications/semantic-search/README.html)

