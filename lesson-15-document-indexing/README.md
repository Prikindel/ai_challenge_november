# 🔥 День 15. Индексация документов

Система индексации документов с генерацией эмбеддингов для семантического поиска. Документы разбиваются на чанки, генерируются эмбеддинги через Ollama, нормализуются и сохраняются в базе знаний для быстрого поиска.

## 📋 Описание

Этот урок демонстрирует создание локальной базы знаний с эмбеддингами:
- **Разбивка документов на чанки** (500-1000 токенов) с перекрытиями (50-100 токенов)
- **Генерация эмбеддингов** через Ollama (модель `nomic-embed-text`)
- **Нормализация векторов** (приведение к [0; 1])
- **Хранение в SQLite** с векторами
- **Семантический поиск** по базе знаний (косинусное сходство)

## Демонстрация

[Посмотреть можно здесь](https://disk.yandex.ru/i/3lC9tazpQNxblg)

## 🎯 Пайплайн индексации

```
1. Загрузка документов (Markdown файлы)
   ↓
2. Разбивка текста на чанки (500-1000 токенов)
   ↓
3. Перекрытие между чанками (50-100 токенов)
   ↓
4. Генерация эмбеддингов через Ollama (nomic-embed-text)
   ↓
5. Нормализация векторов (приведение к [0; 1])
   ↓
6. Сохранение в базу знаний (SQLite с векторами)
   ↓
7. Поиск по базе знаний (косинусное сходство)
```

## 🏗️ Архитектура

```
Документы → TextChunker → Чанки
                              ↓
                    OllamaClient → Эмбеддинги
                              ↓
                    VectorNormalizer → Нормализованные векторы
                              ↓
                    KnowledgeBaseRepository → SQLite БД
                              ↓
                    KnowledgeBaseSearchService → Поиск
```

## 📁 Структура проекта

```
lesson-15-document-indexing/
├── README.md
├── THEORY.md
├── PROMPT.md
├── CHAT_PROMPT.txt
├── documents/                    # Папка с документами для индексации
│   ├── README.md
│   ├── article1.md
│   └── ...
├── config/
│   └── server.yaml
├── data/
│   └── knowledge_base.db         # SQLite база знаний
├── server/
│   └── src/main/kotlin/com/prike/...
└── client/
    ├── index.html
    ├── indexing.html             # Страница индексации
    ├── search.html               # Страница поиска
    ├── style.css
    └── app.js
```

## 🚀 Быстрый старт

### 1. Установка Ollama

1. Скачай и установи Ollama: https://ollama.ai/
2. Загрузи модель:
```bash
ollama pull nomic-embed-text
```
3. Проверь, что Ollama работает:
```bash
curl http://localhost:11434/api/tags
```

### 2. Конфигурация

Настройте `config/server.yaml`:

```yaml
server:
  port: 8080

ollama:
  baseUrl: "http://localhost:11434"
  model: "nomic-embed-text"
  timeout: 30000

knowledgeBase:
  databasePath: "data/knowledge_base.db"
  
indexing:
  chunkSize: 800
  overlapSize: 100
  documentsPath: "documents"
```

**Примечание:** `.env` файл не обязателен. Все параметры настраиваются в `config/server.yaml`. Если нужно переопределить значения для разных окружений, можно использовать `.env` (опционально).

### 3. Подготовка документов

Поместите Markdown файлы в папку `documents/`:

```bash
mkdir documents
# Скопируйте ваши .md файлы в documents/
```

### 4. Запуск

```bash
cd server
./gradlew run
```

Сервер будет доступен на http://localhost:8080

## 🎯 API Endpoints

### Индексация

- `POST /api/indexing/index` — индексировать документ/файл
  ```json
  {
    "filePath": "documents/article1.md"
  }
  ```

- `POST /api/indexing/index-directory` — индексировать директорию
  ```json
  {
    "directoryPath": "documents"
  }
  ```

- `GET /api/indexing/status` — статус индексации
- `GET /api/indexing/documents` — список индексированных документов

### Поиск

- `POST /api/search/query` — поиск по запросу
  ```json
  {
    "query": "Как создать MCP сервер?",
    "limit": 10
  }
  ```

- `GET /api/search/documents` — список индексированных документов

## 🔧 Компоненты системы

### 1. TextChunker

Разбивает текст на чанки с перекрытиями:
- Размер чанка: 500-1000 токенов (настраиваемо)
- Перекрытие: 50-100 токенов (настраиваемо)
- Сохранение структуры Markdown

### 2. OllamaClient

Клиент для работы с Ollama API:
- Генерация эмбеддингов через модель `nomic-embed-text`
- Размерность: 768 чисел
- Обработка ошибок и таймаутов

### 3. VectorNormalizer

Нормализация эмбеддингов:
- Нормализация к диапазону [0; 1] через деление на 768
- Или L2 нормализация (опционально)

### 4. KnowledgeBaseRepository

Хранение в SQLite:
- Таблица `documents` — документы
- Таблица `document_chunks` — чанки с эмбеддингами
- Эмбеддинги хранятся как JSON или BLOB

### 5. KnowledgeBaseSearchService

Поиск по базе знаний:
- Генерация эмбеддинга для запроса
- Поиск похожих чанков (косинусное сходство)
- Ранжирование результатов

## 📊 Примеры использования

### Индексация документа

```bash
curl -X POST http://localhost:8080/api/indexing/index \
  -H "Content-Type: application/json" \
  -d '{"filePath": "documents/article1.md"}'
```

### Поиск по базе знаний

```bash
curl -X POST http://localhost:8080/api/search/query \
  -H "Content-Type: application/json" \
  -d '{"query": "Как создать MCP сервер?", "limit": 5}'
```

### Результат поиска

```json
{
  "results": [
    {
      "chunkId": "chunk-1",
      "documentId": "doc-1",
      "content": "Для создания MCP сервера нужно...",
      "similarity": 0.89,
      "documentTitle": "MCP Server Guide"
    },
    {
      "chunkId": "chunk-2",
      "documentId": "doc-1",
      "content": "Пример кода для MCP сервера...",
      "similarity": 0.76,
      "documentTitle": "MCP Server Guide"
    }
  ]
}
```

## 🧪 Тестирование

### Тест разбивки на чанки

```
Вход: Длинный текст (3000 токенов)
Параметры: chunkSize=800, overlapSize=100

Ожидаемое поведение:
- Текст разбит на чанки по ~800 токенов
- Перекрытие между соседними чанками ~100 токенов
- Все чанки покрывают весь текст
```

### Тест генерации эмбеддингов

```
Вход: Текст чанка
Ожидаемое поведение:
- Эмбеддинг сгенерирован через Ollama
- Размерность: 768
- Все значения в разумных пределах
```

### Тест поиска

```
Вход: Запрос "создание MCP сервера"
Ожидаемое поведение:
- Найдены релевантные чанки
- Результаты отсортированы по сходству
- Показан score для каждого результата
```

## ✅ Проверка готовности

Все компоненты должны быть готовы:

- ✓ Ollama установлена и запущена
- ✓ Модель `nomic-embed-text` загружена
- ✓ Документы подготовлены в папке `documents/`
- ✓ Конфигурация настроена
- ✓ База данных создана

## 📚 Дополнительные материалы

- [THEORY.md](./THEORY.md) - теория по индексации документов и эмбеддингам
- [PROMPT.md](./PROMPT.md) - полное техническое задание
- [Ollama Documentation](https://ollama.ai/docs)
- [nomic-embed-text Model](https://ollama.ai/library/nomic-embed-text)

## 🎯 Итоговый результат

1. ✅ Система разбивки текста на чанки с перекрытиями
2. ✅ Интеграция с Ollama для генерации эмбеддингов
3. ✅ Нормализация векторов
4. ✅ База знаний (SQLite с векторами)
5. ✅ Пайплайн индексации документов
6. ✅ Поиск по базе знаний (косинусное сходство)
7. ✅ UI для индексации и поиска

**Важно:** 
- Документы индексируются один раз, эмбеддинги сохраняются в БД
- Поиск происходит по векторам без повторных вызовов к Ollama
- Можно переиндексировать при добавлении новых документов

---

**Готово к использованию!** 🚀

