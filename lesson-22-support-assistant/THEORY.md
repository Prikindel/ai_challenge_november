# Теория: Ассистент для поддержки пользователей с RAG и MCP

## 📖 Введение

Ассистент для поддержки пользователей объединяет несколько важных компонентов:
1. **CRM MCP** — получение контекста о пользователе и тикете
2. **RAG-поиск** — поиск ответов в FAQ и документации поддержки
3. **LLM генерация** — создание персонализированных ответов
4. **Контекст тикета** — учёт истории обращений при формировании ответа

Это позволяет создавать системы, которые:
- Отвечают на вопросы пользователей на основе документации
- Учитывают контекст пользователя (статус, подписка, история)
- Используют историю тикета для персонализации ответов
- Предлагают релевантные действия и решения
- Автоматически создают и управляют тикетами

---

## 🎯 Проблема: Традиционная поддержка vs AI-поддержка

### Традиционная поддержка (медленно и не всегда точно)

**Пример:**
```
Пользователь задаёт вопрос
  ↓
Оператор ищет ответ в базе знаний вручную
  ↓
Проверяет информацию о пользователе
  ↓
Пишет ответ вручную
  ↓
Пользователь ждёт ответа...
```

**Проблемы:**
- Занимает много времени
- Зависит от опыта оператора
- Может дать неточный ответ
- Не всегда учитывает контекст пользователя
- Повторяющиеся вопросы требуют одинаковых усилий

### AI-поддержка (быстро и точно)

**Пример:**
```
Пользователь задаёт вопрос
  ↓
Система автоматически получает контекст через CRM MCP
  ↓
Ищет ответ в документации через RAG
  ↓
LLM генерирует персонализированный ответ
  ↓
Пользователь получает точный ответ с учётом контекста
```

**Преимущества:**
- Быстрые ответы на основе документации
- Учёт контекста пользователя и тикета
- Персонализированные ответы
- Автоматическое управление тикетами
- Масштабируемость

---

## 🏗️ Архитектура системы поддержки

### Компоненты

1. **CRM MCP сервер** — управление пользователями и тикетами
2. **RAG MCP сервер** — поиск в документации поддержки
3. **SupportService** — оркестрация процесса поддержки
4. **SupportPromptBuilder** — формирование промптов для поддержки
5. **LLMService** — генерация ответов через LLM
6. **SupportController** — API endpoints
7. **UI** — интерфейс для взаимодействия с поддержкой

### Пайплайн поддержки

```
1. Получение контекста пользователя
   ↓
   CRM MCP: get_user(userId, email)
   ↓
   - Информация о пользователе (статус, подписка)
   - История тикетов
   ↓
2. Получение контекста тикета (если указан)
   ↓
   CRM MCP: get_ticket(ticketId)
   ↓
   - История сообщений
   - Статус и приоритет
   - Описание проблемы
   ↓
3. Поиск в документации
   ↓
   RAG MCP: rag_search_project_docs(query)
   ↓
   - Поиск в FAQ
   - Поиск в troubleshooting
   - Поиск в user guide
   - Поиск в auth guide
   ↓
4. Формирование промпта
   ↓
   SupportPromptBuilder:
   - Системный промпт (роль support agent)
   - Пользовательский промпт (вопрос + контекст + RAG)
   ↓
5. Генерация ответа
   ↓
   LLMService: generateAnswer(prompt)
   ↓
   - Персонализированный ответ
   - Учёт контекста тикета
   - Использование RAG-контекста
   ↓
6. Возврат результата
   ↓
   - Ответ
   - Источники (RAG)
   - Предложения (действия)
   - Рекомендация создать тикет (если нужно)
```

---

## 🔧 Технические детали

### CRM MCP сервер

**Модели данных:**

1. **User** — пользователь
   - `id` — уникальный идентификатор
   - `email` — email пользователя
   - `name` — имя пользователя
   - `status` — статус (ACTIVE, SUSPENDED, DELETED)
   - `subscription` — подписка (план, срок действия)
   - `createdAt` — дата создания

2. **Ticket** — тикет
   - `id` — уникальный идентификатор
   - `userId` — ID пользователя
   - `subject` — тема тикета
   - `description` — описание проблемы
   - `status` — статус (OPEN, IN_PROGRESS, RESOLVED, CLOSED)
   - `priority` — приоритет (LOW, MEDIUM, HIGH, URGENT)
   - `messages` — список сообщений
   - `createdAt`, `updatedAt` — даты

3. **TicketMessage** — сообщение в тикете
   - `id` — уникальный идентификатор
   - `ticketId` — ID тикета
   - `author` — автор (user/support)
   - `content` — содержимое сообщения
   - `timestamp` — время отправки

**Инструменты MCP:**

1. **get_user(userId, email)**
   - Получение пользователя по ID или email
   - Возвращает информацию о пользователе и подписке

2. **get_ticket(ticketId)**
   - Получение тикета по ID
   - Возвращает полную информацию о тикете с историей сообщений

3. **get_user_tickets(userId)**
   - Получение всех тикетов пользователя
   - Возвращает список тикетов с краткой информацией

4. **create_ticket(userId, subject, description)**
   - Создание нового тикета
   - Возвращает созданный тикет

5. **add_ticket_message(ticketId, author, content)**
   - Добавление сообщения в тикет
   - Возвращает созданное сообщение

### SupportService

**Основные методы:**

- `answerQuestion(request)` — обработка вопроса пользователя
- `getSupportContext(userId, ticketId)` — получение контекста поддержки
- `searchInDocs(question)` — поиск в документации через RAG
- `generateAnswer(context, question, ragResults)` — генерация ответа через LLM
- `generateSuggestions(question, answer)` — генерация предложений

**Логика работы:**

1. Получает контекст пользователя через CRM MCP
2. Получает контекст тикета (если указан)
3. Ищет релевантную документацию через RAG
4. Формирует промпт с вопросом, контекстом и RAG-результатами
5. Генерирует ответ через LLM
6. Генерирует предложения на основе ответа
7. Определяет, нужно ли создавать тикет

### SupportPromptBuilder

**Методы:**

- `buildSupportPrompt(context, question, ragResults)` — основной промпт для поддержки

**Структура промпта:**

1. **Системный промпт:**
   - Роль: support agent
   - Инструкции по работе с пользователями
   - Учёт контекста пользователя и тикета
   - Использование документации для ответов
   - Формат ответа (JSON)

2. **Пользовательский промпт:**
   - Информация о пользователе (если доступна)
   - Контекст тикета (если указан)
   - История сообщений тикета (если есть)
   - Вопрос пользователя
   - RAG-контекст из документации

### Модели данных

**SupportRequest:**
- `ticketId` — ID тикета (опционально)
- `userId` — ID пользователя (опционально)
- `question` — вопрос пользователя

**SupportContext:**
- `user` — информация о пользователе
- `ticket` — информация о тикете
- `userTickets` — история тикетов пользователя

**SupportResponse:**
- `answer` — ответ на вопрос
- `sources` — источники из RAG
- `suggestions` — предложения действий
- `ticketId` — ID тикета (если использовался)
- `shouldCreateTicket` — нужно ли создавать тикет

**Source:**
- `title` — название документа
- `content` — релевантный фрагмент
- `url` — путь к документу

---

## 🎨 Использование контекста тикета

### Зачем нужен контекст тикета?

1. **История обращений** — понимание предыдущих проблем
2. **Персонализация** — ответы с учётом контекста
3. **Непрерывность** — связь между сообщениями
4. **Эффективность** — не повторять уже данную информацию

### Как работает контекст тикета?

```
1. Пользователь указывает Ticket ID
   ↓
2. Получение тикета через CRM MCP
   ↓
   get_ticket(ticketId)
   ↓
3. Извлечение контекста
   ↓
   - Тема и описание тикета
   - История сообщений
   - Статус и приоритет
   ↓
4. Добавление в промпт
   ↓
   SupportPromptBuilder включает:
   - "Это продолжение тикета #123"
   - "Предыдущие сообщения: ..."
   - "Текущая проблема: ..."
   ↓
5. LLM использует контекст
   ↓
   - Учитывает предыдущие ответы
   - Не повторяет информацию
   - Предлагает следующие шаги
```

### Пример использования контекста

**Тикет #123:**
```
Сообщение 1 (user): "Не могу войти в аккаунт"
Сообщение 2 (support): "Проверьте правильность email и пароля"
Сообщение 3 (user): "Проверил, всё правильно"
```

**Новый вопрос:**
```
"Что делать дальше?"
```

**Ответ с контекстом:**
```
"Поскольку проверка email и пароля не помогла, попробуйте:
1. Восстановить пароль через ссылку 'Забыли пароль?'
2. Очистить кэш браузера
3. Попробовать другой браузер"
```

**Без контекста:**
```
"Попробуйте восстановить пароль" (не учитывает, что уже было предложено)
```

---

## 🔍 Использование RAG для поиска ответов

### Зачем нужен RAG в поддержке?

1. **FAQ** — быстрые ответы на частые вопросы
2. **Troubleshooting** — решение типичных проблем
3. **User Guide** — инструкции по использованию
4. **Auth Guide** — помощь с авторизацией

### Как работает RAG в поддержке?

```
1. Формирование запроса для RAG
   ↓
   Вопрос пользователя: "Почему не работает авторизация?"
   ↓
2. Поиск в документации поддержки
   ↓
   RAG MCP: rag_search_project_docs(query)
   ↓
   Фильтр: project/docs/support/
   ↓
3. Получение релевантных чанков
   ↓
   - FAQ: "Проблемы с авторизацией"
   - Troubleshooting: "Шаги по решению"
   - Auth Guide: "Инструкции по авторизации"
   ↓
4. Добавление в промпт
   ↓
   SupportPromptBuilder включает RAG-контекст в промпт
   ↓
5. LLM использует контекст для ответа
   ↓
   - Использует информацию из документации
   - Цитирует источники
   - Предлагает конкретные шаги
```

### Пример использования RAG-контекста

**Вопрос пользователя:**
```
"Почему не работает авторизация?"
```

**RAG-результаты:**
```
FAQ:
"Проблемы с авторизацией могут быть связаны с:
- Неправильным email или паролем
- Истёкшей сессией
- Проблемами с браузером"
```

**Ответ с RAG:**
```
"Проверьте следующее:
1. Правильность email и пароля
2. Не истекла ли сессия (попробуйте выйти и войти снова)
3. Очистите кэш браузера

Если проблема сохраняется, попробуйте восстановить пароль."
```

**Источники:**
- FAQ - Авторизация (project/docs/support/faq.md)

---

## 📊 Персонализация ответов

### Учёт статуса пользователя

**ACTIVE пользователь:**
```
"Как отменить подписку?"
→ "Вы можете отменить подписку в настройках аккаунта..."
```

**SUSPENDED пользователь:**
```
"Как отменить подписку?"
→ "Ваш аккаунт приостановлен. Для отмены подписки обратитесь в поддержку..."
```

### Учёт подписки

**Premium подписка:**
```
"Как использовать функцию X?"
→ "Как пользователь Premium, вы имеете доступ к функции X..."
```

**Без подписки:**
```
"Как использовать функцию X?"
→ "Функция X доступна только для Premium подписчиков..."
```

### Учёт истории тикетов

**Первый тикет:**
```
"Проблема с авторизацией"
→ Подробное объяснение с шагами
```

**Повторный тикет:**
```
"Проблема с авторизацией"
→ "Я вижу, что вы уже обращались с этой проблемой. Давайте попробуем другой подход..."
```

---

## 🚀 UI для поддержки

### Возможности UI

1. **Задать вопрос:**
   - Поле для вопроса
   - Опциональные поля: Ticket ID, User ID
   - Кнопка отправки

2. **Просмотр ответа:**
   - Ответ с форматированием
   - Источники (RAG) с ссылками
   - Предложения действий

3. **Управление тикетами:**
   - Список тикетов пользователя
   - Фильтрация по статусу
   - Поиск по теме
   - Просмотр истории тикета

4. **Создание тикета:**
   - Форма создания
   - Автоматическое заполнение при создании

5. **Экспорт диалога:**
   - JSON — полная структура
   - Текст — читаемый формат

### Примеры вопросов

На странице поддержки доступны примеры популярных вопросов:
- "Почему не работает авторизация?"
- "Как восстановить пароль?"
- "Как отменить подписку?"
- "Что делать, если сайт не загружается?"
- "Как изменить email?"
- "Как включить двухфакторную аутентификацию?"

---

## 🔗 Интеграция с CRM через MCP

### Преимущества MCP для CRM

1. **Изоляция** — CRM логика в отдельном сервере
2. **Переиспользование** — можно использовать в других проектах
3. **Тестирование** — легко тестировать отдельно
4. **Масштабирование** — можно заменить на реальный CRM

### Структура CRM MCP сервера

```
crm-mcp-server/
├── model/
│   ├── User.kt
│   └── Ticket.kt
├── storage/
│   └── InMemoryCRMStorage.kt
├── tools/
│   ├── GetUserTool.kt
│   ├── GetTicketTool.kt
│   ├── GetUserTicketsTool.kt
│   ├── CreateTicketTool.kt
│   └── AddTicketMessageTool.kt
└── server/
    └── CRMMCPServer.kt
```

### Замена на реальный CRM

Для замены на реальный CRM (например, Zendesk, Freshdesk):
1. Реализовать интерфейс `CRMStorage`
2. Подключиться к API реального CRM
3. Преобразовать данные в модели `User` и `Ticket`
4. Остальной код остаётся без изменений

---

## 📈 Метрики качества поддержки

### Что оценивает система

1. **Релевантность ответов** — соответствие вопросу
2. **Использование документации** — наличие источников
3. **Персонализация** — учёт контекста пользователя
4. **Предложения** — полезность действий

### Улучшение качества

1. **Точные промпты** — чёткие инструкции для LLM
2. **RAG-контекст** — использование документации
3. **Контекст тикета** — учёт истории обращений
4. **Итерации** — улучшение промптов на основе результатов

---

## 🎓 Ключевые выводы

1. **RAG для поддержки** — поиск ответов в документации
2. **CRM MCP** — удобный способ получения контекста
3. **Контекст тикета** — персонализация ответов
4. **Персонализация** — учёт статуса и подписки пользователя
5. **Управление тикетами** — автоматическое создание и обновление
6. **UI для удобства** — визуализация ответов и истории
7. **Источники** — цитирование документации
8. **Предложения** — рекомендации действий

---

## 🔮 Будущие улучшения

1. **Интеграция с реальным CRM** — Zendesk, Freshdesk
2. **Обучение на примерах** — улучшение промптов
3. **Мультиязычность** — поддержка разных языков
4. **Аналитика** — статистика по вопросам
5. **Эскалация** — передача сложных вопросов операторам
6. **Интеграция с чатом** — поддержка в реальном времени
7. **Голосовая поддержка** — интеграция с голосовыми ассистентами
