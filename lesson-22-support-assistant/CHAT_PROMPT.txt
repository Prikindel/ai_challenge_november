# Краткий промпт для реализации: 🔥 День 21. Автоматизация ревью кода

Ты — разработчик, создающий урок «🔥 День 21. Автоматизация ревью кода» в модуле `lesson-21-code-review`. Проект использует стек **Kotlin + Ktor + Clean Architecture + SQLite** с фронтендом на **vanilla JS**.

## Цель

Настроить пайплайн (CI) или кнопку в UI, в котором ассистент анализирует новый PR:

- **С помощью RAG использует документацию и код для контекста**
- **Через MCP получает diff и файлы из PR**
- **Выдаёт текст ревью с замечаниями** (найденные проблемы, потенциальные баги, советы по улучшению)

## Минимальные требования для зачёта

- ✅ GitHub Action запускается на PR (или кнопка в UI для запуска ревью)
- ✅ Ассистент получает diff через MCP
- ✅ Отдаёт: найденные проблемы, потенциальные баги, советы по улучшению

**Примечание:** Если настройка GitHub Actions сложная, можно сделать кнопку в UI, которая будет запускать ревью.

## Ключевые требования

1. **Копирование урока 20** — полностью скопировать код из `lesson-20-dev-assistant/` в `lesson-21-code-review/`, заменив все упоминания 20 на 21

2. **Расширение MCP git-сервера** — добавить инструменты `get_diff`, `get_changed_files`, `get_file_content`

3. **CodeReviewService** — создать сервис для анализа кода и генерации ревью

4. **Промпт для ревью** — специализированный промпт с использованием RAG-контекста

5. **API для ревью** — endpoints для запуска ревью и получения результатов

6. **UI для ревью** — кнопка запуска и отображение результатов

7. **Индексация кода** — возможность индексировать код для RAG-контекста

8. **GitHub Action (опционально)** — автоматический запуск ревью на PR

9. **Документация** — обновить README, THEORY, добавить ссылку в корневой README

## Поэтапная реализация (отдельные коммиты)

**КРИТИЧЕСКИ ВАЖНО:** Каждый шаг = отдельный git-коммит. После каждого шага **СТОП**, покажи изменения пользователю и дождись «ок, продолжай».

### Коммит 1: Копирование урока 20
- Скопировать весь код из `lesson-20-dev-assistant/` в `lesson-21-code-review/`
- Заменить все упоминания `lesson-20-dev-assistant` → `lesson-21-code-review`
- Заменить «День 20» → «День 21»
- Убедиться, что проект собирается и запускается

### Коммит 2: Расширение MCP git-сервера для работы с PR
- Обновить `GitMCPServer` — добавить инструменты:
  - `get_diff(base, head)` — получение diff между ветками
  - `get_changed_files(base, head)` — список изменённых файлов
  - `get_file_content(filePath)` — содержимое файла
- Обработка ошибок (файлы не найдены, не в git-репозитории)
- Обновить конфигурацию MCP сервера

### Коммит 3: Сервис для анализа кода (CodeReviewService)
- Создать `CodeReviewService` для анализа кода и генерации ревью
- Модели: `CodeReview`, `ReviewIssue`, `ReviewSuggestion`
- Методы: `reviewPR()`, `analyzeDiff()`, `checkAgainstDocs()`, `generateReview()`
- Логика: получение diff → RAG-поиск → анализ через LLM → парсинг результатов

### Коммит 4: Промпт для ревью кода
- Создать `CodeReviewPromptBuilder` для формирования промпта ревью
- Структура промпта: системное сообщение + RAG-контекст + diff + инструкции
- Требование JSON формата ответа
- Интеграция с RAG для правил стиля и примеров кода

### Коммит 5: API для ревью кода
- Создать `CodeReviewController` с endpoints:
  - `POST /api/review/pr` — запустить ревью PR
  - `GET /api/review/pr/{reviewId}` — получить результат ревью
  - `POST /api/review/diff` — ревью по diff (для UI)
- DTO: `ReviewPRRequest`, `ReviewPRResponse`, `ReviewDiffRequest`
- Обработка ошибок и валидация

### Коммит 6: UI для ревью кода (кнопка запуска)
- Создать `code-review.html` — страница для ревью кода
- Создать `code-review.js` — логика отправки запроса и отображения результатов
- Форма для указания веток (base и head)
- Кнопка "Запустить ревью"
- Отображение результатов: проблемы, предложения, общая оценка
- Стили: цветовая индикация серьёзности, подсветка кода

### Коммит 7: Индексация кода для RAG-контекста
- Обновить `DocumentIndexer` — метод `indexCodeFiles()`
- Конфигурация: `codeFiles.enabled`, `paths`, `extensions`, `excludePatterns`
- Использование в ревью: индексация изменённых файлов, использование существующего кода как контекста

### Коммит 8: GitHub Action (опционально) или улучшение UI
- Вариант A: Создать `.github/workflows/code-review.yml` для автоматического ревью на PR
- Вариант B: Улучшить UI (загрузка diff файла, примеры, экспорт ревью)
- Рекомендация: начать с улучшения UI (вариант B)

### Коммит 9: Документация и выводы
- Обновить `README.md`, `THEORY.md`, `PROMPT.md`, `CHAT_PROMPT.txt`
- Добавить раздел «Демонстрация» с ссылкой на видео
- Описать результаты и примеры использования
- Обновить корневой `README.md`, добавив ссылку на урок 21

## Технические детали

### Получение diff через MCP
- Вызов MCP инструмента `get_diff(base, head)`
- Парсинг diff для извлечения изменений
- Получение списка изменённых файлов
- При необходимости получение содержимого файлов

### Использование RAG для контекста
- Документация проекта (правила стиля, архитектура)
- Существующий код (паттерны, примеры)
- Индексация изменённых файлов (опционально)

### Генерация ревью через LLM
- Структура промпта: роль code reviewer + RAG-контекст + diff + инструкции
- Требование JSON формата ответа
- Парсинг ответа в модель `CodeReview`
- Валидация структуры

### Типы проблем
- BUG — потенциальные баги (утечки ресурсов, null checks)
- SECURITY — проблемы безопасности (SQL injection, XSS)
- PERFORMANCE — проблемы производительности (неоптимальные алгоритмы)
- STYLE — стиль кода (именование, длина функций)
- LOGIC — логические ошибки (неправильная бизнес-логика)
- DOCUMENTATION — документация (отсутствие комментариев)

## Важно

- Не ломать существующую функциональность из урока 20
- Использовать существующие компоненты: RAG, MCP, LLM
- После каждого коммита останавливаться и показывать изменения
- Пользователь сам делает merge/commit
- Все изменения должны быть протестированы
- Минимальные требования для зачёта должны быть выполнены
- Если GitHub Action сложно, использовать кнопку в UI
