# Краткий промпт для реализации: 🔥 День 22. Ассистент для поддержки пользователей

Ты — разработчик, создающий урок «🔥 День 22. Ассистент для поддержки пользователей» в модуле `lesson-22-support-assistant`. Проект использует стек **Kotlin + Ktor + Clean Architecture + SQLite** с фронтендом на **vanilla JS**.

## Цель

Создать мини-сервис для ответов на вопросы пользователей:

- **С помощью RAG использует FAQ и документацию поддержки для поиска ответов**
- **Через MCP получает контекст о пользователе и тикете из CRM**
- **Выдаёт персонализированные ответы с учётом контекста тикета**

## Минимальные требования для зачёта

- ✅ RAG для поиска ответов в FAQ и документации
- ✅ MCP для интеграции с CRM (получение контекста о пользователе и тикете)
- ✅ Ответы с учётом контекста тикета
- ✅ Управление тикетами (создание, просмотр истории)

## Ключевые требования

1. **Копирование урока 21** — полностью скопировать код из `lesson-21-code-review/` в `lesson-22-support-assistant/`, заменив все упоминания 21 на 22

2. **Создание CRM MCP сервера** — новый MCP сервер для управления пользователями и тикетами

3. **Индексация FAQ и документации поддержки** — добавить индексацию документации поддержки (FAQ, troubleshooting, user guide, auth guide)

4. **SupportService** — создать сервис для обработки вопросов с интеграцией CRM и RAG

5. **Промпт для поддержки** — специализированный промпт с использованием RAG-контекста и контекста тикета

6. **API для поддержки** — endpoints для задавания вопросов и управления тикетами

7. **UI для поддержки** — интерфейс для взаимодействия с поддержкой

8. **Улучшение UI и функциональности** — управление тикетами, экспорт диалога, примеры вопросов

9. **Документация** — обновить README, THEORY, добавить ссылку в корневой README

## Поэтапная реализация (отдельные коммиты)

**КРИТИЧЕСКИ ВАЖНО:** Каждый шаг = отдельный git-коммит. После каждого шага **СТОП**, покажи изменения пользователю и дождись «ок, продолжай».

### Коммит 1: Копирование урока 21
- Скопировать весь код из `lesson-21-code-review/` в `lesson-22-support-assistant/`
- Заменить все упоминания `lesson-21-code-review` → `lesson-22-support-assistant`
- Заменить «День 21» → «День 22»
- Убедиться, что проект собирается и запускается

### Коммит 2: Создание CRM MCP сервера
- Создать новый MCP сервер `crm-mcp-server/`
- Модели: `User`, `Ticket`, `TicketMessage`
- Хранилище: `InMemoryCRMStorage` с тестовыми данными
- Инструменты: `get_user`, `get_ticket`, `get_user_tickets`, `create_ticket`, `add_ticket_message`
- Следовать паттерну существующих MCP серверов

### Коммит 3: Индексация FAQ и документации поддержки
- Добавить документацию поддержки: `faq.md`, `troubleshooting.md`, `user-guide.md`, `auth-guide.md`
- Обновить `DocumentIndexer` — метод `indexSupportDocs()`
- Новый MCP инструмент: `rag_index_support_docs`
- Обновить конфигурацию: `supportDocsPath` в `server.yaml`
- Endpoint: `POST /api/indexing/index-support-docs`

### Коммит 4: SupportService
- Создать `SupportService` для обработки вопросов
- Модели: `SupportRequest`, `SupportContext`, `SupportResponse`, `Source`
- `CRMMCPClient` и `CRMMCPService` для работы с CRM
- Методы: `answerQuestion()`, `getSupportContext()`, `searchInDocs()`, `generateAnswer()`, `generateSuggestions()`
- Логика: получение контекста → RAG-поиск → генерация ответа через LLM

### Коммит 5: Промпт для поддержки
- Создать `SupportPromptBuilder` для формирования промпта поддержки
- Структура промпта: системное сообщение + контекст пользователя/тикета + RAG-контекст + вопрос
- Требование JSON формата ответа
- Интеграция с RAG для использования документации

### Коммит 6: API для поддержки
- Создать `SupportController` с endpoints:
  - `POST /api/support/ask` — задать вопрос
  - `GET /api/support/ticket/{ticketId}` — получить тикет
  - `GET /api/support/user/{userId}/tickets` — получить тикеты пользователя
  - `POST /api/support/ticket` — создать тикет
  - `POST /api/support/ticket/{ticketId}/message` — добавить сообщение
- DTO: `SupportQuestionRequest`, `SupportQuestionResponse`, `TicketDto`, и т.д.
- Конфигурация CRM MCP в `server.yaml`

### Коммит 7: UI для поддержки
- Создать `support.html` — страница для поддержки
- Создать `support.js` — логика отправки вопроса и отображения результатов
- Форма для ввода вопроса (Ticket ID, User ID, Question)
- Отображение ответа, источников, предложений
- Загрузка истории тикета
- Добавить ссылку в навигацию всех HTML файлов

### Коммит 8: Улучшение UI и функциональности
- Управление тикетами: список, фильтрация, поиск, просмотр истории
- Создание тикета: форма с User ID, Subject, Description
- Экспорт диалога: JSON и Текст форматы
- Примеры вопросов: кнопки с популярными вопросами

### Коммит 9: Документация и выводы
- Обновить `README.md`, `THEORY.md`, `PROMPT.md`, `CHAT_PROMPT.txt`
- Добавить раздел «Демонстрация» с ссылкой на видео
- Описать результаты и примеры использования
- Обновить корневой `README.md`, добавив ссылку на урок 22

## Технические детали

### Интеграция с CRM через MCP
- Вызов MCP инструментов: `get_user()`, `get_ticket()`, `get_user_tickets()`
- Получение контекста пользователя (статус, подписка)
- Получение контекста тикета (история сообщений)

### Использование RAG для поиска ответов
- Документация поддержки (FAQ, troubleshooting, user guide, auth guide)
- Фильтр: `project/docs/support/`
- Поиск релевантных чанков для ответа

### Генерация ответа через LLM
- Структура промпта: роль support agent + контекст пользователя/тикета + RAG-контекст + вопрос
- Требование JSON формата ответа
- Парсинг ответа в модель `SupportResponse`

### Контекст тикета
- История сообщений добавляется в промпт
- LLM учитывает предыдущие ответы
- Не повторяет уже данную информацию

### Персонализация
- Учёт статуса пользователя (ACTIVE, SUSPENDED, DELETED)
- Учёт подписки (Premium, без подписки)
- Учёт истории тикетов

## Важно

- Не ломать существующую функциональность из урока 21
- Использовать существующие компоненты: RAG, MCP, LLM
- После каждого коммита останавливаться и показывать изменения
- Пользователь сам делает merge/commit
- Все изменения должны быть протестированы
- Минимальные требования для зачёта должны быть выполнены
