# Заметки по интеграции Koog в урок 22

## Текущее состояние

Урок 22 переделан на использование Koog для работы с AI агентом поддержки. Основные изменения:

### ✅ Реализовано

1. **Koog Tools для CRM**:
   - `GetUserTool` - получение информации о пользователе
   - `GetTicketTool` - получение информации о тикете
   - `GetUserTicketsTool` - получение всех тикетов пользователя
   - `CreateTicketTool` - создание нового тикета
   - `AddTicketMessageTool` - добавление сообщения в тикет

2. **Koog Tool для RAG**:
   - `SearchSupportDocsTool` - поиск в документации поддержки

3. **SupportAgentKoog**:
   - Использует Koog tools для работы с CRM и RAG
   - Формирует системные промпты с инструкциями по использованию инструментов
   - Интегрируется с существующими MCP сервисами

4. **SupportService**:
   - Обёртка над SupportAgentKoog
   - Упрощённый интерфейс для контроллеров

### ⚠️ Временные решения

1. **HTTP вызовы вместо Koog prompt executor**:
   - В `SupportAgentKoog.generateAnswerWithKoog()` используется прямой HTTP вызов к OpenRouter API
   - Причина: точные импорты для Koog prompt executor требуют дополнительного изучения
   - TODO: Заменить на полный Koog prompt executor API

2. **BaseTool обёртка**:
   - Создан базовый класс `BaseTool` для унификации интерфейса инструментов
   - Метод `toKoogTool()` пока возвращает сам объект
   - TODO: Реализовать правильное преобразование в Koog Tool API

### 🔄 Что можно улучшить

1. **Полная интеграция с Koog AIAgent**:
   ```kotlin
   // Вместо прямых HTTP вызовов использовать:
   val executor = simpleOpenRouterExecutor(apiKey)
   val agent = AIAgent(
       executor = executor,
       systemPrompt = systemPrompt,
       tools = allTools.map { it.toKoogTool() },
       llmModel = OpenRouterModels.OpenAIGPT4oMini
   )
   val result = agent.run(userMessage)
   ```

2. **Использование Koog prompt DSL**:
   ```kotlin
   val prompt = prompt {
       system(systemPrompt)
       user(userMessage)
   }
   val result = executor.execute(prompt, model)
   ```

3. **Интеграция с Koog Ktor plugin**:
   - Использовать `install(Koog)` в Main.kt
   - Использовать `llm()` и `agent()` функции в контроллерах

4. **Использование Koog features**:
   - HistoryCompressionFeature для оптимизации токенов
   - MemoryFeature для сохранения контекста
   - OpenTelemetryFeature для трассировки

### 📝 Следующие шаги

1. Изучить правильные импорты для Koog API из документации
2. Заменить HTTP вызовы на Koog prompt executor
3. Реализовать правильное преобразование BaseTool в Koog Tool
4. Добавить Koog features для улучшения функциональности
5. Интегрировать Koog Ktor plugin для упрощения кода

### 📚 Полезные ссылки

- [Koog документация](https://docs.koog.ai/)
- [Koog гайд](./KOOG_GUIDE.md)
- [Примеры Koog](https://docs.koog.ai/examples/)

