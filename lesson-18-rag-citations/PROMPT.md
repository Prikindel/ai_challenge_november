# Промпт для реализации: 🔥 День 18. Цитаты и источники

Ты — разработчик, создающий урок «🔥 День 18. Цитаты и источники» в модуле `lesson-18-rag-citations`. Проект использует стек **Kotlin + Ktor + Clean Architecture + SQLite** с фронтендом на **vanilla JS**. В качестве базы мы **копируем весь код из урока 17** и развиваем его — меняем все упоминания `lesson-17-rag-reranking` на `lesson-18-rag-citations`, не ломая существующую функциональность.

## 🎯 Цель урока

Доработать RAG-систему так, чтобы модель **обязательно возвращала цитаты и ссылки на источники** из базы знаний в каждом ответе. При клике на ссылку должен открываться документ для просмотра. Протестировать на 5 вопросах и оценить, стало ли меньше галлюцинаций.

### Ключевая идея

```
Поиск чанков → Формирование промпта с требованием цитат → Запрос к LLM → Парсинг цитат из ответа → Отображение ссылок → Просмотр документов
```

LLM должен:
- **Обязательно указывать источники** для каждой части ответа
- Использовать формат ссылок, который можно распарсить
- Ссылаться только на те документы, которые были переданы в контексте

Пользователь должен:
- Видеть ссылки на источники прямо в ответе
- Кликать на ссылки и открывать документы для просмотра
- Понимать, откуда взята информация

---

## 📋 Поэтапная реализация (отдельные коммиты)

**КРИТИЧЕСКИ ВАЖНО:**
- Каждый перечисленный шаг = отдельный git-коммит.
- После каждого шага **СТОП**, покажи изменения пользователю и дождись «ок, продолжай».
- Пользователь сам делает merge/commit далее.

### Коммит 1: Копирование урока 17

**Задача:** полностью скопировать содержимое `lesson-17-rag-reranking/` в `lesson-18-rag-citations/`.

**Действия:**
1. Скопировать все файлы (server, client, config, README, THEORY, PROMPT, CHAT_PROMPT, документы, данные).
2. Обновить названия:
   - Папка проекта, Gradle settings, package references, заголовки, README
   - Все текстовые упоминания `lesson-17-rag-reranking` → `lesson-18-rag-citations`
   - «День 17» → «День 18»
3. Убедиться, что проект собирается и запускается без функциональных изменений.

**После коммита:** остановиться, показать diff пользователю.

---

### Коммит 2: Обновление промпта для обязательных цитат

**Задача:** изменить `PromptBuilder`, чтобы LLM обязательно возвращал цитаты и ссылки на источники.

**Компоненты:**
1. `PromptBuilder`:
   - Добавить в системный промпт требование указывать источники
   - Добавить инструкции по формату цитат
   - Включить информацию о доступных документах в контекст

2. Формат цитат:
   ```
   [Источник: название_документа](document_path)
   ```
   или
   ```
   [1] название_документа (document_path)
   ```

3. Обновить системный промпт:
   ```
   Ты — помощник, который отвечает на вопросы на основе предоставленного контекста из базы знаний.
   
   ВАЖНО: Ты ОБЯЗАН указывать источники для каждой части ответа.
   
   Формат цитат:
   - Для каждого факта или утверждения указывай источник в формате: [Источник: название_документа](путь_к_документу)
   - Если информация взята из нескольких источников, указывай все
   - Если в контексте нет информации для ответа, скажи об этом и НЕ придумывай факты
   
   Доступные документы в контексте:
   {список_документов_с_путями}
   
   Контекст из базы знаний:
   {contextChunks}
   
   Вопрос: {question}
   
   Ответ (с обязательными ссылками на источники):
   ```

**Важно:**
- Промпт должен явно требовать цитаты
- Формат должен быть парсируемым
- LLM должен понимать, какие документы доступны

**После коммита:** остановиться.

---

### Коммит 3: Парсер цитат из ответа LLM

**Задача:** создать сервис для парсинга цитат из ответа LLM и извлечения ссылок на источники.

**Компоненты:**
1. `CitationParser` (domain/service):
   - Парсит ответ LLM и извлекает цитаты
   - Поддерживает несколько форматов (Markdown ссылки, нумерованные ссылки)
   - Валидирует, что ссылки указывают на реальные документы из контекста

2. Модель `Citation`:
   ```kotlin
   data class Citation(
       val text: String,              // текст цитаты или фрагмент ответа
       val documentPath: String,      // путь к документу
       val documentTitle: String,     // название документа
       val chunkId: String? = null,   // опционально: ID чанка
       val position: IntRange? = null // позиция в ответе
   )
   ```

3. Модель `AnswerWithCitations`:
   ```kotlin
   data class AnswerWithCitations(
       val answer: String,            // полный ответ
       val citations: List<Citation>, // список цитат
       val rawAnswer: String          // оригинальный ответ от LLM
   )
   ```

4. Логика парсинга:
   - Искать паттерны типа `[Источник: ...](...)` или `[1] ... (...)`
   - Извлекать путь к документу
   - Проверять, что документ был в контексте
   - Сохранять позицию цитаты в тексте

**Примеры форматов для парсинга:**
- `[Источник: Создание MCP сервера](documents/01-mcp-server-creation.md)`
- `[1] Создание MCP сервера (documents/01-mcp-server-creation.md)`
- `Источник: documents/01-mcp-server-creation.md`

**После коммита:** остановиться.

---

### Коммит 4: Интеграция цитат в RAG-сервис

**Задача:** интегрировать парсинг цитат в `RAGService` и обновить ответы.

**Действия:**
1. `RAGService`:
   - После получения ответа от LLM парсить цитаты через `CitationParser`
   - Добавлять информацию о цитатах в `RAGResponse`
   - Валидировать, что все цитаты ссылаются на реальные документы из контекста

2. Обновить `RAGResponse`:
   ```kotlin
   data class RAGResponse(
       val question: String,
       val answer: String,                    // ответ с цитатами
       val citations: List<Citation>,         // список цитат
       val contextChunks: List<RetrievedChunk>,
       val tokensUsed: Int? = null,
       val filterStats: FilterStats? = null,
       val rerankInsights: List<RerankInsight>? = null
   )
   ```

3. Обработка ошибок:
   - Если LLM не вернул цитаты — предупреждение в логах
   - Если цитата ссылается на несуществующий документ — игнорировать или предупреждать
   - Если нет цитат вообще — можно вернуть ответ без цитат, но с предупреждением

**После коммита:** остановиться.

---

### Коммит 5: API для просмотра документов

**Задача:** добавить API endpoint для получения содержимого документа для просмотра.

**Компоненты:**
1. `DocumentController`:
   - `GET /api/documents/{documentPath}` — получить содержимое документа
   - Поддержка разных форматов (Markdown, текст)
   - Возвращать метаданные документа (название, путь, дата индексации)

2. Модель ответа:
   ```kotlin
   data class DocumentViewResponse(
       val documentPath: String,
       val documentTitle: String,
       val content: String,
       val indexedAt: String?,
       val chunksCount: Int?
   )
   ```

3. Безопасность:
   - Валидировать путь документа (не допускать `../`)
   - Проверять, что документ существует в базе знаний
   - Возвращать только документы, которые были проиндексированы

**После коммита:** остановиться.

---

### Коммит 6: UI для отображения цитат и просмотра документов

**Задача:** обновить UI для отображения цитат в ответе и просмотра документов по клику.

**Компоненты:**
1. Обновить `rag-compare.html`:
   - Парсить ответ и выделять ссылки на источники
   - Делать ссылки кликабельными
   - При клике открывать модальное окно или новую вкладку с документом
   - Показывать список всех использованных источников

2. Создать `document-viewer.html`:
   - Страница для просмотра документа
   - Отображение Markdown с подсветкой синтаксиса (опционально)
   - Кнопка "Назад" для возврата к ответу
   - Подсветка релевантных чанков (если есть)

3. JavaScript:
   - `citations.js` — логика для парсинга и отображения цитат
   - `document-viewer.js` — логика для загрузки и отображения документа
   - Интеграция с API `/api/documents/{path}`

4. Стили:
   - Выделение ссылок на источники в ответе
   - Стили для модального окна просмотра документа
   - Индикация использованных источников

**Пример отображения:**
```
Ответ:
Для создания MCP сервера нужно установить SDK [Источник: Создание MCP сервера](documents/01-mcp-server-creation.md).

Использованные источники:
- Создание MCP сервера (documents/01-mcp-server-creation.md) [Просмотреть]
```

**После коммита:** остановиться.

---

### Коммит 7: Тестирование и метрики

**Задача:** добавить функциональность для тестирования на 5 вопросах и оценки галлюцинаций.

**Компоненты:**
1. `CitationAnalyzer` (domain/service):
   - Анализирует ответ и проверяет наличие цитат
   - Проверяет, что все цитаты валидны (ссылаются на реальные документы)
   - Подсчитывает метрики:
     - Процент ответов с цитатами
     - Количество цитат на ответ
     - Процент валидных цитат
     - Процент ответов без галлюцинаций (все факты имеют источники)

2. API endpoint `POST /api/rag/test-citations`:
   - Принимает список вопросов (5 вопросов)
   - Для каждого вопроса делает RAG-запрос
   - Анализирует ответы и возвращает метрики

3. Модель `CitationTestResult`:
   ```kotlin
   data class CitationTestResult(
       val question: String,
       val hasCitations: Boolean,
       val citationsCount: Int,
       val validCitationsCount: Int,
       val answer: String,
       val citations: List<Citation>
   )
   
   data class CitationTestReport(
       val results: List<CitationTestResult>,
       val metrics: CitationMetrics
   )
   
   data class CitationMetrics(
       val totalQuestions: Int,
       val questionsWithCitations: Int,
       val averageCitationsPerAnswer: Double,
       val validCitationsPercentage: Double,
       val answersWithoutHallucinations: Int  // все факты имеют источники
   )
   ```

4. UI для тестирования:
   - Страница `citation-test.html`:
     - Форма для ввода 5 вопросов
     - Кнопка "Запустить тест"
     - Таблица с результатами для каждого вопроса
     - Общие метрики (процент ответов с цитатами, среднее количество цитат и т.д.)

**После коммита:** остановиться.

---

### Коммит 8: Документация и выводы

**Задача:** задокументировать урок и подготовить выводы о влиянии цитат на галлюцинации.

**Действия:**
1. Обновить `README.md`, `THEORY.md`, `PROMPT.md`, `CHAT_PROMPT.txt` под День 18.
2. Добавить раздел «Демонстрация» с ссылкой на видео (формат: Видео + Код).
3. Описать результаты тестирования:
   - Стало ли меньше галлюцинаций с обязательными цитатами?
   - Какой процент ответов содержит цитаты?
   - Как цитаты влияют на качество ответов?
4. Добавить примеры тестовых вопросов и ожидаемых результатов.
5. Обновить корневой `README.md`, добавив ссылку на урок 18.

**После коммита:** показать изменения пользователю.

---

## 🧠 Технические детали

### Формат цитат

LLM должен использовать один из форматов:
1. Markdown ссылки: `[Источник: название](путь)`
2. Нумерованные ссылки: `[1] название (путь)`
3. Простые ссылки: `Источник: путь`

Парсер должен поддерживать все форматы и извлекать:
- Текст источника (название документа)
- Путь к документу
- Позицию в ответе

### Валидация цитат

- Проверять, что путь документа существует в базе знаний
- Проверять, что документ был в контексте запроса
- Игнорировать невалидные цитаты с предупреждением в логах

### Просмотр документов

- Документы должны открываться в модальном окне или новой вкладке
- Поддержка Markdown с базовой подсветкой
- Возможность вернуться к ответу
- Показывать метаданные документа (дата индексации, количество чанков)

### Метрики галлюцинаций

Галлюцинация = факт в ответе без источника в контексте.

Метрики:
- **Процент ответов с цитатами** — сколько ответов содержат хотя бы одну цитату
- **Среднее количество цитат** — сколько цитат в среднем на ответ
- **Процент валидных цитат** — сколько цитат ссылаются на реальные документы
- **Процент ответов без галлюцинаций** — сколько ответов, где все факты имеют источники

### Тестовые вопросы

Рекомендуемые 5 вопросов для тестирования:
1. "Как создать MCP сервер?" — должен найти релевантные чанки и указать источники
2. "Что такое косинусное сходство?" — должен использовать цитаты из документации
3. "Как работает реранкинг?" — должен ссылаться на соответствующие документы
4. "Как генерировать эмбеддинги через Ollama?" — должен указать источники
5. "Что такое перекрытие чанков?" — должен использовать цитаты

---

## 📊 Пример сценария

1. **Вопрос:** "Как создать MCP сервер?"
   - **Ответ с цитатами:**
     ```
     Для создания MCP сервера нужно установить SDK [Источник: Создание MCP сервера](documents/01-mcp-server-creation.md).
     Затем создать класс Server и зарегистрировать инструменты [Источник: Создание MCP сервера](documents/01-mcp-server-creation.md).
     ```
   - **Использованные источники:**
     - Создание MCP сервера (documents/01-mcp-server-creation.md) [Просмотреть]

2. **Клик на ссылку:**
   - Открывается модальное окно с содержимым документа
   - Пользователь может прочитать полный документ
   - Кнопка "Назад" возвращает к ответу

3. **Тестирование:**
   - Запускается тест на 5 вопросах
   - Система анализирует каждый ответ
   - Выводится отчет: 100% ответов содержат цитаты, среднее 2.4 цитаты на ответ

---

## 🎓 Цели урока

- Научиться требовать от LLM обязательные цитаты и ссылки на источники
- Реализовать парсинг и валидацию цитат из ответов
- Добавить функциональность просмотра документов по ссылкам
- Протестировать систему на 5 вопросах
- Оценить влияние обязательных цитат на галлюцинации
- Получить RAG-систему с обязательными ссылками на источники (Видео + Код)

---

## 📚 Документация

- `PROMPT.md` — текущее ТЗ (этот файл).
- `THEORY.md` — теория про цитаты, источники и борьбу с галлюцинациями в RAG.
- `README.md` — руководство по запуску и использованию урока.
- `CHAT_PROMPT.txt` — краткая версия ТЗ для агентной разработки.

