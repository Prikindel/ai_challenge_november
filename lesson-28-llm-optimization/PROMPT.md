# Промпт для реализации: 🔥 День 28. Оптимизация и адаптация локальной LLM

Ты — разработчик, создающий урок «🔥 День 28. Оптимизация и адаптация локальной LLM» в модуле `lesson-28-llm-optimization`. База проекта — приложение из Дня 25/26/27 с локальной LLM (локально или на VPS).

## 🎯 Цель урока

Оптимизировать локальную LLM под конкретную задачу, изменяя параметры модели (квантование, контекстное окно, температура, max tokens) и настраивая prompt-шаблоны для повышения точности ответов.

### Ключевая идея

```
Базовая LLM → Изменение параметров → Настройка промптов → Тестирование → Сравнение результатов
```

**Система должна:**
- Поддерживать изменение параметров модели (temperature, max_tokens, context_window)
- Позволять настраивать prompt-шаблоны для разных задач
- Тестировать и сравнивать результаты до/после оптимизации
- Документировать найденные оптимальные параметры

---

## 📋 Поэтапная реализация (отдельные коммиты)

**КРИТИЧЕСКИ ВАЖНО:**
- Каждый перечисленный шаг = отдельный git-коммит.
- После каждого шага **СТОП**, покажи изменения пользователю и дождись «ок, продолжай».
- Пользователь сам делает merge/commit далее.

---

### Коммит 1: Выбор базового урока и подготовка

**Задача:** выбрать базовый урок с локальной LLM и подготовить структуру для оптимизации.

**Действия:**
1. Выбрать базовый урок (рекомендуется `lesson-25-local-llm` или `lesson-27-llm-vps`)
2. Скопировать структуру проекта в `lesson-28-llm-optimization/`
3. Обновить названия:
   - Папка проекта, Gradle settings, package references
   - Все текстовые упоминания → `lesson-28-llm-optimization`
   - «День X» → «День 28»
4. Убедиться, что проект собирается и запускается

**После коммита:** остановиться, показать структуру проекта.

---

### Коммит 2: Модель конфигурации параметров LLM

**Задача:** создать модель данных для параметров LLM и обновить конфигурацию.

**Компоненты:**

1. **Модель параметров** (`domain/model/LLMParameters.kt`):
   ```kotlin
   data class LLMParameters(
       val temperature: Double = 0.7,        // 0.0-2.0, креативность
       val maxTokens: Int = 2048,            // максимальная длина ответа
       val topP: Double = 0.9,                // nucleus sampling
       val topK: Int = 40,                    // top-k sampling
       val repeatPenalty: Double = 1.1,       // штраф за повторения
       val contextWindow: Int = 4096,         // размер контекстного окна
       val seed: Int? = null                  // seed для воспроизводимости
   )
   ```

2. **Конфигурация** (`config/server.yaml`):
   ```yaml
   localLLM:
     enabled: true
     provider: "ollama"
     baseUrl: "http://localhost:11434"  # или VPS URL
     model: "llama3.2"
     apiPath: "/api/generate"
     timeout: 120000
     parameters:
       temperature: 0.7
       maxTokens: 2048
       topP: 0.9
       topK: 40
       repeatPenalty: 1.1
       contextWindow: 4096
       seed: null
   ```

3. **Config класс** (`config/Config.kt`):
   - Парсинг `LLMParameters` из конфигурации
   - Значения по умолчанию

**После коммита:** остановиться.

---

### Коммит 3: Обновление LocalLLMClient для поддержки параметров

**Задача:** добавить передачу параметров в запросы к LLM.

**Компоненты:**

1. **Обновление LocalLLMClient** (`data/client/LocalLLMClient.kt`):
   ```kotlin
   suspend fun generate(
       model: String,
       prompt: String,
       parameters: LLMParameters = LLMParameters(),
       stream: Boolean = false
   ): String {
       val request = OllamaGenerateRequest(
           model = model,
           prompt = prompt,
           stream = stream,
           options = mapOf(
               "temperature" to parameters.temperature,
               "num_predict" to parameters.maxTokens,
               "top_p" to parameters.topP,
               "top_k" to parameters.topK,
               "repeat_penalty" to parameters.repeatPenalty,
               "num_ctx" to parameters.contextWindow,
               "seed" to (parameters.seed ?: -1)
           )
       )
       // ... реализация запроса
   }
   ```

2. **Модель запроса** (`domain/model/LocalLLMModels.kt`):
   ```kotlin
   data class OllamaGenerateRequest(
       val model: String,
       val prompt: String,
       val stream: Boolean = false,
       val options: Map<String, Any> = emptyMap()
   )
   ```

3. **Обновление LLMService** (`domain/service/LLMService.kt`):
   - Использование параметров из конфигурации
   - Передача параметров в `LocalLLMClient`

**После коммита:** остановиться.

---

### Коммит 4: Система prompt-шаблонов

**Задача:** создать систему шаблонов промптов для разных задач.

**Компоненты:**

1. **Модель шаблона** (`domain/model/PromptTemplate.kt`):
   ```kotlin
   data class PromptTemplate(
       val id: String,
       val name: String,
       val description: String,
       val template: String,  // шаблон с плейсхолдерами {user_message}, {context}, etc.
       val systemPrompt: String? = null  // системный промпт
   )
   ```

2. **Репозиторий шаблонов** (`domain/repository/PromptTemplateRepository.kt`):
   ```kotlin
   interface PromptTemplateRepository {
       fun getTemplate(id: String): PromptTemplate?
       fun getAllTemplates(): List<PromptTemplate>
       fun addTemplate(template: PromptTemplate)
   }
   
   class InMemoryPromptTemplateRepository : PromptTemplateRepository {
       private val templates = mutableMapOf<String, PromptTemplate>()
       
       init {
           // Предустановленные шаблоны
           addTemplate(PromptTemplate(
               id = "default",
               name = "По умолчанию",
               description = "Стандартный промпт",
               template = "{user_message}"
           ))
           addTemplate(PromptTemplate(
               id = "code_assistant",
               name = "Ассистент кода",
               description = "Для вопросов о программировании",
               template = """Ты опытный программист. Отвечай кратко и по делу.
   
   Вопрос: {user_message}
   
   Ответ:""",
               systemPrompt = "Ты помощник для программирования. Отвечай на русском языке."
           ))
           addTemplate(PromptTemplate(
               id = "qa_assistant",
               name = "Вопрос-ответ",
               description = "Для точных ответов на вопросы",
               template = """Используй следующую информацию для ответа:
   
   {context}
   
   Вопрос: {user_message}
   
   Ответ (только факты, без выдумывания):"""
           ))
       }
   }
   ```

3. **Сервис шаблонов** (`domain/service/PromptTemplateService.kt`):
   ```kotlin
   class PromptTemplateService(
       private val repository: PromptTemplateRepository
   ) {
       fun applyTemplate(
           templateId: String,
           userMessage: String,
           context: String? = null
       ): String {
           val template = repository.getTemplate(templateId) ?: 
               repository.getTemplate("default")!!
           
           var result = template.template
           result = result.replace("{user_message}", userMessage)
           if (context != null) {
               result = result.replace("{context}", context)
           }
           
           return result
       }
   }
   ```

4. **Конфигурация** (`config/server.yaml`):
   ```yaml
   promptTemplates:
     default: "default"
     available:
       - "default"
       - "code_assistant"
       - "qa_assistant"
   ```

**После коммита:** остановиться.

---

### Коммит 5: Интеграция шаблонов в LLMService

**Задача:** интегрировать систему шаблонов в процесс генерации ответов.

**Компоненты:**

1. **Обновление LLMService** (`domain/service/LLMService.kt`):
   ```kotlin
   class LLMService(
       private val localLLMClient: LocalLLMClient,
       private val promptTemplateService: PromptTemplateService,
       private val config: Config
   ) {
       suspend fun generateResponse(
           userMessage: String,
           context: String? = null,
           templateId: String? = null
       ): String {
           val templateIdToUse = templateId ?: config.promptTemplates.default
           val prompt = promptTemplateService.applyTemplate(
               templateIdToUse,
               userMessage,
               context
           )
           
           val parameters = config.localLLM.parameters
           return localLLMClient.generate(
               model = config.localLLM.model,
               prompt = prompt,
               parameters = parameters
           )
       }
   }
   ```

2. **Обновление ChatController** (`presentation/controller/ChatController.kt`):
   - Добавить параметр `templateId` в запрос
   - Передавать `templateId` в `LLMService`

**После коммита:** остановиться.

---

### Коммит 6: UI для выбора параметров и шаблонов

**Задача:** добавить UI для выбора параметров LLM и шаблонов промптов.

**Компоненты:**

1. **HTML** (`client/index.html`):
   - Форма для выбора параметров (temperature, maxTokens, contextWindow)
   - Выпадающий список шаблонов промптов
   - Кнопка "Применить настройки"
   - Отображение текущих параметров

2. **JavaScript** (`client/app.js`):
   - Загрузка доступных шаблонов через API
   - Сохранение выбранных параметров в localStorage
   - Отправка параметров с каждым запросом
   - Отображение используемого шаблона в сообщениях

3. **API endpoint** (`presentation/controller/ChatController.kt`):
   ```kotlin
   get("/api/prompt-templates") {
       val templates = promptTemplateService.getAllTemplates()
       call.respond(templates)
   }
   ```

**После коммита:** остановиться.

---

### Коммит 7: Система тестирования и сравнения

**Задача:** создать систему для тестирования разных конфигураций и сравнения результатов.

**Компоненты:**

1. **Модель теста** (`domain/model/LLMTest.kt`):
   ```kotlin
   data class LLMTest(
       val id: String,
       val name: String,
       val questions: List<String>,  // список тестовых вопросов
       val expectedTopics: List<String>? = null  // ожидаемые темы в ответе
   )
   
   data class TestResult(
       val testId: String,
       val configuration: String,  // описание конфигурации
       val parameters: LLMParameters,
       val templateId: String,
       val results: List<QuestionResult>
   )
   
   data class QuestionResult(
       val question: String,
       val answer: String,
       val responseTime: Long,  // время ответа в мс
       val tokenCount: Int? = null
   )
   ```

2. **Сервис тестирования** (`domain/service/LLMTestService.kt`):
   ```kotlin
   class LLMTestService(
       private val llmService: LLMService
   ) {
       suspend fun runTest(
           test: LLMTest,
           parameters: LLMParameters,
           templateId: String
       ): TestResult {
           val results = test.questions.map { question ->
               val startTime = System.currentTimeMillis()
               val answer = llmService.generateResponse(
                   userMessage = question,
                   templateId = templateId
               )
               val responseTime = System.currentTimeMillis() - startTime
               
               QuestionResult(
                   question = question,
                   answer = answer,
                   responseTime = responseTime
               )
           }
           
           return TestResult(
               testId = test.id,
               configuration = "temp=${parameters.temperature}, maxTokens=${parameters.maxTokens}",
               parameters = parameters,
               templateId = templateId,
               results = results
           )
       }
   }
   ```

3. **Предустановленные тесты** (`data/repository/TestRepository.kt`):
   ```kotlin
   class TestRepository {
       fun getDefaultTests(): List<LLMTest> = listOf(
           LLMTest(
               id = "code_questions",
               name = "Вопросы о программировании",
               questions = listOf(
                   "Что такое рефакторинг?",
                   "Объясни разницу между async и await",
                   "Как работает garbage collection в Java?"
               )
           ),
           LLMTest(
               id = "general_qa",
               name = "Общие вопросы",
               questions = listOf(
                   "Что такое искусственный интеллект?",
                   "Объясни принцип работы нейронных сетей",
                   "Какие есть типы машинного обучения?"
               )
           )
       )
   }
   ```

4. **API endpoint** (`presentation/controller/TestController.kt`):
   ```kotlin
   post("/api/test/run") {
       val request = call.receive<TestRequest>()
       val test = testRepository.getTest(request.testId)
       val result = testService.runTest(
           test = test,
           parameters = request.parameters,
           templateId = request.templateId
       )
       call.respond(result)
   }
   ```

**После коммита:** остановиться.

---

### Коммит 8: UI для тестирования и сравнения

**Задача:** добавить UI для запуска тестов и сравнения результатов.

**Компоненты:**

1. **HTML** (`client/index.html`):
   - Секция "Тестирование"
   - Выбор тестового набора
   - Выбор конфигурации (параметры + шаблон)
   - Кнопка "Запустить тест"
   - Таблица результатов с сравнением

2. **JavaScript** (`client/app.js`):
   - Загрузка доступных тестов
   - Запуск тестов с разными конфигурациями
   - Отображение результатов в таблице
   - Сравнение времени ответа, качества ответов
   - Экспорт результатов в JSON

**После коммита:** остановиться.

---

### Коммит 9: Документация результатов и рекомендаций

**Задача:** задокументировать найденные оптимальные параметры.

**Действия:**
1. Создать файл `OPTIMIZATION_RESULTS.md`:
   - Описание тестовых наборов
   - Результаты для разных конфигураций
   - Рекомендации по параметрам для разных задач
   - Сравнительная таблица (temperature, maxTokens, шаблоны)

2. Обновить `README.md`:
   - Описание параметров и их влияния
   - Инструкции по настройке
   - Примеры использования шаблонов

3. Создать `PARAMETERS_GUIDE.md`:
   - Подробное описание каждого параметра
   - Рекомендуемые значения для разных задач
   - Примеры промпт-шаблонов

**После коммита:** финал.

---

## Технические детали

### Параметры LLM и их влияние

**Temperature (0.0-2.0):**
- 0.0-0.3: детерминированные, точные ответы (хорошо для фактов)
- 0.7-0.9: баланс между точностью и креативностью (по умолчанию)
- 1.0-2.0: более креативные, разнообразные ответы (хорошо для творческих задач)

**Max Tokens:**
- 512-1024: короткие ответы (хорошо для чата)
- 2048-4096: средние ответы (по умолчанию)
- 8192+: длинные ответы (хорошо для генерации текста)

**Context Window:**
- 2048: базовый контекст
- 4096: стандартный (по умолчанию)
- 8192+: большой контекст (требует больше памяти)

**Top-P (0.0-1.0):**
- 0.9: стандартное значение
- Меньше = более детерминированные ответы
- Больше = более разнообразные ответы

### Примеры prompt-шаблонов

**Для точных ответов:**
```
Отвечай только на основе предоставленной информации. Если информации недостаточно, скажи "Не знаю".

Информация: {context}
Вопрос: {user_message}
Ответ:
```

**Для программирования:**
```
Ты опытный программист. Отвечай кратко, с примерами кода когда возможно.

Вопрос: {user_message}
Ответ:
```

**Для творческих задач:**
```
Ты креативный помощник. Предлагай несколько вариантов идей.

Задача: {user_message}
Варианты:
```

## Риски и рекомендации

- **Память:** большие context_window требуют больше RAM
- **Скорость:** больше maxTokens = дольше генерация
- **Качество:** слишком низкая temperature может давать шаблонные ответы
- **Тестирование:** всегда тестируйте на реальных задачах перед продакшеном
- **Документация:** фиксируйте найденные оптимальные параметры

