# 🔥 День 12. Планировщик + MCP

Система автоматической суммаризации данных из разных источников с использованием MCP (Model Context Protocol).

## 📋 Описание

Этот урок демонстрирует создание системы автоматической суммаризации, которая:
- Анализирует данные из веб-чата (история из lesson-09)
- Анализирует сообщения из Telegram группы
- Автоматически генерирует summary через LLM
- Отправляет summary пользователю в Telegram

## Демонстрация

[Посмотреть можно здесь](https://disk.yandex.ru/i/0TEvA9R36juZzw)

## 🏗️ Архитектура

```
Планировщик (каждые N минут)
    ↓
Вызывает LLM: "Проанализируй данные из источника X за период Y-Z"
    ↓
LLM решает: нужны данные из источника
    ↓
LLM вызывает MCP инструмент: get_chat_history() или get_telegram_messages()
    ↓
MCP инструмент читает из БД:
  - Веб-чат → memory.db (lesson-09)
  - Telegram → summary.db (telegram_messages таблица)
    ↓
MCP возвращает данные LLM
    ↓
LLM анализирует и генерирует summary
    ↓
Сохраняем summary в БД (summaries таблица)
    ↓
Отправляем summary лично пользователю в Telegram от бота
```

## 📁 Структура проекта

```
lesson-12-reminder-mcp/
├── README.md
├── THEORY.md
├── PROMPT.md
├── example.env
├── telegram_messages_example.txt
├── config/
│   ├── server.yaml                    # Конфигурация основного сервера
│   ├── chat-history-mcp-server.yaml  # Конфигурация MCP сервера для веб-чата
│   └── telegram-mcp-server.yaml      # Конфигурация MCP сервера для Telegram
├── data/
│   └── summary.db                     # База данных (создается автоматически)
├── chat-history-mcp-server/          # MCP сервер для веб-чата
│   └── src/main/kotlin/com/prike/mcpserver/...
├── telegram-mcp-server/              # MCP сервер для Telegram
│   ├── src/main/kotlin/com/prike/mcpserver/...
│   └── IMPORT_EXAMPLES.md            # Инструкция по импорту примеров
├── server/                           # Основное приложение (MCP клиент, LLM агент, планировщик)
│   └── src/main/kotlin/com/prike/...
└── client/                           # Веб-интерфейс
    ├── index.html
    ├── style.css
    └── app.js
```

**Архитектура:** Два независимых MCP сервера для разделения ответственности:
- `chat-history-mcp-server` - только веб-чат (инструмент `get_chat_history`)
- `telegram-mcp-server` - только Telegram (инструменты `get_telegram_messages`, `send_telegram_message` + polling)

## 🔄 Разделение на два MCP сервера

Проект использует **два независимых MCP сервера** для разделения ответственности:

1. **chat-history-mcp-server** - только веб-чат
   - Инструмент: `get_chat_history(startTime, endTime)`
   - Читает из `memory.db` (lesson-09)
   - Не требует Telegram конфигурации

2. **telegram-mcp-server** - только Telegram
   - Инструмент: `get_telegram_messages(startTime, endTime)` - получение сообщений
   - Инструмент: `send_telegram_message(userId, message)` - отправка сообщений
   - Polling для получения сообщений из группы
   - Сохранение в `summary.db`
   - Отправка summary пользователю через MCP инструмент

**Преимущества разделения:**
- Независимое развертывание
- Можно использовать только нужные источники
- Изоляция ошибок
- Соответствие принципу единственной ответственности

---

## 🚀 Коммит 1: MCP Server - веб-чат инструмент

### Что реализовано:

1. **MCP Server** (`mcp-server/`)
   - Структура проекта с Gradle
   - Интеграция с официальным Kotlin SDK для MCP
   - Конфигурация через YAML

2. **ChatHistoryRepository**
   - Чтение истории сообщений из `memory.db` (lesson-09)
   - Фильтрация по временному диапазону
   - Использует подход из `SqliteMemoryRepository`

3. **ChatHistoryTool**
   - MCP инструмент `get_chat_history(startTime, endTime)`
   - Возвращает JSON с историей сообщений за период
   - Валидация параметров

4. **Конфигурация**
   - `config/mcp-server.yaml` - конфигурация сервера
   - Путь к БД настраивается через конфиг

### Запуск MCP сервера:

```bash
cd chat-history-mcp-server
./gradlew run
```

Или соберите JAR:

```bash
./gradlew jar
java -jar build/libs/chat-history-mcp-server-1.0.0.jar
```

### Требования:

- Java 17+
- Gradle 8.14+
- База данных `memory.db` из lesson-09 должна существовать по пути, указанному в конфиге

---

## 🚀 Коммит 2: MCP Server - Telegram инструмент + Telegram Bot

### Что реализовано:

1. **TelegramMessageRepository**
   - Создание таблицы `telegram_messages` в `summary.db`
   - Сохранение и чтение сообщений из Telegram
   - Фильтрация по группе и временному диапазону
   - Индексы для быстрого поиска

2. **TelegramBotClient**
   - Получение сообщений из Telegram через polling
   - Автоматическое сохранение сообщений в БД
   - Фильтрация по группе
   - Работа в фоновом режиме (корутины)

3. **GetTelegramMessagesHandler**
   - MCP инструмент `get_telegram_messages(groupId, startTime, endTime)`
   - Наследуется от базового класса `ToolHandler`
   - Возвращает JSON с сообщениями за период

4. **TelegramBotService**
   - Отправка summary пользователю в Telegram
   - Личные сообщения (не в группу)
   - Обработка ошибок

5. **Обновленная конфигурация**
   - Поддержка опциональной конфигурации Telegram
   - Переменные окружения для токена и ID

### Настройка Telegram:

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Добавьте бота в группу
4. Получите ID группы (можно через @userinfobot или API)
5. Получите свой user ID (через @userinfobot)

6. Обновите `config/telegram-mcp-server.yaml`:
```yaml
telegram:
  botToken: "${TELEGRAM_BOT_TOKEN}"
  groupId: "${TELEGRAM_GROUP_ID}"
  databasePath: "data/summary.db"
```

7. Добавьте переменные в `.env` файл (см. `example.env`)

### Запуск Telegram MCP сервера:

```bash
cd telegram-mcp-server
./gradlew run
```

Или соберите JAR:

```bash
./gradlew jar
java -jar build/libs/telegram-mcp-server-1.0.0.jar
```

MCP сервер автоматически:
- Запустит polling для получения сообщений из группы
- Сохранит сообщения в БД
- Зарегистрирует инструмент `get_telegram_messages`

### Требования:

- Telegram Bot Token
- ID группы в Telegram
- База данных `summary.db` будет создана автоматически

---

---

## 🚀 Коммит 3: Основное приложение - MCP клиент ✅

### Что реализовано:

1. **Структура server/ проекта**
   - Gradle конфигурация с зависимостями Ktor, MCP SDK
   - Настройка для работы с несколькими MCP серверами

2. **Конфигурация источников данных**
   - `config/server.yaml` - конфигурация сервера и источников данных
   - Поддержка нескольких MCP серверов (webChat, telegram)
   - Настройка путей к JAR файлам или режим разработки

3. **MCPClient и MCPClientManager**
   - `MCPClient` - клиент для подключения к одному MCP серверу
   - `MCPClientManager` - менеджер для управления несколькими подключениями
   - Автоматическое подключение ко всем включенным источникам при старте

4. **API для работы с инструментами**
   - `GET /api/tools` - получить список всех доступных инструментов
   - `GET /api/connections` - получить статус подключений к MCP серверам
   - `GET /` - health check

5. **Main.kt**
   - Загрузка конфигурации из YAML
   - Автоматическое подключение к MCP серверам при старте
   - Корректное отключение при остановке сервера

### Запуск:

```bash
cd server
./gradlew run
```

Сервер автоматически подключится к включенным MCP серверам и будет доступен на http://localhost:8080

### API endpoints:

- `GET /` - веб-интерфейс
- `GET /api/health` - health check
- `GET /api/tools` - список доступных инструментов
- `GET /api/connections` - статус подключений к MCP серверам
- `GET /api/summaries` - список summary
- `GET /api/scheduler` - статус планировщика
- `POST /api/chat` - отправка сообщения в чат с LLM агентом

---

## 🚀 Коммит 4: LLM интеграция ✅

### Что реализовано:

1. **LLMWithSummaryAgent**
   - Метод `generateSummary()` для генерации summary из разных источников
   - Интеграция с MCP инструментами через `MCPToolAgent`
   - Автоматический вызов нужного инструмента (`get_chat_history` или `get_telegram_messages`)
   - Формирование системного промпта для суммаризации
   - Обработка результатов и генерация summary через LLM

2. **Интеграция с OpenAI/OpenRouter**
   - Поддержка function calling через `OpenAIClient`
   - Преобразование MCP tools в формат OpenAI tools
   - Обработка tool_calls от LLM
   - Многошаговая обработка (LLM → инструмент → LLM → ответ)

### Использование:

```kotlin
val summary = llmAgent.generateSummary(
    source = "telegram",
    startTime = startTimestamp,
    endTime = endTimestamp
)
```

---

## 🚀 Коммит 5: Планировщик ✅

### Что реализовано:

1. **SummaryRepository**
   - Работа с таблицей `summaries` в `summary.db`
   - Сохранение и чтение summary
   - Отметка о доставке в Telegram
   - Индексы для быстрого поиска

2. **SchedulerService**
   - Автоматическая генерация summary по расписанию
   - Настраиваемый интервал (через `intervalMinutes` в конфиге)
   - Настраиваемый период анализа (через `periodHours` в конфиге)
   - Выбор активного источника данных
   - Запуск после подключения MCP серверов
   - Задержка перед первой генерацией (не генерирует сразу при старте)

3. **Отправка summary через MCP**
   - Использование MCP инструмента `send_telegram_message` для отправки
   - Интеграция с `telegram-mcp-server`
   - Обработка ошибок доставки

4. **Конфигурация планировщика**
   - `config/server.yaml` - настройки планировщика
   - Выбор активного источника (`activeSource`)
   - Настройка интервала и периода
   - Настройка доставки в Telegram

### Конфигурация:

```yaml
scheduler:
  enabled: true
  intervalMinutes: 15  # как часто генерировать summary
  periodHours: 24      # за какой период анализировать
  activeSource: "telegram"  # "web_chat", "telegram"
  delivery:
    telegram:
      enabled: true
      userId: "${TELEGRAM_CHAT_ID}"
```

### Логика работы:

1. Планировщик запускается после подключения MCP серверов
2. Ждет первый интервал (например, 15 минут)
3. Генерирует summary для активного источника
4. Сохраняет в БД
5. Отправляет в Telegram (если включено)
6. Повторяет каждые N минут

---

## 🚀 Коммит 6: UI ✅

### Что реализовано:

1. **SummaryController**
   - `GET /api/summaries` - получить список summary
   - `GET /api/scheduler` - получить статус планировщика
   - Типизированные DTO для ответов API

2. **Веб-интерфейс**
   - Статус планировщика (работает/остановлен, интервал, период, активный источник)
   - Статус подключений к MCP серверам
   - Список доступных инструментов
   - История summary с форматированием:
     - Источник данных
     - Период анализа
     - Текст summary (с поддержкой Markdown)
     - Количество сообщений
     - Статус доставки в Telegram
   - Автообновление каждые 10 секунд

3. **Стилизация**
   - Современный дизайн с градиентами
   - Карточки для summary
   - Статусные индикаторы
   - Адаптивная верстка

### Использование:

Откройте http://localhost:8080 в браузере для просмотра интерфейса.

---

## 🛠️ Настройка и запуск

### 1. Переменные окружения

Создайте `.env` файл в корне проекта (`ai_challenge_november/.env`):

```env
OPENAI_API_KEY=your_openai_api_key
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_GROUP_ID=your_telegram_group_id
TELEGRAM_CHAT_ID=your_telegram_chat_id
```

### 2. Конфигурация

Настройте `config/server.yaml`:

```yaml
dataSources:
  webChat:
    enabled: true  # или false
  telegram:
    enabled: true  # или false

scheduler:
  enabled: true
  intervalMinutes: 15
  periodHours: 24
  activeSource: "telegram"  # или "web_chat"
```

### 3. Импорт примеров сообщений

Для тестирования можно импортировать примеры сообщений:

```bash
cd telegram-mcp-server
./gradlew importExamples
```

Это добавит сообщения из `telegram_messages_example.txt` в БД.

### 4. Запуск

1. **Соберите MCP серверы:**
```bash
cd chat-history-mcp-server
./gradlew build

cd ../telegram-mcp-server
./gradlew build
```

2. **Запустите основной сервер:**
```bash
cd server
./gradlew run
```

Сервер автоматически:
- Подключится к включенным MCP серверам
- Запустит планировщик
- Будет доступен на http://localhost:8080

---

## 📊 Структура БД

### summary.db

```sql
-- Сообщения из Telegram
CREATE TABLE telegram_messages (
    id TEXT PRIMARY KEY,
    message_id INTEGER NOT NULL,
    group_id TEXT NOT NULL,
    content TEXT NOT NULL,
    author TEXT,
    timestamp INTEGER NOT NULL,
    created_at INTEGER NOT NULL
);

-- Сгенерированные summary
CREATE TABLE summaries (
    id TEXT PRIMARY KEY,
    source TEXT NOT NULL,
    period_start INTEGER NOT NULL,
    period_end INTEGER NOT NULL,
    summary_text TEXT NOT NULL,
    message_count INTEGER NOT NULL,
    generated_at INTEGER NOT NULL,
    delivered_to_telegram BOOLEAN DEFAULT 0,
    llm_model TEXT
);
```

**Примечание:** Для веб-чата используется существующая БД `lesson-09-external-memory/data/memory.db`, не создается дубликат.

---

## 🎯 Реализованные функции

✅ MCP сервер для веб-чата (`get_chat_history`)  
✅ MCP сервер для Telegram (`get_telegram_messages`, `send_telegram_message`)  
✅ Telegram Bot polling для получения сообщений  
✅ LLM интеграция с автоматическим вызовом инструментов  
✅ Планировщик с автоматической генерацией summary  
✅ Сохранение summary в БД  
✅ Отправка summary в Telegram через MCP  
✅ Веб-интерфейс для просмотра summary и статуса  
✅ Импорт примеров сообщений для тестирования  

---

## 📚 Дополнительные материалы

- [THEORY.md](./THEORY.md) - теория по планировщикам, MCP и суммаризации
- [PROMPT.md](./PROMPT.md) - полное техническое задание
- [telegram_messages_example.txt](./telegram_messages_example.txt) - примеры сообщений для тестирования

