# 🧠 Теория: Планировщик + MCP Summary

## Что такое автоматическая суммаризация и зачем MCP?

**Автоматическая суммаризация** — это процесс периодического анализа данных из различных источников (чат, Telegram, новости) и создания кратких сводок с помощью LLM. Это позволяет пользователям быть в курсе важных событий без необходимости просматривать все сообщения вручную.

### Зачем использовать MCP для суммаризации?

1. **Стандартизация** — единый протокол для доступа к данным из разных источников
2. **Расширяемость** — легко добавлять новые источники данных (веб-чат, Telegram, новости)
3. **Интеграция с LLM** — LLM может запрашивать данные через MCP инструменты
4. **Разделение ответственности** — MCP сервер отвечает за доступ к данным, основное приложение — за анализ и суммаризацию

### Архитектура Summary MCP:

```
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│ Пользователь│───►│ LLM Агент   │───►│ MCP Client  │───►│ MCP Server  │
│             │    │ (планирует) │    │             │    │ (reminder)  │
└─────────────┘    └─────────────┘    └─────────────┘    └─────────────┘
                          ▲                    │                  │
                          │                    │                  │
                    ┌─────┴─────┐             │                  │
                    │ Планировщик│             │                  │
                    │ (24/7)     │             │                  │
                    └────────────┘             │                  │
                                               │                  │
                                               ▼                  ▼
                                        ┌─────────────┐    ┌─────────────┐
                                        │ Telegram Bot│    │   SQLite    │
                                        │  (summary)  │    │  (storage)  │
                                        └─────────────┘    └─────────────┘
```

---

## Принципы 24/7 агентов

### Что такое 24/7 агент?

**24/7 агент** — это приложение, которое работает непрерывно, выполняя задачи в фоновом режиме без постоянного участия пользователя. В контексте reminder-системы это означает:

1. **Автоматический запуск** — агент стартует при запуске приложения
2. **Фоновые задачи** — планировщик работает независимо от пользовательских запросов
3. **Персистентность** — данные сохраняются между перезапусками
4. **Автономность** — агент может принимать решения и выполнять действия без запроса

### Компоненты 24/7 агента:

1. **Планировщик (Scheduler)** — компонент, который запускает задачи по расписанию
2. **Хранилище данных** — база данных для персистентности (исходные данные + summary)
3. **MCP инструменты** — доступ к данным из разных источников
4. **Интеграция с LLM** — для анализа данных и формирования понятных сводок
5. **Telegram Bot** — для получения сообщений и отправки summary пользователю

---

## Планировщики: корутины vs CRON

### Kotlin Coroutines (рекомендуется)

**Преимущества:**
- Нативный для Kotlin
- Легко интегрируется с Ktor
- Асинхронность без блокировки потоков
- Простое управление жизненным циклом

**Пример:**
```kotlin
class SchedulerService {
    private val scope = CoroutineScope(Dispatchers.Default + SupervisorJob())
    
    fun start(intervalMinutes: Int) {
        scope.launch {
            while (isActive) {
                // Выполнить задачу
                generateSummary()
                delay(intervalMinutes * 60 * 1000L)
            }
        }
    }
}
```

### CRON-подобные подходы

**Альтернативы:**
- **Quartz Scheduler** — мощная библиотека для Java/Kotlin
- **Kotlinx Coroutines Ticker** — встроенный механизм для периодических задач
- **ScheduledExecutorService** — стандартная Java библиотека

**Когда использовать:**
- Сложные расписания (каждый день в 9:00, каждую пятницу)
- Множественные задачи с разными интервалами
- Требуется точное время выполнения

---

## SQLite для хранения reminders

### Почему SQLite?

1. **Персистентность** — данные сохраняются между перезапусками
2. **Производительность** — быстрые запросы для поиска по времени (индексы)
3. **Надёжность** — ACID транзакции, целостность данных
4. **Простота** — не требует отдельного сервера, один файл
5. **Масштабируемость** — подходит для тысяч сообщений и summary
6. **Гибкость** — можно хранить данные из разных источников в одной БД

### Структура базы данных

#### Таблица `telegram_messages` (для Telegram источника):

```sql
CREATE TABLE telegram_messages (
    id TEXT PRIMARY KEY,
    message_id INTEGER NOT NULL,  -- ID сообщения в Telegram
    group_id TEXT NOT NULL,
    content TEXT NOT NULL,
    author TEXT,
    timestamp INTEGER NOT NULL,  -- Unix timestamp
    created_at INTEGER NOT NULL
);

CREATE INDEX idx_telegram_timestamp ON telegram_messages(timestamp);
CREATE INDEX idx_telegram_group ON telegram_messages(group_id);
```

**Примечание:** Для веб-чата используем существующую таблицу `memory_entries` из lesson-09 (memory.db), не создаём дубликат.

#### Таблица `summary_log`:

```sql
CREATE TABLE summary_log (
    id TEXT PRIMARY KEY,
    summary_text TEXT NOT NULL,
    reminder_count INTEGER NOT NULL,
    generated_at INTEGER NOT NULL,
    delivered_to_user BOOLEAN DEFAULT 0,
    source TEXT  -- 'scheduler', 'manual'
);
```

### Операции с SQLite

**Используй подход из lesson-09:**
- `SqliteReminderRepository` — репозиторий для работы с БД
- JDBC через `org.xerial:sqlite-jdbc`
- Транзакции для атомарности операций
- Подготовленные запросы для безопасности

---

## Хранение исходных данных vs только summary

### Вариант 1: Хранить только summary

**Преимущества:**
- Меньше места в БД
- Не нужно синхронизировать с внешними источниками
- Быстрее работа

**Недостатки:**
- Нельзя пересоздать summary для старого периода
- Нет доступа к исходным данным для детального анализа

### Вариант 2: Хранить исходные данные + summary (выбранный)

**Преимущества:**
- Можно пересоздать summary для любого периода
- Доступ к исходным данным для анализа
- История сохраняется

**Недостатки:**
- Больше места в БД
- Нужно синхронизировать с источниками (для Telegram)

**Вывод:** Для Telegram храним сообщения в БД (так как нельзя запросить историю через Bot API). Для веб-чата используем существующую БД lesson-09.

## JSON vs SQLite: сравнение

### JSON

**Преимущества:**
- Простота — не требует SQL
- Человекочитаемость
- Легко отлаживать

**Недостатки:**
- Медленные запросы (нужно загружать весь файл)
- Нет индексов
- Сложно делать сложные фильтры
- Проблемы с конкурентным доступом

### SQLite (выбранный вариант)

**Преимущества:**
- Быстрые запросы с индексами
- ACID транзакции
- Поддержка сложных фильтров (WHERE, JOIN)
- Масштабируемость
- Стандартный SQL

**Недостатки:**
- Требует знания SQL
- Немного сложнее в настройке

**Вывод:** Для системы суммаризации SQLite предпочтительнее из-за необходимости частых запросов по времени и фильтрации по источникам.

---

## Как LLM интегрируется с MCP инструментами

### Function Calling (OpenAI API)

**Механизм:**
1. LLM получает список доступных функций (tools) в системном промпте
2. LLM анализирует запрос пользователя
3. LLM решает, нужен ли инструмент
4. LLM возвращает структурированный запрос на вызов функции
5. Приложение вызывает функцию через MCP
6. Результат возвращается LLM
7. LLM формирует финальный ответ

**Пример системного промпта:**
```
Ты — ассистент-планировщик. У тебя есть доступ к инструментам:

- add_reminder(title, description, dueAt): создать напоминание
- list_reminders(status?): получить список напоминаний
- summary_due(): получить сводку актуальных напоминаний
- complete_reminder(id): отметить выполненным

Когда пользователь просит создать напоминание, используй add_reminder.
```

### Парсинг текстового протокола (fallback)

Если LLM не поддерживает function calling, можно использовать текстовый протокол:

```
LLM ответ: "TOOL_CALL:add_reminder{\"title\":\"Отчёт\",\"dueAt\":\"2024-01-15T10:00:00Z\"}"
```

Приложение парсит этот ответ и вызывает инструмент.

---

## Безопасность и ограничения

### Ограничения

1. **Максимальное количество напоминаний** — например, 200 активных
2. **TTL для старых записей** — автоматическое удаление завершённых напоминаний старше 30 дней
3. **Ретеншн summary** — хранить последние 100 сводок
4. **Валидация входных данных** — проверка дат, форматов

### Безопасность

1. **SQL Injection** — используй подготовленные запросы
2. **Валидация дат** — проверка, что dueAt в будущем (или разумном диапазоне)
3. **Ограничение размера** — максимальная длина title/description
4. **Rate limiting** — ограничение частоты вызовов инструментов

---

## Архитектура планировщика

### Компоненты

1. **SchedulerService** — основной сервис планировщика
2. **SummaryRepository** — доступ к summary в БД
3. **MCPClient** — вызов инструментов MCP
4. **LLMWithSummaryAgent** — формирование summary через LLM
5. **TelegramBotService** — отправка summary пользователю в Telegram

### Поток работы планировщика

```
1. SchedulerService запускается при старте приложения
2. Каждые N минут:
   a. Определяет активный источник (web_chat или telegram)
   b. Вычисляет период (последние X часов)
   c. Вызывает LLM: "Проанализируй данные из источника X за период Y-Z"
   d. LLM решает: нужны данные → вызывает MCP инструмент
   e. MCP инструмент читает из БД (memory.db или summary.db)
   f. LLM получает данные, анализирует и генерирует summary
   g. Сохраняет summary в БД (summaries таблица)
   h. Отправляет summary пользователю в Telegram (личное сообщение)
3. Продолжает работать в фоне
```

### Управление жизненным циклом

- **Запуск:** при старте приложения
- **Остановка:** при остановке приложения (graceful shutdown)
- **Пауза:** возможность временно отключить через API/конфиг
- **Логирование:** все действия планировщика логируются

---

## Примеры использования

### Сценарий 1: Создание напоминания через LLM

```
Пользователь: "Напомни мне через 2 часа про отчёт"

LLM анализирует → вызывает add_reminder({
  title: "Отчёт",
  dueAt: "2024-01-15T14:00:00Z"  // текущее время + 2 часа
})

MCP Server сохраняет в SQLite

LLM отвечает: "Создал напоминание 'Отчёт' на 14:00"
```

### Сценарий 2: Автоматическая сводка

```
Планировщик (каждые 15 минут):
1. Вызывает summary_due()
2. Получает: [
     {title: "Отчёт", dueAt: "2024-01-15T14:00:00Z"},
     {title: "Встреча", dueAt: "2024-01-15T15:30:00Z"}
   ]
3. Отправляет в LLM: "Сформируй сводку из этих напоминаний"
4. LLM: "📋 Сводка напоминаний:\n- Отчёт в 14:00\n- Встреча в 15:30"
5. Сохраняет в summary_log
6. Отображает в UI
```

### Сценарий 3: Завершение напоминания

```
Пользователь: "Отметить 'Отчёт' как выполненный"

LLM → complete_reminder(id="...")

MCP Server обновляет статус в SQLite

LLM: "Напоминание 'Отчёт' отмечено как выполненное"
```

---

## Выводы

1. **MCP + SQLite** — мощная комбинация для систем суммаризации
2. **24/7 агенты** требуют планировщика и персистентного хранилища
3. **LLM интеграция** делает систему более интеллектуальной и понятной
4. **SQLite** предпочтительнее JSON для частых запросов по времени
5. **Корутины** — простой и эффективный способ реализации планировщика в Kotlin
6. **Хранение исходных данных** необходимо для Telegram (нельзя запросить историю через Bot API)
7. **Разделение источников** позволяет легко добавлять новые типы данных

---

## Дополнительные ресурсы

- [SQLite Documentation](https://www.sqlite.org/docs.html)
- [Kotlin Coroutines Guide](https://kotlinlang.org/docs/coroutines-guide.html)
- [MCP Protocol Specification](https://modelcontextprotocol.io/)
- [OpenAI Function Calling](https://platform.openai.com/docs/guides/function-calling)

