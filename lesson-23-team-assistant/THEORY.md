# Теория: Ассистент команды с RAG и MCP

## 📖 Введение

Ассистент команды объединяет несколько важных компонентов:
1. **RAG-поиск** — поиск информации в документации проекта
2. **Task Management MCP** — управление задачами команды
3. **LLM генерация** — создание рекомендаций и ответов
4. **Анализ приоритетов** — предложение оптимальных действий

Это позволяет создавать системы, которые:
- Отвечают на вопросы команды на основе документации проекта
- Управляют задачами через MCP
- Анализируют статус проекта и предлагают приоритеты
- Генерируют рекомендации на основе анализа задач
- Предлагают конкретные действия для улучшения работы команды

---

## 🎯 Проблема: Управление задачами команды

### Традиционное управление (медленно и не всегда оптимально)

**Пример:**
```
Член команды задаёт вопрос
  ↓
Руководитель ищет задачи вручную
  ↓
Анализирует приоритеты вручную
  ↓
Предлагает действия
  ↓
Команда ждёт рекомендаций...
```

**Проблемы:**
- Занимает много времени
- Зависит от опыта руководителя
- Может пропустить важные задачи
- Не всегда учитывает зависимости между задачами
- Повторяющиеся вопросы требуют одинаковых усилий

### AI-ассистент команды (быстро и оптимально)

**Пример:**
```
Член команды задаёт вопрос
  ↓
Система автоматически получает задачи через Task MCP
  ↓
Ищет информацию в документации через RAG
  ↓
LLM анализирует и генерирует рекомендации
  ↓
Команда получает оптимальные рекомендации с действиями
```

**Преимущества:**
- Быстрые ответы на основе анализа задач
- Учёт зависимостей между задачами
- Персонализированные рекомендации
- Автоматическое управление задачами
- Масштабируемость

---

## 🏗️ Архитектура системы ассистента команды

### Компоненты

1. **Task Management MCP сервер** — управление задачами команды
2. **RAG MCP сервер** — поиск в документации проекта
3. **TeamAssistantService** — оркестрация процесса
4. **TeamAssistantPromptBuilder** — формирование промптов для команды
5. **LLMService** — генерация ответов и рекомендаций через LLM
6. **TeamAssistantController** — API endpoints
7. **UI** — интерфейс для взаимодействия с ассистентом

### Пайплайн ассистента команды

```
1. Получение контекста проекта
   ↓
   RAG MCP: rag_search_project_docs(query)
   ↓
   - Документация проекта
   - Архитектура
   - Best practices
   ↓
2. Получение задач и статуса
   ↓
   Task MCP: get_tasks(filters)
   Task MCP: get_project_status()
   ↓
   - Список задач с фильтрами
   - Статус проекта
   - Распределение по приоритетам
   ↓
3. Формирование промпта
   ↓
   TeamAssistantPromptBuilder:
   - Системный промпт (роль team assistant)
   - Пользовательский промпт (вопрос + контекст + задачи)
   ↓
4. Генерация ответа и рекомендаций
   ↓
   LLMService: generateAnswer(prompt)
   ↓
   - Анализ задач
   - Рекомендации по приоритетам
   - Предложения действий
   ↓
5. Возврат результата
   ↓
   - Ответ
   - Задачи
   - Рекомендации
   - Действия
   - Источники (RAG)
```

---

## 🔧 Технические детали

### Task Management MCP сервер

**Модели данных:**

1. **Task** — задача
   - `id` — уникальный идентификатор
   - `title` — название задачи
   - `description` — описание
   - `status` — статус (TODO, IN_PROGRESS, IN_REVIEW, DONE, BLOCKED)
   - `priority` — приоритет (LOW, MEDIUM, HIGH, URGENT)
   - `assignee` — исполнитель
   - `dueDate` — срок выполнения
   - `blockedBy` — список ID задач, которые блокируют эту
   - `blocks` — список ID задач, которые эта задача блокирует
   - `createdAt`, `updatedAt` — даты

2. **ProjectStatus** — статус проекта
   - `totalTasks` — всего задач
   - `tasksByStatus` — распределение по статусам
   - `tasksByPriority` — распределение по приоритетам
   - `blockedTasks` — количество заблокированных задач

**Инструменты MCP:**

1. **get_tasks(filters)**
   - Получение списка задач с фильтрами
   - Фильтры: status, priority, assignee
   - Возвращает список задач

2. **get_task(taskId)**
   - Получение задачи по ID
   - Возвращает полную информацию о задаче

3. **create_task(title, description, priority, assignee)**
   - Создание новой задачи
   - Возвращает созданную задачу

4. **update_task(taskId, updates)**
   - Обновление задачи
   - Возвращает обновлённую задачу

5. **get_project_status()**
   - Получение статуса проекта
   - Возвращает статистику по задачам

6. **get_tasks_by_priority(priority)**
   - Получение задач по приоритету
   - Возвращает список задач

### TeamAssistantService

**Основные методы:**

- `answerQuestion(request)` — обработка вопроса команды
- `getTeamContext()` — получение контекста команды
- `searchProjectDocs(question)` — поиск в документации через RAG
- `getRelevantTasks(question)` — получение релевантных задач
- `generateAnswer(context, question, tasks, ragResults)` — генерация ответа через LLM
- `generateRecommendations(tasks, projectStatus)` — генерация рекомендаций

**Логика работы:**

1. Получает контекст проекта через RAG
2. Получает задачи через Task MCP (с фильтрами из вопроса)
3. Получает статус проекта
4. Формирует промпт с вопросом, контекстом, задачами и статусом
5. Генерирует ответ через LLM
6. Генерирует рекомендации на основе анализа задач
7. Предлагает конкретные действия

### TeamAssistantPromptBuilder

**Методы:**

- `buildTeamPrompt(context, question, tasks, projectStatus, ragResults)` — основной промпт для команды

**Структура промпта:**

1. **Системный промпт:**
   - Роль: team assistant
   - Инструкции по анализу задач
   - Учёт зависимостей между задачами
   - Использование документации для контекста
   - Формат ответа (JSON)

2. **Пользовательский промпт:**
   - Вопрос команды
   - Контекст проекта из RAG
   - Список задач
   - Статус проекта
   - Инструкции по анализу

### Модели данных

**TeamRequest:**
- `question` — вопрос команды

**TeamContext:**
- `tasks` — список задач
- `projectStatus` — статус проекта
- `ragContext` — контекст из документации

**TeamResponse:**
- `answer` — ответ на вопрос
- `tasks` — релевантные задачи
- `recommendations` — рекомендации по приоритетам
- `actions` — предложения действий
- `sources` — источники из RAG

**Recommendation:**
- `priority` — приоритет рекомендации
- `task` — связанная задача
- `reason` — обоснование

**Action:**
- `type` — тип действия (UPDATE_TASK, CREATE_TASK, VIEW_TASK, VIEW_STATUS)
- `description` — описание действия
- `task` — связанная задача (опционально)

---

## 🎨 Анализ приоритетов

### Зачем нужен анализ приоритетов?

1. **Оптимизация работы** — фокус на важных задачах
2. **Учёт зависимостей** — понимание блокирующих задач
3. **Распределение ресурсов** — эффективное использование команды
4. **Планирование** — понимание последовательности выполнения

### Как работает анализ приоритетов?

```
1. Получение задач через Task MCP
   ↓
   get_tasks(filters)
   ↓
2. Анализ зависимостей
   ↓
   - Блокирующие задачи
   - Блокируемые задачи
   - Цепочки зависимостей
   ↓
3. Анализ приоритетов
   ↓
   - Высокоприоритетные задачи
   - Срочные задачи
   - Задачи с дедлайнами
   ↓
4. Формирование рекомендаций
   ↓
   LLM анализирует:
   - Зависимости
   - Приоритеты
   - Статусы
   - Исполнителей
   ↓
5. Генерация рекомендаций
   ↓
   - Приоритетные задачи
   - Обоснование
   - Предложения действий
```

### Пример анализа приоритетов

**Задачи:**
```
1. Исправить критический баг (HIGH, BLOCKED)
2. Добавить новую функцию (MEDIUM, TODO)
3. Обновить документацию (LOW, TODO)
```

**Анализ:**
```
Задача 1 блокирует задачу 2
→ Рекомендация: начать с задачи 1
→ Обоснование: "Критический баг блокирует разработку новой функции"
→ Действие: UPDATE_TASK (status: IN_PROGRESS)
```

---

## 🔍 Использование RAG для контекста проекта

### Зачем нужен RAG в ассистенте команды?

1. **Документация проекта** — понимание архитектуры и правил
2. **Best practices** — рекомендации по разработке
3. **API документация** — понимание интерфейсов
4. **Схема данных** — понимание структуры данных

### Как работает RAG в ассистенте команды?

```
1. Формирование запроса для RAG
   ↓
   Вопрос команды: "Как правильно создать задачу?"
   ↓
2. Поиск в документации проекта
   ↓
   RAG MCP: rag_search_project_docs(query)
   ↓
   Фильтр: project/docs/
   ↓
3. Получение релевантных чанков
   ↓
   - API документация: "Создание задачи через API"
   - Best practices: "Рекомендации по приоритетам"
   - Архитектура: "Структура задач"
   ↓
4. Добавление в промпт
   ↓
   TeamAssistantPromptBuilder включает RAG-контекст
   ↓
5. LLM использует контекст для ответа
   ↓
   - Использует информацию из документации
   - Цитирует источники
   - Предлагает конкретные шаги
```

### Пример использования RAG-контекста

**Вопрос команды:**
```
"Как правильно создать задачу для исправления бага?"
```

**RAG-результаты:**
```
API документация:
"Создание задачи через POST /api/team/tasks:
- title: название задачи
- description: описание
- priority: HIGH для багов
- assignee: исполнитель"
```

**Ответ с RAG:**
```
"Для создания задачи на исправление бага:
1. Используйте API: POST /api/team/tasks
2. Установите priority: HIGH
3. Укажите подробное описание бага
4. Назначьте исполнителя

Источники: API документация (project/docs/api.md)"
```

---

## 📊 Генерация рекомендаций

### Учёт зависимостей между задачами

**Блокирующие задачи:**
```
Задача A блокирует задачу B
→ Рекомендация: начать с задачи A
→ Обоснование: "Задача A блокирует задачу B, необходимо решить её первой"
```

**Цепочки зависимостей:**
```
Задача A → Задача B → Задача C
→ Рекомендация: выполнять по порядку A → B → C
→ Обоснование: "Цепочка зависимостей требует последовательного выполнения"
```

### Учёт приоритетов

**Высокоприоритетные задачи:**
```
Задачи с priority: HIGH или URGENT
→ Рекомендация: начать с высокоприоритетных
→ Обоснование: "Высокий приоритет требует немедленного внимания"
```

**Комбинация приоритета и зависимостей:**
```
Задача A (HIGH, блокирует B)
Задача B (MEDIUM)
→ Рекомендация: начать с A
→ Обоснование: "Высокий приоритет + блокирует другую задачу"
```

### Учёт статусов

**Заблокированные задачи:**
```
Задачи со статусом BLOCKED
→ Рекомендация: разблокировать или решить блокирующие
→ Обоснование: "Заблокированные задачи не могут быть выполнены"
```

**Задачи в работе:**
```
Задачи со статусом IN_PROGRESS
→ Рекомендация: завершить текущие задачи перед началом новых
→ Обоснование: "Завершение текущих задач освободит ресурсы"
```

---

## 🚀 UI для ассистента команды

### Возможности UI

1. **Задать вопрос:**
   - Поле для вопроса
   - Кнопка отправки
   - Примеры вопросов

2. **Просмотр ответа:**
   - Ответ с форматированием
   - Список задач
   - Рекомендации с обоснованием
   - Действия с кнопками
   - Источники (RAG)

3. **Управление задачами:**
   - Список задач с фильтрами
   - Создание задачи
   - Обновление задачи
   - Фильтрация по статусу, приоритету, исполнителю

4. **Статус проекта:**
   - Дашборд со статистикой
   - Распределение по статусам
   - Распределение по приоритетам
   - Визуализация метрик

### Примеры вопросов

На странице ассистента команды доступны примеры популярных вопросов:
- "Покажи задачи с приоритетом high"
- "Что делать первым?"
- "Какой статус проекта?"
- "Создай задачу для исправления бага"
- "Какие задачи заблокированы?"
- "Покажи задачи в работе"

---

## 🔗 Интеграция с Task Management через MCP

### Преимущества MCP для Task Management

1. **Изоляция** — логика управления задачами в отдельном сервере
2. **Переиспользование** — можно использовать в других проектах
3. **Тестирование** — легко тестировать отдельно
4. **Масштабирование** — можно заменить на реальную систему управления задачами

### Структура Task MCP сервера

```
task-mcp-server/
├── model/
│   ├── Task.kt
│   └── Project.kt
├── storage/
│   └── InMemoryTaskStorage.kt
├── tools/
│   ├── GetTasksTool.kt
│   ├── GetTaskTool.kt
│   ├── CreateTaskTool.kt
│   ├── UpdateTaskTool.kt
│   ├── GetProjectStatusTool.kt
│   └── GetTasksByPriorityTool.kt
└── server/
    └── TaskMCPServer.kt
```

### Замена на реальную систему управления задачами

Для замены на реальную систему (например, Jira, Trello, Asana):
1. Реализовать интерфейс `TaskStorage`
2. Подключиться к API реальной системы
3. Преобразовать данные в модели `Task` и `ProjectStatus`
4. Остальной код остаётся без изменений

---

## 📈 Метрики качества ассистента команды

### Что оценивает система

1. **Релевантность ответов** — соответствие вопросу
2. **Использование документации** — наличие источников
3. **Качество рекомендаций** — полезность предложений
4. **Анализ приоритетов** — правильность расстановки приоритетов

### Улучшение качества

1. **Точные промпты** — чёткие инструкции для LLM
2. **RAG-контекст** — использование документации
3. **Анализ зависимостей** — учёт блокирующих задач
4. **Итерации** — улучшение промптов на основе результатов

---

## 🎓 Ключевые выводы

1. **RAG для проекта** — поиск информации в документации
2. **Task Management MCP** — удобный способ управления задачами
3. **Анализ приоритетов** — оптимизация работы команды
4. **Генерация рекомендаций** — предложение оптимальных действий
5. **Управление задачами** — автоматическое создание и обновление
6. **UI для удобства** — визуализация задач и рекомендаций
7. **Источники** — цитирование документации
8. **Действия** — конкретные предложения для выполнения

---

## 🔮 Будущие улучшения

1. **Интеграция с реальными системами** — Jira, Trello, Asana
2. **Обучение на примерах** — улучшение промптов
3. **Планирование спринтов** — автоматическое планирование
4. **Аналитика** — статистика по задачам и производительности
5. **Уведомления** — автоматические уведомления о важных задачах
6. **Интеграция с календарём** — учёт дедлайнов
7. **Голосовой ассистент** — интеграция с голосовыми командами
