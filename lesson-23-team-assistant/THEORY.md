# Теория: Ассистент команды с RAG и MCP

## 📖 Введение

Ассистент команды объединяет несколько важных компонентов:
1. **RAG-поиск** — поиск информации о проекте в документации
2. **Task Management MCP** — управление задачами команды
3. **LLM анализ** — генерация рекомендаций и приоритетов
4. **Интеграция сервисов** — взаимодействие с различными сервисами команды

Это позволяет создавать системы, которые:
- Знают проект через документацию (RAG)
- Взаимодействуют с сервисами команды (MCP)
- Создают и управляют задачами
- Анализируют статус проекта
- Дают рекомендации по приоритетам
- Отвечают на вопросы команды

---

## 🎯 Проблема: Управление командой vs AI-ассистент

### Традиционное управление командой (ручное и медленное)

**Пример:**
```
Член команды задаёт вопрос о проекте
  ↓
Руководитель ищет информацию вручную
  ↓
Проверяет задачи в системе управления
  ↓
Анализирует приоритеты вручную
  ↓
Формирует ответ и рекомендации
  ↓
Создаёт задачи вручную
```

**Проблемы:**
- Занимает много времени
- Зависит от доступности руководителя
- Может пропустить важные задачи
- Не всегда учитывает контекст проекта
- Повторяющиеся вопросы требуют одинаковых усилий

### AI-ассистент команды (автоматически и быстро)

**Пример:**
```
Член команды задаёт вопрос
  ↓
Система автоматически ищет информацию в документации через RAG
  ↓
Получает задачи и статус проекта через Task MCP
  ↓
LLM анализирует задачи и генерирует рекомендации
  ↓
Возвращает ответ с действиями и приоритетами
  ↓
Может автоматически создавать задачи
```

**Преимущества:**
- Быстрые ответы на основе документации и задач
- Учёт контекста проекта и текущих задач
- Автоматический анализ приоритетов
- Автоматическое создание задач
- Масштабируемость

---

## 🏗️ Архитектура ассистента команды

### Компоненты

1. **Task Management MCP сервер** — управление задачами и проектами
2. **RAG MCP сервер** — поиск в документации проекта
3. **TeamAssistantService** — оркестрация процесса
4. **TeamAssistantPromptBuilder** — формирование промптов
5. **LLMService** — генерация ответов и рекомендаций
6. **TeamAssistantController** — API endpoints
7. **UI** — интерфейс для взаимодействия с ассистентом

### Пайплайн ассистента команды

```
1. Получение контекста проекта
    ↓
    RAG MCP: rag_search_project_docs(query)
    ↓
    - Документация проекта
    - Архитектура
    - Best practices
    ↓
2. Получение задач и статуса
    ↓
    Task MCP: get_tasks(filters)
    Task MCP: get_project_status()
    ↓
    - Список задач
    - Статус проекта
    - Статистика
    ↓
3. Формирование промпта
    ↓
    TeamAssistantPromptBuilder:
    - Системный промпт (роль team assistant)
    - Пользовательский промпт (вопрос + контекст + задачи)
    ↓
4. Генерация ответа
    ↓
    LLMService: generateAnswer(prompt)
    ↓
    - Анализ задач
    - Рекомендации по приоритетам
    - Предложения действий
    ↓
5. Возврат результата
    ↓
    - Ответ
    - Задачи
    - Рекомендации
    - Действия
```

---

## 🔧 Технические детали

### Task Management MCP сервер

**Модели данных:**

1. **Task** — задача
   - `id` — уникальный идентификатор
   - `title` — название задачи
   - `description` — описание
   - `status` — статус (TODO, IN_PROGRESS, IN_REVIEW, DONE, BLOCKED)
   - `priority` — приоритет (LOW, MEDIUM, HIGH, URGENT)
   - `assignee` — исполнитель
   - `dueDate` — срок выполнения
   - `createdAt`, `updatedAt` — даты

2. **Project** — проект
   - `id` — уникальный идентификатор
   - `name` — название проекта
   - `description` — описание
   - `status` — статус проекта
   - `tasks` — список задач
   - `createdAt` — дата создания

**Инструменты MCP:**

1. **get_tasks(filters)**
   - Получение списка задач с фильтрами
   - Фильтры: статус, приоритет, исполнитель
   - Возвращает список задач

2. **get_task(taskId)**
   - Получение задачи по ID
   - Возвращает полную информацию о задаче

3. **create_task(title, description, priority, assignee)**
   - Создание новой задачи
   - Возвращает созданную задачу

4. **update_task(taskId, updates)**
   - Обновление задачи
   - Обновления: статус, приоритет, исполнитель, описание
   - Возвращает обновлённую задачу

5. **get_project_status()**
   - Получение статуса проекта
   - Возвращает статистику: всего задач, по статусам, по приоритетам

6. **get_tasks_by_priority(priority)**
   - Получение задач по приоритету
   - Возвращает список задач с указанным приоритетом

### TeamAssistantService

**Основные методы:**

- `answerQuestion(request)` — обработка вопроса команды
- `getTeamContext(question)` — получение контекста команды
- `searchProjectDocs(question)` — поиск в документации через RAG
- `getRelevantTasks(question)` — получение релевантных задач
- `generateAnswer(context, question, ragResults, tasks)` — генерация ответа через LLM
- `generateRecommendations(tasks)` — генерация рекомендаций по приоритетам
- `extractActions(answer)` — извлечение действий из ответа

**Логика работы:**

1. Получает контекст проекта через RAG
2. Получает задачи через Task MCP (в зависимости от вопроса)
3. Получает статус проекта через Task MCP
4. Формирует промпт с вопросом, контекстом проекта, задачами и статусом
5. Генерирует ответ через LLM
6. Извлекает действия и рекомендации из ответа
7. Возвращает ответ с действиями и рекомендациями

### TeamAssistantPromptBuilder

**Методы:**

- `buildTeamPrompt(context, question, ragResults, tasks, projectStatus)` — основной промпт

**Структура промпта:**

1. **Системный промпт:**
   - Роль: team assistant
   - Инструкции по работе с командой
   - Учёт контекста проекта и задач
   - Использование документации для ответов
   - Формат ответа (JSON)

2. **Пользовательский промпт:**
   - Информация о проекте (из RAG)
   - Текущие задачи
   - Статус проекта
   - Вопрос команды

### Модели данных

**TeamRequest:**
- `question` — вопрос команды
- `context` — контекст команды (опционально)

**TeamContext:**
- `projectInfo` — информация о проекте из RAG
- `tasks` — задачи из Task MCP
- `projectStatus` — статус проекта

**TeamResponse:**
- `answer` — ответ на вопрос
- `actions` — предложенные действия
- `recommendations` — рекомендации по приоритетам
- `tasks` — релевантные задачи
- `sources` — источники из RAG

**Action:**
- `type` — тип действия (CREATE_TASK, UPDATE_TASK, VIEW_TASKS, GET_STATUS)
- `description` — описание действия
- `task` — задача (если действие связано с задачей)

**Recommendation:**
- `priority` — приоритет
- `task` — задача
- `reason` — причина рекомендации

---

## 🎨 Использование RAG для контекста проекта

### Зачем нужен RAG в ассистенте команды?

1. **Документация проекта** — понимание структуры и архитектуры
2. **API документация** — знание доступных API
3. **Best practices** — рекомендации по разработке
4. **Примеры кода** — понимание паттернов проекта

### Как работает RAG в ассистенте команды?

```
1. Формирование запроса для RAG
    ↓
    Вопрос команды: "Как создать новую задачу?"
    ↓
2. Поиск в документации проекта
    ↓
    RAG MCP: rag_search_project_docs(query)
    ↓
    Фильтр: project/docs/
    ↓
3. Получение релевантных чанков
    ↓
    - API документация: "Создание задачи через API"
    - User guide: "Как работать с задачами"
    - Best practices: "Рекомендации по созданию задач"
    ↓
4. Добавление в промпт
    ↓
    TeamAssistantPromptBuilder включает RAG-контекст в промпт
    ↓
5. LLM использует контекст для ответа
    ↓
    - Использует информацию из документации
    - Предлагает конкретные шаги
    - Ссылается на API
```

### Пример использования RAG-контекста

**Вопрос команды:**
```
"Как создать задачу для исправления бага?"
```

**RAG-результаты:**
```
API документация:
"Для создания задачи используйте POST /api/tasks с параметрами:
- title: название задачи
- description: описание
- priority: приоритет (LOW, MEDIUM, HIGH, URGENT)
- assignee: исполнитель"
```

**Ответ с RAG:**
```
"Для создания задачи используйте API:
POST /api/tasks
{
  'title': 'Исправить баг',
  'description': 'Описание бага',
  'priority': 'HIGH',
  'assignee': 'user-id'
}

Или я могу создать задачу за вас. Хотите, чтобы я создал?"
```

**Источники:**
- API документация (project/docs/api.md)

---

## 📊 Генерация рекомендаций по приоритетам

### Зачем нужны рекомендации?

1. **Приоритизация** — помощь в определении важности задач
2. **Эффективность** — предложение оптимального порядка выполнения
3. **Блокировки** — выявление блокирующих задач
4. **Зависимости** — учёт зависимостей между задачами

### Как генерируются рекомендации?

```
1. Получение задач
    ↓
    Task MCP: get_tasks(filters)
    ↓
2. Анализ задач
    ↓
    - Статусы задач
    - Приоритеты
    - Зависимости
    - Блокировки
    - Сроки выполнения
    ↓
3. Формирование промпта
    ↓
    TeamAssistantPromptBuilder:
    - Список задач
    - Контекст проекта
    - Вопрос команды
    ↓
4. Генерация рекомендаций
    ↓
    LLMService: generateRecommendations(tasks)
    ↓
    - Анализ приоритетов
    - Выявление блокирующих задач
    - Предложение порядка выполнения
    ↓
5. Возврат рекомендаций
```

### Пример генерации рекомендаций

**Вопрос:**
```
"Покажи задачи с приоритетом high и предложи, что делать первым"
```

**Задачи:**
```
1. Исправить критический баг (HIGH, BLOCKED)
2. Добавить новую функцию (HIGH, TODO)
3. Обновить документацию (HIGH, TODO)
```

**Рекомендации:**
```
1. "Исправить критический баг" — приоритет: HIGH
   Причина: Задача блокирует другие задачи и критична для проекта
   
2. "Добавить новую функцию" — приоритет: HIGH
   Причина: Важная функция для пользователей
   
3. "Обновить документацию" — приоритет: HIGH
   Причина: Необходимо для завершения функции
```

---

## 🚀 Управление задачами

### Создание задачи

**Когда создавать:**
- Вопрос команды требует создания задачи
- Обнаружена проблема, требующая решения
- Предложено новое действие

**Процесс:**
```
1. Анализ вопроса команды
    ↓
2. Определение необходимости создания задачи
    ↓
3. Извлечение параметров задачи из ответа LLM
    ↓
4. Создание задачи через Task MCP
    ↓
5. Возврат созданной задачи
```

### Обновление задачи

**Когда обновлять:**
- Изменение статуса задачи
- Изменение приоритета
- Назначение исполнителя
- Обновление описания

**Процесс:**
```
1. Получение задачи по ID
    ↓
2. Определение обновлений из ответа LLM
    ↓
3. Обновление задачи через Task MCP
    ↓
4. Возврат обновлённой задачи
```

### Анализ статуса проекта

**Что анализируется:**
- Общее количество задач
- Распределение по статусам
- Распределение по приоритетам
- Блокирующие задачи
- Просроченные задачи

**Использование:**
- Ответы на вопросы о статусе проекта
- Генерация рекомендаций
- Визуализация в UI

---

## 🎯 Типы вопросов и ответов

### Вопросы о задачах

**Пример:** "Покажи задачи с приоритетом high"

**Ответ:**
```
Найдено 5 задач с приоритетом HIGH:
1. Исправить критический баг (TODO)
2. Добавить новую функцию (IN_PROGRESS)
3. Обновить документацию (TODO)
...

Рекомендую начать с задачи "Исправить критический баг", так как она блокирует другие задачи.
```

### Вопросы о статусе проекта

**Пример:** "Какой статус проекта?"

**Ответ:**
```
Статус проекта:
- Всего задач: 25
- TODO: 10
- IN_PROGRESS: 8
- DONE: 7
- BLOCKED: 2

Приоритеты:
- HIGH: 5
- MEDIUM: 15
- LOW: 5

Рекомендации:
- Обратить внимание на 2 заблокированные задачи
- Приоритет на задачах с HIGH приоритетом
```

### Вопросы о создании задач

**Пример:** "Создай задачу для исправления бага"

**Ответ:**
```
Создана задача:
- Название: "Исправить баг в авторизации"
- Приоритет: HIGH
- Статус: TODO
- ID: task-123

Хотите назначить исполнителя или обновить описание?
```

### Вопросы о рекомендациях

**Пример:** "Что делать первым?"

**Ответ:**
```
Рекомендую начать с:
1. "Исправить критический баг" (HIGH, BLOCKED)
   Причина: Блокирует другие задачи
   
2. "Добавить новую функцию" (HIGH, TODO)
   Причина: Важно для пользователей
   
3. "Обновить документацию" (MEDIUM, TODO)
   Причина: Необходимо для завершения функции
```

---

## 🔗 Интеграция с сервисами команды

### Преимущества MCP для Task Management

1. **Изоляция** — логика управления задачами в отдельном сервере
2. **Переиспользование** — можно использовать в других проектах
3. **Тестирование** — легко тестировать отдельно
4. **Масштабирование** — можно заменить на реальный Task Management (Jira, Trello)

### Структура Task MCP сервера

```
task-mcp-server/
├── model/
│   ├── Task.kt
│   └── Project.kt
├── storage/
│   └── InMemoryTaskStorage.kt
├── tools/
│   ├── GetTasksTool.kt
│   ├── GetTaskTool.kt
│   ├── CreateTaskTool.kt
│   ├── UpdateTaskTool.kt
│   ├── GetProjectStatusTool.kt
│   └── GetTasksByPriorityTool.kt
└── server/
    └── TaskMCPServer.kt
```

### Замена на реальный Task Management

Для замены на реальный Task Management (например, Jira, Trello):
1. Реализовать интерфейс `TaskStorage`
2. Подключиться к API реального Task Management
3. Преобразовать данные в модели `Task` и `Project`
4. Остальной код остаётся без изменений

---

## 📈 Метрики качества ассистента

### Что оценивает система

1. **Релевантность ответов** — соответствие вопросу
2. **Использование документации** — наличие источников
3. **Точность рекомендаций** — полезность приоритетов
4. **Качество действий** — корректность создания/обновления задач

### Улучшение качества

1. **Точные промпты** — чёткие инструкции для LLM
2. **RAG-контекст** — использование документации проекта
3. **Контекст задач** — учёт текущих задач и статуса
4. **Итерации** — улучшение промптов на основе результатов

---

## 🎓 Ключевые выводы

1. **RAG для проекта** — поиск информации в документации
2. **Task MCP** — удобный способ управления задачами
3. **Рекомендации** — помощь в определении приоритетов
4. **Интеграция** — объединение RAG, MCP и LLM
5. **Автоматизация** — создание и обновление задач
6. **UI для удобства** — визуализация задач и рекомендаций
7. **Контекст проекта** — учёт документации и задач
8. **Масштабируемость** — легко заменить на реальные сервисы

---

## 🔮 Будущие улучшения

1. **Интеграция с реальным Task Management** — Jira, Trello, Asana
2. **Анализ зависимостей** — автоматическое выявление зависимостей между задачами
3. **Прогнозирование** — оценка времени выполнения задач
4. **Уведомления** — автоматические уведомления о важных задачах
5. **Аналитика** — статистика по задачам и производительности команды
6. **Интеграция с Git** — связь задач с коммитами и PR
7. **Мультипроектность** — работа с несколькими проектами

