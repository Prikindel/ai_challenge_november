# Краткий промпт для реализации: 🔥 День 23. Ассистент команды

Ты — разработчик, создающий урок «🔥 День 23. Ассистент команды» в модуле `lesson-23-team-assistant`. Проект использует стек **Kotlin + Ktor + Clean Architecture + SQLite** с фронтендом на **vanilla JS**.

## Цель

Создать интегрированный ассистент команды, который объединяет всё:
- **RAG: знает проект** — использует документацию проекта для контекста
- **MCP: взаимодействует с сервисами** — создаёт задачи, получает статус проекта
- **LLM: даёт рекомендации** — анализирует задачи и предлагает приоритеты

**Пример:** "Покажи задачи с приоритетом high и предложи, что делать первым" — ассистент показывает задачи, анализирует их и даёт рекомендации.

## Минимальные требования для зачёта

- ✅ RAG для получения контекста проекта
- ✅ MCP для взаимодействия с Task Management
- ✅ Создание задач через APIф
- ✅ Ответы на вопросы о статусе проекта
- ✅ Рекомендации по приоритетам

## Ключевые требования

1. **Копирование урока 22** — полностью скопировать код из `lesson-22-support-assistant/` в `lesson-23-team-assistant/`, заменив все упоминания 22 на 23

2. **Создание Task Management MCP сервера** — новый MCP сервер для управления задачами команды

3. **TeamAssistantService** — создать сервис для обработки запросов команды с интеграцией RAG и Task MCP

4. **Промпт для ассистента команды** — специализированный промпт с использованием RAG-контекста и контекста задач

5. **API для ассистента команды** — endpoints для задавания вопросов и управления задачами

6. **UI для ассистента команды** — интерфейс для взаимодействия с ассистентом

7. **Улучшение UI и функциональности** — управление задачами, статус проекта, примеры вопросов

8. **Документация** — обновить README, THEORY, добавить ссылку в корневой README

## Поэтапная реализация (отдельные коммиты)

**КРИТИЧЕСКИ ВАЖНО:** Каждый шаг = отдельный git-коммит. После каждого шага **СТОП**, покажи изменения пользователю и дождись «ок, продолжай».

### Коммит 1: Копирование урока 22
- Скопировать весь код из `lesson-22-support-assistant/` в `lesson-23-team-assistant/`
- Заменить все упоминания `lesson-22-support-assistant` → `lesson-23-team-assistant`
- Заменить «День 22» → «День 23»
- Убедиться, что проект собирается и запускается

### Коммит 2: Создание Task Management MCP сервера
- Создать новый MCP сервер `task-mcp-server/`
- Модели: `Task`, `Project`
- Хранилище: `InMemoryTaskStorage` с тестовыми данными
- Инструменты: `get_tasks`, `get_task`, `create_task`, `update_task`, `get_project_status`, `get_tasks_by_priority`
- Следовать паттерну существующих MCP серверов

### Коммит 3: TeamAssistantService
- Создать `TeamAssistantService` для обработки вопросов команды
- Модели: `TeamRequest`, `TeamContext`, `TeamResponse`, `Action`, `Recommendation`
- `TaskMCPClient` и `TaskMCPService` для работы с Task MCP
- Методы: `answerQuestion()`, `getTeamContext()`, `searchProjectDocs()`, `getRelevantTasks()`, `generateAnswer()`, `generateRecommendations()`
- Логика: получение контекста → RAG-поиск → Task MCP → генерация ответа через LLM

### Коммит 4: Промпт для ассистента команды
- Создать `TeamAssistantPromptBuilder` для формирования промпта команды
- Структура промпта: системное сообщение + контекст проекта/задач + RAG-контекст + вопрос
- Требование JSON формата ответа
- Интеграция с RAG для использования документации проекта

### Коммит 5: API для ассистента команды
- Создать `TeamAssistantController` с endpoints:
  - `POST /api/team/ask` — задать вопрос
  - `GET /api/team/tasks` — получить задачи
  - `GET /api/team/tasks/{taskId}` — получить задачу
  - `POST /api/team/tasks` — создать задачу
  - `PUT /api/team/tasks/{taskId}` — обновить задачу
  - `GET /api/team/status` — получить статус проекта
- DTO: `TeamQuestionRequest`, `TeamQuestionResponse`, `TaskDto`, и т.д.
- Конфигурация Task MCP в `server.yaml`

### Коммит 6: UI для ассистента команды
- Создать `team-assistant.html` — страница для ассистента команды
- Создать `team-assistant.js` — логика отправки вопроса и отображения результатов
- Форма для ввода вопроса
- Отображение ответа, задач, рекомендаций, действий
- Добавить ссылку в навигацию всех HTML файлов

### Коммит 7: Улучшение UI и функциональности
- Управление задачами: список, фильтрация, поиск, создание, обновление
- Статус проекта: дашборд со статистикой, визуализация
- Рекомендации: отображение, кнопки для быстрых действий
- Примеры вопросов: кнопки с популярными вопросами

### Коммит 8: Документация и выводы
- Обновить `README.md`, `THEORY.md`, `PROMPT.md`, `CHAT_PROMPT.txt`
- Добавить раздел «Демонстрация» с ссылкой на видео
- Описать результаты и примеры использования
- Обновить корневой `README.md`, добавив ссылку на урок 23

## Технические детали

### Интеграция с Task Management через MCP
- Вызов MCP инструментов: `get_tasks()`, `get_task()`, `create_task()`, `update_task()`, `get_project_status()`
- Получение задач с фильтрами (статус, приоритет, исполнитель)
- Создание и обновление задач

### Использование RAG для контекста проекта
- Документация проекта (README, API, схема данных, архитектура)
- Фильтр: `project/docs/`
- Поиск релевантных чанков для ответа

### Генерация рекомендаций через LLM
- Структура промпта: роль team assistant + контекст проекта/задач + RAG-контекст + вопрос
- Требование JSON формата ответа
- Парсинг ответа в модель `TeamResponse`

### Анализ приоритетов
- Анализ зависимостей между задачами
- Учёт блокирующих задач
- Учёт сроков выполнения
- Учёт важности для проекта

## Важно

- Не ломать существующую функциональность из урока 22
- Использовать существующие компоненты: RAG, MCP, LLM
- После каждого коммита останавливаться и показывать изменения
- Пользователь сам делает merge/commit
- Все изменения должны быть протестированы
- Минимальные требования для зачёта должны быть выполнены
- Рекомендации должны быть релевантными и полезными

