# 🔥 День 23. Ассистент команды

Реализация интегрированного ассистента команды, который объединяет RAG (знание проекта через документацию), MCP (взаимодействие с сервисами команды) и LLM (генерация рекомендаций) для создания задач, ответов на вопросы о статусе проекта и рекомендаций по приоритетам.

## Демонстрация

> 📹 Видео появится после записи (формат: Видео + Код).

**Сценарий использования:**
1. Запуск сервера и открытие UI
2. Индексация документации проекта (project/docs/ и project/README.md)
3. Открытие страницы "Ассистент команды"
4. Задание вопроса команды (например, "Покажи задачи с приоритетом high и предложи, что делать первым")
5. Получение ответа с задачами, рекомендациями и действиями
6. Просмотр статуса проекта
7. Создание задачи через ассистента
8. Управление задачами (просмотр, обновление, фильтрация)

## 📋 Описание

В этом уроке мы:
- Копируем систему из урока 22 (RAG, MCP, поддержка)
- Создаём **Task Management MCP сервер** для управления задачами команды:
  - Инструмент `get_tasks` — получение списка задач с фильтрами
  - Инструмент `get_task` — получение задачи по ID
  - Инструмент `create_task` — создание новой задачи
  - Инструмент `update_task` — обновление задачи
  - Инструмент `get_project_status` — получение статуса проекта
  - Инструмент `get_tasks_by_priority` — получение задач по приоритету
- Создаём **TeamAssistantService** для обработки запросов команды:
  - Получение контекста проекта через RAG
  - Получение задач и статуса через Task MCP
  - Генерация ответов и рекомендаций через LLM
- Создаём **TeamAssistantPromptBuilder** для формирования промптов команды
- Реализуем **API endpoints** для ассистента команды:
  - `POST /api/team/ask` — задать вопрос ассистенту команды
  - `GET /api/team/tasks` — получить список задач
  - `GET /api/team/tasks/{taskId}` — получить задачу по ID
  - `POST /api/team/tasks` — создать новую задачу
  - `PUT /api/team/tasks/{taskId}` — обновить задачу
  - `GET /api/team/status` — получить статус проекта
- Создаём **UI для ассистента команды**:
  - Форма для ввода вопроса
  - Отображение ответа с задачами, рекомендациями и действиями
  - Управление задачами (список, создание, обновление)
  - Статус проекта (дашборд со статистикой)

## 🎯 Пайплайн ассистента команды

```
Вопрос команды
  ↓
Получение контекста проекта через RAG (rag_search_project_docs):
  - Документация проекта
  - Архитектура
  - Best practices
  ↓
Получение задач и статуса через Task MCP:
  - get_tasks(filters) — список задач
  - get_project_status() — статус проекта
  ↓
Формирование промпта для команды (TeamAssistantPromptBuilder):
  - Системный промпт с ролью team assistant
  - Пользовательский промпт с вопросом, контекстом проекта, задачами и статусом
  ↓
Генерация ответа через LLM (LLMService):
  - Анализ задач
  - Рекомендации по приоритетам
  - Предложения действий
  ↓
Возврат результата: ответ, задачи, рекомендации, действия
```

## 🏗️ Архитектура

```
lesson-23-team-assistant/
├── config/server.yaml
├── data/knowledge_base.db    # база знаний + чат
├── documents/                # Документы для индексации
├── project/                  # Документация проекта
│   ├── docs/
│   │   ├── api.md           # API документация
│   │   └── schema.md        # Схема базы данных
│   └── README.md            # Корневой README проекта
├── task-mcp-server/         # MCP сервер для управления задачами
│   └── src/main/kotlin/com/prike/taskmcpserver/
│       ├── Main.kt
│       ├── model/
│       │   ├── Task.kt
│       │   └── Project.kt
│       ├── storage/
│       │   └── InMemoryTaskStorage.kt
│       ├── server/
│       │   └── TaskMCPServer.kt
│       └── tools/
│           ├── GetTasksTool.kt
│           ├── GetTaskTool.kt
│           ├── CreateTaskTool.kt
│           ├── UpdateTaskTool.kt
│           ├── GetProjectStatusTool.kt
│           └── GetTasksByPriorityTool.kt
├── git-mcp-server/           # MCP сервер для работы с git
├── rag-mcp-server/           # MCP сервер для RAG
├── crm-mcp-server/           # MCP сервер для CRM
├── server/
│   └── src/main/kotlin/com/prike/
│       ├── data/
│       │   ├── client/
│       │   │   ├── GitMCPClient.kt
│       │   │   ├── RagMCPClient.kt
│       │   │   ├── CRMMCPClient.kt
│       │   │   └── TaskMCPClient.kt  # новый: клиент для Task MCP
│       │   └── repository/
│       ├── domain/
│       │   ├── model/
│       │   │   ├── TeamAssistantModels.kt  # новый: модели ассистента команды
│       │   │   └── ...
│       │   └── service/
│       │       ├── TeamAssistantService.kt  # новый: сервис ассистента команды
│       │       ├── TeamAssistantPromptBuilder.kt  # новый: построитель промптов
│       │       ├── TaskMCPService.kt  # новый: сервис Task MCP
│       │       ├── GitMCPService.kt
│       │       ├── RagMCPService.kt
│       │       ├── CRMMCPService.kt
│       │       └── LLMService.kt
│       └── presentation/
│           ├── controller/
│           │   ├── TeamAssistantController.kt  # новый: API для ассистента команды
│           │   └── ...
│           └── dto/
│               └── TeamAssistantDtos.kt  # новый: DTO для ассистента команды
└── client/                # Клиентская часть (Vanilla JS)
    ├── team-assistant.html  # новый: страница для ассистента команды
    ├── team-assistant.js    # новый: логика ассистента команды
    └── ...
```

## 🚀 Быстрый старт

### Предварительные требования

- Java 17+
- Gradle 8+
- Ollama (для эмбеддингов)
- OpenRouter API ключ (для LLM)

### Установка и запуск

1. **Клонируйте репозиторий и перейдите в директорию урока:**
   ```bash
   cd lesson-23-team-assistant
   ```

2. **Настройте переменные окружения:**
   ```bash
   cp example.env .env
   # Отредактируйте .env и укажите OPENAI_API_KEY
   ```

3. **Запустите Ollama (для эмбеддингов):**
   ```bash
   ollama serve
   ollama pull nomic-embed-text
   ```

4. **Соберите и запустите сервер:**
   ```bash
   cd server
   ./gradlew build
   ./gradlew run
   ```

5. **Откройте браузер:**
   ```
   http://localhost:8080
   ```

6. **Индексируйте документацию проекта:**
   - Перейдите на страницу "Индексация"
   - Нажмите "Индексировать документацию проекта"

7. **Используйте ассистента команды:**
   - Перейдите на страницу "Ассистент команды"
   - Задайте вопрос (например, "Покажи задачи с приоритетом high")
   - Получите ответ с задачами, рекомендациями и действиями

## 📖 Использование

### Задать вопрос ассистенту команды

1. Откройте страницу "Ассистент команды"
2. Введите ваш вопрос
3. Нажмите "Задать вопрос"
4. Просмотрите ответ, задачи, рекомендации и действия

### Управление задачами

1. **Просмотреть задачи:**
   - Нажмите "Показать задачи"
   - Используйте фильтры (статус, приоритет, исполнитель)
   - Просмотрите список задач

2. **Создать задачу:**
   - Нажмите "Создать задачу"
   - Заполните форму (название, описание, приоритет, исполнитель)
   - Нажмите "Создать"
   - Или попросите ассистента создать задачу через вопрос

3. **Обновить задачу:**
   - Выберите задачу из списка
   - Нажмите "Редактировать"
   - Измените статус, приоритет или исполнителя
   - Или попросите ассистента обновить задачу через вопрос

### Статус проекта

1. Нажмите "Статус проекта"
2. Просмотрите статистику:
   - Всего задач
   - Распределение по статусам
   - Распределение по приоритетам
   - Блокирующие задачи

### Примеры вопросов

На странице ассистента команды доступны примеры популярных вопросов:
- "Покажи задачи с приоритетом high"
- "Что делать первым?"
- "Какой статус проекта?"
- "Создай задачу для исправления бага"
- "Какие задачи заблокированы?"
- "Покажи задачи в работе"

Клик по примеру автоматически заполняет поле вопроса.

## 🔧 API Endpoints

### POST /api/team/ask

Задать вопрос ассистенту команды.

**Запрос:**
```json
{
  "question": "Покажи задачи с приоритетом high и предложи, что делать первым"
}
```

**Ответ:**
```json
{
  "answer": "Найдено 5 задач с приоритетом HIGH. Рекомендую начать с задачи 'Исправить критический баг', так как она блокирует другие задачи.",
  "tasks": [
    {
      "id": "task-1",
      "title": "Исправить критический баг",
      "status": "BLOCKED",
      "priority": "HIGH",
      "assignee": "user-1"
    }
  ],
  "recommendations": [
    {
      "priority": "HIGH",
      "task": {...},
      "reason": "Блокирует другие задачи"
    }
  ],
  "actions": [
    {
      "type": "UPDATE_TASK",
      "description": "Обновить статус задачи на IN_PROGRESS",
      "task": {...}
    }
  ],
  "sources": [
    {
      "title": "API документация",
      "content": "Информация о задачах...",
      "url": "/docs/api.md"
    }
  ]
}
```

### GET /api/team/tasks

Получить список задач с фильтрами.

**Параметры запроса:**
- `status` — фильтр по статусу (TODO, IN_PROGRESS, DONE, BLOCKED)
- `priority` — фильтр по приоритету (LOW, MEDIUM, HIGH, URGENT)
- `assignee` — фильтр по исполнителю

**Ответ:**
```json
{
  "tasks": [
    {
      "id": "task-1",
      "title": "Исправить баг",
      "description": "Описание бага",
      "status": "TODO",
      "priority": "HIGH",
      "assignee": "user-1",
      "dueDate": "2024-12-31",
      "createdAt": 1234567890,
      "updatedAt": 1234567890
    }
  ]
}
```

### POST /api/team/tasks

Создать новую задачу.

**Запрос:**
```json
{
  "title": "Исправить баг",
  "description": "Описание бага",
  "priority": "HIGH",
  "assignee": "user-1",
  "dueDate": "2024-12-31"
}
```

**Ответ:** `TaskDto` (201 Created)

### PUT /api/team/tasks/{taskId}

Обновить задачу.

**Запрос:**
```json
{
  "status": "IN_PROGRESS",
  "priority": "HIGH",
  "assignee": "user-2"
}
```

**Ответ:** `TaskDto` (200 OK)

### GET /api/team/status

Получить статус проекта.

**Ответ:**
```json
{
  "totalTasks": 25,
  "tasksByStatus": {
    "TODO": 10,
    "IN_PROGRESS": 8,
    "DONE": 7,
    "BLOCKED": 2
  },
  "tasksByPriority": {
    "LOW": 5,
    "MEDIUM": 15,
    "HIGH": 5
  },
  "blockedTasks": 2
}
```

## 🎨 Статусы задач

- **TODO** — к выполнению (серый)
- **IN_PROGRESS** — в работе (синий)
- **IN_REVIEW** — на проверке (жёлтый)
- **DONE** — выполнено (зелёный)
- **BLOCKED** — заблокировано (красный)

## 🚨 Приоритеты задач

- **LOW** — низкий
- **MEDIUM** — средний
- **HIGH** — высокий
- **URGENT** — срочный

## 📝 Конфигурация

### Task MCP сервер

В `config/server.yaml` настроен Task MCP сервер:

```yaml
task:
  mcp:
    enabled: true
    jarPath: "task-mcp-server/build/libs/task-mcp-server-1.0.0.jar"
```

## 🔗 Интеграция с Task Management через MCP

Система использует Task MCP сервер для управления задачами:
- `get_tasks(filters)` — получение задач с фильтрами
- `get_task(taskId)` — получение задачи по ID
- `create_task(title, description, priority, assignee)` — создание задачи
- `update_task(taskId, updates)` — обновление задачи
- `get_project_status()` — получение статуса проекта
- `get_tasks_by_priority(priority)` — получение задач по приоритету

## 📚 Дополнительные материалы

- [THEORY.md](./THEORY.md) — теория ассистента команды
- [PROMPT.md](./PROMPT.md) — промпт для реализации
- [CHAT_PROMPT.txt](./CHAT_PROMPT.txt) — промпт для чата

## ✅ Минимальные требования для зачёта

- ✅ RAG для получения контекста проекта
- ✅ MCP для взаимодействия с Task Management
- ✅ Создание задач через API
- ✅ Ответы на вопросы о статусе проекта
- ✅ Рекомендации по приоритетам

## 🎓 Что мы изучили

1. **Создание Task Management MCP сервера** — интеграция с системой управления задачами
2. **Использование RAG для проекта** — поиск информации в документации проекта
3. **TeamAssistantService** — обработка запросов команды с интеграцией RAG и Task MCP
4. **Генерация рекомендаций** — анализ задач и предложение приоритетов
5. **UI для ассистента команды** — создание удобного интерфейса для работы с командами
6. **Управление задачами** — создание, просмотр, обновление, фильтрация
