# Промпт для реализации: 🔥 День 23. Ассистент команды

Ты — разработчик, создающий урок «🔥 День 23. Ассистент команды» в модуле `lesson-23-team-assistant`. Проект использует стек **Kotlin + Ktor + Clean Architecture + SQLite** с фронтендом на **vanilla JS**. В качестве базы мы **копируем весь код из урока 22** и развиваем его — меняем все упоминания `lesson-22-support-assistant` на `lesson-23-team-assistant`, не ломая существующую функциональность.

## 🎯 Цель урока

Создать интегрированный ассистент команды, который объединяет всё:
- **RAG: знает проект** — использует документацию проекта для контекста
- **MCP: взаимодействует с сервисами** — создаёт задачи, получает статус проекта
- **LLM: даёт рекомендации** — анализирует задачи и предлагает приоритеты

**Пример:** "Покажи задачи с приоритетом high и предложи, что делать первым" — ассистент показывает задачи, анализирует их и даёт рекомендации.

### Ключевая идея

```
Вопрос команды → RAG-поиск в документации проекта → MCP запросы к сервисам (задачи, статус) → LLM анализ и рекомендации → Ответ с действиями и приоритетами
```

Система должна:
- Использовать RAG для поиска информации о проекте
- Взаимодействовать с сервисами команды через MCP (создание задач, получение статуса)
- Анализировать задачи и давать рекомендации по приоритетам
- Отвечать на вопросы о текущем статусе проекта
- Предлагать конкретные действия для команды

---

## 📋 Поэтапная реализация (отдельные коммиты)

**КРИТИЧЕСКИ ВАЖНО:**
- Каждый перечисленный шаг = отдельный git-коммит.
- После каждого шага **СТОП**, покажи изменения пользователю и дождись «ок, продолжай».
- Пользователь сам делает merge/commit далее.

### Коммит 1: Копирование урока 22

**Задача:** полностью скопировать содержимое `lesson-22-support-assistant/` в `lesson-23-team-assistant/`.

**Действия:**
1. Скопировать все файлы (server, client, config, README, THEORY, PROMPT, CHAT_PROMPT, документы, данные, git-mcp-server, rag-mcp-server, crm-mcp-server, mcp-common).
2. Обновить названия:
   - Папка проекта, Gradle settings, package references, заголовки, README
   - Все текстовые упоминания `lesson-22-support-assistant` → `lesson-23-team-assistant`
   - «День 22» → «День 23»
3. Убедиться, что проект собирается и запускается без функциональных изменений.

**После коммита:** остановиться, показать diff пользователю.

---

### Коммит 2: Создание Task Management MCP сервера

**Задача:** создать новый MCP сервер для управления задачами команды.

**Компоненты:**
1. **Модели данных** (`task-mcp-server/src/main/kotlin/com/prike/taskmcpserver/model/`):
   - `Task.kt` — задача (id, title, description, status, priority, assignee, dueDate, createdAt, updatedAt)
   - `Project.kt` — проект (id, name, description, status, tasks, createdAt)
   - Enums: `TaskStatus` (TODO, IN_PROGRESS, IN_REVIEW, DONE, BLOCKED), `Priority` (LOW, MEDIUM, HIGH, URGENT)

2. **Хранилище** (`task-mcp-server/src/main/kotlin/com/prike/taskmcpserver/storage/InMemoryTaskStorage.kt`):
   - In-memory хранилище для задач и проектов
   - Тестовые данные (несколько проектов и задач)
   - CRUD операции для задач и проектов

3. **MCP инструменты** (`task-mcp-server/src/main/kotlin/com/prike/taskmcpserver/tools/`):
   - `GetTasksTool` — получение списка задач (с фильтрами по статусу, приоритету, исполнителю)
   - `GetTaskTool` — получение задачи по ID
   - `CreateTaskTool` — создание новой задачи
   - `UpdateTaskTool` — обновление задачи (статус, приоритет, исполнитель)
   - `GetProjectStatusTool` — получение статуса проекта (статистика задач)
   - `GetTasksByPriorityTool` — получение задач по приоритету

4. **Handlers** (`task-mcp-server/src/main/kotlin/com/prike/taskmcpserver/tools/handlers/`):
   - Обработчики для всех инструментов

5. **Task MCP сервер** (`task-mcp-server/src/main/kotlin/com/prike/taskmcpserver/server/TaskMCPServer.kt`):
   - Наследуется от `BaseMCPServer`
   - Регистрирует все инструменты через `ToolRegistry`

6. **Main.kt** (`task-mcp-server/src/main/kotlin/com/prike/taskmcpserver/Main.kt`):
   - Инициализация хранилища с тестовыми данными
   - Создание и запуск Task MCP сервера

**Важно:**
- Следовать паттерну существующих MCP серверов
- Использовать `mcp-common` для базовых классов
- Обрабатывать ошибки (задача не найдена, проект не найден)
- Логировать все операции

**После коммита:** остановиться.

---

### Коммит 3: TeamAssistantService

**Задача:** создать сервис для обработки запросов команды с интеграцией RAG и Task MCP.

**Компоненты:**
1. **Модели данных** (`server/src/main/kotlin/com/prike/domain/model/TeamAssistantModels.kt`):
   ```kotlin
   data class TeamRequest(
       val question: String,
       val context: TeamContext?
   )
   
   data class TeamContext(
       val projectInfo: String?,  // информация о проекте из RAG
       val tasks: List<Task>?,  // задачи из Task MCP
       val projectStatus: ProjectStatus?  // статус проекта
   )
   
   data class TeamResponse(
       val answer: String,
       val actions: List<Action>?,  // предложенные действия
       val recommendations: List<Recommendation>?,  // рекомендации по приоритетам
       val tasks: List<Task>?,  // релевантные задачи
       val sources: List<Source>?  // источники из RAG
   )
   
   data class Action(
       val type: ActionType,  // CREATE_TASK, UPDATE_TASK, VIEW_TASKS
       val description: String,
       val task: Task?  // если действие связано с задачей
   )
   
   data class Recommendation(
       val priority: Priority,
       val task: Task,
       val reason: String  // почему это приоритет
   )
   
   enum class ActionType {
       CREATE_TASK, UPDATE_TASK, VIEW_TASKS, GET_STATUS
   }
   
   data class ProjectStatus(
       val totalTasks: Int,
       val tasksByStatus: Map<TaskStatus, Int>,
       val tasksByPriority: Map<Priority, Int>,
       val blockedTasks: Int
   )
   ```

2. **TaskMCPClient** (`server/src/main/kotlin/com/prike/data/client/TaskMCPClient.kt`):
   - Клиент для подключения к Task MCP серверу
   - Аналогичен `GitMCPClient`, `RagMCPClient`, `CRMMCPClient`

3. **TaskMCPService** (`server/src/main/kotlin/com/prike/domain/service/TaskMCPService.kt`):
   - `getTasks(filters)` — получение задач с фильтрами
   - `getTask(taskId)` — получение задачи по ID
   - `createTask(title, description, priority, assignee)` — создание задачи
   - `updateTask(taskId, updates)` — обновление задачи
   - `getProjectStatus()` — получение статуса проекта
   - `getTasksByPriority(priority)` — получение задач по приоритету

4. **TeamAssistantService** (`server/src/main/kotlin/com/prike/domain/service/TeamAssistantService.kt`):
   - `answerQuestion(request)` — основной метод для обработки вопроса
   - `getTeamContext(question)` — получение контекста команды
   - `searchProjectDocs(question)` — поиск в документации проекта через RAG
   - `getRelevantTasks(question)` — получение релевантных задач через Task MCP
   - `generateAnswer(context, question, ragResults, tasks)` — генерация ответа через LLM
   - `generateRecommendations(tasks)` — генерация рекомендаций по приоритетам
   - `extractActions(answer)` — извлечение действий из ответа LLM

**Логика работы:**
1. Получает контекст проекта через RAG (документация проекта)
2. Получает задачи через Task MCP (в зависимости от вопроса)
3. Получает статус проекта через Task MCP
4. Формирует промпт с вопросом, контекстом проекта, задачами и статусом
5. Генерирует ответ через LLM
6. Извлекает действия и рекомендации из ответа
7. Возвращает ответ с действиями и рекомендациями

**После коммита:** остановиться.

---

### Коммит 4: Промпт для ассистента команды

**Задача:** создать отдельный построитель промптов для ассистента команды.

**Компоненты:**
1. **TeamAssistantPromptBuilder** (`server/src/main/kotlin/com/prike/domain/service/TeamAssistantPromptBuilder.kt`):
   - `buildTeamPrompt(context, question, ragResults, tasks, projectStatus)` — основной промпт
   - `buildSystemPrompt()` — системный промпт с ролью team assistant
   - `buildUserPrompt(context, question, ragResults, tasks, projectStatus)` — пользовательский промпт

2. **Структура промпта:**
   - Системный промпт: роль team assistant, инструкции, учёт контекста проекта и задач
   - Пользовательский промпт: информация о проекте, задачи, статус проекта, вопрос

3. **Обновить TeamAssistantService**:
   - Использовать `TeamAssistantPromptBuilder` вместо внутренних методов
   - Удалить методы построения промптов из сервиса

**Важно:**
- Учитывать контекст проекта из RAG
- Учитывать текущие задачи и их статусы
- Требовать JSON формат ответа от LLM
- Включать рекомендации по приоритетам

**После коммита:** остановиться.

---

### Коммит 5: API для ассистента команды

**Задача:** создать HTTP API endpoints для ассистента команды.

**Компоненты:**
1. **DTO** (`server/src/main/kotlin/com/prike/presentation/dto/TeamAssistantDtos.kt`):
   - `TeamQuestionRequest` — запрос на вопрос
   - `TeamQuestionResponse` — ответ с результатом
   - `ActionDto`, `RecommendationDto`, `TaskDto`, `ProjectStatusDto` — DTO для компонентов

2. **TeamAssistantController** (`server/src/main/kotlin/com/prike/presentation/controller/TeamAssistantController.kt`):
   - `POST /api/team/ask` — задать вопрос ассистенту команды
   - `GET /api/team/tasks` — получить список задач (с фильтрами)
   - `GET /api/team/tasks/{taskId}` — получить задачу по ID
   - `POST /api/team/tasks` — создать новую задачу
   - `PUT /api/team/tasks/{taskId}` — обновить задачу
   - `GET /api/team/status` — получить статус проекта

3. **Обновить конфигурацию** (`config/server.yaml`):
   ```yaml
   task:
     mcp:
       enabled: true
       jarPath: "task-mcp-server/build/libs/task-mcp-server-1.0.0.jar"
   ```

4. **Обновить Config.kt** (`server/src/main/kotlin/com/prike/config/Config.kt`):
   - Добавить `TaskMCPConfig` и `TaskConfig`
   - Парсинг конфигурации Task MCP из `server.yaml`

5. **Интеграция в Main.kt**:
   - Создать `TaskMCPService` с подключением к Task MCP серверу
   - Создать `TeamAssistantService` с зависимостями
   - Зарегистрировать `TeamAssistantController`
   - Добавить маршруты в routing

**Важно:**
- Обработка ошибок с информативными сообщениями
- Валидация входных данных
- Логирование всех операций

**После коммита:** остановиться.

---

### Коммит 6: UI для ассистента команды

**Задача:** создать веб-интерфейс для взаимодействия с ассистентом команды.

**Компоненты:**
1. **team-assistant.html**:
   - Форма для ввода вопроса
   - Кнопка "Задать вопрос"
   - Область для отображения ответа
   - Область для отображения задач
   - Область для отображения рекомендаций
   - Область для отображения действий
   - Область для отображения статуса проекта
   - Навигация с вкладкой "Ассистент команды"

2. **team-assistant.js**:
   - `handleQuestionSubmit()` — обработка отправки вопроса
   - `displayAnswer(response)` — отображение ответа
   - `displayTasks(tasks)` — отображение задач
   - `displayRecommendations(recommendations)` — отображение рекомендаций
   - `displayActions(actions)` — отображение действий
   - `displayProjectStatus(status)` — отображение статуса проекта
   - `loadTasks(filters)` — загрузка задач с фильтрами
   - `createTask(taskData)` — создание задачи
   - `updateTask(taskId, updates)` — обновление задачи

3. **Обновить навигацию** (все HTML файлы в `client/`):
   - Добавить ссылку на `team-assistant.html` в навигацию

**Важно:**
- Индикация загрузки
- Информативные сообщения об ошибках
- Удобное отображение задач, рекомендаций и действий

**После коммита:** остановиться.

---

### Коммит 7: Улучшение UI и функциональности

**Задача:** улучшить UI и добавить дополнительные функции.

**Компоненты:**
1. **Управление задачами:**
   - Список задач с фильтрацией (статус, приоритет, исполнитель)
   - Поиск по задачам
   - Создание задачи через форму
   - Обновление задачи (статус, приоритет, исполнитель)
   - Просмотр деталей задачи

2. **Статус проекта:**
   - Дашборд со статистикой (всего задач, по статусам, по приоритетам)
   - Визуализация (графики, диаграммы)
   - Фильтры по времени

3. **Рекомендации:**
   - Отображение рекомендаций по приоритетам
   - Кнопки для быстрых действий (создать задачу, обновить приоритет)
   - История рекомендаций

4. **Примеры вопросов:**
   - Кнопки с популярными вопросами:
     - "Покажи задачи с приоритетом high"
     - "Что делать первым?"
     - "Какой статус проекта?"
     - "Создай задачу для исправления бага"
     - "Какие задачи заблокированы?"

**Обновить team-assistant.html и team-assistant.js:**
- Добавить новые секции и функции
- Улучшить стили и UX

**После коммита:** остановиться.

---

### Коммит 8: Документация и выводы

**Задача:** обновить документацию и подготовить выводы.

**Действия:**
1. Обновить `README.md`:
   - Описание урока 23
   - Пайплайн ассистента команды
   - Архитектура с новыми компонентами
   - Инструкции по использованию
   - Примеры API endpoints

2. Обновить `THEORY.md`:
   - Теория ассистента команды
   - Использование RAG для контекста проекта
   - Интеграция с Task Management через MCP
   - Генерация рекомендаций по приоритетам

3. Обновить `PROMPT.md`:
   - Промпт для реализации урока 23
   - Поэтапная реализация
   - Технические детали

4. Обновить `CHAT_PROMPT.txt`:
   - Краткая версия промпта

5. Обновить корневой `README.md`:
   - Добавить ссылку на урок 23

**После коммита:** показать изменения пользователю.

---

## 🧠 Технические детали

### Интеграция с Task Management через MCP

**Инструменты Task MCP:**
- `get_tasks(filters)` — получение задач с фильтрами
- `get_task(taskId)` — получение задачи по ID
- `create_task(title, description, priority, assignee)` — создание задачи
- `update_task(taskId, updates)` — обновление задачи
- `get_project_status()` — получение статуса проекта
- `get_tasks_by_priority(priority)` — получение задач по приоритету

**Использование:**
```kotlin
val tasks = taskMCPService.getTasks(filters = mapOf("priority" to "HIGH"))
val task = taskMCPService.createTask("Исправить баг", "Описание", Priority.HIGH, "user-1")
val status = taskMCPService.getProjectStatus()
```

### Использование RAG для контекста проекта

**Запрос к RAG:**
```kotlin
val ragResults = ragMCPService.callTool("rag_search_project_docs", JsonObject {
    put("query", question)
    put("topK", 5)
    put("filterPath", "project/docs")
})
```

**Что ищется:**
- Документация проекта (README, API, схема данных)
- Архитектура проекта
- Best practices
- Примеры кода

### Генерация рекомендаций через LLM

**Промпт:**
1. Системный промпт: роль team assistant, инструкции, учёт контекста
2. Пользовательский промпт: информация о проекте, задачи, статус проекта, вопрос

**Формат ответа:**
```json
{
  "answer": "Ответ на вопрос команды",
  "recommendations": [
    {
      "priority": "HIGH",
      "task": {...},
      "reason": "Эта задача блокирует другие"
    }
  ],
  "actions": [
    {
      "type": "CREATE_TASK",
      "description": "Создать задачу для исправления бага",
      "task": {...}
    }
  ]
}
```

### Анализ приоритетов

**Логика:**
- Анализ зависимостей между задачами
- Учёт блокирующих задач
- Учёт сроков выполнения
- Учёт важности для проекта

---

## 📊 Пример сценария

1. **Задать вопрос:**
   ```
   POST /api/team/ask
   {
     "question": "Покажи задачи с приоритетом high и предложи, что делать первым"
   }
   ```

2. **Получение контекста:**
   - Task MCP: get_tasks_by_priority("HIGH")
   - Task MCP: get_project_status()
   - RAG: поиск в документации проекта

3. **Генерация ответа:**
   ```
   {
     "answer": "Найдено 5 задач с приоритетом HIGH. Рекомендую начать с задачи 'Исправить критический баг', так как она блокирует другие задачи.",
     "tasks": [...],
     "recommendations": [
       {
         "priority": "HIGH",
         "task": {...},
         "reason": "Блокирует другие задачи"
       }
     ],
     "actions": [
       {
         "type": "UPDATE_TASK",
         "description": "Обновить статус задачи на IN_PROGRESS",
         "task": {...}
       }
     ]
   }
   ```

---

## 🎓 Цели урока

- Реализовать интегрированный ассистент команды
- Использовать RAG для получения контекста проекта
- Интегрировать Task Management через MCP
- Создавать задачи через API
- Отвечать на вопросы о статусе проекта
- Генерировать рекомендации по приоритетам
- Создать UI для взаимодействия с ассистентом
- Получить полноценный командный ассистент (Видео + Код)

---

## 📚 Документация

- `PROMPT.md` — текущее ТЗ (этот файл).
- `THEORY.md` — теория про ассистента команды.
- `README.md` — руководство по запуску и использованию урока.
- `CHAT_PROMPT.txt` — краткая версия ТЗ для агентной разработки.

