# Краткий промпт для реализации: 🔥 День 20. Ассистент разработчика

Ты — разработчик, создающий урок «🔥 День 20. Ассистент разработчика» в модуле `lesson-20-dev-assistant`. Проект использует стек **Kotlin + Ktor + Clean Architecture + SQLite** с фронтендом на **vanilla JS**.

## Цель

Реализовать ассистента разработчика, который:
- **Использует RAG для подключения к документации проекта** (README, API, схемы данных в `project/docs`)
- **Через MCP подключается к текущему git-репозиторию** (получение текущей ветки через `git branch`)
- **Имеет команду /help**, которая отвечает на вопросы о проекте, подсказывая фрагменты кода или правила стиля

## Минимальные требования для зачёта

- ✅ README + 1 папка `project/docs` в RAG
- ✅ MCP только для получения текущей ветки (`git branch`)
- ✅ Команда `/help` отвечает на вопросы о структуре проекта

## Ключевые требования

1. **Копирование урока 19** — полностью скопировать код из `lesson-19-rag-chat/` в `lesson-20-dev-assistant/`, заменив все упоминания 19 на 20
2. **Подготовка документации проекта** — создать папку `project/docs/` с документацией (api.md, schema.md и т.д.)
3. **Индексация документации проекта** — проиндексировать README + `project/docs/` в базу знаний
4. **MCP сервер для git** — создать MCP сервер с инструментом `get_current_branch`
5. **Интеграция MCP git-сервера** — подключить MCP сервер к основному серверу
6. **Команда /help** — реализовать команду для вопросов о проекте
7. **Обновление промпта** — системный промпт для ассистента разработчика
8. **UI для ассистента** — обновить UI для работы с ассистентом
9. **Документация** — обновить README, THEORY, добавить ссылку в корневой README

## Поэтапная реализация (отдельные коммиты)

**КРИТИЧЕСКИ ВАЖНО:** Каждый шаг = отдельный git-коммит. После каждого шага **СТОП**, покажи изменения пользователю и дождись «ок, продолжай».

### Коммит 1: Копирование урока 19
- Скопировать весь код из `lesson-19-rag-chat/` в `lesson-20-dev-assistant/`
- Заменить все упоминания `lesson-19-rag-chat` → `lesson-20-dev-assistant`
- Заменить «День 19» → «День 20»
- Убедиться, что проект собирается и запускается

### Коммит 2: Подготовка документации проекта
- Создать папку `project/docs/` в корне урока
- Добавить примеры документации: `api.md`, `schema.md`
- Скопировать или создать ссылку на корневой `README.md`
- Убедиться, что файлы доступны для индексации

### Коммит 3: Индексация документации проекта в RAG
- Обновить `DocumentIndexer` для индексации `project/docs/` и `README.md`
- API endpoint `POST /api/indexing/index-project-docs`
- Конфигурация в `config/server.yaml`: `projectDocsPath`, `projectReadmePath`
- Обновить UI: кнопка "Индексировать документацию проекта"

### Коммит 4: MCP сервер для работы с git
- Создать MCP сервер `GitMCPServer` с инструментом `get_current_branch`
- Реализация через `git branch --show-current`
- Обработка ошибок (git не установлен, не в git-репозитории)
- Запуск MCP сервера как отдельный процесс

### Коммит 5: Интеграция MCP git-сервера в основной сервер
- Создать `GitMCPService` для подключения к MCP git-серверу
- Интеграция в `ChatService` — получение текущей ветки
- Обновить `PromptBuilder` — добавить информацию о git-ветке
- Кэширование результата (ветка меняется редко)

### Коммит 6: Команда /help для вопросов о проекте
- Обработчик команд в `ChatService` — распознавание `/help`
- Логика команды `/help [вопрос]` — RAG-поиск только в документации проекта
- Обновить `RAGService` — метод `queryProjectDocs()` с фильтрацией
- Обновить UI — подсказка о команде `/help`

### Коммит 7: Улучшение промпта для ассистента разработчика
- Обновить `PromptBuilder` — роль "ассистент разработчика"
- Системный промпт с описанием доступных ресурсов (RAG, MCP, история)
- Информация о git-ветке в промпте
- Требование обязательных цитат (из урока 18)

### Коммит 8: UI для ассистента разработчика
- Обновить `chat.html` — информация о git-ветке, подсказка о `/help`
- Обновить `chat.js` — обработка команды `/help`
- Создать `dev-assistant.html` — специализированная страница для ассистента
- Обновить `index.html` — ссылка на страницу ассистента

### Коммит 9: Документация и выводы
- Обновить `README.md`, `THEORY.md`, `PROMPT.md`, `CHAT_PROMPT.txt`
- Добавить раздел «Демонстрация» с ссылкой на видео
- Описать результаты и примеры использования
- Обновить корневой `README.md`, добавив ссылку на урок 20

## Технические детали

### Индексация документации проекта
- Структура: `project/docs/` + корневой `README.md`
- Метаданные: тип документа "PROJECT_DOCS"
- Фильтрация в RAG: только чанки из документации проекта

### MCP сервер для git
- Инструмент: `get_current_branch`
- Реализация: `git branch --show-current`
- Обработка ошибок: возвращать "unknown" при ошибках
- Кэширование: на 1-5 минут

### Команда /help
- Формат: `/help [вопрос]` или `/help`
- RAG-поиск: только в документации проекта (фильтрация по метаданным)
- Ответы: с обязательными цитатами из документации проекта

### Системный промпт
- Роль: ассистент разработчика
- Контекст: документация проекта (RAG), git-ветка (MCP), история диалога
- Задачи: отвечать на вопросы о проекте, подсказывать код, объяснять правила стиля

## Важно

- Не ломать существующую функциональность из урока 19
- Использовать существующие компоненты: RAG, MCP, чат
- После каждого коммита останавливаться и показывать изменения
- Пользователь сам делает merge/commit
- Все изменения должны быть протестированы
- Минимальные требования для зачёта должны быть выполнены
