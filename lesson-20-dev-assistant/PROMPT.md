# Промпт для реализации: 🔥 День 20. Ассистент разработчика

Ты — разработчик, создающий урок «🔥 День 20. Ассистент разработчика» в модуле `lesson-20-dev-assistant`. Проект использует стек **Kotlin + Ktor + Clean Architecture + SQLite** с фронтендом на **vanilla JS**. В качестве базы мы **копируем весь код из урока 19** и развиваем его — меняем все упоминания `lesson-19-rag-chat` на `lesson-20-dev-assistant`, не ломая существующую функциональность.

## 🎯 Цель урока

Реализовать ассистента разработчика, который:
- **Использует RAG для подключения к документации проекта** (README, API, схемы данных в `project/docs`)
- **Через MCP подключается к текущему git-репозиторию** (получение текущей ветки через `git branch`)
- **Имеет команду /help**, которая отвечает на вопросы о проекте, подсказывая фрагменты кода или правила стиля

### Ключевая идея

```
Чат с историей + RAG (документация проекта) + MCP (git информация) + Команда /help → Ассистент разработчика
```

Ассистент должен:
- Помнить историю диалога (из урока 19)
- Использовать RAG для поиска в документации проекта
- Получать информацию о git через MCP (текущая ветка)
- Отвечать на вопросы о структуре проекта через команду /help
- Указывать источники для каждого ответа (из урока 18)

### Минимальные требования для зачёта:

- ✅ README + 1 папка `project/docs` в RAG
- ✅ MCP только для получения текущей ветки (`git branch`)
- ✅ Команда `/help` отвечает на вопросы о структуре проекта

---

## 📋 Поэтапная реализация (отдельные коммиты)

**КРИТИЧЕСКИ ВАЖНО:**
- Каждый перечисленный шаг = отдельный git-коммит.
- После каждого шага **СТОП**, покажи изменения пользователю и дождись «ок, продолжай».
- Пользователь сам делает merge/commit далее.

### Коммит 1: Копирование урока 19

**Задача:** полностью скопировать содержимое `lesson-19-rag-chat/` в `lesson-20-dev-assistant/`.

**Действия:**
1. Скопировать все файлы (server, client, config, README, THEORY, PROMPT, CHAT_PROMPT, документы, данные).
2. Обновить названия:
   - Папка проекта, Gradle settings, package references, заголовки, README
   - Все текстовые упоминания `lesson-19-rag-chat` → `lesson-20-dev-assistant`
   - «День 19» → «День 20»
3. Убедиться, что проект собирается и запускается без функциональных изменений.

**После коммита:** остановиться, показать diff пользователю.

---

### Коммит 2: Подготовка документации проекта для индексации

**Задача:** подготовить структуру для индексации документации проекта.

**Действия:**
1. Создать папку `project/docs/` в корне урока (если её нет)
2. Добавить примеры документации:
   - `project/docs/api.md` — описание API
   - `project/docs/schema.md` — схемы данных
   - `project/docs/architecture.md` — архитектура проекта (опционально)
   - `project/docs/style-guide.md` — правила стиля кода (опционально)
3. Скопировать `README.md` из корня проекта в `project/docs/` (или создать ссылку)
4. Убедиться, что файлы доступны для индексации

**Структура:**
```
lesson-20-dev-assistant/
├── project/
│   └── docs/
│       ├── README.md (или ссылка на корневой)
│       ├── api.md
│       └── schema.md
└── ...
```

**Важно:**
- Документация должна быть в формате Markdown
- Файлы должны содержать информацию о структуре проекта, API, схемах данных

**После коммита:** остановиться.

---

### Коммит 3: Индексация документации проекта в RAG

**Задача:** проиндексировать документацию проекта (README + `project/docs/`) в базу знаний.

**Компоненты:**
1. Обновить `DocumentIndexer` или создать `ProjectDocsIndexer`:
   - Индексировать файлы из `project/docs/`
   - Индексировать корневой `README.md`
   - Сохранять метаданные (путь к файлу, тип документа)

2. API endpoint для индексации:
   - `POST /api/indexing/index-project-docs` — индексировать документацию проекта
   - Автоматически находит файлы в `project/docs/` и корневом `README.md`

3. Конфигурация в `config/server.yaml`:
   ```yaml
   indexing:
     chunkSize: 400
     overlapSize: 50
     documentsPath: "documents"
     projectDocsPath: "project/docs"  # новый: путь к документации проекта
     projectReadmePath: "README.md"    # новый: путь к корневому README
   ```

4. Обновить UI:
   - Добавить кнопку "Индексировать документацию проекта" на странице индексации
   - Показывать статус индексации

**Важно:**
- Использовать существующую систему индексации из урока 15-19
- Не дублировать логику, переиспользовать `DocumentIndexer`
- Документация проекта должна быть в отдельной категории или помечена метаданными

**После коммита:** остановиться.

---

### Коммит 4: MCP сервер для работы с git

**Задача:** создать MCP сервер для получения информации о git-репозитории.

**Компоненты:**
1. Создать MCP сервер `GitMCPServer`:
   - Инструмент `get_current_branch` — получение текущей ветки через `git branch`
   - Возвращает название текущей ветки

2. Структура MCP сервера (как в уроке 11):
   ```
   lesson-20-dev-assistant/
   ├── mcp-git-server/
   │   ├── src/
   │   │   └── main/
   │   │       └── kotlin/
   │   │           └── GitMCPServer.kt
   │   ├── build.gradle.kts
   │   └── README.md
   ```

3. Реализация инструмента:
   ```kotlin
   @Tool("get_current_branch", "Получить текущую ветку git-репозитория")
   suspend fun getCurrentBranch(): String {
       val process = ProcessBuilder("git", "branch", "--show-current")
           .directory(File("."))
           .start()
       
       val output = process.inputStream.bufferedReader().readText().trim()
       process.waitFor()
       
       return output.ifEmpty { "unknown" }
   }
   ```

4. Запуск MCP сервера:
   - Сервер должен запускаться как отдельный процесс
   - Использовать stdio transport (как в уроке 11)
   - Конфигурация в `config/server.yaml`:
     ```yaml
     mcp:
       git:
         enabled: true
         command: ["node", "mcp-git-server/dist/index.js"]  # или путь к исполняемому файлу
     ```

**Важно:**
- MCP сервер должен работать в том же репозитории, где запущен основной сервер
- Обрабатывать ошибки (если git не установлен, не в git-репозитории)
- Возвращать понятные сообщения об ошибках

**После коммита:** остановиться.

---

### Коммит 5: Интеграция MCP git-сервера в основной сервер

**Задача:** подключить MCP git-сервер к основному серверу и использовать информацию о ветке.

**Компоненты:**
1. Обновить `MCPService` (из урока 14) или создать `GitMCPService`:
   - Подключение к MCP git-серверу
   - Вызов инструмента `get_current_branch`
   - Кэширование результата (ветка меняется редко)

2. Интеграция в `ChatService`:
   - При обработке сообщения получать текущую ветку через MCP
   - Добавлять информацию о ветке в контекст промпта (опционально)
   - Использовать для ответов на вопросы о проекте

3. Обновить `PromptBuilder`:
   - Добавить информацию о git-ветке в системное сообщение (если доступна)
   - Формат: "Текущая ветка git: {branch}"

4. Конфигурация:
   ```yaml
   mcp:
     git:
       enabled: true
       cacheTimeout: 60000  # кэшировать на 1 минуту
   ```

**Важно:**
- MCP сервер должен запускаться автоматически при старте основного сервера
- Обрабатывать ошибки подключения (если MCP сервер недоступен)
- Не блокировать работу чата, если git-информация недоступна

**После коммита:** остановиться.

---

### Коммит 6: Команда /help для вопросов о проекте

**Задача:** реализовать команду `/help`, которая отвечает на вопросы о структуре проекта.

**Компоненты:**
1. Обработчик команд в `ChatService`:
   - Распознавать команды, начинающиеся с `/`
   - Команда `/help [вопрос]` — отвечает на вопросы о проекте
   - Команда `/help` без параметров — показывает справку

2. Логика команды `/help`:
   - Если есть вопрос: использовать RAG для поиска в документации проекта
   - Если вопроса нет: показать справку по доступным командам
   - Использовать только документацию проекта (не общую базу знаний)

3. Обновить `RAGService`:
   - Добавить метод `queryProjectDocs()` — поиск только в документации проекта
   - Фильтровать чанки по метаданным (путь начинается с `project/docs/` или `README.md`)

4. Формат ответа:
   ```
   /help Как работает API?
   → [RAG-поиск в project/docs/api.md]
   → Ответ с цитатами из документации проекта
   ```

5. Обновить UI:
   - Показывать подсказку о команде `/help` в чате
   - Выделять команды визуально
   - Показывать справку при вводе `/help`

**Важно:**
- Команда должна работать только с документацией проекта
- Ответы должны содержать цитаты из документации
- Если документация не проиндексирована, показывать предупреждение

**После коммита:** остановиться.

---

### Коммит 7: Улучшение промпта для ассистента разработчика

**Задача:** обновить системный промпт, чтобы ассистент понимал контекст разработки.

**Компоненты:**
1. Обновить `PromptBuilder`:
   - Добавить роль "ассистент разработчика"
   - Указать, что ассистент знает о структуре проекта через документацию
   - Добавить информацию о git-ветке (если доступна)

2. Системный промпт:
   ```
   Ты — ассистент разработчика, который помогает с вопросами о проекте.
   
   Ты имеешь доступ к:
   - Документации проекта (README, API, схемы данных)
   - Информации о текущей git-ветке
   - Истории диалога
   
   Твои задачи:
   - Отвечать на вопросы о структуре проекта
   - Подсказывать фрагменты кода из документации
   - Объяснять правила стиля и архитектуру
   - Указывать источники для каждого ответа
   
   Текущая git-ветка: {branch}
   
   Команды:
   - /help [вопрос] — ответить на вопрос о проекте
   - /help — показать справку
   ```

3. Обновить промпт для RAG-запросов:
   - Приоритизировать документацию проекта
   - Использовать информацию о ветке для контекста

**После коммита:** остановиться.

---

### Коммит 8: UI для ассистента разработчика

**Задача:** обновить UI для работы с ассистентом разработчика.

**Компоненты:**
1. Обновить `chat.html`:
   - Добавить информацию о текущей git-ветке (если доступна)
   - Показать подсказку о команде `/help`
   - Выделить команды визуально

2. Обновить `chat.js`:
   - Обработка команды `/help`
   - Отображение справки
   - Показ информации о git-ветке

3. Добавить страницу "Ассистент разработчика":
   - `dev-assistant.html` — специализированная страница для ассистента
   - Показывает текущую ветку
   - Быстрый доступ к индексации документации проекта
   - Примеры вопросов для `/help`

4. Обновить `index.html`:
   - Добавить ссылку на страницу ассистента разработчика

**После коммита:** остановиться.

---

### Коммит 9: Документация и выводы

**Задача:** задокументировать урок и подготовить выводы.

**Действия:**
1. Обновить `README.md`, `THEORY.md`, `PROMPT.md`, `CHAT_PROMPT.txt` под День 20.
2. Добавить раздел «Демонстрация» с ссылкой на видео (формат: Видео + Код).
3. Описать результаты:
   - Как работает индексация документации проекта
   - Как MCP получает информацию о git
   - Как команда `/help` отвечает на вопросы
4. Добавить примеры использования:
   - Вопросы о структуре проекта
   - Использование команды `/help`
   - Получение информации о git-ветке
5. Обновить корневой `README.md`, добавив ссылку на урок 20.

**После коммита:** показать изменения пользователю.

---

## 🧠 Технические детали

### Индексация документации проекта

**Структура:**
- `project/docs/` — папка с документацией проекта
- `README.md` — корневой README проекта
- Все файлы должны быть в формате Markdown

**Процесс индексации:**
1. Найти все `.md` файлы в `project/docs/`
2. Добавить корневой `README.md`
3. Индексировать через существующий `DocumentIndexer`
4. Сохранить метаданные (путь, тип: "project_docs")

**Фильтрация в RAG:**
- При запросе через `/help` фильтровать только чанки из документации проекта
- Использовать метаданные для фильтрации

### MCP сервер для git

**Инструменты:**
- `get_current_branch` — получение текущей ветки

**Реализация:**
- Выполнять команду `git branch --show-current`
- Обрабатывать ошибки (не в git-репозитории, git не установлен)
- Возвращать понятные сообщения

**Интеграция:**
- MCP сервер запускается как отдельный процесс
- Подключается через stdio transport
- Кэширование результата (ветка меняется редко)

### Команда /help

**Формат:**
- `/help` — показать справку
- `/help [вопрос]` — ответить на вопрос о проекте

**Логика:**
1. Распознать команду `/help`
2. Если есть вопрос: выполнить RAG-поиск только в документации проекта
3. Сформировать ответ с цитатами из документации
4. Если вопроса нет: показать справку

**RAG-поиск:**
- Фильтровать чанки по метаданным (путь начинается с `project/docs/` или `README.md`)
- Использовать существующий `RAGService` с фильтрацией

### Системный промпт

**Роль:** Ассистент разработчика

**Контекст:**
- Документация проекта (через RAG)
- Текущая git-ветка (через MCP)
- История диалога

**Задачи:**
- Отвечать на вопросы о структуре проекта
- Подсказывать фрагменты кода
- Объяснять правила стиля
- Указывать источники

---

## 📊 Примеры использования

### Пример 1: Вопрос о структуре проекта

```
Пользователь: /help Какая структура проекта?

Ассистент: [RAG-поиск в project/docs/architecture.md и README.md]
Ответ: Структура проекта следующая:
- server/ — серверная часть (Kotlin + Ktor)
- client/ — клиентская часть (Vanilla JS)
- config/ — конфигурация
[Источник: README.md](README.md)
[Источник: project/docs/architecture.md](project/docs/architecture.md)
```

### Пример 2: Вопрос об API

```
Пользователь: /help Как работает API?

Ассистент: [RAG-поиск в project/docs/api.md]
Ответ: API работает следующим образом:
- POST /api/chat/sessions — создание сессии
- POST /api/chat/sessions/{id}/messages — отправка сообщения
[Источник: project/docs/api.md](project/docs/api.md)
```

### Пример 3: Информация о git-ветке

```
Пользователь: На какой ветке я сейчас?

Ассистент: [MCP: get_current_branch]
Ответ: Вы находитесь на ветке `feature/rag-chat`.
```

### Пример 4: Справка по командам

```
Пользователь: /help

Ассистент: Доступные команды:
- /help [вопрос] — ответить на вопрос о проекте
- /help — показать эту справку

Примеры вопросов:
- /help Какая структура проекта?
- /help Как работает API?
- /help Какие схемы данных используются?
```

---

## 🎓 Цели урока

- Реализовать индексацию документации проекта в RAG
- Создать MCP сервер для работы с git (получение текущей ветки)
- Интегрировать MCP git-сервер в основной сервер
- Реализовать команду `/help` для вопросов о проекте
- Обновить системный промпт для ассистента разработчика
- Создать UI для работы с ассистентом
- Получить ассистента разработчика с RAG, MCP и командой /help (Видео + Код)

---

## 📚 Документация

- `PROMPT.md` — текущее ТЗ (этот файл).
- `THEORY.md` — теория про ассистентов разработчика, RAG для документации, MCP для git.
- `README.md` — руководство по запуску и использованию урока.
- `CHAT_PROMPT.txt` — краткая версия ТЗ для агентной разработки.
