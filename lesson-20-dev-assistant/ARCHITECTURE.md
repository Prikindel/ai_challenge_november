# 🏗️ Архитектура системы "Ассистент разработчика"

## 📋 Содержание
1. [Ответ на вопрос о библиотеках](#ответ-на-вопрос-о-библиотеках)
2. [Архитектурная идея](#архитектурная-идея)
3. [Подход к реализации](#подход-к-реализации)
4. [Компоненты системы](#компоненты-системы)
5. [Потоки данных](#потоки-данных)
6. [Преимущества архитектуры](#преимущества-архитектуры)

---

## Ответ на вопрос о библиотеках

### 1. Запрос к LLM (общая логика)

**Есть ли смысл выносить в библиотеку?** ✅ **ДА, определенно есть смысл**

**Текущая ситуация:**
- `OpenAIClient` дублируется в `server` и `rag-mcp-server`
- Одинаковая логика: HTTP-клиент, сериализация, обработка ошибок, логирование
- Различаются только пакеты (`com.prike.data.client` vs `com.prike.ragmcpserver.data.client`)

**Что стоит вынести:**
- Базовый `LLMClient` с настройкой логирования, retry-логикой, метриками
- Конфигурация через единый интерфейс (API key, model, timeout)
- Единый формат логирования запросов/ответов
- Обработка rate limits и ошибок

**Предложение:**
```kotlin
// Общая библиотека: mcp-common
class LLMClient(
    config: LLMConfig,
    logger: Logger,
    metrics: Metrics? = null
) {
    suspend fun chatCompletion(...): LLMResponse
    suspend fun chatCompletionWithTools(...): LLMToolResponse
}
```

**Преимущества:**
- Единая точка настройки логирования и мониторинга
- Переиспользование кода
- Легче поддерживать и обновлять

---

### 2. Настройка MCP серверов

**Есть ли смысл выносить в библиотеку?** ✅ **ДА, очень большой смысл**

**Текущая ситуация:**
- `GitMCPServer` и `RagMCPServer` имеют **идентичный** код:
  - Создание `Server` с `ServerOptions`
  - Настройка `StdioServerTransport`
  - Регистрация инструментов через `ToolRegistry`
  - Обработка сессий и закрытия

**Что стоит вынести:**
- Базовый класс `BaseMCPServer` с общей логикой запуска
- Шаблонный метод для регистрации инструментов
- Общая обработка ошибок и логирования
- Стандартизация lifecycle (start, stop, health check)

**Предложение:**
```kotlin
// Общая библиотека: mcp-common
abstract class BaseMCPServer(
    serverInfo: Implementation,
    private val toolRegistry: ToolRegistry
) {
    protected val server = Server(...)
    
    fun start() {
        toolRegistry.registerTools(server)
        val transport = StdioServerTransport(...)
        runBlocking {
            val session = server.createSession(transport)
            // Общая логика
        }
    }
}

// Конкретные серверы наследуются
class GitMCPServer(...) : BaseMCPServer(...)
class RagMCPServer(...) : BaseMCPServer(...)
```

**Преимущества:**
- Устранение дублирования кода (DRY)
- Единообразие всех MCP серверов
- Легче добавлять новые MCP серверы
- Централизованная обработка ошибок

---

### 3. RAG (может ли быть несколько разных?)

**Вопрос:** Нормально ли иметь несколько разных RAG систем?

**Ответ:** ✅ **ДА, это нормально и даже полезно**

**Сценарии использования нескольких RAG:**

1. **Разные источники данных:**
   - `rag-project-docs` — документация проекта
   - `rag-codebase` — исходный код
   - `rag-external` — внешние документы (Stack Overflow, документация библиотек)

2. **Разные модели эмбеддингов:**
   - Быстрая модель для общего поиска
   - Точная модель для критичных запросов

3. **Разные стратегии поиска:**
   - Semantic search (векторный поиск)
   - Keyword search (TF-IDF, BM25)
   - Hybrid search (комбинация)

4. **Разные базы знаний:**
   - Отдельная БД для каждого домена
   - Изоляция данных по проектам

**Текущая реализация:**
- Один `rag-mcp-server` с одной БД
- Можно расширить: несколько инструментов (`rag_search_general`, `rag_search_code`, `rag_search_docs`)

**Предложение для множественных RAG:**
```kotlin
// Каждый RAG — отдельный MCP сервер или инструмент
rag-project-docs-mcp-server  // Документация проекта
rag-codebase-mcp-server      // Исходный код
rag-external-mcp-server      // Внешние источники

// Или один сервер с несколькими инструментами
rag-mcp-server:
  - rag_search_project_docs
  - rag_search_codebase
  - rag_search_external
```

**Преимущества множественных RAG:**
- Изоляция данных и конфигураций
- Независимое масштабирование
- Специализация под конкретные задачи
- Легче тестировать и отлаживать

---

## Архитектурная идея

### Основная концепция

**"Модульная система с динамическим обнаружением возможностей"**

Система построена на принципах:
1. **Разделение ответственности** — каждый компонент решает одну задачу
2. **Динамическое обнаружение** — LLM сам выбирает нужные инструменты
3. **Модульность** — новые возможности добавляются без изменения основного кода
4. **Протокол MCP** — единый интерфейс для всех внешних возможностей

### Ключевые принципы

#### 1. Основной сервер — оркестратор
- Не содержит бизнес-логики RAG, Git и т.д.
- Только HTTP API, управление сессиями, координация MCP-клиентов
- LLM принимает решения о выборе инструментов

#### 2. MCP серверы — специализированные модули
- Каждый MCP сервер решает одну задачу (Git, RAG, файлы и т.д.)
- Автономные процессы, общающиеся через stdio
- Предоставляют инструменты через протокол MCP

#### 3. Динамический роутинг через LLM
- `RequestRouterService` использует LLM tool calling
- LLM видит все доступные инструменты и их описания
- Автоматический выбор подходящего инструмента

#### 4. Расширяемость без изменений
- Новый MCP сервер → просто подключить клиент
- LLM автоматически увидит новые инструменты
- Не нужно править код основного сервера

---

## Подход к реализации

### Трехуровневая архитектура

```
┌─────────────────────────────────────────┐
│         Основной сервер (Ktor)          │
│  - HTTP API                             │
│  - Управление сессиями                  │
│  - LLM Service                          │
│  - Request Router (LLM-based)          │
└──────────────┬──────────────────────────┘
               │
               │ MCP Protocol (stdio)
               │
    ┌──────────┴──────────┐
    │                     │
┌───▼──────┐      ┌──────▼──────┐
│ Git MCP  │      │  RAG MCP    │
│ Server   │      │  Server     │
│          │      │              │
│ - branch │      │ - search     │
│ - read   │      │ - index      │
│ - list   │      │ - rerank     │
└──────────┘      └──────────────┘
```

### Поток обработки запроса

1. **Пользователь** → HTTP POST `/api/chat/message`
2. **ChatController** → `ChatService.processMessage()`
3. **RequestRouterService.route()** → 
   - Собирает все доступные инструменты через `listTools()`
   - Формирует промпт с описанием инструментов
   - Вызывает `llmService.generateAnswer()` для получения решения
   - Парсит JSON ответ и извлекает `RoutingDecision`
4. **ChatService** выполняет выбранное действие:
   - `RAG_SEARCH` → `ragMCPService.callTool("rag_search", ...)`
   - `RAG_SEARCH_PROJECT` → `ragMCPService.callTool("rag_search_project_docs", ...)`
   - `MCP_TOOL` → `gitMCPService.callTool(toolName, ...)` или `ragMCPService.callTool(...)`
   - `DIRECT_ANSWER` → пропускает инструменты
5. **Результат инструмента** → добавляется в контекст как `additionalContext`
6. **LLM генерирует финальный ответ** → `llmService.generateAnswerWithMessages()` с контекстом
7. **Ответ** → Пользователю через HTTP

### Динамическое обнаружение инструментов

**Текущая реализация (через парсинг JSON):**

```kotlin
// RequestRouterService собирает все инструменты
val tools = mutableListOf<MCPTool>()
gitMCPService?.listTools()?.let { tools.addAll(it) }
ragMCPService?.listTools()?.let { tools.addAll(it) }

// Формируем промпт с описанием инструментов
val prompt = buildRoutingPrompt(question, toolsDescription)

// LLM генерирует JSON с решением
val response = llmService.generateAnswer(prompt, systemPrompt)

// Парсим JSON и извлекаем решение
val decision = parseRoutingDecision(response.answer, tools)

// Выполняем выбранный инструмент
when (decision.action) {
    RAG_SEARCH -> ragMCPService.callTool("rag_search", ...)
    MCP_TOOL -> {
        when (decision.toolName) {
            "read_file" -> gitMCPService.callTool("read_file", ...)
            "rag_search" -> ragMCPService.callTool("rag_search", ...)
        }
    }
}
```

**Потенциальное улучшение (через tool calling):**

В `LLMService` уже есть метод `chatCompletionWithTools()`, который можно использовать для более надежного выбора инструментов:

```kotlin
// LLM видит описания и выбирает через tool calling
val response = llmService.chatCompletionWithTools(messages, tools)
// → LLM возвращает toolCall с именем и аргументами напрямую

// Выполняем выбранный инструмент
when (response.toolCall?.name) {
    "rag_search" -> ragMCPService.callTool(...)
    "read_file" -> gitMCPService.callTool(...)
}
```

**Примечание:** Текущая реализация работает, но использование tool calling было бы более надежным и соответствовало бы стандартам MCP.

---

## Компоненты системы

### 1. Основной сервер (`server/`)

**Ответственность:**
- HTTP API для фронтенда
- Управление сессиями чата
- Интеграция с LLM (OpenRouter)
- Координация MCP-клиентов
- Динамический роутинг запросов

**Ключевые классы:**
- `Main.kt` — точка входа, инициализация
- `ChatService` — обработка сообщений
- `RequestRouterService` — LLM-based роутинг
- `LLMService` — работа с OpenRouter API
- `GitMCPService` / `RagMCPService` — клиенты MCP серверов

**Не содержит:**
- ❌ Логику RAG (индексация, поиск, reranking)
- ❌ Прямую работу с Git
- ❌ Бизнес-логику инструментов

---

### 2. Git MCP Server (`git-mcp-server/`)

**Ответственность:**
- Инструменты для работы с Git
- Чтение файлов
- Листинг директорий

**Инструменты:**
- `get_current_branch` — текущая ветка Git
- `read_file` — чтение содержимого файла
- `list_directory` — список файлов в директории

**Архитектура:**
```
Main.kt
  → GitMCPServer
    → ToolRegistry
      → GetCurrentBranchTool
      → ReadFileTool
      → ListDirectoryTool
        → Handlers (GetCurrentBranchHandler, etc.)
```

---

### 3. RAG MCP Server (`rag-mcp-server/`)

**Ответственность:**
- Индексация документов
- Семантический поиск
- Reranking результатов

**Инструменты:**
- `rag_search` — поиск по всем документам
- `rag_search_project_docs` — поиск только в документации проекта
- `rag_index_documents` — индексация документов
- `rag_index_project_docs` — индексация документации проекта

**Компоненты:**
- `OllamaClient` — эмбеддинги
- `OpenAIClient` — reranking
- `KnowledgeBaseRepository` — SQLite БД
- `DocumentIndexer` — индексация
- `KnowledgeBaseSearchService` — поиск
- `RerankerService` — переранжирование

---

## Потоки данных

### Индексация документации

```
1. HTTP POST /api/indexing/index-project-docs
   ↓
2. IndexingController
   ↓
3. RagMCPService.callTool("rag_index_project_docs")
   ↓
4. RagMCPClient → stdio → rag-mcp-server
   ↓
5. RagIndexProjectDocsTool → Handler
   ↓
6. DocumentIndexer
   ↓
7. DocumentLoader → TextChunker → EmbeddingService
   ↓
8. KnowledgeBaseRepository.save()
```

### Поиск через RAG

```
1. Пользователь: "Как работает API?"
   ↓
2. RequestRouterService.route()
   ↓
3. LLM tool calling → выбирает "rag_search_project_docs"
   ↓
4. RagMCPService.callTool("rag_search_project_docs", {query: "..."})
   ↓
5. rag-mcp-server:
   - EmbeddingService.embed(query)
   - KnowledgeBaseSearchService.search()
   - RerankerService.rerank()
   ↓
6. Результат → ChatService
   ↓
7. LLM генерирует ответ с контекстом
   ↓
8. Ответ пользователю
```

### Чтение файла через MCP

```
1. Пользователь: "Покажи содержимое project/docs/api.md"
   ↓
2. RequestRouterService.route()
   ↓
3. LLM tool calling → выбирает "read_file"
   ↓
4. GitMCPService.callTool("read_file", {path: "project/docs/api.md"})
   ↓
5. git-mcp-server → ReadFileHandler
   ↓
6. File.readText() → результат
   ↓
7. ChatService → LLM генерирует ответ
```

---

## Преимущества архитектуры

### 1. Модульность
- ✅ Каждый компонент независим
- ✅ Легко добавлять новые MCP серверы
- ✅ Не нужно править основной код

### 2. Масштабируемость
- ✅ MCP серверы можно запускать отдельно
- ✅ Можно распределить по разным машинам
- ✅ Независимое масштабирование компонентов

### 3. Тестируемость
- ✅ Каждый компонент тестируется изолированно
- ✅ MCP серверы можно тестировать через stdio
- ✅ Легко мокировать для unit-тестов

### 4. Гибкость
- ✅ LLM сам выбирает инструменты
- ✅ Не нужно хардкодить логику выбора
- ✅ Легко добавлять новые типы запросов

### 5. Поддерживаемость
- ✅ Четкое разделение ответственности
- ✅ Понятная структура кода
- ✅ Легко найти и исправить баги

---

## Рекомендации по улучшению

### Краткосрочные (сейчас можно сделать)

1. **Вынести общую логику MCP серверов**
   - Создать `mcp-common` библиотеку
   - Базовый класс `BaseMCPServer`
   - Общие утилиты для работы с MCP

2. **Вынести LLM клиент**
   - Общий `LLMClient` в `mcp-common`
   - Единое логирование и метрики
   - Переиспользование в server и rag-mcp-server

3. **Стандартизировать конфигурацию**
   - Единый формат конфигов для всех MCP серверов
   - Общие настройки логирования

### Долгосрочные (для будущего)

1. **Множественные RAG системы**
   - Отдельные MCP серверы для разных источников
   - Или один сервер с несколькими инструментами

2. **Мониторинг и метрики**
   - Централизованный сбор метрик
   - Трассировка запросов через систему

3. **Кэширование**
   - Кэш для результатов RAG поиска
   - Кэш для LLM ответов

4. **Безопасность**
   - Валидация путей файлов
   - Ограничение доступа к инструментам
   - Rate limiting

---

## Проверка соответствия идее

### ✅ Соответствие архитектурной идее

**Основная идея:** Модульная система с динамическим обнаружением возможностей через LLM

**Проверка:**

1. **✅ Основной сервер не содержит RAG логики**
   - `Main.kt` не инициализирует RAG компоненты
   - `ChatService` использует только `ragMCPService.callTool()`
   - `IndexingController` использует только MCP инструменты

2. **✅ MCP серверы автономны**
   - `git-mcp-server` и `rag-mcp-server` — отдельные процессы
   - Общаются через stdio по протоколу MCP
   - Не зависят от основного сервера

3. **✅ Динамический выбор инструментов**
   - `RequestRouterService` собирает все доступные инструменты
   - LLM выбирает подходящий инструмент на основе описаний
   - Не нужно хардкодить логику выбора

4. **✅ Расширяемость без изменений основного кода**
   - Новый MCP сервер → просто подключить клиент в `Main.kt`
   - LLM автоматически увидит новые инструменты через `listTools()`
   - Не нужно править `ChatService` или `RequestRouterService`

### ⚠️ Области для улучшения

1. **RequestRouterService использует парсинг JSON вместо tool calling**
   - Текущая реализация: LLM генерирует JSON, который парсится
   - Можно улучшить: использовать `chatCompletionWithTools()` для прямого tool calling
   - Это более надежно и соответствует стандартам MCP

2. **Дублирование кода**
   - `OpenAIClient` дублируется в `server` и `rag-mcp-server`
   - `GitMCPServer` и `RagMCPServer` имеют идентичный код
   - Рекомендация: вынести в общую библиотеку

3. **Конфигурация не стандартизирована**
   - Каждый MCP сервер загружает конфиг по-своему
   - Рекомендация: единый формат конфигурации

## Заключение

Текущая архитектура **соответствует идее** модульной системы с динамическим обнаружением возможностей. 

**Сильные стороны:**
- ✅ Четкое разделение ответственности
- ✅ Динамический выбор инструментов через LLM
- ✅ Легкая расширяемость
- ✅ Основной сервер не содержит бизнес-логики RAG/Git

**Что можно улучшить:**
- 🔄 Использовать tool calling вместо парсинга JSON в `RequestRouterService`
- 🔄 Вынести общую логику в библиотеки (MCP серверы, LLM клиент)
- 🔄 Стандартизировать конфигурацию
- 🔄 Добавить мониторинг и метрики

**Рекомендация:** 
1. **Краткосрочно:** Вынести общую логику MCP серверов и LLM клиента в библиотеку `mcp-common`
2. **Среднесрочно:** Перейти на tool calling в `RequestRouterService` для более надежного выбора инструментов
3. **Долгосрочно:** Добавить мониторинг, метрики и поддержку множественных RAG систем

