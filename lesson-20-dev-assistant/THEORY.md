# Теория: Мини-чат с памятью на RAG

## 📖 Введение

Чат-боты с RAG-памятью объединяют два важных компонента:
1. **История диалога** — контекст предыдущих сообщений
2. **RAG-поиск** — актуальная информация из базы знаний

Это позволяет создавать более умных и контекстуальных ассистентов, которые:
- Помнят предыдущие сообщения в диалоге
- Используют актуальную информацию из документов
- Ссылаются на источники для прозрачности

---

## 🎯 Проблема: Чат без памяти vs Чат с памятью

### Чат без памяти (плохо)

**Пример:**
```
Пользователь: Как создать MCP сервер?
Бот: Для создания MCP сервера нужно установить SDK...

Пользователь: А как его установить?
Бот: Что именно вы хотите установить? (не понимает контекст "его")
```

**Проблемы:**
- Бот не помнит предыдущие сообщения
- Каждый вопрос обрабатывается изолированно
- Нет контекста диалога
- Пользователю приходится повторять информацию

### Чат с памятью (хорошо)

**Пример:**
```
Пользователь: Как создать MCP сервер?
Бот: Для создания MCP сервера нужно установить SDK [Источник: ...](...)

Пользователь: А как его установить?
Бот: SDK можно установить через npm: npm install mcp-sdk [Источник: ...](...)
```

**Преимущества:**
- Бот помнит предыдущие сообщения
- Понимает контекст ("его" = SDK)
- Отвечает с учётом истории
- Более естественный диалог

---

## 💡 Решение: RAG + История диалога

### Архитектура

```
┌─────────────────┐
│  Пользователь   │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  История диалога│ ← Хранит предыдущие сообщения
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  RAG-поиск       │ ← Ищет релевантную информацию
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Формирование   │ ← Объединяет историю + RAG-контекст
│  промпта         │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  LLM             │ ← Генерирует ответ
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Парсинг цитат   │ ← Извлекает источники
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Сохранение     │ ← Сохраняет в историю
│  в историю      │
└─────────────────┘
```

### Компоненты

1. **История диалога:**
   - Хранит все сообщения пользователя и бота
   - Организована по сессиям
   - Доступна для формирования промпта

2. **RAG-поиск:**
   - Выполняется при каждом новом вопросе
   - Находит релевантные чанки из базы знаний
   - Используется как контекст для ответа

3. **Формирование промпта:**
   - Системное сообщение
   - История диалога (последние N сообщений)
   - Контекст из RAG
   - Текущий вопрос

4. **Генерация ответа:**
   - LLM получает полный контекст
   - Генерирует ответ с учётом истории и RAG
   - Обязательно указывает источники

5. **Парсинг и сохранение:**
   - Извлекает цитаты из ответа
   - Сохраняет вопрос и ответ в историю

---

## 🔧 Технические подходы

### 1. Хранение истории

#### SQLite (рекомендуется)

**Таблицы:**
```sql
chat_sessions:
  - id (TEXT PRIMARY KEY)
  - created_at (INTEGER)
  - updated_at (INTEGER)
  - message_count (INTEGER)
  - title (TEXT)

chat_messages:
  - id (TEXT PRIMARY KEY)
  - session_id (TEXT, FOREIGN KEY)
  - role (TEXT)  -- USER или ASSISTANT
  - content (TEXT)
  - citations (TEXT)  -- JSON массив
  - timestamp (INTEGER)
  - metadata (TEXT)  -- JSON объект
```

**Преимущества:**
- Простота реализации
- Персистентность данных
- Быстрый доступ по индексам
- Подходит для небольших и средних объёмов

#### Альтернативы

- **In-memory** (HashMap) — для тестирования, данные теряются при перезапуске
- **PostgreSQL/MySQL** — для больших объёмов и масштабирования
- **Redis** — для кэширования и быстрого доступа

### 2. Управление контекстом

#### Проблема: Лимиты токенов

История диалога может быть очень длинной, но LLM имеет лимиты на количество токенов.

#### Решения:

**A. Sliding Window (скользящее окно)**
- Берём только последние N сообщений
- Старые сообщения игнорируются

**Пример:**
```kotlin
fun getRecentHistory(sessionId: String, limit: Int = 10): List<ChatMessage> {
    return chatRepository.getHistory(sessionId)
        .takeLast(limit)
}
```

**Плюсы:**
- Простота реализации
- Предсказуемое использование токенов

**Минусы:**
- Теряется контекст из старых сообщений

**B. Token Limit (лимит по токенам)**
- Берём сообщения, пока не превысим лимит токенов
- Начинаем с самых новых

**Пример:**
```kotlin
fun getHistoryWithinTokenLimit(
    sessionId: String,
    maxTokens: Int = 2000
): List<ChatMessage> {
    val history = chatRepository.getHistory(sessionId)
    var tokens = 0
    val result = mutableListOf<ChatMessage>()
    
    for (message in history.reversed()) {
        val messageTokens = estimateTokens(message.content)
        if (tokens + messageTokens > maxTokens) break
        tokens += messageTokens
        result.add(0, message)  // добавляем в начало
    }
    
    return result
}
```

**Плюсы:**
- Более гибкое управление контекстом
- Учитывает длину сообщений

**Минусы:**
- Нужно считать токены

**C. Summary (суммаризация)** — опционально
- Старые сообщения суммируются
- Суммаризация хранится отдельно
- В текущей реализации не используется, но может быть добавлена в будущем

**Плюсы:**
- Сохраняется контекст из старых сообщений
- Экономия токенов

**Минусы:**
- Дополнительный запрос к LLM
- Может потерять детали

**Примечание:** В уроке 19 реализованы стратегии A (sliding window) и B (token limit), которые можно выбирать через UI.

### 3. Формирование промпта

**Структура промпта:**
```
{systemMessage}

История диалога:
{historyMessages}

Контекст из базы знаний:
{ragContext}

Текущий вопрос: {currentQuestion}

Инструкции:
- Отвечай с учётом истории диалога
- Используй контекст из базы знаний
- ОБЯЗАТЕЛЬНО указывай источники

Ответ:
```

**Важно:**
- История должна быть актуальной (последние сообщения)
- RAG-контекст должен быть релевантным текущему вопросу
- Системное сообщение должно требовать цитаты (из урока 18)

### 4. Интеграция RAG

**Когда выполнять RAG-поиск:**
- При каждом новом сообщении пользователя
- Только для вопросов пользователя (не для ответов бота)

**Параметры поиска:**
- `topK` — количество чанков (обычно 3-5)
- `minSimilarity` — минимальное сходство (обычно 0.4-0.6)
- Фильтрация и реранкинг (из урока 17)

**Использование контекста:**
- RAG-контекст добавляется в промпт
- LLM использует его для ответа
- Цитаты извлекаются из ответа

---

## 📊 Примеры использования

### Пример 1: Простой диалог

```
Пользователь: Как создать MCP сервер?
Бот: Для создания MCP сервера нужно установить SDK [Источник: ...](...)

Пользователь: А как его установить?
Бот: SDK можно установить через npm: npm install mcp-sdk [Источник: ...](...)
```

**Что происходит:**
1. Первый вопрос → RAG-поиск → ответ с цитатой
2. Второй вопрос → история + RAG-поиск → ответ с учётом контекста "его"

### Пример 2: Многошаговый диалог

```
Пользователь: Что такое RAG?
Бот: RAG (Retrieval-Augmented Generation) — это техника... [Источник: ...](...)

Пользователь: Как его использовать?
Бот: RAG можно использовать для... [Источник: ...](...)

Пользователь: А как работает поиск?
Бот: Поиск в RAG работает через... [Источник: ...](...)
```

**Что происходит:**
- Каждый вопрос использует историю
- RAG-поиск выполняется для каждого вопроса
- Контекст накапливается в истории

### Пример 3: Уточняющие вопросы

```
Пользователь: Как создать MCP сервер?
Бот: Для создания MCP сервера нужно установить SDK [Источник: ...](...)

Пользователь: А что такое SDK?
Бот: SDK (Software Development Kit) — это набор инструментов... [Источник: ...](...)

Пользователь: Понятно, вернёмся к созданию сервера
Бот: Итак, для создания MCP сервера после установки SDK нужно... [Источник: ...](...)
```

**Что происходит:**
- Бот помнит контекст "создание сервера"
- Понимает, что нужно вернуться к предыдущей теме
- Использует историю для контекста

---

## 🎓 Лучшие практики

### 1. Управление историей

- **Ограничивайте историю** — не более 10-20 последних сообщений (sliding window)
- **Используйте токен-лимиты** — не превышайте лимиты LLM (token limit)
- **Выбирайте стратегию** — в зависимости от типа диалога (через UI в уроке 19)
- **Суммируйте старые сообщения** — если диалог очень длинный (опционально, не реализовано)

### 2. RAG-поиск

- **Выполняйте при каждом вопросе** — даже если есть история
- **Настраивайте параметры** — topK, minSimilarity, фильтрация
- **Используйте реранкинг** — для лучшего качества (из урока 17)

### 3. Формирование промпта

- **Приоритизируйте недавние сообщения** — они более релевантны
- **Включайте RAG-контекст** — даже если есть история
- **Требуйте цитаты** — обязательно указывать источники (из урока 18)

### 4. Обработка ошибок

- **Fallback при недоступности RAG** — использовать только историю
- **Обработка пустого контекста** — если RAG не нашёл релевантных чанков
- **Валидация цитат** — проверять, что источники существуют

### 5. UX

- **Показывать историю** — пользователь должен видеть предыдущие сообщения
- **Отображать цитаты** — ссылки на источники должны быть кликабельными
- **Индикаторы загрузки** — показывать, когда идёт поиск или генерация ответа

---

## 🔬 Исследования и выводы

### Влияние истории на качество ответов

**Исследования показывают:**
- История диалога улучшает качество ответов на 20-30%
- Контекстуальные ответы воспринимаются как более естественные
- Пользователи предпочитают чаты с памятью

### Влияние RAG на точность

**Исследования показывают:**
- RAG-поиск повышает точность ответов на 40-50%
- Обязательные цитаты снижают галлюцинации на 30-50%
- Комбинация истории + RAG даёт лучшие результаты

### Рекомендации

1. **Используйте историю** — для контекстуальных ответов
2. **Используйте RAG** — для актуальной информации
3. **Требуйте цитаты** — для прозрачности и снижения галлюцинаций
4. **Управляйте контекстом** — не превышайте лимиты токенов

---

## 📚 Дополнительные ресурсы

- [RAG: Retrieval-Augmented Generation](https://arxiv.org/abs/2005.11401) — оригинальная статья
- [Conversational AI with Memory](https://www.pinecone.io/learn/conversational-ai/) — про чаты с памятью
- [Context Management in LLMs](https://arxiv.org/abs/2305.14627) — про управление контекстом
- [Chatbot Architecture](https://www.pinecone.io/learn/chatbot-architecture/) — архитектура чат-ботов

---

## 🎯 Выводы

1. **История диалога** — критически важна для контекстуальных ответов
2. **RAG-поиск** — обеспечивает актуальную информацию из документов
3. **Комбинация истории + RAG** — даёт лучшие результаты
4. **Обязательные цитаты** — повышают прозрачность и снижают галлюцинации
5. **Управление контекстом** — необходимо для работы с длинными диалогами
6. **Веб-интерфейс** — улучшает UX и делает чат доступным

