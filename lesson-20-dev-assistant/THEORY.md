# Теория: Ассистент разработчика с RAG и MCP

## 📖 Введение

Ассистент разработчика — это AI-агент, который помогает разработчикам работать с проектом, отвечая на вопросы о структуре, API, схемах данных и правилах стиля. Он объединяет:

1. **RAG (Retrieval-Augmented Generation)** — для поиска информации в документации проекта
2. **MCP (Model Context Protocol)** — для получения контекста из инструментов разработки (git, IDE и т.д.)
3. **Чат с памятью** — для ведения диалога с контекстом

Это позволяет создавать умных ассистентов, которые:
- Понимают структуру проекта через документацию
- Знают текущее состояние разработки (git-ветка, открытые файлы)
- Отвечают на вопросы с учётом контекста проекта
- Ссылаются на источники для прозрачности

---

## 🎯 Проблема: Разработчик без ассистента vs С ассистентом

### Разработчик без ассистента (плохо)

**Сценарий:**
```
Разработчик: "Как работает API в этом проекте?"
→ Нужно искать в документации вручную
→ Может не найти нужную информацию
→ Тратит время на поиск
```

**Проблемы:**
- Нужно вручную искать информацию в документации
- Может не знать о существовании документации
- Тратит время на поиск вместо разработки
- Может использовать устаревшую информацию

### Разработчик с ассистентом (хорошо)

**Сценарий:**
```
Разработчик: "/help Как работает API?"
Ассистент: [RAG-поиск в документации] → Ответ с цитатами
→ Быстрый ответ с источниками
→ Разработчик может сразу использовать информацию
```

**Преимущества:**
- Быстрый доступ к информации через естественный язык
- Автоматический поиск в документации
- Актуальная информация (из документации проекта)
- Ссылки на источники для проверки

---

## 💡 Решение: RAG + MCP + Чат

### Архитектура

```
┌─────────────────┐
│  Разработчик    │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Чат с историей  │ ← История диалога (из урока 19)
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  Обработчик     │ ← Распознавание команд (/help)
│  команд         │
└────────┬────────┘
         │
         ├─────────────────┬─────────────────┐
         ▼                 ▼                 ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  RAG-поиск   │  │  MCP (git)   │  │  История     │
│  в документации│  │  информация  │  │  диалога     │
└──────────────┘  └──────────────┘  └──────────────┘
         │                 │                 │
         └─────────────────┴─────────────────┘
                           │
                           ▼
                  ┌─────────────────┐
                  │  Формирование   │
                  │  промпта        │
                  └────────┬────────┘
                           │
                           ▼
                  ┌─────────────────┐
                  │  LLM            │
                  └────────┬────────┘
                           │
                           ▼
                  ┌─────────────────┐
                  │  Ответ с        │
                  │  цитатами       │
                  └─────────────────┘
```

### Компоненты

1. **RAG для документации проекта:**
   - Индексация документации (README, `project/docs/`)
   - Поиск релевантной информации при запросах
   - Фильтрация только по документации проекта

2. **MCP для git:**
   - Получение текущей ветки через `git branch`
   - Интеграция в контекст промпта
   - Кэширование для производительности

3. **Чат с памятью:**
   - История диалога (из урока 19)
   - Команды (например, `/help`)
   - Контекстуальные ответы

4. **Команда /help:**
   - Распознавание команд
   - RAG-поиск в документации проекта
   - Ответы с цитатами

---

## 🔧 Технические подходы

### 1. Индексация документации проекта

#### Структура документации

**Рекомендуемая структура:**
```
project/
└── docs/
    ├── README.md          # общее описание проекта
    ├── api.md             # описание API
    ├── schema.md          # схемы данных
    ├── architecture.md    # архитектура (опционально)
    └── style-guide.md     # правила стиля (опционально)
```

**Минимальные требования:**
- README.md (корневой или в `project/docs/`)
- 1 папка `project/docs/` с документацией

#### Процесс индексации

**Шаги:**
1. Найти все `.md` файлы в `project/docs/`
2. Добавить корневой `README.md`
3. Индексировать через существующий `DocumentIndexer` (из урока 15)
4. Сохранить метаданные (путь, тип: "project_docs")

**Метаданные:**
```kotlin
data class DocumentMetadata(
    val path: String,
    val type: DocumentType,  // PROJECT_DOCS, GENERAL
    val indexedAt: Long
)

enum class DocumentType {
    PROJECT_DOCS,  // документация проекта
    GENERAL        // общая документация
}
```

#### Фильтрация в RAG

**При запросе через `/help`:**
- Фильтровать только чанки из документации проекта
- Использовать метаданные для фильтрации
- Игнорировать общую базу знаний

**Пример:**
```kotlin
fun queryProjectDocs(question: String): List<RetrievedChunk> {
    val allChunks = ragService.queryWithRAG(question)
    return allChunks.filter { chunk ->
        chunk.documentPath.startsWith("project/docs/") ||
        chunk.documentPath == "README.md"
    }
}
```

### 2. MCP сервер для git

#### Архитектура MCP сервера

**Структура (как в уроке 11):**
```
mcp-git-server/
├── src/
│   └── main/
│       └── kotlin/
│           └── GitMCPServer.kt
├── build.gradle.kts
└── README.md
```

**Инструменты:**
- `get_current_branch` — получение текущей ветки

#### Реализация инструмента

**Пример:**
```kotlin
@Tool(
    name = "get_current_branch",
    description = "Получить текущую ветку git-репозитория"
)
suspend fun getCurrentBranch(): String {
    try {
        val process = ProcessBuilder("git", "branch", "--show-current")
            .directory(File("."))
            .start()
        
        val output = process.inputStream.bufferedReader().readText().trim()
        process.waitFor()
        
        if (process.exitValue() != 0) {
            return "unknown"
        }
        
        return output.ifEmpty { "unknown" }
    } catch (e: Exception) {
        return "unknown"
    }
}
```

**Обработка ошибок:**
- Git не установлен → возвращать "unknown"
- Не в git-репозитории → возвращать "unknown"
- Ошибки выполнения → логировать и возвращать "unknown"

#### Интеграция в основной сервер

**Подключение MCP сервера:**
```kotlin
class GitMCPService {
    private val mcpClient: MCPClient
    
    suspend fun getCurrentBranch(): String {
        return mcpClient.callTool("get_current_branch", emptyMap())
    }
}
```

**Кэширование:**
- Ветка меняется редко
- Кэшировать результат на 1-5 минут
- Обновлять при необходимости

### 3. Команда /help

#### Распознавание команд

**Формат:**
- `/help` — показать справку
- `/help [вопрос]` — ответить на вопрос о проекте

**Обработка:**
```kotlin
fun processCommand(message: String): CommandResult {
    if (!message.startsWith("/")) {
        return CommandResult.NotACommand
    }
    
    val parts = message.split(" ", limit = 2)
    val command = parts[0]
    val argument = parts.getOrNull(1)
    
    return when (command) {
        "/help" -> {
            if (argument != null) {
                CommandResult.HelpQuestion(argument)
            } else {
                CommandResult.HelpInfo
            }
        }
        else -> CommandResult.UnknownCommand
    }
}
```

#### Логика команды /help

**Если есть вопрос:**
1. Выполнить RAG-поиск только в документации проекта
2. Фильтровать чанки по метаданным
3. Сформировать ответ с цитатами
4. Вернуть ответ пользователю

**Если вопроса нет:**
1. Показать справку по доступным командам
2. Показать примеры вопросов

#### RAG-поиск для /help

**Специализированный метод:**
```kotlin
suspend fun queryProjectDocs(
    question: String,
    topK: Int = 5
): RAGResponse {
    // 1. RAG-поиск
    val allChunks = ragService.queryWithRAG(question, topK)
    
    // 2. Фильтрация только документации проекта
    val projectChunks = allChunks.contextChunks.filter { chunk ->
        chunk.documentPath.startsWith("project/docs/") ||
        chunk.documentPath == "README.md"
    }
    
    // 3. Формирование ответа
    val prompt = promptBuilder.buildProjectDocsPrompt(question, projectChunks)
    val answer = llmService.getCompletion(prompt)
    
    // 4. Парсинг цитат
    val citations = citationParser.parseCitations(answer.content, projectChunks)
    
    return RAGResponse(question, answer.content, citations, projectChunks)
}
```

### 4. Системный промпт для ассистента

**Роль:** Ассистент разработчика

**Структура промпта:**
```
Ты — ассистент разработчика, который помогает с вопросами о проекте.

Ты имеешь доступ к:
- Документации проекта (README, API, схемы данных) через RAG
- Информации о текущей git-ветке через MCP
- Истории диалога

Твои задачи:
- Отвечать на вопросы о структуре проекта
- Подсказывать фрагменты кода из документации
- Объяснять правила стиля и архитектуру
- Указывать источники для каждого ответа

Текущая git-ветка: {branch}

Команды:
- /help [вопрос] — ответить на вопрос о проекте
- /help — показать справку

ВАЖНО: При использовании команды /help ищи информацию ТОЛЬКО в документации проекта.
```

**Обновление контекста:**
- Git-ветка обновляется при каждом запросе (с кэшированием)
- Документация проекта индексируется один раз
- История диалога обновляется при каждом сообщении

---

## 📊 Примеры использования

### Пример 1: Вопрос о структуре проекта

```
Пользователь: /help Какая структура проекта?

Ассистент: [RAG-поиск в project/docs/architecture.md и README.md]
Ответ: Структура проекта следующая:
- server/ — серверная часть (Kotlin + Ktor)
- client/ — клиентская часть (Vanilla JS)
- config/ — конфигурация
- project/docs/ — документация проекта

[Источник: README.md](README.md)
[Источник: project/docs/architecture.md](project/docs/architecture.md)
```

### Пример 2: Вопрос об API

```
Пользователь: /help Как работает API?

Ассистент: [RAG-поиск в project/docs/api.md]
Ответ: API работает следующим образом:
- POST /api/chat/sessions — создание сессии
- POST /api/chat/sessions/{id}/messages — отправка сообщения
- GET /api/chat/sessions/{id}/messages — получение истории

[Источник: project/docs/api.md](project/docs/api.md)
```

### Пример 3: Информация о git-ветке

```
Пользователь: На какой ветке я сейчас?

Ассистент: [MCP: get_current_branch]
Ответ: Вы находитесь на ветке `feature/rag-chat`.
```

### Пример 4: Справка по командам

```
Пользователь: /help

Ассистент: Доступные команды:
- /help [вопрос] — ответить на вопрос о проекте
- /help — показать эту справку

Примеры вопросов:
- /help Какая структура проекта?
- /help Как работает API?
- /help Какие схемы данных используются?
```

---

## 🎓 Лучшие практики

### 1. Индексация документации

- **Структурируйте документацию** — используйте чёткую структуру папок
- **Обновляйте документацию** — переиндексируйте при изменениях
- **Используйте метаданные** — помечайте документацию проекта отдельно

### 2. MCP серверы

- **Обрабатывайте ошибки** — git может быть недоступен
- **Кэшируйте результаты** — ветка меняется редко
- **Используйте понятные сообщения** — для отладки

### 3. Команды

- **Распознавайте команды явно** — начинаются с `/`
- **Показывайте справку** — если команда не распознана
- **Используйте специализированный поиск** — для `/help` только документация проекта

### 4. Системный промпт

- **Указывайте роль** — ассистент разработчика
- **Описывайте доступные ресурсы** — RAG, MCP, история
- **Требуйте цитаты** — для прозрачности

---

## 🔬 Исследования и выводы

### Влияние RAG на качество ответов

**Исследования показывают:**
- RAG повышает точность ответов о проекте на 40-50%
- Документация проекта более релевантна, чем общая база знаний
- Фильтрация по типу документации улучшает качество

### Влияние MCP на контекст

**Исследования показывают:**
- Информация о git-ветке помогает в контексте разработки
- MCP позволяет получать актуальную информацию из инструментов
- Кэширование важно для производительности

### Рекомендации

1. **Индексируйте документацию проекта** — отдельно от общей базы знаний
2. **Используйте MCP** — для получения контекста из инструментов
3. **Реализуйте команды** — для специализированных запросов
4. **Требуйте цитаты** — для прозрачности и проверки

---

## 📚 Дополнительные ресурсы

- [RAG: Retrieval-Augmented Generation](https://arxiv.org/abs/2005.11401) — оригинальная статья
- [MCP: Model Context Protocol](https://modelcontextprotocol.io/) — протокол MCP
- [AI Coding Assistants](https://www.pinecone.io/learn/ai-coding-assistants/) — про AI-ассистентов для разработки
- [Documentation as Code](https://www.writethedocs.org/guide/docs-as-code/) — документация как код

---

## 🎯 Выводы

1. **RAG для документации проекта** — критически важен для точных ответов
2. **MCP для git** — предоставляет контекст разработки
3. **Команда /help** — специализированный интерфейс для вопросов о проекте
4. **Фильтрация по типу документации** — улучшает релевантность ответов
5. **Системный промпт** — определяет роль и доступные ресурсы
6. **Чат с памятью** — обеспечивает контекстуальные ответы
