# Анализ GitHub репозитория Koog

## 📚 Репозиторий

**URL:** https://github.com/JetBrains/koog
**Ветка:** `develop` (основная)
**Лицензия:** Apache-2.0
**Звёзд:** 3.5k

## 🔍 Структура репозитория

### Основные модули:

1. **koog-agents** - основной модуль для создания агентов
2. **koog-ktor** - интеграция с Ktor
3. **koog-spring-boot-starter** - интеграция с Spring Boot
4. **examples** - примеры использования
5. **prompt** - работа с промптами
6. **tools** - инструменты
7. **rag** - RAG функциональность
8. **embeddings** - эмбеддинги

### Важные файлы:

- `README.md` - основная документация
- `TESTING.md` - информация о тестировании
- `build.gradle.kts` - конфигурация сборки

## 📝 Ключевая информация из README

### Quickstart пример:

```kotlin
fun main() = runBlocking {
    // Перед запуском установите API ключ в переменную окружения
    val apiKey = System.getenv("OPENAI_API_KEY") // или Anthropic, Google, OpenRouter и др.

    val agent = AIAgent(
        executor = simpleOpenAIExecutor(apiKey), // или Anthropic, Google, OpenRouter и др.
        systemPrompt = "You are a helpful assistant. Answer user questions concisely.",
        llmModel = OpenAIModels.Chat.GPT4o
    )

    val result = agent.run("Hello! How can you help me?")
    println(result)
}
```

### Зависимость:

```kotlin
dependencies {
    implementation("ai.koog:koog-agents:0.5.4")
}

repositories {
    mavenCentral()
}
```

## 🎯 Ключевые находки

### 1. Создание агента

```kotlin
val agent = AIAgent(
    executor = simpleOpenAIExecutor(apiKey),
    systemPrompt = "Системный промпт",
    llmModel = OpenAIModels.Chat.GPT4o
)
```

### 2. Запуск агента

```kotlin
val result = agent.run("Вопрос пользователя")
```

### 3. Поддержка LLM провайдеров:

- OpenAI
- Anthropic
- Google
- DeepSeek
- OpenRouter
- Ollama
- Bedrock

### 4. Требования:

- JDK 17 или выше
- kotlinx-coroutines 1.10.2
- kotlinx-serialization 1.8.1

## 📦 Примеры в репозитории

Структура папки `examples/`:
- Различные примеры использования Koog
- Примеры с инструментами
- Примеры интеграции с Ktor/Spring Boot

## 🔗 Полезные ссылки

- **Репозиторий:** https://github.com/JetBrains/koog
- **Документация:** https://docs.koog.ai/
- **Slack канал:** (указан в README)
- **YouTrack:** для багов и feature requests

## ⚠️ Важные замечания

1. **Репозиторий активно развивается** - 692 коммита, 98 контрибьюторов
2. **Последний релиз:** 0.5.4 (Dec 3, 2025)
3. **Проект в разработке** - могут быть изменения API

## 📝 Что нужно изучить дальше

1. Конкретные примеры из папки `examples/`
2. Использование `@Tool` аннотаций
3. Интеграция с Ktor
4. Работа с промптами и prompt executor

## 💡 Выводы для проекта

1. ✅ Зависимость правильная: `ai.koog:koog-agents:0.5.4`
2. ✅ API простой: `AIAgent` с `executor`, `systemPrompt`, `llmModel`
3. ✅ Запуск простой: `agent.run("запрос")`
4. ✅ Много примеров в репозитории для изучения

---

**Статус:** Репозиторий изучен, основная информация собрана. Готов к изучению конкретных примеров кода.

