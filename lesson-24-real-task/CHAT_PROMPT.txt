# Краткий промпт для реализации: 🔥 День 24. Реальная задача

**Полный промпт:** См. `PROMPT.md` в корне проекта

---

Ты — разработчик, создающий урок «🔥 День 24. Реальная задача» в модуле `lesson-24-real-task`. Проект использует стек **Kotlin + Ktor + Koog + SQLite** с фронтендом на **vanilla JS**. Проект создаётся **с нуля**, используя наработанные знания из предыдущих уроков, но всё строится на **Koog фреймворке**.

## Цель

Реализовать **анализатор отзывов с недельным сравнением**:
- Сбор отзывов с Company Mobile Stores API
- Классификация отзывов через LLM (Koog)
- Сравнение текущей недели с предыдущей
- Выявление улучшений и ухудшений
- Генерация отчёта через LLM
- Отправка отчёта в Telegram через MCP
- Хранение истории в локальной БД (SQLite)

## Ключевые технологии

- **Koog** — фреймворк для создания AI-агента с инструментами (@Tool)
  - Документация: https://docs.koog.ai/
  - Использовать `@Tool` аннотации для создания инструментов
  - Использовать `koog.ask()` для запросов к LLM
  - Встроенная поддержка MCP
- **MCP** — для отправки отчётов в Telegram
- **LLM через Koog** — для анализа и генерации отчётов
- **SQLite** — локальная БД для хранения истории

## API для получения отзывов

**Endpoint:** `GET /api/v1/stores/{store}/apps/{package_id}/reviews`

**Параметры:**
- `store`: `appstore`, `googleplay`, `appgallery`, `rustore`
- `package_id`: bundle id (iOS) или package name (Android)

**Нужные поля:** `id`, `text`, `rating` (остальные игнорируем)

**Конфигурация:**
```yaml
reviews:
  api:
    baseUrl: "https://stores.mobile.company-team.ru"
    store: "googleplay"
    packageId: "com.example.app"
```

## Поэтапная реализация (9 коммитов)

**КРИТИЧЕСКИ ВАЖНО:** Каждый шаг = отдельный git-коммит. После каждого шага **СТОП**, покажи изменения пользователю и дождись «ок, продолжай».

### Коммит 1: Базовая структура проекта на Koog
- Создать структуру проекта с нуля
- Настроить зависимости (Ktor, Koog, SQLite)
- Создать базовый Ktor сервер
- Создать конфигурацию `config/server.yaml`
- Создать базовый HTML

### Коммит 2: Модели данных и Koog агент
- Создать модели: `Review`, `ReviewAnalysis`, `WeekStats`, `WeekComparison`
- Создать `ReviewsAnalyzerAgent.kt` с базовой структурой
- Создать Config классы для парсинга YAML

### Коммит 3: HTTP клиент и Koog инструмент для сбора отзывов
- Создать `ReviewsApiClient.kt` для работы с API
- Добавить Koog инструмент `@Tool("Сбор отзывов за период")`
- Обработка пагинации
- Извлечение только `id`, `text`, `rating`

### Коммит 4: LLM анализ через Koog
- Добавить Koog инструмент `@Tool("Классификация отзывов через LLM")`
- Добавить Koog инструмент `@Tool("Сравнение недель через LLM")`
- Использовать `koog.ask()` для запросов к LLM
- Парсинг JSON ответов

### Коммит 5: Локальная БД (SQLite) и Koog инструменты
- Создать схему БД (Exposed)
- Создать `ReviewsRepository.kt`
- Добавить Koog инструменты для работы с БД:
  - `@Tool("Сохранение отзывов в локальную БД")`
  - `@Tool("Получение отзывов за неделю из БД")`
  - `@Tool("Сохранение анализа недели в БД")`
  - `@Tool("Получение анализа предыдущей недели из БД")`

### Коммит 6: Генерация отчёта через Koog и MCP для Telegram
- Добавить Koog инструмент `@Tool("Генерация Markdown отчёта")`
- Настроить MCP для Telegram (использовать существующий или создать)
- Добавить Koog инструмент `@Tool("Отправка отчёта в Telegram через MCP")`
- Создать `TelegramMCPClient.kt`

### Коммит 7: Оркестрация анализа и API endpoints
- Добавить метод `analyzeWeek()` в `ReviewsAnalyzerAgent`
- Оркестрировать весь процесс через Koog инструменты
- Создать DTO (`AnalyzeReviewsRequest`, `AnalysisResponse`)
- Создать `ReviewsController.kt` с endpoints:
  - `POST /api/reviews/analyze`
  - `GET /api/reviews/week/{weekStart}`

### Коммит 8: UI для анализа отзывов
- Создать `client/reviews-analysis.html`
- Создать `client/reviews-analysis.js`
- Форма для запуска анализа
- Отображение результатов (статистика, сравнение, отчёт)
- История анализов

### Коммит 9: Улучшения и документация
- Обработка ошибок
- Валидация данных
- Обновить `README.md`, `THEORY.md`, `CHAT_PROMPT.txt`
- Создать `TASK_DESCRIPTION.md`
- Обновить корневой `README.md`

## Важно

- Проект создаётся **с нуля**, не копируем предыдущие уроки
- Всё строится на **Koog фреймворке**
- Используем `@Tool` аннотации для инструментов
- Используем `koog.ask()` для запросов к LLM
- Используем MCP для Telegram
- После каждого коммита останавливаться и показывать изменения
- Пользователь сам делает merge/commit

## Детали реализации

См. полный промпт в `PROMPT.md` для:
- Детального описания каждого коммита
- Примеров кода
- Структуры данных
- Технических деталей
- Примеров API запросов
