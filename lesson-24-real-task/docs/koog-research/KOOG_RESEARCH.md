# Полное исследование Koog Framework

## 📚 Источники исследования

1. ✅ **Официальная документация:** https://docs.koog.ai/
2. ✅ **GitHub репозиторий:** https://github.com/JetBrains/koog
3. ✅ **Ветка:** `develop`
4. ✅ **README.md:** изучен (Quickstart пример найден)
5. ✅ **Папка docs:** проверена (содержит файлы для генерации сайта документации)

## ✅ Основная информация

**Koog** — официальный Kotlin-фреймворк от JetBrains для создания предсказуемых, отказоустойчивых и готовых к использованию в корпоративной среде AI-агентов.

### Ключевые характеристики

- **Мультиплатформенность:** JVM, JS, WasmJS, Android, iOS (Kotlin Multiplatform)
- **Лицензия:** Apache-2.0
- **Звёзд на GitHub:** 3.5k+
- **Контрибьюторов:** 98
- **Коммитов:** 692+
- **Последний релиз:** 0.5.4 (Dec 3, 2025)
- **Активное развитие:** постоянные обновления

## 🔧 Зависимости (добавлены в проект)

### Базовая зависимость

```kotlin
dependencies {
    implementation("ai.koog:koog-agents:0.5.4")
}

repositories {
    mavenCentral()
}
```

### Дополнительные зависимости (опционально)

```kotlin
// Для Ktor интеграции
implementation("ai.koog:koog-ktor:0.5.4")

// Для Spring Boot
implementation("ai.koog:koog-spring-boot-starter:0.5.4")
```

### Системные требования

- **JDK:** 17 или выше (обязательно)
- **Kotlin Coroutines:** 1.10.2
- **Kotlin Serialization:** 1.8.1
- **API-ключ LLM:** OpenAI, Anthropic, Google и др. (не требуется для Ollama)

## 🎯 Quickstart пример (из README GitHub)

```kotlin
fun main() = runBlocking {
    // Перед запуском установите API ключ в переменную окружения
    val apiKey = System.getenv("OPENAI_API_KEY") // или Anthropic, Google, OpenRouter и др.

    val agent = AIAgent(
        executor = simpleOpenAIExecutor(apiKey), // или Anthropic, Google, OpenRouter и др.
        systemPrompt = "You are a helpful assistant. Answer user questions concisely.",
        llmModel = OpenAIModels.Chat.GPT4o
    )

    val result = agent.run("Hello! How can you help me?")
    println(result)
}
```

### Ключевые компоненты:

1. **AIAgent** - основной класс агента
2. **simpleOpenAIExecutor(apiKey)** - создание prompt executor для OpenAI
3. **systemPrompt** - системный промпт для агента
4. **llmModel** - модель LLM (`OpenAIModels.Chat.GPT4o`)
5. **agent.run("запрос")** - метод запуска агента

## 🏗️ Структура репозитория

### Основные модули:

1. **koog-agents/** - основной модуль для создания агентов
2. **koog-ktor/** - интеграция с Ktor
3. **koog-spring-boot-starter/** - интеграция с Spring Boot
4. **examples/** - примеры использования
5. **prompt/** - работа с промптами
6. **tools/** - инструменты
7. **rag/** - RAG функциональность
8. **embeddings/** - эмбеддинги
9. **docs/** - файлы для генерации документации на docs.koog.ai

### Важные файлы:

- `README.md` - основная документация с Quickstart примером
- `TESTING.md` - информация о тестировании
- `build.gradle.kts` - конфигурация сборки

## 🎨 Типы агентов

### 1. Базовые агенты (Basic Agents)
- Обрабатывают один ввод и предоставляют ответ
- Простая структура для простых задач

### 2. Функциональные агенты (Functional Agents)
- Легковесные агенты с пользовательской логикой
- Идеальны для задач с инструментами
- Пользовательская логика на чистом Kotlin

### 3. Агенты с комплексными рабочими процессами
- Для сложных задач с графовыми стратегиями
- Настраиваемые workflow
- Параллельное выполнение узлов

## 🔧 Основные возможности

### Промпты (Prompts)

- Создание и выполнение промптов через LLM-клиентов
- Переключение между различными LLM провайдерами без потери истории
- Обработка сбоев с встроенными повторными попытками
- Управление историей разговоров

### Инструменты (Tools)

**Типы инструментов:**

1. **Встроенные инструменты** - готовые инструменты от Koog
2. **Аннотационные инструменты** - через `@Tool` аннотации
3. **Классовые инструменты** - через классы

**Пример аннотационного инструмента:**

```kotlin
@Tool("Описание инструмента для LLM")
fun myTool(@Param("paramName") param: String): ReturnType {
    // Реализация инструмента
    // Может получать доступ к внешним системам и API
}
```

### Стратегии (Strategies)

- Графовые рабочие процессы
- Параллельное выполнение узлов
- Передача данных между узлами
- Настраиваемые компоненты

### Дополнительные возможности

- **Сжатие истории** - оптимизация использования токенов в длительных разговорах
- **Сохранение состояния** - восстановление состояния агента в определенные моменты
- **Структурированный вывод** - генерация ответов в JSON и других форматах
- **Streaming API** - обработка ответов в реальном времени
- **Знания и память** - векторные эмбеддинги, RAG, ранжированное хранение документов
- **Трассировка** - отладка и мониторинг выполнения агента
- **События** - мониторинг жизненного цикла агента

## 🔗 Интеграции

### Поддерживаемые LLM провайдеры:

- OpenAI ✅
- Anthropic
- Google
- DeepSeek
- OpenRouter
- Ollama (локально)
- Bedrock

### Интеграция с фреймворками:

#### Ktor

Koog предоставляет плагин для интеграции с Ktor:
- Идиоматичная интеграция Kotlin/Ktor
- Настройка LLM-провайдеров через конфигурационные файлы
- Вызов агентов прямо из маршрутов

#### Spring Boot

Koog предоставляет стартер для Spring Boot:
- Автонастройка LLM-клиентов
- Готовые бины для внедрения зависимостей
- Минимальная настройка

### Протоколы:

- **Model Context Protocol (MCP)** - стандартизированный протокол для инструментов
- **OpenTelemetry** - мониторинг и отладка
  - Langfuse Exporter
  - Weave Exporter

## 📝 Рекомендации для проекта Reviews Analyzer

### Структура ReviewsAnalyzerAgent

```kotlin
class ReviewsAnalyzerAgent(
    private val promptExecutor: PromptExecutor,
    private val apiClient: ReviewsApiClient,
    private val repository: ReviewsRepository,
    private val config: ReviewsConfig
) {
    // Создание агента
    private val agent = AIAgent(
        executor = promptExecutor,
        systemPrompt = "Ты анализируешь отзывы приложения. " +
                      "Классифицируй отзывы, выявляй проблемы, сравнивай недели.",
        llmModel = OpenAIModels.Chat.GPT4oMini
    )
    
    // Инструмент: Сбор отзывов
    @Tool("Сбор отзывов за период с сервера Company Mobile Stores")
    fun fetchReviews(
        @Param("fromDate") fromDate: String,
        @Param("toDate") toDate: String
    ): List<Review> {
        return apiClient.fetchReviews(
            store = config.api.store,
            packageId = config.api.packageId,
            fromDate = fromDate,
            toDate = toDate
        )
    }
    
    // Инструмент: Классификация через LLM
    @Tool("Классификация отзывов через LLM")
    suspend fun classifyReviews(
        @Param("reviews") reviews: List<Review>
    ): List<ReviewAnalysis> {
        val prompt = createClassificationPrompt(reviews)
        val response = agent.run(prompt)
        return parseAnalysisResponse(response)
    }
    
    // Запуск анализа недели
    suspend fun analyzeWeek(): AnalysisResponse {
        val result = agent.run("Проанализируй отзывы за неделю...")
        // Обработка и возврат результата
        return parseResult(result)
    }
}
```

## 🔗 Полезные ссылки

### Документация:
- **Главная:** https://docs.koog.ai/
- **Getting Started:** https://docs.koog.ai/getting-started/
- **Functional Agents:** https://docs.koog.ai/agent-types/functional-agents/
- **Annotation-based Tools:** https://docs.koog.ai/tools/annotation-based-tools/
- **Prompts:** https://docs.koog.ai/prompts/
- **Ktor Plugin:** https://docs.koog.ai/ktor-plugin/
- **Spring Boot:** https://docs.koog.ai/spring-boot/
- **Examples:** https://docs.koog.ai/examples/overview/
- **API Reference:** https://docs.koog.ai/api-reference/

### GitHub:
- **Репозиторий:** https://github.com/JetBrains/koog
- **Ветка develop:** https://github.com/JetBrains/koog/tree/develop
- **Issues:** для багов и feature requests
- **Pull Requests:** для контрибьютов

## ✅ Статус исследования

- ✅ Зависимость добавлена: `ai.koog:koog-agents:0.5.4`
- ✅ Репозиторий Maven Central настроен
- ✅ Официальная документация изучена
- ✅ GitHub репозиторий изучен
- ✅ README.md изучен (Quickstart пример найден)
- ✅ Структура репозитория понятна
- ✅ Основные концепции освоены
- ✅ Примеры использования найдены
- ✅ Готов к реализации проекта

## 📌 Ключевые выводы для проекта

1. ✅ **Зависимость добавлена** и готова к использованию
2. ✅ **API простой и понятный:**
   - `AIAgent(executor, systemPrompt, llmModel)` - создание агента
   - `agent.run("запрос")` - запуск агента
   - `simpleOpenAIExecutor(apiKey)` - создание executor
3. ✅ **Инструменты создаются через @Tool аннотации:**
   ```kotlin
   @Tool("Описание")
   fun tool(@Param("param") param: String): ReturnType
   ```
4. ✅ **LLM вызовы** через prompt executor и agent.run()
5. ✅ **Примеры кода** найдены в README и документации

## 🎯 Готовность к реализации

Все необходимые данные для начала реализации проекта собраны:

- ✅ Зависимости настроены
- ✅ Примеры кода изучены
- ✅ Структура агента понятна
- ✅ Концепции освоены
- ✅ Документация изучена

**Готов к началу реализации коммита 2: Модели данных и Koog агент.**

---

*Документация создана на основе:*
- *Официальной документации https://docs.koog.ai/*
- *GitHub репозитория https://github.com/JetBrains/koog*
- *README.md из репозитория (Quickstart пример)*
- *Структуры репозитория и модулей*
