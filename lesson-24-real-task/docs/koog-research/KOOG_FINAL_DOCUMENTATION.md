# Финальная документация по Koog Framework

## 📚 Источники исследования

1. **Официальная документация:** https://docs.koog.ai/ ✅
2. **GitHub репозиторий:** https://github.com/JetBrains/koog ✅
3. **Ветка:** `develop` ✅
4. **README.md:** изучен ✅

## ✅ Основная информация

**Koog** — официальный Kotlin-фреймворк от JetBrains для создания предсказуемых, отказоустойчивых и готовых к использованию в корпоративной среде AI-агентов.

### Ключевые характеристики

- **Мультиплатформенность:** JVM, JS, WasmJS, Android, iOS
- **Лицензия:** Apache-2.0
- **Звёзд на GitHub:** 3.5k+
- **Контрибьюторов:** 98
- **Последний релиз:** 0.5.4 (Dec 3, 2025)

## 🔧 Зависимости (добавлены в проект)

```kotlin
dependencies {
    implementation("ai.koog:koog-agents:0.5.4")
}

repositories {
    mavenCentral()
}
```

### Требования

- JDK 17 или выше
- kotlinx-coroutines 1.10.2
- kotlinx-serialization 1.8.1

## 🎯 Quickstart пример (из README)

```kotlin
fun main() = runBlocking {
    val apiKey = System.getenv("OPENAI_API_KEY")
    
    val agent = AIAgent(
        executor = simpleOpenAIExecutor(apiKey),
        systemPrompt = "You are a helpful assistant. Answer user questions concisely.",
        llmModel = OpenAIModels.Chat.GPT4o
    )
    
    val result = agent.run("Hello! How can you help me?")
    println(result)
}
```

### Ключевые компоненты:

- **AIAgent** - основной класс
- **simpleOpenAIExecutor()** - создание executor для OpenAI
- **OpenAIModels.Chat.GPT4o** - выбор модели
- **agent.run()** - запуск агента

## 🏗️ Структура репозитория

### Основные модули:

- `koog-agents/` - основной модуль
- `koog-ktor/` - интеграция с Ktor
- `koog-spring-boot-starter/` - интеграция с Spring Boot
- `examples/` - примеры использования
- `prompt/` - работа с промптами
- `tools/` - инструменты
- `rag/` - RAG функциональность
- `embeddings/` - эмбеддинги
- `docs/` - файлы для генерации документации

## 🎨 Типы агентов

1. **Базовые агенты** - простые, один ввод → один вывод
2. **Функциональные агенты** - легковесные с пользовательской логикой
3. **Агенты с комплексными workflows** - графовые стратегии

## 🔧 Основные возможности

### Инструменты (@Tool)

```kotlin
@Tool("Описание для LLM")
fun myTool(@Param("param") param: String): ReturnType {
    // Реализация
}
```

### Промпты
- Создание и выполнение через LLM
- Переключение между провайдерами
- Обработка ошибок с retry

### Дополнительно
- Сжатие истории
- Сохранение состояния
- Streaming API
- RAG и эмбеддинги

## 🔗 Поддерживаемые LLM провайдеры

- OpenAI ✅
- Anthropic
- Google
- DeepSeek
- OpenRouter
- Ollama
- Bedrock

## 📝 Рекомендации для проекта

### Структура ReviewsAnalyzerAgent

```kotlin
class ReviewsAnalyzerAgent(
    private val promptExecutor: PromptExecutor,
    private val apiClient: ReviewsApiClient,
    private val repository: ReviewsRepository
) {
    private val agent = AIAgent(
        executor = promptExecutor,
        systemPrompt = "Ты анализируешь отзывы...",
        llmModel = OpenAIModels.Chat.GPT4oMini
    )
    
    @Tool("Сбор отзывов")
    fun fetchReviews(...): List<Review> { }
    
    suspend fun analyzeWeek(): AnalysisResponse {
        return agent.run("Проанализируй отзывы...")
    }
}
```

## ✅ Статус

- ✅ Зависимость добавлена
- ✅ Концепции изучены
- ✅ Примеры найдены
- ✅ Готов к реализации

---

**Все необходимое для начала реализации проекта собрано!**

