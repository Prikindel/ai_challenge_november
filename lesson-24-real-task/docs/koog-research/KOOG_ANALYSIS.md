# Анализ Koog Framework для проекта lesson-24-real-task

## ✅ Выполнено

1. **Зависимость добавлена** в `server/build.gradle.kts`:
   ```kotlin
   implementation("ai.koog:koog-agents:0.5.4")
   ```

2. **Репозиторий проверен** - `mavenCentral()` уже присутствует в build.gradle.kts

## 🔍 Что нужно изучить далее

### 1. Структура API Koog

Нужно понять:
- Как создавать агентов (BasicAgent, FunctionalAgent)
- Как использовать `@Tool` аннотации
- Как вызывать LLM (метод `ask()` или другой)
- Как конфигурировать LLM провайдеров (OpenAI)

### 2. Интеграция с Ktor

Koog имеет плагин для Ktor (`koog-ktor`), нужно:
- Изучить как устанавливать плагин
- Понять как вызывать агентов из Ktor маршрутов
- Настроить конфигурацию через файлы или код

### 3. Инструменты (@Tool)

Из промпта понятно, что нужно:
- Использовать аннотации `@Tool("Описание")`
- Параметры через `@Param("name")`
- Но нужны конкретные примеры синтаксиса

### 4. Примеры кода

Нужно найти:
- Минимальный пример создания агента
- Пример использования @Tool
- Пример вызова LLM через агента
- Пример интеграции с Ktor

## 📚 Следующие шаги исследования

1. Изучить страницу "Getting started" полностью - найти примеры создания агента
2. Изучить "Annotation-based tools" - понять синтаксис @Tool
3. Изучить "Ktor Plugin" - понять интеграцию
4. Найти примеры кода в документации
5. Проверить GitHub репозиторий Koog для примеров

## 💡 Важные находки

1. **Версия библиотеки:** 0.5.4 (предоставлена пользователем)
2. **Координаты:** `ai.koog:koog-agents:0.5.4`
3. **Репозиторий:** mavenCentral() (возможно нужен дополнительный Grazi)
4. **Требования:** Java 17+
5. **Поддержка:** Kotlin Multiplatform, Ktor, Spring Boot

## 📝 Заметки для реализации

Из промпта понятно, что проект должен использовать:
- `@Tool` аннотации для создания инструментов
- `koog.ask()` для запросов к LLM (нужно проверить точный синтаксис)
- Интеграцию с MCP для Telegram

Структура агента из промпта:
```kotlin
class ReviewsAnalyzerAgent(
    private val koog: Koog,
    private val apiClient: ReviewsApiClient,
    private val repository: ReviewsRepository
) {
    @Tool("Описание инструмента")
    fun toolName(@Param("param") param: String): ReturnType {
        // Реализация
    }
    
    fun analyze() {
        val result = koog.ask("Промпт для LLM")
    }
}
```

Нужно проверить, соответствует ли это реальному API Koog.

