# 🔥 День 24. Реальная задача

Реализация реальной практической задачи с использованием всех накопленных знаний (RAG, MCP, LLM). Выберите одну из предложенных задач или придумайте свою.

## Демонстрация

> 📹 Видео появится после записи (формат: Видео + Код).

**Важно:** В документации должна быть указана выбранная задача.

## 📋 Описание

В этом уроке мы реализуем **реальную задачу**, применяя все изученные технологии:
- **RAG** — для поиска информации в документации
- **MCP** — для взаимодействия с внешними сервисами
- **LLM** — для анализа результатов и генерации рекомендаций

### Варианты задач

#### 1. Автоматизация деплоя на хостинг (Рекомендуется)
Создать систему автоматического деплоя приложения на хостинг (VPS, Heroku, Vercel, Railway).

**Возможности:**
- Автоматический деплой при коммите
- Управление окружениями (dev, staging, production)
- Откат к предыдущей версии
- Мониторинг статуса деплоя

#### 2. Автоматизация публикации в App Store / Google Play
Создать систему автоматической публикации приложения в сторы.

**Возможности:**
- Автоматическая сборка приложения
- Загрузка в сторы
- Управление версиями
- Генерация release notes

#### 3. Автоматизация CI/CD пайплайна
Создать систему управления CI/CD пайплайном.

**Возможности:**
- Создание и настройка пайплайнов
- Запуск тестов и сборок
- Управление окружениями
- Отчёты о результатах

#### 4. Автоматизация мониторинга и алертинга
Создать систему мониторинга приложения с умными алертами.

**Возможности:**
- Мониторинг метрик (CPU, память, запросы)
- Умные алерты на основе анализа
- Автоматическое масштабирование
- Анализ логов

#### 5. Автоматизация управления доменами и SSL
Создать систему управления доменами и SSL сертификатами.

**Возможности:**
- Автоматическое обновление DNS записей
- Управление SSL сертификатами
- Мониторинг истечения
- Автоматическое продление

#### 6. Автоматизация резервного копирования
Создать систему автоматического резервного копирования.

**Возможности:**
- Автоматическое создание бэкапов
- Управление расписанием
- Восстановление из бэкапа
- Мониторинг успешности

## 🎯 Пайплайн реальной задачи

```
Запрос на выполнение задачи
  ↓
Поиск документации через RAG (rag_search_project_docs):
  - Документация по задаче
  - Best practices
  - Примеры использования
  ↓
Выполнение задачи через MCP:
  - Взаимодействие с внешним API
  - Выполнение команд
  - Получение результатов
  ↓
Анализ результатов через LLM (LLMService):
  - Анализ успешности
  - Выявление проблем
  - Генерация рекомендаций
  ↓
Возврат результата: статус, результаты, рекомендации, логи
```

## 🏗️ Архитектура

```
lesson-24-real-task/
├── config/server.yaml
├── data/knowledge_base.db
├── documents/
├── project/
│   └── docs/              # Документация по выбранной задаче
├── {task}-mcp-server/     # MCP сервер для выбранной задачи
│   └── src/main/kotlin/com/prike/{task}mcpserver/
│       ├── Main.kt
│       ├── model/
│       ├── storage/
│       ├── server/
│       └── tools/
├── git-mcp-server/
├── rag-mcp-server/
├── server/
│   └── src/main/kotlin/com/prike/
│       ├── data/
│       │   └── client/
│       │       └── {Task}MCPClient.kt
│       ├── domain/
│       │   ├── model/
│       │   │   └── {Task}Models.kt
│       │   └── service/
│       │       ├── {Task}Service.kt
│       │       └── {Task}PromptBuilder.kt
│       └── presentation/
│           ├── controller/
│           │   └── {Task}Controller.kt
│           └── dto/
│               └── {Task}Dtos.kt
└── client/
    ├── {task}.html
    └── {task}.js
```

## 🚀 Быстрый старт

### Предварительные требования

- Java 17+
- Gradle 8+
- Ollama (для эмбеддингов)
- OpenRouter API ключ (для LLM)
- Доступ к внешним сервисам (в зависимости от выбранной задачи)

### Установка и запуск

1. **Клонируйте репозиторий и перейдите в директорию урока:**
   ```bash
   cd lesson-24-real-task
   ```

2. **Настройте переменные окружения:**
   ```bash
   cp example.env .env
   # Отредактируйте .env и укажите необходимые ключи
   ```

3. **Запустите Ollama:**
   ```bash
   ollama serve
   ollama pull nomic-embed-text
   ```

4. **Соберите и запустите сервер:**
   ```bash
   cd server
   ./gradlew build
   ./gradlew run
   ```

5. **Откройте браузер:**
   ```
   http://localhost:8080
   ```

6. **Индексируйте документацию:**
   - Перейдите на страницу "Индексация"
   - Нажмите "Индексировать документацию"

7. **Используйте систему:**
   - Перейдите на страницу выбранной задачи
   - Выполните задачу через UI

## 📖 Использование

### Выбор задачи

1. Изучите предложенные варианты задач
2. Выберите задачу, которая вам интересна
3. Опишите задачу в `TASK_DESCRIPTION.md`
4. Реализуйте решение

### Выполнение задачи

1. Откройте страницу выбранной задачи
2. Заполните форму с параметрами
3. Нажмите "Выполнить"
4. Просмотрите результаты и рекомендации

## 🔧 API Endpoints

API endpoints зависят от выбранной задачи. Пример для деплоя:

### POST /api/deployment/deploy

Выполнить деплой приложения.

**Запрос:**
```json
{
  "environment": "production",
  "version": "1.0.0",
  "config": {
    "host": "example.com",
    "port": 8080
  }
}
```

**Ответ:**
```json
{
  "deploymentId": "deploy-123",
  "status": "SUCCESS",
  "logs": ["Building...", "Deploying...", "Done"],
  "recommendations": [
    "Рекомендуется проверить метрики после деплоя"
  ]
}
```

### GET /api/deployment/{deploymentId}

Получить статус деплоя.

**Ответ:**
```json
{
  "deploymentId": "deploy-123",
  "status": "SUCCESS",
  "environment": "production",
  "version": "1.0.0",
  "createdAt": 1234567890
}
```

## 📝 Конфигурация

### Настройка внешних сервисов

В `config/server.yaml` настройте интеграции:

```yaml
{task}:
  mcp:
    enabled: true
    jarPath: "{task}-mcp-server/build/libs/{task}-mcp-server-1.0.0.jar"
  api:
    endpoint: "https://api.example.com"
    apiKey: "${TASK_API_KEY}"
```

### Переменные окружения

В `.env` укажите необходимые ключи:

```env
OPENAI_API_KEY=your_key_here
TASK_API_KEY=your_task_key_here
```

## 📚 Дополнительные материалы

- [TASK_DESCRIPTION.md](./TASK_DESCRIPTION.md) — описание выбранной задачи
- [THEORY.md](./THEORY.md) — теория автоматизации реальных задач
- [PROMPT.md](./PROMPT.md) — промпт для реализации
- [CHAT_PROMPT.txt](./CHAT_PROMPT.txt) — промпт для чата

## ✅ Минимальные требования для зачёта

- ✅ Выбрана реальная задача (указана в документации)
- ✅ Реализован MCP сервер для задачи
- ✅ Используется RAG для документации
- ✅ Используется LLM для анализа
- ✅ Создан API для управления задачей
- ✅ Создан UI для взаимодействия
- ✅ Пайплайн реализован (может не работать из-за ключей)
- ✅ Видео демонстрация работы системы

## 🎓 Что мы изучили

1. **Применение знаний на практике** — реализация реальной задачи
2. **Интеграция с внешними сервисами** — через MCP
3. **Использование RAG** — для поиска документации
4. **Использование LLM** — для анализа и рекомендаций
5. **Автоматизация процессов** — выполнение задач без ручного вмешательства
6. **Обработка ошибок** — надёжность системы
7. **Мониторинг и логирование** — отслеживание работы

## 💡 Советы

1. **Выберите задачу, которая вам интересна** — мотивация важна
2. **Начните с простого** — можно усложнить позже
3. **Используйте существующие знания** — не изобретайте велосипед
4. **Документируйте процесс** — поможет в будущем
5. **Тестируйте тщательно** — особенно обработку ошибок

## 📝 Примечание

**Важно:** Пайплайн может не работать полностью из-за отсутствия реальных ключей API. Это нормально — главное показать структуру и логику работы системы. В видео можно продемонстрировать работу с мок-данными или показать процесс без реальных вызовов API.

