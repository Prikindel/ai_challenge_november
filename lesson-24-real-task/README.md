# 🔥 День 24. Реальная задача - Анализатор отзывов мобильных приложений

Диалоговый AI-ассистент для анализа отзывов мобильных приложений с использованием RAG (Retrieval-Augmented Generation), MCP (Model Context Protocol) и интеграции с LLM.

## 📹 Демо видео

[![Демо видео](https://img.shields.io/badge/Видео-Демо-blue)](https://disk.yandex.ru/i/veV3uBKH3VXAig)

Смотрите демо видео: [https://disk.yandex.ru/i/veV3uBKH3VXAig](https://disk.yandex.ru/i/veV3uBKH3VXAig)

## 📋 Описание

Этот проект реализует интеллектуальную систему анализа отзывов, которая:

- **Получает отзывы** из магазинов мобильных приложений через API
- **Анализирует отзывы** с помощью LLM для извлечения саммари, категорий, тем и критичности
- **Сохраняет саммари** в базе данных (не сырые отзывы) для эффективного поиска
- **Использует RAG** для поиска по проанализированным саммари и предоставления контекстных ответов
- **Предоставляет диалоговый интерфейс** где пользователи могут задавать вопросы на естественном языке
- **Генерирует планы действий** для критических проблем пользователей на основе анализа отзывов
- **Отправляет отчеты** в Telegram через интеграцию MCP

### Основные возможности

- 🤖 **Диалоговый AI-агент** - Взаимодействие на естественном языке с использованием фреймворка Koog
- 🔍 **RAG поиск** - Векторный поиск по саммари отзывов с использованием локальных эмбеддингов Ollama
- 📊 **Пакетный анализ** - Обрабатывает отзывы по дням с автоматическим батчингом
- 💾 **Умное хранение** - Сохраняет только саммари и анализ, а не сырые отзывы
- 📱 **Интеграция с Telegram** - Отправка отчетов анализа через MCP
- 🎯 **Классификация тем** - Автоматическая категоризация тем отзывов
- ⚠️ **Оценка критичности** - Выявление проблем высокого приоритета

## 🏗️ Архитектура

```
lesson-24-real-task/
├── config/
│   └── server.yaml          # Конфигурация сервера
├── client/                  # Frontend (HTML, CSS, JavaScript)
│   ├── index.html          # Главная страница
│   ├── chat.html           # Интерфейс чата
│   ├── reviews-analysis.html # Страница пакетного анализа
│   └── styles.css          # Стили
├── server/                  # Backend (Kotlin, Ktor)
│   └── src/main/kotlin/com/prike/
│       ├── domain/
│       │   ├── model/      # Модели данных
│       │   ├── service/    # Бизнес-логика (RAG, LLM, Chat)
│       │   └── tools/      # Инструменты Koog агента
│       ├── data/
│       │   ├── client/     # API клиенты (Telegram MCP)
│       │   └── repository/ # Доступ к базе данных
│       └── presentation/
│           └── controller/ # REST API endpoints
└── data/
    └── reviews.db          # SQLite база данных
```

## 🚀 Быстрый старт

### Предварительные требования

- Java 17+
- Gradle 8+
- Ollama (для локальных эмбеддингов)
- OpenRouter API ключ (для LLM) или OpenAI API ключ
- OAuth токен для API отзывов (опционально)

### Установка

1. **Клонируйте репозиторий и перейдите в директорию урока:**
   ```bash
   cd lesson-24-real-task
   ```

2. **Настройте переменные окружения:**
   ```bash
   cp example.env .env
   # Отредактируйте .env и добавьте ваши API ключи
   ```

3. **Запустите Ollama:**
   ```bash
   ollama serve
   ollama pull nomic-embed-text
   ```

4. **Соберите и запустите сервер:**
   ```bash
   cd server
   ./gradlew build
   ./gradlew run
   ```

5. **Откройте приложение:**
   ```
   http://localhost:8080
   ```

## 📖 Использование

### Интерфейс чата

1. Откройте страницу чата
2. Задавайте вопросы на естественном языке:
   - "Получи 10 отзывов, проанализируй их и дай краткую сводку"
   - "Сравни отзывы за последний месяц по неделям. Что улучшилось, а что ухудшилось?"
   - "Составь план по критическим проблемам пользователей"
   - "Отправь отчет в Telegram"

### Пакетный анализ

1. Перейдите на страницу "Анализ отзывов"
2. Выберите диапазон дат
3. Нажмите "Анализировать" для обработки всех отзывов за период
4. Отзывы анализируются по дням и сохраняются в базе данных

### Индексация RAG

После анализа отзывов проиндексируйте их для RAG поиска:
- Используйте API endpoint: `POST /api/reviews/index-all-rag`
- Или запустите автоматически во время пакетного анализа

## 🔧 API Endpoints

### Чат

- `POST /api/chat/sessions` - Создать новую сессию чата
- `GET /api/chat/sessions` - Список всех сессий
- `GET /api/chat/sessions/{sessionId}/messages` - Получить историю чата
- `POST /api/chat/sessions/{sessionId}/messages` - Отправить сообщение

### Отзывы

- `POST /api/reviews/analyze-batch` - Анализ отзывов за диапазон дат
- `POST /api/reviews/index-all-rag` - Индексация всех саммари для RAG

## 📝 Конфигурация

### Конфигурация сервера (`config/server.yaml`)

```yaml
server:
  port: 8080
  host: "0.0.0.0"

reviews:
  api:
    baseUrl: "https://stores.mobile.example.com"
    store: "googleplay"
    packageId: "com.example.app"
    oauthToken: "${REVIEWS_API_OAUTH_TOKEN}"

koog:
  enabled: true
  model: "gpt-4o-mini"
  apiKey: "${OPENAI_API_KEY}"
  useOpenRouter: true

ollama:
  baseUrl: "http://localhost:11434"
  model: "nomic-embed-text"
  timeout: 120000

database:
  path: "data/reviews.db"
```

### Переменные окружения (`.env`)

```env
OPENAI_API_KEY=your_key_here
REVIEWS_API_OAUTH_TOKEN=your_token_here
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```

## 🎯 Используемые технологии

- **Kotlin** - Язык backend
- **Ktor** - Web фреймворк
- **Exposed** - SQL фреймворк
- **SQLite** - База данных
- **Koog** - Фреймворк AI агентов
- **Ollama** - Генерация локальных эмбеддингов
- **OpenRouter/OpenAI** - LLM API
- **MCP** - Model Context Protocol для интеграции с Telegram
- **RAG** - Retrieval-Augmented Generation для контекстного поиска

## 📚 Структура проекта

- **Модели домена**: Review, ReviewSummary, ReviewTopic, ReviewCategory, Criticality
- **Сервисы**: ReviewsChatService, ReviewSummaryRagService, EmbeddingService, KoogAgentService
- **Инструменты**: ReviewsTools (доступны Koog агенту)
- **Контроллеры**: ChatController, ReviewsController, ClientController

## ✅ Реализованные возможности

- ✅ Диалоговый AI-агент с пониманием естественного языка
- ✅ RAG система для поиска по саммари отзывов
- ✅ Пакетный анализ отзывов с обработкой по дням
- ✅ Автоматическая классификация тем и оценка критичности
- ✅ Интеграция с Telegram через MCP
- ✅ Web интерфейс со страницами чата и анализа
- ✅ Векторный поиск с использованием локальных эмбеддингов Ollama
- ✅ База данных SQLite с режимом WAL для параллельного доступа

## 🎓 Что мы изучили

1. **Оркестрация AI агентов** - Использование фреймворка Koog для инструментальных AI агентов
2. **Реализация RAG** - Векторный поиск и извлечение контекста
3. **Интеграция MCP** - Подключение внешних сервисов через Model Context Protocol
4. **Пакетная обработка** - Эффективная обработка больших наборов данных
5. **Обработка ошибок** - Надежная обработка ошибок и механизмы отката
6. **Оптимизация базы данных** - Режим WAL и управление транзакциями
7. **Разработка Frontend** - Создание интерактивных web интерфейсов
