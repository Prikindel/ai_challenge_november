# 🔥 День 10. Подключение MCP

Модуль для подключения к MCP (Model Context Protocol) серверу и получения списка доступных инструментов и ресурсов.

## 📋 Описание модуля

Этот модуль демонстрирует работу с **Model Context Protocol (MCP)** — открытым протоколом от Anthropic для стандартизации взаимодействия больших языковых моделей (LLM) с внешними источниками данных и инструментами.

### Основные возможности:

- ✅ Подключение к MCP серверу через stdio транспорт
- ✅ Получение списка доступных инструментов (tools)
- ✅ Получение списка доступных ресурсов (resources)
- ✅ Визуализация подключения и списка инструментов через веб-интерфейс
- ✅ Настройка нескольких MCP серверов через YAML конфигурацию

## 🧠 Теория

### Что такое MCP (Model Context Protocol)?

**MCP (Model Context Protocol)** — это открытый протокол от Anthropic, разработанный для стандартизации взаимодействия больших языковых моделей (LLM) с внешними источниками данных и инструментами. MCP позволяет LLM получать доступ к инструментам (tools), ресурсам (resources) и промптам (prompts) через единый интерфейс.

### Основные концепции:

#### 1. **MCP Server** 
Сервер, который предоставляет инструменты, ресурсы и промпты. Сервер может быть:
- **Локальным** — запускается как отдельный процесс (stdio транспорт)
- **Удалённым** — доступен через HTTP/WebSocket (SSE/WebSocket транспорт)

#### 2. **MCP Client**
Клиент, который подключается к серверу и использует предоставленные возможности. В нашем случае — это наше приложение на Kotlin.

#### 3. **Транспорты (Transports)**
Способы связи между клиентом и сервером:
- **stdio** — стандартный ввод/вывод (для локальных серверов)
- **SSE (Server-Sent Events)** — для HTTP соединений
- **WebSocket** — для двусторонней связи

#### 4. **Инструменты (Tools)**
Функции, которые может вызывать LLM через MCP. Примеры:
- Чтение файла
- Выполнение SQL запроса
- Работа с Git
- Получение прогноза погоды

#### 5. **Ресурсы (Resources)**
Данные, к которым может получить доступ LLM. Примеры:
- Содержимое файла
- Данные из БД
- Конфигурационные файлы

#### 6. **Промпты (Prompts)**
Шаблоны промптов, которые может использовать LLM для стандартизированных запросов.

### Зачем нужен MCP?

- **Стандартизация** — единый протокол для всех инструментов
- **Безопасность** — контроль доступа к инструментам и данным
- **Расширяемость** — легко добавлять новые инструменты
- **Разделение ответственности** — LLM не знает, как работает инструмент, только как его вызвать

### Архитектура MCP:

```
┌─────────────┐         ┌─────────────┐
│ MCP Client  │ ◄─────► │ MCP Server  │
│ (наше       │         │ (инструменты│
│  приложение)│         │  и ресурсы)  │
└─────────────┘         └─────────────┘
      │                        │
      │                        │
      ▼                        ▼
┌─────────────┐         ┌─────────────┐
│   LLM API   │         │  Инструменты │
│  (OpenAI)   │         │  (файлы, БД) │
└─────────────┘         └─────────────┘
```

### Основные методы протокола:

- `initialize` — инициализация соединения
- `tools/list` — получение списка доступных инструментов
- `tools/call` — вызов инструмента
- `resources/list` — получение списка ресурсов
- `resources/read` — чтение ресурса

## 🚀 Быстрый старт

### Предварительные требования

- Java 17 или выше
- Gradle (включён в проект через wrapper)
- MCP сервер (опционально, для тестирования)

### Запуск проекта

1. **Перейдите в директорию сервера:**
   ```bash
   cd lesson-10-mcp-connection/server
   ```

2. **Соберите проект:**
   ```bash
   ./gradlew clean build
   ```

3. **Запустите сервер:**
   ```bash
   ./gradlew run
   ```

4. **Откройте браузер:**
   ```
   http://localhost:8080
   ```

### Настройка MCP серверов

1. **Отредактируйте конфигурацию:**
   ```bash
   nano config/mcp.yaml
   ```

2. **Включите нужный сервер:**
   ```yaml
   - name: "weather-server"
     type: "stdio"
     enabled: true  # Измените на true
     command: "java"
     args: ["-jar", "mcp-servers/weather-stdio-server.jar"]
   ```

3. **Подготовьте MCP сервер (если нужно):**
   - См. раздел "Подготовка Weather MCP Server" ниже

## 📁 Структура проекта

```
lesson-10-mcp-connection/
├── server/                          # Kotlin + Ktor сервер
│   └── src/main/kotlin/com/prike/
│       ├── Main.kt                  # Точка входа, настройка Ktor
│       ├── Config.kt                # Загрузка конфигурации
│       │
│       ├── config/                  # Конфигурация
│       │   └── MCPConfig.kt         # Загрузка конфигурации MCP из YAML
│       │
│       ├── domain/                  # Доменный слой
│       │   ├── agent/
│       │   │   └── MCPConnectionAgent.kt  # Агент для подключения к MCP
│       │   └── exception/
│       │       └── MCPException.kt        # Исключения для MCP
│       │
│       ├── data/                    # Слой данных
│       │   ├── client/
│       │   │   └── MCPClient.kt     # Обёртка над MCP SDK Client
│       │   ├── repository/
│       │   │   └── MCPRepository.kt  # Репозиторий для работы с MCP
│       │   └── dto/
│       │       └── MCPModels.kt      # DTO для работы с MCP
│       │
│       ├── presentation/            # Слой презентации
│       │   ├── controller/
│       │   │   └── MCPController.kt # HTTP контроллер для MCP API
│       │   └── dto/
│       │       └── MCPDtos.kt       # DTO для HTTP запросов/ответов
│       │
│       └── di/                      # Dependency Injection
│           └── AppModule.kt         # Создание и связывание зависимостей
│
├── client/                          # Веб-клиент
│   ├── index.html                   # Главная страница
│   ├── style.css                    # Стили
│   └── app.js                       # JavaScript логика
│
├── config/                          # Конфигурационные файлы
│   └── mcp.yaml                     # Конфигурация MCP серверов
│
└── README.md                        # Документация
```

## ⚙️ Конфигурация

### config/mcp.yaml

Файл конфигурации для настройки MCP серверов:

```yaml
mcp:
  servers:
    - name: "weather-server"
      type: "stdio"
      enabled: true
      command: "java"
      args: ["-jar", "mcp-servers/weather-stdio-server.jar"]
      description: "MCP сервер для получения прогноза погоды"
  
  default:
    timeout: 30
    retryAttempts: 3
```

### Параметры конфигурации:

- **name** — уникальное имя сервера
- **type** — тип транспорта (`stdio`, `sse`, `websocket`)
- **enabled** — включён ли сервер
- **command** — команда для запуска (для stdio)
- **args** — аргументы команды (для stdio)
- **url** — URL сервера (для SSE/WebSocket)
- **description** — описание сервера

## 🔌 API Endpoints

### GET /api/mcp/servers

Получить список доступных MCP серверов.

**Ответ:**
```json
{
  "servers": [
    {
      "name": "weather-server",
      "type": "stdio",
      "enabled": true,
      "description": "MCP сервер для получения прогноза погоды"
    }
  ]
}
```

### POST /api/mcp/connect

Подключиться к MCP серверу.

**Запрос:**
```json
{
  "serverName": "weather-server"
}
```

**Ответ:**
```json
{
  "success": true,
  "serverName": "weather-server",
  "serverDescription": "MCP сервер для получения прогноза погоды",
  "tools": [
    {
      "name": "get_weather",
      "description": "Получить прогноз погоды для указанного города"
    }
  ],
  "resources": []
}
```

### POST /api/mcp/disconnect

Отключиться от текущего MCP сервера.

**Ответ:**
```json
{
  "success": true,
  "message": "Disconnected"
}
```

## 📝 Примеры использования

### Пример 1: Подключение к Weather Server

1. **Подготовьте Weather MCP Server:**
   ```bash
   # Клонируйте репозиторий kotlin-sdk
   git clone https://github.com/modelcontextprotocol/kotlin-sdk.git
   cd kotlin-sdk/samples/weather-stdio-server
   
   # Соберите проект
   ./gradlew build
   
   # Скопируйте JAR в директорию урока
   mkdir -p ../../../lesson-10-mcp-connection/mcp-servers
   cp build/libs/weather-stdio-server-*.jar ../../../lesson-10-mcp-connection/mcp-servers/weather-stdio-server.jar
   ```

2. **Настройте конфигурацию:**
   ```yaml
   - name: "weather-server"
     type: "stdio"
     enabled: true
     command: "java"
     args: ["-jar", "mcp-servers/weather-stdio-server.jar"]
   ```

3. **Запустите сервер и откройте веб-интерфейс:**
   - Выберите "weather-server" из списка
   - Нажмите "Подключиться"
   - Просмотрите список доступных инструментов

### Пример 2: Создание собственного MCP сервера

Вы можете создать собственный MCP сервер на Kotlin, используя официальный SDK:

1. Добавьте зависимость в ваш проект:
   ```kotlin
   implementation("io.modelcontextprotocol:kotlin-sdk:0.7.7")
   ```

2. Создайте сервер, реализующий интерфейсы MCP SDK

3. Добавьте сервер в конфигурацию `config/mcp.yaml`

## 🛠 Подготовка Weather MCP Server

**Weather MCP Server** — это официальный пример MCP сервера из репозитория [kotlin-sdk](https://github.com/modelcontextprotocol/kotlin-sdk/tree/main/samples/weather-stdio-server). Он предоставляет инструменты для получения прогноза погоды.

### Инструкция по сборке:

1. **Клонируйте репозиторий kotlin-sdk:**
   ```bash
   git clone https://github.com/modelcontextprotocol/kotlin-sdk.git
   cd kotlin-sdk/samples/weather-stdio-server
   ```

2. **Соберите проект:**
   ```bash
   ./gradlew build
   ```

3. **Найдите JAR файл:**
   ```bash
   ls build/libs/weather-stdio-server-*.jar
   ```

4. **Создайте директорию для MCP серверов и скопируйте JAR:**
   ```bash
   mkdir -p lesson-10-mcp-connection/mcp-servers
   cp build/libs/weather-stdio-server-*.jar lesson-10-mcp-connection/mcp-servers/weather-stdio-server.jar
   ```

5. **Обновите путь в `config/mcp.yaml`:**
   ```yaml
   - name: "weather-server"
     type: "stdio"
     enabled: true
     command: "java"
     args: ["-jar", "mcp-servers/weather-stdio-server.jar"]
   ```

**Важно:** 
- Убедитесь, что путь к JAR файлу указан относительно директории урока или абсолютный
- Проверьте, что Java установлена и доступна в PATH
- Weather server может требовать API ключ для погодного сервиса (проверьте документацию примера)

### Инструменты, которые предоставляет weather-server:

- `get_weather` — получение прогноза погоды для указанного города
- `get_weather_alerts` — получение предупреждений о погоде

## 🎯 Выводы

### Что получилось:

✅ **Подключение к MCP серверу** через официальный Kotlin SDK  
✅ **Выбор между разными MCP серверами** через YAML конфигурацию и UI  
✅ **Получение списка инструментов** от выбранного сервера  
✅ **Получение списка ресурсов** от выбранного сервера  
✅ **UI для визуализации** инструментов и ресурсов с информацией о сервере  
✅ **Полная документация** с примерами использования  

### Что можно улучшить:

- 🔄 Поддержка SSE и WebSocket транспортов
- 🔄 Вызов инструментов через UI
- 🔄 Чтение ресурсов через UI
- 🔄 Управление несколькими подключениями одновременно
- 🔄 Кэширование списка инструментов и ресурсов
- 🔄 Автоматическое переподключение при обрыве связи

## 📚 Дополнительные ресурсы

- [Официальный репозиторий MCP](https://github.com/modelcontextprotocol)
- [Kotlin SDK для MCP](https://github.com/modelcontextprotocol/kotlin-sdk)
- [Документация MCP](https://modelcontextprotocol.io)

## 🔧 Технологии

- **Backend:** Kotlin + Ktor + Clean Architecture
- **Frontend:** HTML + CSS + Vanilla JavaScript
- **MCP SDK:** официальный Kotlin SDK от Model Context Protocol (`io.modelcontextprotocol:kotlin-sdk:0.7.7`)

