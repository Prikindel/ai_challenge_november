# Краткий промпт для реализации: 🔥 День 19. Мини-чат с памятью на RAG

Ты — разработчик, создающий урок «🔥 День 19. Мини-чат с памятью на RAG» в модуле `lesson-19-rag-chat`. Проект использует стек **Kotlin + Ktor + Clean Architecture + SQLite** с фронтендом на **vanilla JS**.

## Цель

Реализовать простой чат-бот, который:
- **Хранит историю диалога** (все сообщения пользователя и ответы бота)
- **При каждом новом вопросе ищет контекст в базе документов** через RAG
- **Возвращает ответ с учётом найденной информации** и истории диалога
- **Обязательно выводит источники**, откуда был взят ответ (из урока 18)

## Ключевые требования

1. **Копирование урока 18** — полностью скопировать код из `lesson-18-rag-citations/` в `lesson-19-rag-chat/`, заменив все упоминания 18 на 19
2. **Модель истории диалога** — создать модели `ChatMessage`, `ChatSession` и репозиторий `ChatRepository`
3. **ChatService** — создать сервис для интеграции RAG с историей диалога
4. **API для чата** — endpoints для создания сессий, отправки сообщений, получения истории
5. **UI для чата** — веб-интерфейс с отображением истории и цитат
6. **Управление сессиями** — создание, переключение, удаление сессий
7. **Оптимизация промпта** — управление историей (sliding window, token limit)
8. **Документация** — обновить README, THEORY, добавить ссылку в корневой README

## Поэтапная реализация (отдельные коммиты)

**КРИТИЧЕСКИ ВАЖНО:** Каждый шаг = отдельный git-коммит. После каждого шага **СТОП**, покажи изменения пользователю и дождись «ок, продолжай».

### Коммит 1: Копирование урока 18
- Скопировать весь код из `lesson-18-rag-citations/` в `lesson-19-rag-chat/`
- Заменить все упоминания `lesson-18-rag-citations` → `lesson-19-rag-chat`
- Заменить «День 18» → «День 19»
- Убедиться, что проект собирается и запускается

### Коммит 2: Модель истории диалога и репозиторий
- Создать модели `ChatMessage`, `ChatSession`
- Создать `ChatRepository` для работы с SQLite
- Создать таблицы `chat_sessions` и `chat_messages`
- Методы: `saveMessage()`, `getHistory()`, `createSession()`, `getSession()`

### Коммит 3: ChatService — интеграция RAG с историей
- Создать `ChatService` для объединения RAG-поиска с историей диалога
- Метод `processMessage()`: получить историю → RAG-поиск → формирование промпта → запрос к LLM → парсинг цитат → сохранение в историю
- Обновить `PromptBuilder` — добавить метод `buildChatPrompt()` для формирования промпта с историей

### Коммит 4: API для чата
- Создать `ChatController` с endpoints:
  - `POST /api/chat/sessions` — создать сессию
  - `POST /api/chat/sessions/{sessionId}/messages` — отправить сообщение
  - `GET /api/chat/sessions/{sessionId}/messages` — получить историю
  - `GET /api/chat/sessions` — получить список сессий
  - `DELETE /api/chat/sessions/{sessionId}` — удалить сессию
- Создать DTO: `CreateSessionRequest`, `SendMessageRequest`, `ChatHistoryResponse`

### Коммит 5: UI для чата (веб-интерфейс)
- Создать `chat.html` — страница чата с историей сообщений
- Создать `chat.js` — логика отправки сообщений, отображения истории, парсинга цитат
- Обновить `style.css` — стили для чата (пузыри сообщений, цитаты)
- Интеграция с API endpoints
- Отображение цитат с кликабельными ссылками (использовать функциональность из урока 18)

### Коммит 6: Управление сессиями в UI
- Обновить `chat.html` — добавить боковую панель со списком сессий
- Обновить `chat.js` — загрузка списка сессий, создание новой сессии, переключение между сессиями
- Сохранение текущей сессии в localStorage

### Коммит 7: Оптимизация промпта с историей
- Создать `ChatPromptBuilder` для управления историей
- Реализовать стратегии: sliding window, token limit, summary (опционально)
- Конфигурация в `config/server.yaml`: `chat.history.maxMessages`, `chat.history.maxTokens`

### Коммит 8: Документация и выводы
- Обновить `README.md`, `THEORY.md`, `PROMPT.md`, `CHAT_PROMPT.txt`
- Добавить раздел «Демонстрация» с ссылкой на видео
- Описать результаты и примеры использования
- Обновить корневой `README.md`, добавив ссылку на урок 19

## Технические детали

### Хранение истории
- SQLite таблицы: `chat_sessions`, `chat_messages`
- Цитаты хранятся в JSON формате
- Индексы для быстрого поиска по sessionId и timestamp

### Интеграция RAG
- RAG-поиск выполняется при каждом новом сообщении
- Использовать существующий `RAGService` из урока 18
- Использовать существующий `CitationParser` из урока 18

### Формирование промпта
- Системное сообщение
- История диалога (последние N сообщений)
- Контекст из RAG
- Текущий вопрос
- Требование обязательных цитат (из урока 18)

### Управление контекстом
- Sliding window: последние N сообщений
- Token limit: ограничение по токенам
- Summary: суммаризация старых сообщений (опционально)

## Важно

- Не ломать существующую функциональность из урока 18
- Использовать существующие компоненты: `RAGService`, `CitationParser`, просмотр документов
- После каждого коммита останавливаться и показывать изменения
- Пользователь сам делает merge/commit
- Все изменения должны быть протестированы

