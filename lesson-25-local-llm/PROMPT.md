# Промпт для реализации: 🔥 День 25. Запустить локальную модель

Ты — разработчик, создающий урок «🔥 День 25. Запустить локальную модель» в модуле `lesson-25-local-llm`. Проект использует стек **Kotlin + Ktor + SQLite** с фронтендом на **vanilla JS**. В качестве базы можно использовать один из прошлых уроков с простым чатом (например, lesson-19-rag-chat).

## 🎯 Цель урока

Установить и запустить локальную LLM модель (Ollama, LM Studio, LLaMA, Mistral, Phi, Qwen и т.п.), проверить доступность через CLI/API и интегрировать её в существующий чат вместо внешнего API.

### Ключевая идея

```
Локальная LLM → Установка и запуск → Проверка через CLI/API → Интеграция в чат → Тестирование
```

Система должна:
- Установить локальную LLM (Ollama или другую)
- Проверить доступность через CLI
- Проверить доступность через API
- Интегрировать в существующий чат
- Протестировать работу с вопросами

---

## 📋 Поэтапная реализация (отдельные коммиты)

**КРИТИЧЕСКИ ВАЖНО:**
- Каждый перечисленный шаг = отдельный git-коммит.
- После каждого шага **СТОП**, покажи изменения пользователю и дождись «ок, продолжай».
- Пользователь сам делает merge/commit далее.

### Коммит 1: Выбор базового урока и копирование

**Задача:** выбрать базовый урок с чатом и скопировать его структуру.

**Действия:**
1. Выбрать базовый урок (рекомендуется `lesson-19-rag-chat` или `lesson-01-simple-chat-agent`)
2. Скопировать структуру проекта в `lesson-25-local-llm/`
3. Обновить названия:
   - Папка проекта, Gradle settings, package references
   - Все текстовые упоминания старого урока → `lesson-25-local-llm`
   - «День X» → «День 25»
4. Убедиться, что проект собирается и запускается

**После коммита:** остановиться, показать структуру проекта.

---

### Коммит 2: Установка и настройка локальной LLM

**Задача:** установить локальную LLM и проверить её работу.

**Варианты установки:**

**⚠️ ВАЖНО:** Ollama используется и для эмбеддингов (как в предыдущих уроках с `nomic-embed-text` для RAG), и для LLM моделей. В этом уроке мы используем Ollama для запуска **LLM моделей для генерации текста** (llama3.2, mistral и т.д.), которые заменяют внешний API (OpenRouter), а не для эмбеддингов.

**Разница:**
- **Эмбеддинги** (`nomic-embed-text`) — для векторизации текста, используются в RAG (предыдущие уроки)
- **LLM модели** (`llama3.2`, `mistral` и т.д.) — для генерации текста, используются в этом уроке

#### Вариант 1: Ollama (Рекомендуется)

**Примечание:** Ollama используется в предыдущих уроках для эмбеддингов, но также поддерживает полноценные LLM модели для генерации текста. В этом уроке используем Ollama именно для LLM (генерации ответов), а не для эмбеддингов.

**Установка Ollama:**
```bash
# macOS
brew install ollama

# Linux
curl -fsSL https://ollama.com/install.sh | sh

# Windows
# Скачать установщик с https://ollama.com/download
```

**Запуск Ollama:**
```bash
ollama serve
```

**Установка LLM модели (не эмбеддингов!):**
```bash
# Популярные LLM модели для генерации текста:
ollama pull llama3.2        # LLaMA 3.2 (3B параметров, быстрая)
ollama pull mistral         # Mistral 7B
ollama pull phi3            # Phi-3 (3.8B, быстрая)
ollama pull qwen2.5         # Qwen 2.5 (7B)
ollama pull gemma2          # Gemma 2 (2B, очень быстрая)

# НЕ используем модели для эмбеддингов (nomic-embed-text и т.п.)
```

**Проверка через CLI:**
```bash
ollama run llama3.2 "Привет! Как дела?"
```

**Проверка через API (генерация текста):**
```bash
curl http://localhost:11434/api/generate -d '{
  "model": "llama3.2",
  "prompt": "Привет! Как дела?",
  "stream": false
}'
```

**Важно:** Используем `/api/generate` для генерации текста, а не `/api/embeddings` (который используется для эмбеддингов в предыдущих уроках).

#### Вариант 2: LM Studio

**Установка:**
- Скачать с https://lmstudio.ai/
- Установить и запустить
- Загрузить модель через интерфейс

**Проверка через API:**
```bash
curl http://localhost:1234/v1/chat/completions -H "Content-Type: application/json" -d '{
  "model": "local-model",
  "messages": [{"role": "user", "content": "Привет!"}]
}'
```

#### Вариант 3: LLaMA.cpp

**Установка:**
```bash
git clone https://github.com/ggerganov/llama.cpp
cd llama.cpp
make
```

**Запуск:**
```bash
./server -m models/llama-3.2-3b-instruct.gguf
```

**Действия:**
1. Выбрать один из вариантов (рекомендуется Ollama)
2. Установить и запустить локальную LLM
3. Проверить работу через CLI
4. Проверить работу через API
5. Создать файл `LOCAL_LLM_SETUP.md` с инструкциями:
   - Какую модель установили
   - Как запустить
   - Как проверить через CLI
   - Как проверить через API
   - Примеры запросов

**Важно:**
- Документировать все шаги установки
- Убедиться, что модель отвечает на вопросы
- Записать URL и порт API

**После коммита:** остановиться.

---

### Коммит 3: Создание клиента для локальной LLM

**Задача:** создать HTTP клиент для работы с локальной LLM API.

**Компоненты:**
1. **Модели данных** (`domain/model/LocalLLMModels.kt`):
   ```kotlin
   data class LocalLLMRequest(
       val model: String,
       val prompt: String,
       val stream: Boolean = false
   )
   
   data class LocalLLMResponse(
       val response: String,
       val model: String,
       val done: Boolean
   )
   
   // Для Ollama API
   data class OllamaGenerateRequest(
       val model: String,
       val prompt: String,
       val stream: Boolean = false
   )
   
   data class OllamaGenerateResponse(
       val response: String,
       val done: Boolean,
       val model: String
   )
   ```

2. **HTTP клиент** (`data/client/LocalLLMClient.kt`):
   ```kotlin
   class LocalLLMClient(
       private val baseUrl: String = "http://localhost:11434",  // Ollama по умолчанию
       private val httpClient: HttpClient
   ) {
       suspend fun generate(
           model: String,
           prompt: String,
           stream: Boolean = false
       ): String {
           // Реализация запроса к локальной LLM API
       }
   }
   ```

3. **Конфигурация** (`config/server.yaml`):
   ```yaml
   localLLM:
     enabled: true
     provider: "ollama"  # ollama, lmstudio, llamacpp
     baseUrl: "http://localhost:11434"
     model: "llama3.2"
     apiPath: "/api/generate"  # для Ollama
   ```

4. **Config класс** (`config/Config.kt`):
   - Парсинг конфигурации локальной LLM
   - `LocalLLMConfig` с полями: `enabled`, `provider`, `baseUrl`, `model`, `apiPath`

**Важно:**
- Поддержка разных провайдеров (Ollama, LM Studio, LLaMA.cpp)
- Обработка ошибок подключения
- Таймауты для запросов

**После коммита:** остановиться.

---

### Коммит 4: Интеграция локальной LLM в LLMService

**Задача:** интегрировать локальную LLM в существующий LLMService.

**Компоненты:**
1. **Обновить LLMService** (`domain/service/LLMService.kt`):
   ```kotlin
   class LLMService(
       private val localLLMClient: LocalLLMClient?,
       private val openRouterClient: OpenRouterClient?,
       private val config: LLMConfig
   ) {
       suspend fun generateResponse(
           messages: List<ChatMessage>,
           temperature: Double = 0.7
       ): String {
           // Если локальная LLM включена, использовать её
           if (config.localLLM?.enabled == true && localLLMClient != null) {
               val prompt = buildPrompt(messages)
               return localLLMClient.generate(
                   model = config.localLLM.model,
                   prompt = prompt
               )
           }
           
           // Иначе использовать OpenRouter
           return openRouterClient?.generateResponse(messages, temperature) ?: ""
       }
       
       private fun buildPrompt(messages: List<ChatMessage>): String {
           // Преобразование списка сообщений в промпт
           return messages.joinToString("\n") { "${it.role}: ${it.content}" }
       }
   }
   ```

2. **Обновить конфигурацию** (`config/server.yaml`):
   ```yaml
   llm:
     provider: "local"  # local или openrouter
     local:
       enabled: true
       baseUrl: "http://localhost:11434"
       model: "llama3.2"
     openrouter:
       enabled: false
       apiKey: "${OPENAI_API_KEY}"
   ```

3. **Обновить Main.kt**:
   - Создать `LocalLLMClient` если локальная LLM включена
   - Передать в `LLMService`
   - Обработка ошибок подключения

**Важно:**
- Поддержка переключения между локальной и внешней LLM
- Fallback на внешнюю LLM при ошибках
- Логирование использования локальной LLM

**После коммита:** остановиться.

---

### Коммит 5: Тестирование и проверка работы

**Задача:** протестировать работу локальной LLM в чате.

**Действия:**
1. Запустить локальную LLM (Ollama или другую)
2. Запустить сервер приложения
3. Протестировать через UI:
   - Открыть чат
   - Задать вопрос
   - Проверить ответ от локальной LLM
4. Протестировать через API:
   ```bash
   curl -X POST http://localhost:8080/api/chat/message \
     -H "Content-Type: application/json" \
     -d '{
       "sessionId": "test-session",
       "message": "Привет! Как дела?"
     }'
   ```
5. Проверить логи:
   - Подключение к локальной LLM
   - Время ответа
   - Качество ответов

6. Создать файл `TESTING.md`:
   - Результаты тестирования
   - Примеры вопросов и ответов
   - Сравнение с внешней LLM (если есть)
   - Проблемы и решения

**Важно:**
- Убедиться, что локальная LLM отвечает
- Проверить качество ответов
- Документировать результаты

**После коммита:** остановиться.

---

### Коммит 6: UI для переключения между LLM

**Задача:** добавить UI для переключения между локальной и внешней LLM.

**Компоненты:**
1. **Обновить HTML** (`client/index.html` или `client/chat.html`):
   - Добавить переключатель LLM:
     ```html
     <div class="llm-selector">
       <label>
         <input type="radio" name="llm" value="local" checked> Локальная LLM
       </label>
       <label>
         <input type="radio" name="llm" value="openrouter"> OpenRouter
       </label>
     </div>
     ```

2. **Обновить JavaScript**:
   - Обработка переключения LLM
   - Отправка выбора на сервер
   - Индикация текущей LLM

3. **API endpoint** (`presentation/controller/LLMController.kt`):
   - `POST /api/llm/switch` — переключение LLM
   - `GET /api/llm/status` — статус локальной LLM

**Важно:**
- Визуальная индикация текущей LLM
- Сохранение выбора в сессии
- Обработка ошибок подключения

**После коммит:** остановиться.

---

### Коммит 7: Улучшения и документация

**Задача:** улучшить функциональность и обновить документацию.

**Действия:**
1. **Улучшения:**
   - Обработка ошибок подключения к локальной LLM
   - Автоматическое переключение на внешнюю LLM при недоступности локальной
   - Кэширование статуса локальной LLM
   - Метрики производительности (время ответа)

2. **Документация:**
   - Обновить `README.md`:
     - Инструкции по установке локальной LLM
     - Настройка конфигурации
     - Примеры использования
   - Обновить `THEORY.md`:
     - Теория локальных LLM
     - Сравнение с облачными решениями
     - Преимущества и недостатки
   - Обновить `PROMPT.md` (уже обновлён)
   - Обновить `CHAT_PROMPT.txt`
   - Обновить корневой `README.md`

3. **Примеры:**
   - Примеры запросов к локальной LLM
   - Примеры конфигурации для разных провайдеров
   - Скриншоты работы

**Важно:**
- Полная документация по установке и настройке
- Примеры для разных провайдеров
- Результаты тестирования

**После коммита:** показать изменения пользователю.

---

## 🧠 Технические детали

### Поддержка разных провайдеров

#### Ollama API

**Endpoint:** `POST http://localhost:11434/api/generate`

**Запрос:**
```json
{
  "model": "llama3.2",
  "prompt": "Привет! Как дела?",
  "stream": false
}
```

**Ответ:**
```json
{
  "response": "Привет! У меня всё отлично, спасибо!",
  "done": true,
  "model": "llama3.2"
}
```

#### LM Studio API

**Endpoint:** `POST http://localhost:1234/v1/chat/completions`

**Запрос:**
```json
{
  "model": "local-model",
  "messages": [
    {"role": "user", "content": "Привет! Как дела?"}
  ]
}
```

**Ответ:**
```json
{
  "choices": [
    {
      "message": {
        "content": "Привет! У меня всё отлично!"
      }
    }
  ]
}
```

#### LLaMA.cpp Server API

**Endpoint:** `POST http://localhost:8080/completion`

**Запрос:**
```json
{
  "prompt": "Привет! Как дела?",
  "n_predict": 100
}
```

### Конфигурация для разных провайдеров

```yaml
# Ollama
localLLM:
  provider: "ollama"
  baseUrl: "http://localhost:11434"
  model: "llama3.2"
  apiPath: "/api/generate"

# LM Studio
localLLM:
  provider: "lmstudio"
  baseUrl: "http://localhost:1234"
  model: "local-model"
  apiPath: "/v1/chat/completions"

# LLaMA.cpp
localLLM:
  provider: "llamacpp"
  baseUrl: "http://localhost:8080"
  model: "llama-3.2-3b-instruct"
  apiPath: "/completion"
```

---

## 📊 Примеры использования

### Проверка через CLI (Ollama)

```bash
# Запуск Ollama
ollama serve

# Установка модели
ollama pull llama3.2

# Тест через CLI
ollama run llama3.2 "Привет! Как дела?"

# Тест через API
curl http://localhost:11434/api/generate -d '{
  "model": "llama3.2",
  "prompt": "Привет! Как дела?",
  "stream": false
}'
```

### Интеграция в приложение

1. Запустить локальную LLM
2. Настроить конфигурацию в `config/server.yaml`
3. Запустить сервер приложения
4. Открыть чат в браузере
5. Задать вопрос и получить ответ от локальной LLM

---

## 🎓 Цели урока

- Установить и запустить локальную LLM модель
- Проверить доступность через CLI
- Проверить доступность через API
- Интегрировать локальную LLM в существующий чат
- Протестировать работу с вопросами
- Сравнить с внешней LLM (опционально)
- Получить результат: локальная LLM запущена и отвечает на запросы

---

## 📚 Документация

- `PROMPT.md` — текущее ТЗ (этот файл).
- `LOCAL_LLM_SETUP.md` — инструкции по установке локальной LLM.
- `TESTING.md` — результаты тестирования.
- `THEORY.md` — теория локальных LLM.
- `README.md` — руководство по запуску и использованию урока.
- `CHAT_PROMPT.txt` — краткая версия ТЗ для агентной разработки.

---

## ✅ Требования к результату

1. **Локальная LLM установлена и запущена**
2. **Проверена работа через CLI** (документировано)
3. **Проверена работа через API** (документировано)
4. **Интегрирована в существующий чат**
5. **Протестирована работа с вопросами**
6. **Документация** описывает процесс установки и настройки
7. **Видео демонстрация** показывает работу локальной LLM

