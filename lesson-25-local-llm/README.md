# 🔥 День 25. Запустить локальную модель

Установка и запуск локальной LLM модели (Ollama, LM Studio, LLaMA, Mistral, Phi, Qwen и т.п.), проверка доступности через CLI/API и интеграция в существующий чат.

## Демонстрация

> 📹 Видео появится после записи (формат: Видео + Код).

**Сценарий использования:**
1. Установка локальной LLM (Ollama)
2. Запуск локальной LLM сервера
3. Проверка работы через CLI
4. Проверка работы через API
5. Интеграция в существующий чат
6. Тестирование работы с вопросами
7. Сравнение с внешней LLM (опционально)

## 📋 Описание

В этом уроке мы:
- Выбираем базовый урок с чатом (lesson-20-dev-assistant с RAG и MCP)
- Устанавливаем локальную LLM (Ollama, LM Studio или другую)
- Проверяем работу через CLI и API
- Создаём клиент для локальной LLM (LocalLLMClient)
- Интегрируем локальную LLM в LLMService с автоматическим fallback
- Добавляем UI для отображения статуса LLM провайдера
- Тестируем работу с вопросами и сравниваем с внешней LLM

## 🎯 Пайплайн

```
Установка локальной LLM
  ↓
Запуск локальной LLM сервера
  ↓
Проверка через CLI
  ↓
Проверка через API
  ↓
Создание клиента для локальной LLM
  ↓
Интеграция в LLMService
  ↓
Тестирование в чате
```

## 🚀 Быстрый старт

### Предварительные требования

- Java 17+
- Gradle 8+
- Локальная LLM (Ollama, LM Studio или другая)
- Минимум 8GB RAM (для моделей 3-7B)

### Установка локальной LLM

#### Вариант 1: Ollama (Рекомендуется)

**Установка:**
```bash
# macOS
brew install ollama

# Linux
curl -fsSL https://ollama.com/install.sh | sh

# Windows
# Скачать установщик с https://ollama.com/download
```

**Запуск:**
```bash
ollama serve
```

**Установка LLM модели (не эмбеддинга!):**
```bash
# Популярные LLM модели для генерации текста:
ollama pull llama3.2        # LLaMA 3.2 (3B, быстрая)
ollama pull mistral          # Mistral 7B
ollama pull phi3             # Phi-3 (3.8B, быстрая)
ollama pull qwen2.5          # Qwen 2.5 (7B)
ollama pull gemma2           # Gemma 2 (2B, очень быстрая)

# НЕ путать с эмбеддингами:
# ollama pull nomic-embed-text  # Это для эмбеддингов (RAG), не для этого урока!
```

**Примечание:** В предыдущих уроках Ollama использовался для эмбеддингов (`nomic-embed-text`). В этом уроке мы используем Ollama для запуска **LLM моделей для генерации текста**, которые заменяют внешний API (OpenRouter).

**Проверка:**
```bash
# Через CLI
ollama run llama3.2 "Привет! Как дела?"

# Через API
curl http://localhost:11434/api/generate -d '{
  "model": "llama3.2",
  "prompt": "Привет! Как дела?",
  "stream": false
}'
```

#### Вариант 2: LM Studio

1. Скачать с https://lmstudio.ai/
2. Установить и запустить
3. Загрузить модель через интерфейс
4. Включить локальный сервер (Settings → Local Server)

**Проверка:**
```bash
curl http://localhost:1234/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "local-model",
    "messages": [{"role": "user", "content": "Привет!"}]
  }'
```

### Настройка приложения

1. **Клонируйте репозиторий и перейдите в директорию урока:**
   ```bash
   cd lesson-25-local-llm
   ```

2. **Настройте конфигурацию** (`config/server.yaml`):
   ```yaml
   localLLM:
     enabled: true  # Включить локальную LLM
     provider: "ollama"  # ollama, lmstudio, llamacpp
     baseUrl: "http://localhost:11434"
     model: "llama3.2"  # Модель для генерации текста
     apiPath: "/api/generate"
     timeout: 120000  # Таймаут в миллисекундах
   ```
   
   **Примечание:** Если `localLLM.enabled: false`, будет использоваться OpenRouter (требуется API ключ в `.env`).

3. **Запустите локальную LLM:**
   ```bash
   ollama serve
   ```

4. **Соберите и запустите сервер:**
   ```bash
   cd server
   ./gradlew build
   ./gradlew run
   ```

5. **Откройте браузер:**
   ```
   http://localhost:8080
   ```

6. **Протестируйте чат:**
   - Откройте чат
   - Задайте вопрос
   - Проверьте ответ от локальной LLM

## 📖 Использование

### Проверка локальной LLM

**Через CLI (Ollama):**
```bash
ollama run llama3.2 "Привет! Как дела?"
```

**Через API:**
```bash
curl http://localhost:11434/api/generate -d '{
  "model": "llama3.2",
  "prompt": "Привет! Как дела?",
  "stream": false
}'
```

### Использование в чате

1. Откройте чат в браузере
2. Убедитесь, что выбрана локальная LLM (если есть переключатель)
3. Задайте вопрос
4. Получите ответ от локальной LLM

### Переключение между LLM

Если реализован переключатель:
1. Выберите "Локальная LLM" или "OpenRouter"
2. Задайте вопрос
3. Получите ответ от выбранной LLM

## 🔧 API Endpoints

### GET /api/llm/status

Получение статуса LLM провайдера.

**Ответ:**
```json
{
  "provider": "local (ollama: llama3.2)",
  "localLLM": {
    "enabled": true,
    "provider": "ollama",
    "baseUrl": "http://localhost:11434",
    "model": "llama3.2",
    "available": true
  }
}
```

**Примечание:** Переключение между локальной и внешней LLM выполняется через конфигурацию (`config/server.yaml`). Измените `localLLM.enabled: true/false` и перезапустите сервер.

## 📝 Конфигурация

### Настройка локальной LLM

В `config/server.yaml`:

```yaml
localLLM:
  enabled: true
  provider: "ollama"  # ollama, lmstudio, llamacpp
  baseUrl: "http://localhost:11434"
  model: "llama3.2"
  apiPath: "/api/generate"  # для Ollama

llm:
  provider: "local"  # local или openrouter
  local:
    enabled: true
    baseUrl: "http://localhost:11434"
    model: "llama3.2"
  openrouter:
    enabled: false
    apiKey: "${OPENAI_API_KEY}"
```

### Настройка для разных провайдеров

**Ollama:**
```yaml
localLLM:
  provider: "ollama"
  baseUrl: "http://localhost:11434"
  model: "llama3.2"
  apiPath: "/api/generate"
```

**LM Studio:**
```yaml
localLLM:
  provider: "lmstudio"
  baseUrl: "http://localhost:1234"
  model: "local-model"
  apiPath: "/v1/chat/completions"
```

**LLaMA.cpp:**
```yaml
localLLM:
  provider: "llamacpp"
  baseUrl: "http://localhost:8080"
  model: "llama-3.2-3b-instruct"
  apiPath: "/completion"
```

## 📚 Дополнительные материалы

- [THEORY.md](./THEORY.md) — теория локальных LLM
- [PROMPT.md](./PROMPT.md) — промпт для реализации
- [LOCAL_LLM_SETUP.md](./LOCAL_LLM_SETUP.md) — инструкции по установке
- [TESTING.md](./TESTING.md) — результаты тестирования
- [CHAT_PROMPT.txt](./CHAT_PROMPT.txt) — промпт для чата

## ✅ Минимальные требования для зачёта

- ✅ Локальная LLM установлена и запущена
- ✅ Проверена работа через CLI (документировано)
- ✅ Проверена работа через API (документировано)
- ✅ Интегрирована в существующий чат
- ✅ Протестирована работа с вопросами
- ✅ Документация описывает процесс установки и настройки

## 🎓 Что мы изучили

1. **Установка локальной LLM** — Ollama, установка и настройка моделей для генерации текста (llama3.2)
2. **Проверка работы** — через CLI (`ollama run`) и API (`/api/generate`)
3. **Создание клиента** — `LocalLLMClient` для работы с Ollama API
4. **Интеграция в приложение** — интеграция в `LLMService` с автоматическим fallback на внешнюю LLM
5. **Обработка ошибок** — автоматический fallback при недоступности локальной LLM
6. **UI для статуса** — отображение текущего провайдера LLM в интерфейсе чата
7. **Тестирование** — проверка качества ответов и сравнение с внешней LLM
8. **Разница между LLM и эмбеддингами** — понимание, когда использовать что (LLM для генерации, эмбеддинги для RAG)

## 💡 Советы

1. **Начните с малой модели** — Gemma 2 (2B) или Phi-3 (3.8B) для быстрого старта
2. **Проверьте ресурсы** — убедитесь, что достаточно RAM
3. **Используйте GPU** — если есть, это значительно ускорит работу
4. **Тестируйте качество** — сравните ответы с облачной LLM
5. **Документируйте процесс** — запишите все шаги установки

## 🐛 Решение проблем

### Локальная LLM не запускается

- Проверьте, что порт не занят
- Проверьте логи Ollama/LM Studio
- Убедитесь, что модель установлена

### Медленные ответы

- Используйте меньшую модель
- Используйте GPU вместо CPU
- Уменьшите параметры генерации

### Недостаточно памяти

- Используйте квантованную модель (Q4_0)
- Закройте другие приложения
- Используйте меньшую модель

### Ошибки подключения

- Проверьте, что локальная LLM запущена
- Проверьте URL и порт в конфигурации
- Проверьте firewall настройки

