# Теория: Локальные LLM модели

## 📖 Введение

Локальные LLM (Large Language Models) — это языковые модели, которые работают на вашем компьютере, без необходимости подключения к облачным сервисам. Это позволяет использовать AI без зависимости от внешних API, с полным контролем над данными и конфиденциальностью.

**Ключевые преимущества:**
- **Конфиденциальность** — данные не покидают ваш компьютер
- **Независимость** — не нужен интернет и API ключи
- **Контроль** — полный контроль над моделью и настройками
- **Экономия** — нет платы за использование API
- **Кастомизация** — можно дообучить модель под свои нужды

**Недостатки:**
- **Требования к ресурсам** — нужна мощная видеокарта или CPU
- **Скорость** — может быть медленнее облачных решений
- **Качество** — меньшие модели могут давать менее качественные ответы
- **Установка** — требует настройки и конфигурации

---

## 🎯 Облачные vs Локальные LLM

### Облачные LLM (OpenAI, Anthropic, OpenRouter)

**Преимущества:**
- Высокое качество ответов
- Быстрая скорость генерации
- Не требуют ресурсов компьютера
- Простота использования (только API ключ)

**Недостатки:**
- Зависимость от интернета
- Плата за использование
- Данные отправляются на внешние серверы
- Ограничения по использованию

### Локальные LLM

**Преимущества:**
- Полная конфиденциальность
- Нет платы за использование
- Работает офлайн
- Полный контроль

**Недостатки:**
- Требуют мощное железо
- Могут быть медленнее
- Требуют настройки
- Меньшие модели — меньше качество

---

## 🛠️ Популярные решения для локальных LLM

### 1. Ollama (Рекомендуется)

**Описание:**
Ollama — простой инструмент для запуска LLM локально. Поддерживает множество моделей и прост в использовании.

**Важно:** Ollama используется и для эмбеддингов (например, `nomic-embed-text` для RAG), и для LLM моделей для генерации текста. В этом уроке мы используем Ollama для запуска **LLM моделей** (llama3.2, mistral и т.д.), а не для эмбеддингов.

**Примечание:** В предыдущих уроках Ollama использовался для эмбеддингов (nomic-embed-text), но Ollama также поддерживает полноценные LLM модели для генерации текста. В этом уроке используем Ollama именно для LLM (генерации ответов), а не для эмбеддингов.

**Установка:**
```bash
# macOS
brew install ollama

# Linux
curl -fsSL https://ollama.com/install.sh | sh

# Windows
# Скачать с https://ollama.com/download
```

**Использование:**
```bash
# Запуск сервера
ollama serve

# Установка LLM модели (для генерации текста)
ollama pull llama3.2

# Использование через CLI
ollama run llama3.2 "Привет!"

# API для генерации текста (LLM)
curl http://localhost:11434/api/generate -d '{
  "model": "llama3.2",
  "prompt": "Привет!"
}'

# API для эмбеддингов (используется в предыдущих уроках, НЕ в этом)
# curl http://localhost:11434/api/embeddings -d '{
#   "model": "nomic-embed-text",
#   "prompt": "текст для эмбеддинга"
# }'
```

**Поддерживаемые модели:**
- LLaMA 3.2 (3B, 7B)
- Mistral (7B, 8x7B)
- Phi-3 (3.8B)
- Qwen 2.5 (7B)
- Gemma 2 (2B, 9B)
- И многие другие

**API:**
- Endpoint: `http://localhost:11434/api/generate`
- Формат: JSON
- Поддержка streaming

### 2. LM Studio

**Описание:**
LM Studio — графический интерфейс для работы с локальными LLM. Удобен для начинающих.

**Установка:**
- Скачать с https://lmstudio.ai/
- Установить и запустить
- Загрузить модель через интерфейс

**Использование:**
- Графический интерфейс для чата
- API на `http://localhost:1234`
- Совместим с OpenAI API

**API:**
```bash
curl http://localhost:1234/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "local-model",
    "messages": [{"role": "user", "content": "Привет!"}]
  }'
```

### 3. LLaMA.cpp

**Описание:**
LLaMA.cpp — C++ реализация LLaMA для эффективной работы на CPU.

**Установка:**
```bash
git clone https://github.com/ggerganov/llama.cpp
cd llama.cpp
make
```

**Использование:**
```bash
# Запуск сервера
./server -m models/llama-3.2-3b-instruct.gguf

# API на http://localhost:8080
```

**Особенности:**
- Очень эффективная работа на CPU
- Поддержка GPU (CUDA, Metal)
- Множество оптимизаций

### 4. Mistral AI (локально)

**Описание:**
Mistral предоставляет модели для локального использования через Ollama или собственные инструменты.

**Модели:**
- Mistral 7B
- Mistral 8x7B (MoE)
- Mistral Large

### 5. Phi-3 (Microsoft)

**Описание:**
Phi-3 — компактные модели от Microsoft, оптимизированные для локального использования.

**Модели:**
- Phi-3-mini (3.8B)
- Phi-3-medium (14B)
- Phi-3-small (7B)

---

## 🔧 Технические детали

### Требования к ресурсам

**Минимальные требования:**
- CPU: 4+ ядра
- RAM: 8GB+ (для моделей 3-7B)
- Диск: 5-10GB для модели

**Рекомендуемые требования:**
- CPU: 8+ ядер или GPU с 8GB+ VRAM
- RAM: 16GB+
- Диск: SSD для быстрой загрузки

**Для больших моделей (13B+):**
- GPU: 16GB+ VRAM
- RAM: 32GB+
- Диск: 20GB+

### Форматы моделей

**GGUF (Рекомендуется):**
- Оптимизированный формат для LLaMA.cpp
- Квантование (сжатие) для экономии памяти
- Быстрая загрузка

**PyTorch:**
- Оригинальный формат PyTorch
- Больший размер
- Требует больше памяти

**ONNX:**
- Универсальный формат
- Оптимизация для разных платформ

### Квантование моделей

**Что это:**
Сжатие модели для уменьшения размера и требований к памяти.

**Типы квантования:**
- Q4_0 — 4 бита, хорошее качество
- Q4_1 — 4 бита, немного лучше
- Q5_0 — 5 бит, лучше качество
- Q8_0 — 8 бит, почти без потерь

**Компромисс:**
- Меньше размер → меньше памяти → немного хуже качество
- Больше размер → больше памяти → лучше качество

---

## 🚀 Интеграция в приложение

### Архитектура

```
Приложение
  ↓
LLMService (абстракция)
  ↓
LocalLLMClient (локальная LLM)
  или
OpenRouterClient (облачная LLM)
  ↓
HTTP запрос к локальной LLM API
  ↓
Ответ от локальной LLM
```

### Переключение между LLM

**Стратегии:**
1. **Конфигурация** — выбор через YAML конфиг
2. **Runtime переключение** — выбор через API/UI
3. **Fallback** — автоматическое переключение при ошибках
4. **Гибридный режим** — использование обеих LLM

### Обработка ошибок

**Типы ошибок:**
- Локальная LLM не запущена
- Таймаут запроса
- Недостаточно памяти
- Модель не найдена

**Стратегии:**
- Автоматический fallback на облачную LLM
- Уведомление пользователя
- Логирование ошибок
- Retry механизм

---

## 📊 Сравнение моделей

### Малые модели (1-3B параметров)

**Примеры:**
- Gemma 2 (2B)
- Phi-3-mini (3.8B)
- LLaMA 3.2 (3B)

**Характеристики:**
- Быстрая генерация
- Низкие требования к памяти
- Хорошее качество для простых задач
- Могут быть ограничены в сложных задачах

**Использование:**
- Простые вопросы
- Быстрые ответы
- Ограниченные ресурсы

### Средние модели (7-13B параметров)

**Примеры:**
- Mistral 7B
- LLaMA 3.2 (7B)
- Qwen 2.5 (7B)

**Характеристики:**
- Хорошее качество ответов
- Умеренные требования к памяти
- Баланс скорости и качества

**Использование:**
- Большинство задач
- Хороший баланс качества и скорости

### Большие модели (13B+ параметров)

**Примеры:**
- LLaMA 3 (70B)
- Mistral 8x7B
- Qwen 2.5 (32B)

**Характеристики:**
- Высокое качество ответов
- Высокие требования к памяти
- Медленная генерация на CPU

**Использование:**
- Сложные задачи
- Высокое качество важнее скорости
- Мощное железо

---

## 🎓 Ключевые выводы

1. **Локальные LLM** — альтернатива облачным решениям
2. **Ollama** — самый простой способ начать
3. **Требования** — нужно мощное железо для больших моделей
4. **Качество** — зависит от размера модели
5. **Конфиденциальность** — данные остаются локально
6. **Интеграция** — можно использовать через API
7. **Гибридный подход** — комбинация локальной и облачной LLM

---

## 🔮 Будущее локальных LLM

1. **Улучшение качества** — модели становятся лучше при меньшем размере
2. **Оптимизация** — более эффективные алгоритмы
3. **Квантование** — лучшее сжатие без потери качества
4. **Специализация** — модели для конкретных задач
5. **Интеграция** — встроенные в операционные системы
6. **Энергоэффективность** — меньше потребление энергии

---

## 📚 Дополнительные ресурсы

- [Ollama Documentation](https://ollama.com/docs)
- [LM Studio](https://lmstudio.ai/)
- [LLaMA.cpp](https://github.com/ggerganov/llama.cpp)
- [Hugging Face Models](https://huggingface.co/models)
- [Model Comparison](https://huggingface.co/spaces/lmsys/chatbot-arena-leaderboard)

