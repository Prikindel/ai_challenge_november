# Краткий промпт для реализации: 🔥 День 25. Запустить локальную модель

**Полный промпт:** См. `PROMPT.md` в корне проекта

---

Ты — разработчик, создающий урок «🔥 День 25. Запустить локальную модель» в модуле `lesson-25-local-llm`. Проект использует стек **Kotlin + Ktor + SQLite** с фронтендом на **vanilla JS**. В качестве базы можно использовать один из прошлых уроков с простым чатом (например, lesson-19-rag-chat).

## Цель

Установить и запустить локальную LLM модель (Ollama, LM Studio, LLaMA, Mistral, Phi, Qwen и т.п.), проверить доступность через CLI/API и интегрировать её в существующий чат вместо внешнего API.

## Минимальные требования для зачёта

- ✅ Локальная LLM установлена и запущена
- ✅ Проверена работа через CLI (документировано)
- ✅ Проверена работа через API (документировано)
- ✅ Интегрирована в существующий чат
- ✅ Протестирована работа с вопросами
- ✅ Документация описывает процесс установки и настройки

## Ключевые требования

1. **Выбор базового урока** — выбрать урок с чатом (lesson-19-rag-chat или lesson-01-simple-chat-agent) и скопировать структуру

2. **Установка локальной LLM** — установить Ollama, LM Studio или другую
   - Документировать процесс установки
   - Проверить работу через CLI
   - Проверить работу через API

3. **Создание клиента для локальной LLM** — HTTP клиент для работы с локальной LLM API

4. **Интеграция в LLMService** — заменить или дополнить существующий LLMService для поддержки локальной LLM

5. **Тестирование** — протестировать работу локальной LLM в чате

6. **UI для переключения** (опционально) — добавить переключатель между локальной и внешней LLM

7. **Документация** — обновить README, THEORY, добавить инструкции по установке

## Поэтапная реализация (отдельные коммиты)

**КРИТИЧЕСКИ ВАЖНО:** Каждый шаг = отдельный git-коммит. После каждого шага **СТОП**, покажи изменения пользователю и дождись «ок, продолжай».

### Коммит 1: Выбор базового урока и копирование
- Выбрать базовый урок с чатом (lesson-19-rag-chat или lesson-01-simple-chat-agent)
- Скопировать структуру проекта
- Обновить названия и конфигурацию
- Убедиться, что проект собирается и запускается

### Коммит 2: Установка и настройка локальной LLM
- Установить локальную LLM (Ollama рекомендуется)
- Запустить локальную LLM сервер
- Проверить работу через CLI
- Проверить работу через API
- Создать `LOCAL_LLM_SETUP.md` с инструкциями

### Коммит 3: Создание клиента для локальной LLM
- Создать модели данных (`LocalLLMRequest`, `LocalLLMResponse`)
- Создать `LocalLLMClient.kt` для работы с API
- Поддержка разных провайдеров (Ollama, LM Studio, LLaMA.cpp)
- Конфигурация в `server.yaml`

### Коммит 4: Интеграция локальной LLM в LLMService
- Обновить `LLMService.kt` для поддержки локальной LLM
- Переключение между локальной и внешней LLM
- Fallback на внешнюю LLM при ошибках
- Обновить конфигурацию

### Коммит 5: Тестирование и проверка работы
- Запустить локальную LLM
- Запустить сервер приложения
- Протестировать через UI
- Протестировать через API
- Создать `TESTING.md` с результатами

### Коммит 6: UI для переключения между LLM (опционально)
- Добавить переключатель LLM в UI
- Обработка переключения в JavaScript
- API endpoint для переключения
- Индикация текущей LLM

### Коммит 7: Улучшения и документация
- Обработка ошибок подключения
- Автоматический fallback
- Обновить `README.md`, `THEORY.md`, `CHAT_PROMPT.txt`
- Обновить корневой `README.md`

## Технические детали

**Важно:** Ollama используется и для эмбеддингов (как в предыдущих уроках), и для LLM моделей. В этом уроке мы используем Ollama для запуска **LLM моделей для генерации текста** (llama3.2, mistral и т.д.), а не для эмбеддингов.

### Ollama API

**Endpoint:** `POST http://localhost:11434/api/generate`

**Запрос:**
```json
{
  "model": "llama3.2",
  "prompt": "Привет! Как дела?",
  "stream": false
}
```

**Ответ:**
```json
{
  "response": "Привет! У меня всё отлично!",
  "done": true,
  "model": "llama3.2"
}
```

### LM Studio API

**Endpoint:** `POST http://localhost:1234/v1/chat/completions`

**Запрос:**
```json
{
  "model": "local-model",
  "messages": [{"role": "user", "content": "Привет!"}]
}
```

### Конфигурация

```yaml
localLLM:
  enabled: true
  provider: "ollama"  # ollama, lmstudio, llamacpp
  baseUrl: "http://localhost:11434"
  model: "llama3.2"
  apiPath: "/api/generate"

llm:
  provider: "local"  # local или openrouter
```

## Важно

- Использовать существующий чат как базу
- Документировать все шаги установки
- Проверить работу через CLI и API
- Обрабатывать ошибки подключения
- После каждого коммита останавливаться и показывать изменения
- Пользователь сам делает merge/commit
- Все изменения должны быть протестированы
- Минимальные требования для зачёта должны быть выполнены

## Детали реализации

См. полный промпт в `PROMPT.md` для:
- Детального описания каждого коммита
- Примеров кода
- Инструкций по установке разных провайдеров
- Технических деталей
- Примеров API запросов

