Ты — разработчик, создающий урок «🔥 День 17. Реранкинг и фильтрация» в модуле `lesson-17-rag-reranking`.

Полное техническое задание — в `PROMPT.md`. Перед стартом обязательно прочитай его целиком.

## Кратко

1. **Копируем** весь код из урока 16 → новая папка `lesson-17-rag-reranking` (заменяем названия, убеждаемся, что всё работает).
2. **Добавляем фильтр и реранкер** после поиска:
   - ThresholdFilter (порог по сходству, `minSimilarity`, `keepTop`).
   - LLM Reranker (через OpenRouter, выдаёт `rerankScore`, `reason`, `shouldUse`).
   - Гибридная стратегия (`threshold → reranker`).
3. **Расширяем API и UI**:
   - Настройка стратегии и порога (REST + UI).
   - `RAGService` возвращает `filterStats`, `rerankInsights`.
   - `ComparisonService` сравнивает режимы: без фильтра, с фильтром.
4. **Документируем** (README, THEORY, PROMPT, CHAT_PROMPT) и обновляем корневой `README.md`.

## Коммиты (обязательно по шагам!)

1. **Коммит 1** — копия урока 16, переименование в 17, никаких функциональных изменений. После коммита остановись.
2. **Коммит 2** — ThresholdFilter + конфигурация + статистика. Стоп.
3. **Коммит 3** — LLM Reranker (через OpenRouter) и поддержка стратегий. Стоп.
4. **Коммит 4** — интеграция фильтра/реранкера в RAGService, ComparisonService, API. Стоп.
5. **Коммит 5** — обновления UI (rag-compare.html/js, визуализация, настройка порога). Стоп.
6. **Коммит 6** — документация, теория, добавление ссылки на урок 17 в корневом README. Стоп.

После каждого шага показывай изменения пользователю и жди подтверждения!

## Технические требования

- **Стек**: Kotlin + Ktor + SQLite + Vanilla JS (как в уроке 16).
- **LLM**: OpenRouter (конфигурация в `config/server.yaml`, ключ `OPENAI_API_KEY` в корне репозитория).
- **Embeddings**: Ollama `nomic-embed-text`.
- **Фильтрация**:
  - Конфиг `rag.filter.strategy` (`none | threshold | reranker | hybrid`).
  - Порог `rag.filter.threshold.minSimilarity`, параметр `keepTop`.
  - Reranker использует `LLMService`, есть лимит чанков (`maxChunks`).
  - Логи/статистика: сколько чанков удалено, причины, средние скоры.
- **API**: `POST /api/rag/query`, `POST /api/rag/compare`, `GET/POST /api/rag/filter/config`.
- **UI**: страница `/rag-compare.html` должна позволять менять стратегию и порог, показывать сравнение и объяснения.
- **Формат результата**: Видео + Код (в README есть раздел «Демонстрация»).

## Цель урока

Показать, как второй этап (фильтр или реранкер) улучшает RAG:
- Настроить порог релевантности.
- Сравнить ответы без фильтра и с фильтром.
- Объяснить пользователю, почему чанки были удалены/оставлены.


