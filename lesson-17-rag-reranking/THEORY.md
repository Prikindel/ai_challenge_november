# 🧠 Теория: Реранкинг и фильтрация в RAG

## 1. Почему простого top-K недостаточно

Базовый RAG (как в уроке 16) берёт top-K чанков по косинусному сходству. Проблемы:
- **Шум**: даже при высоком сходстве могут попадать нерелевантные фрагменты, особенно если вопрос общий.
- **Нечувствительность к нюансам**: одинаковые значения у разных чанков не гарантируют равную полезность.
- **Отсутствие контроля**: тяжело объяснить, почему конкретный чанк попал в контекст.

Решение — добавить второй этап: **filter / rerank**.

---

## 2. Пороговая фильтрация

### Идея
Отбрасываем чанки, у которых `similarity < threshold`.

### Плюсы
- Очень дёшево вычислительно.
- Понятный параметр: `minSimilarity`.

### Минусы
- Линейная шкала не всегда коррелирует с реальной полезностью.
- Не различает два чанка с одинаковым score.

### Практические советы
| Порог | Поведение | Когда использовать |
|-------|-----------|--------------------|
| 0.3-0.4 | Берём почти всё | Исследование, когда боимся потерять контекст |
| 0.5-0.7 | Баланс точности/полноты | Базовый режим |
| 0.8+ | Только «железобетонные» совпадения | Очень критичные ответы |

Также полезно вводить параметр `keepTop` — минимальное количество чанков, которые остаются, даже если их score чуть ниже порога.

---

## 3. Реранкинг через LLM

### Принцип
1. Берём top-N чанков.
2. Отправляем в LLM короткий промпт:
   ```
   Вопрос: ...
   Чанки:
   1) id=..., текст=...
   2) ...
   Оцени релевантность каждого чанка в [0..1], верни JSON.
   ```
3. Получаем `rerankScore`, `reason`, `shouldUse`.
4. Сортируем / фильтруем по новому скору.

### Преимущества
- LLM умеет учитывать семантику и структуру.
- Можно объяснить решение (reason).
- Гибкость (можно менять модель, промпт).

### Недостатки
- Стоимость (дополнительный запрос к OpenRouter).
- Латентность (нужно ждать второй ответ).
- Требует контроля токенов (ограничиваем количество чанков).

### Когда полезен
- Вопросы с неоднозначной формулировкой.
- Документы с похожими терминами.
- Нужна объяснимость («почему чанк удалён»).

---

## 4. Гибридный подход

```
ThresholdFilter → LLM Reranker
```

1. Быстрым порогом отбрасываем явно нерелевантные чанки.
2. Оставшиеся переупорядочиваем через LLM.
3. Ограничиваем итоговое число чанков (например, 3).

Такой подход сохраняет скорость и даёт качество LLM.

---

## 5. Метрики качества

Чтобы сравнивать режимы «без фильтра» и «с фильтром», полезно считать:
- `retrievedCount` — сколько чанков вернул поиск.
- `keptCount / droppedCount`.
- `avgSimilarityBefore / avgSimilarityAfter`.
- `avgRerankScore`.
- `tokensUsed` — влияет на стоимость ответа.
- `answerQuality` — субъективная оценка (например, через checklist).

В UI удобно показать табличку:
```
Метрика             | Без фильтра | С фильтром
--------------------|-------------|-----------
Чанков в контексте  | 5           | 3
Ср. сходство        | 0.52        | 0.73
Rerank score        | -           | 0.81
Tokens              | 1500        | 1100
Вывод               | шум, нет деталей | точный пошаговый ответ
```

---

## 6. Настройка порогов

1. **Сбор статистики**:
   - Логируйте распределение сходства для «хороших» и «плохих» чанков.
   - Настраивайте порог на основе реальных данных.
2. **Адаптивность**:
   - Позвольте пользователю менять порог на лету (UI + API).
   - Добавьте пресеты: «Максимальная точность», «Баланс», «Максимальная полнота».
3. **Fail-safe**:
   - Если после фильтра ничего не осталось, автоматически откатываемся к baseline (выводим предупреждение).

---

## 7. Сравнение режимов

| Режим            | Применение              | Преимущества                       | Недостатки                |
|------------------|-------------------------|------------------------------------|---------------------------|
| Без фильтра      | Быстрые черновые ответы | Максимальная полнота               | Шум, лишние токены       |
| Пороговый       | Стандартный режим       | Дёшево, прозрачно                  | Нужна ручная настройка   |
| LLM-реранкер     | Критично важные запросы | Семантическая точность, объяснимость | Стоимость и латентность |
| Гибрид           | Большинство сценариев   | Баланс скорости и качества         | Больше кода и логики     |

---

## 8. Лучшие практики

1. **Логируйте всё** — массив ids, причины удаления, пороги.
2. **Кэшируйте** — если вопрос повторяется, используйте прошлый результат reranker’а.
3. **Ограничивайте токены** — и в reranker, и в основном ответе.
4. **Визуализируйте** — пользователю важно видеть, почему что-то удалено.
5. **Пишите тесты**:
   - ThresholdFilterTest (границы порога, keepTop).
   - RerankerPromptBuilderTest.
   - Integration test: «если всё отфильтровали, fallback к baseline».

---

## 9. Куда двигаться дальше

- Подключить специализированные библиотеки (BM25, ColBERT, Cohere Rerank).
- Использовать обучение на пользовательских фидбеках.
- Добавить многоступенчатые фильтры (тип документа, свежесть, автор).

---

## Итог

Реранкинг и фильтрация — обязательный этап зрелого RAG. Они:
- Повышают точность и осмысленность ответов.
- Сокращают токены (меньше контекста → дешевле).
- Делают систему объяснимой («почему чанк попал / не попал»).

Урок 17 показывает, как внедрить эти техники поверх уже работающего RAG и дать пользователю инструмент для экспериментов.


